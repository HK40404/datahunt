{
  "total_analyzed": 234,
  "analyzed_at": "2026-01-18T00:54:34.907955",
  "input_file": "src/pipeline/output/langgraph/failed_questions.json",
  "category_summary": {
    "SQL 逻辑错误": 31,
    "其他": 8,
    "SQL逻辑错误": 62,
    "语义理解错误": 82,
    "返回格式错误": 29,
    "日期格式错误": 11,
    "SQL语法错误": 2,
    "数值计算错误": 6,
    "SQL 语法错误": 2,
    "SQL logic error": 1
  },
  "results": [
    {
      "question_id": 1481,
      "difficulty": "challenging",
      "db_id": "debit_card_specializing",
      "question": "What is the difference in the annual average consumption of the customers with the least amount of consumption paid in CZK for 2013 between SME and LAM, LAM and KAM, and KAM and SME?",
      "evidence": "annual average consumption of customer with the lowest consumption in each segment = total consumption per year / the number of customer with lowest consumption in each segment; Difference in annual average = SME's annual average - LAM's annual average; Difference in annual average = LAM's annual average - KAM's annual average; Year 2013 can be presented as Between 201301 And 201312; The first 4 strings of the Date values in the yearmonth table can represent year.",
      "generated_sql": "SELECT (SELECT annual_avg FROM (SELECT Segment, SUM(total_consumption)/COUNT(*) as annual_avg FROM (SELECT t.CustomerID, t.Segment, t.total_consumption FROM (SELECT c.CustomerID, c.Segment, SUM(y.Consumption) as total_consumption FROM customers c INNER JOIN yearmonth y ON c.CustomerID = y.CustomerID WHERE c.Currency = 'CZK' AND SUBSTRING(y.Date, 1, 4) = '2013' GROUP BY c.CustomerID, c.Segment) as t INNER JOIN (SELECT Segment, MIN(total_consumption) as min_consumption FROM (SELECT c.CustomerID, c.Segment, SUM(y.Consumption) as total_consumption FROM customers c INNER JOIN yearmonth y ON c.CustomerID = y.CustomerID WHERE c.Currency = 'CZK' AND SUBSTRING(y.Date, 1, 4) = '2013' GROUP BY c.CustomerID, c.Segment) as t2 WHERE Segment IN ('SME', 'LAM', 'KAM') GROUP BY Segment) as mins ON t.Segment = mins.Segment AND t.total_consumption = mins.min_consumption WHERE t.Segment IN ('SME', 'LAM', 'KAM') ) as lowest_customers GROUP BY Segment) as avg WHERE Segment = 'SME') - (SELECT annual_avg FROM (SELECT Segment, SUM(total_consumption)/COUNT(*) as annual_avg FROM (SELECT t.CustomerID, t.Segment, t.total_consumption FROM (SELECT c.CustomerID, c.Segment, SUM(y.Consumption) as total_consumption FROM customers c INNER JOIN yearmonth y ON c.CustomerID = y.CustomerID WHERE c.Currency = 'CZK' AND SUBSTRING(y.Date, 1, 4) = '2013' GROUP BY c.CustomerID, c.Segment) as t INNER JOIN (SELECT Segment, MIN(total_consumption) as min_consumption FROM (SELECT c.CustomerID, c.Segment, SUM(y.Consumption) as total_consumption FROM customers c INNER JOIN yearmonth y ON c.CustomerID = y.CustomerID WHERE c.Currency = 'CZK' AND SUBSTRING(y.Date, 1, 4) = '2013' GROUP BY c.CustomerID, c.Segment) as t2 WHERE Segment IN ('SME', 'LAM', 'KAM') GROUP BY Segment) as mins ON t.Segment = mins.Segment AND t.total_consumption = mins.min_consumption WHERE t.Segment IN ('SME', 'LAM', 'KAM') ) as lowest_customers GROUP BY Segment) as avg WHERE Segment = 'LAM') as diff_SME_LAM, (SELECT annual_avg FROM (SELECT Segment, SUM(total_consumption)/COUNT(*) as annual_avg FROM (SELECT t.CustomerID, t.Segment, t.total_consumption FROM (SELECT c.CustomerID, c.Segment, SUM(y.Consumption) as total_consumption FROM customers c INNER JOIN yearmonth y ON c.CustomerID = y.CustomerID WHERE c.Currency = 'CZK' AND SUBSTRING(y.Date, 1, 4) = '2013' GROUP BY c.CustomerID, c.Segment) as t INNER JOIN (SELECT Segment, MIN(total_consumption) as min_consumption FROM (SELECT c.CustomerID, c.Segment, SUM(y.Consumption) as total_consumption FROM customers c INNER JOIN yearmonth y ON c.CustomerID = y.CustomerID WHERE c.Currency = 'CZK' AND SUBSTRING(y.Date, 1, 4) = '2013' GROUP BY c.CustomerID, c.Segment) as t2 WHERE Segment IN ('SME', 'LAM', 'KAM') GROUP BY Segment) as mins ON t.Segment = mins.Segment AND t.total_consumption = mins.min_consumption WHERE t.Segment IN ('SME', 'LAM', 'KAM') ) as lowest_customers GROUP BY Segment) as avg WHERE Segment = 'LAM') - (SELECT annual_avg FROM (SELECT Segment, SUM(total_consumption)/COUNT(*) as annual_avg FROM (SELECT t.CustomerID, t.Segment, t.total_consumption FROM (SELECT c.CustomerID, c.Segment, SUM(y.Consumption) as total_consumption FROM customers c INNER JOIN yearmonth y ON c.CustomerID = y.CustomerID WHERE c.Currency = 'CZK' AND SUBSTRING(y.Date, 1, 4) = '2013' GROUP BY c.CustomerID, c.Segment) as t INNER JOIN (SELECT Segment, MIN(total_consumption) as min_consumption FROM (SELECT c.CustomerID, c.Segment, SUM(y.Consumption) as total_consumption FROM customers c INNER JOIN yearmonth y ON c.CustomerID = y.CustomerID WHERE c.Currency = 'CZK' AND SUBSTRING(y.Date, 1, 4) = '2013' GROUP BY c.CustomerID, c.Segment) as t2 WHERE Segment IN ('SME', 'LAM', 'KAM') GROUP BY Segment) as mins ON t.Segment = mins.Segment AND t.total_consumption = mins.min_consumption WHERE t.Segment IN ('SME', 'LAM', 'KAM') ) as lowest_customers GROUP BY Segment) as avg WHERE Segment = 'KAM') as diff_LAM_KAM, (SELECT annual_avg FROM (SELECT Segment, SUM(total_consumption)/COUNT(*) as annual_avg FROM (SELECT t.CustomerID, t.Segment, t.total_consumption FROM (SELECT c.CustomerID, c.Segment, SUM(y.Consumption) as total_consumption FROM customers c INNER JOIN yearmonth y ON c.CustomerID = y.CustomerID WHERE c.Currency = 'CZK' AND SUBSTRING(y.Date, 1, 4) = '2013' GROUP BY c.CustomerID, c.Segment) as t INNER JOIN (SELECT Segment, MIN(total_consumption) as min_consumption FROM (SELECT c.CustomerID, c.Segment, SUM(y.Consumption) as total_consumption FROM customers c INNER JOIN yearmonth y ON c.CustomerID = y.CustomerID WHERE c.Currency = 'CZK' AND SUBSTRING(y.Date, 1, 4) = '2013' GROUP BY c.CustomerID, c.Segment) as t2 WHERE Segment IN ('SME', 'LAM', 'KAM') GROUP BY Segment) as mins ON t.Segment = mins.Segment AND t.total_consumption = mins.min_consumption WHERE t.Segment IN ('SME', 'LAM', 'KAM') ) as lowest_customers GROUP BY Segment) as avg WHERE Segment = 'KAM') - (SELECT annual_avg FROM (SELECT Segment, SUM(total_consumption)/COUNT(*) as annual_avg FROM (SELECT t.CustomerID, t.Segment, t.total_consumption FROM (SELECT c.CustomerID, c.Segment, SUM(y.Consumption) as total_consumption FROM customers c INNER JOIN yearmonth y ON c.CustomerID = y.CustomerID WHERE c.Currency = 'CZK' AND SUBSTRING(y.Date, 1, 4) = '2013' GROUP BY c.CustomerID, c.Segment) as t INNER JOIN (SELECT Segment, MIN(total_consumption) as min_consumption FROM (SELECT c.CustomerID, c.Segment, SUM(y.Consumption) as total_consumption FROM customers c INNER JOIN yearmonth y ON c.CustomerID = y.CustomerID WHERE c.Currency = 'CZK' AND SUBSTRING(y.Date, 1, 4) = '2013' GROUP BY c.CustomerID, c.Segment) as t2 WHERE Segment IN ('SME', 'LAM', 'KAM') GROUP BY Segment) as mins ON t.Segment = mins.Segment AND t.total_consumption = mins.min_consumption WHERE t.Segment IN ('SME', 'LAM', 'KAM') ) as lowest_customers GROUP BY Segment) as avg WHERE Segment = 'SME') as diff_KAM_SME",
      "ground_truth_sql": "SELECT\n  CAST(SUM(CASE WHEN `T1`.`Segment` = 'SME' THEN `T2`.`Consumption` ELSE 0 END) AS DOUBLE) / COUNT(`T1`.`CustomerID`) - CAST(SUM(CASE WHEN `T1`.`Segment` = 'LAM' THEN `T2`.`Consumption` ELSE 0 END) AS DOUBLE) / COUNT(`T1`.`CustomerID`),\n  CAST(SUM(CASE WHEN `T1`.`Segment` = 'LAM' THEN `T2`.`Consumption` ELSE 0 END) AS DOUBLE) / COUNT(`T1`.`CustomerID`) - CAST(SUM(CASE WHEN `T1`.`Segment` = 'KAM' THEN `T2`.`Consumption` ELSE 0 END) AS DOUBLE) / COUNT(`T1`.`CustomerID`),\n  CAST(SUM(CASE WHEN `T1`.`Segment` = 'KAM' THEN `T2`.`Consumption` ELSE 0 END) AS DOUBLE) / COUNT(`T1`.`CustomerID`) - CAST(SUM(CASE WHEN `T1`.`Segment` = 'SME' THEN `T2`.`Consumption` ELSE 0 END) AS DOUBLE) / COUNT(`T1`.`CustomerID`)\nFROM `customers` AS `T1`\nINNER JOIN `yearmonth` AS `T2`\n  ON `T1`.`CustomerID` = `T2`.`CustomerID`\nWHERE\n  `T1`.`Currency` = 'CZK'\n  AND `T2`.`Consumption` = (\n    SELECT\n      MIN(`Consumption`)\n    FROM `yearmonth`\n  )\n  AND `T2`.`Date` BETWEEN 201301 AND 201312",
      "error_category": "SQL 逻辑错误",
      "error_reason": "生成的 SQL 试图计算每个段（SME、LAM、KAM）中消费最低的客户的年平均消费差异，这符合问题意图和证据。然而，预期的 SQL 使用了全局最小消费（`T2`.`Consumption` = (SELECT MIN(`Consumption`) FROM `yearmonth`)），而不是每个段的最低消费，导致逻辑不匹配。生成的 SQL 逻辑正确但过于复杂，而预期的 SQL 逻辑错误，因为它没有按段分组计算最低消费，可能返回错误的结果。此外，生成的 SQL 在日期处理上使用 `SUBSTRING(y.Date, 1, 4) = '2013'`，而预期的 SQL 使用 `BETWEEN 201301 AND 201312`，两者都符合证据，但生成的 SQL 更准确地处理了字符串日期格式。",
      "suggested_fix": "如果预期的 SQL 是正确的，则修改生成的 SQL 以使用全局最小消费，但根据问题意图，每个段的最低消费更合理。建议简化生成的 SQL，使用 CTE 或子查询来明确计算每个段的最低消费，然后计算年平均消费差异。例如：\n1. 使用子查询找到每个段的最低消费值。\n2. 过滤消费等于该段最低值的客户。\n3. 计算每个段的平均消费（总消费除以客户数）。\n4. 计算差异（SME - LAM、LAM - KAM、KAM - SME）。\n同时，统一日期处理，根据数据库类型选择合适的方法（如使用 `SUBSTRING` 或 `BETWEEN`）。"
    },
    {
      "question_id": 1482,
      "difficulty": "challenging",
      "db_id": "debit_card_specializing",
      "question": "Which of the three segments—SME, LAM and KAM—has the biggest and lowest percentage increases in consumption paid in EUR between 2012 and 2013?",
      "evidence": "Increase or Decrease = consumption for 2013 - consumption for 2012; Percentage of Increase = (Increase or Decrease / consumption for 2012) * 100%; The first 4 strings of the Date values in the yearmonth table can represent year",
      "generated_sql": "SELECT \n    Segment,\n    PercentageIncrease,\n    'Biggest' AS Type\nFROM (\n    SELECT \n        Segment,\n        CAST( (SUM(CASE WHEN Year = '2013' THEN TotalConsumption ELSE 0 END) - \n               SUM(CASE WHEN Year = '2012' THEN TotalConsumption ELSE 0 END)) AS DOUBLE) / \n        CAST( SUM(CASE WHEN Year = '2012' THEN TotalConsumption ELSE 0 END) AS DOUBLE ) * 100 AS PercentageIncrease\n    FROM (\n        SELECT \n            c.Segment,\n            SUBSTRING(y.Date, 1, 4) AS Year,\n            SUM(y.Consumption) AS TotalConsumption\n        FROM customers c\n        INNER JOIN yearmonth y ON c.CustomerID = y.CustomerID\n        WHERE c.Currency = 'EUR'\n          AND c.Segment IN ('SME', 'LAM', 'KAM')\n          AND SUBSTRING(y.Date, 1, 4) IN ('2012', '2013')\n        GROUP BY c.Segment, SUBSTRING(y.Date, 1, 4)\n    ) AS ConsumptionByYear\n    GROUP BY Segment\n    HAVING SUM(CASE WHEN Year = '2012' THEN TotalConsumption ELSE 0 END) > 0\n) AS t\nORDER BY PercentageIncrease DESC\nLIMIT 1\nUNION ALL\nSELECT \n    Segment,\n    PercentageIncrease,\n    'Lowest' AS Type\nFROM (\n    SELECT \n        Segment,\n        CAST( (SUM(CASE WHEN Year = '2013' THEN TotalConsumption ELSE 0 END) - \n               SUM(CASE WHEN Year = '2012' THEN TotalConsumption ELSE 0 END)) AS DOUBLE) / \n        CAST( SUM(CASE WHEN Year = '2012' THEN TotalConsumption ELSE 0 END) AS DOUBLE ) * 100 AS PercentageIncrease\n    FROM (\n        SELECT \n            c.Segment,\n            SUBSTRING(y.Date, 1, 4) AS Year,\n            SUM(y.Consumption) AS TotalConsumption\n        FROM customers c\n        INNER JOIN yearmonth y ON c.CustomerID = y.CustomerID\n        WHERE c.Currency = 'EUR'\n          AND c.Segment IN ('SME', 'LAM', 'KAM')\n          AND SUBSTRING(y.Date, 1, 4) IN ('2012', '2013')\n        GROUP BY c.Segment, SUBSTRING(y.Date, 1, 4)\n    ) AS ConsumptionByYear\n    GROUP BY Segment\n    HAVING SUM(CASE WHEN Year = '2012' THEN TotalConsumption ELSE 0 END) > 0\n) AS t\nORDER BY PercentageIncrease ASC\nLIMIT 1",
      "ground_truth_sql": "SELECT\n  CAST((\n    SUM(\n      CASE\n        WHEN `T1`.`Segment` = 'SME' AND `T2`.`Date` LIKE '2013%'\n        THEN `T2`.`Consumption`\n        ELSE 0\n      END\n    ) - SUM(\n      CASE\n        WHEN `T1`.`Segment` = 'SME' AND `T2`.`Date` LIKE '2012%'\n        THEN `T2`.`Consumption`\n        ELSE 0\n      END\n    )\n  ) AS DOUBLE) * 100 / SUM(\n    CASE\n      WHEN `T1`.`Segment` = 'SME' AND `T2`.`Date` LIKE '2012%'\n      THEN `T2`.`Consumption`\n      ELSE 0\n    END\n  ),\n  CAST(SUM(\n    CASE\n      WHEN `T1`.`Segment` = 'LAM' AND `T2`.`Date` LIKE '2013%'\n      THEN `T2`.`Consumption`\n      ELSE 0\n    END\n  ) - SUM(\n    CASE\n      WHEN `T1`.`Segment` = 'LAM' AND `T2`.`Date` LIKE '2012%'\n      THEN `T2`.`Consumption`\n      ELSE 0\n    END\n  ) AS DOUBLE) * 100 / SUM(\n    CASE\n      WHEN `T1`.`Segment` = 'LAM' AND `T2`.`Date` LIKE '2012%'\n      THEN `T2`.`Consumption`\n      ELSE 0\n    END\n  ),\n  CAST(SUM(\n    CASE\n      WHEN `T1`.`Segment` = 'KAM' AND `T2`.`Date` LIKE '2013%'\n      THEN `T2`.`Consumption`\n      ELSE 0\n    END\n  ) - SUM(\n    CASE\n      WHEN `T1`.`Segment` = 'KAM' AND `T2`.`Date` LIKE '2012%'\n      THEN `T2`.`Consumption`\n      ELSE 0\n    END\n  ) AS DOUBLE) * 100 / SUM(\n    CASE\n      WHEN `T1`.`Segment` = 'KAM' AND `T2`.`Date` LIKE '2012%'\n      THEN `T2`.`Consumption`\n      ELSE 0\n    END\n  )\nFROM `customers` AS `T1`\nINNER JOIN `yearmonth` AS `T2`\n  ON `T1`.`CustomerID` = `T2`.`CustomerID`",
      "error_category": "其他",
      "error_reason": "生成的SQL在语义理解上是正确的，因为它直接回答了用户问题，通过UNION ALL组合了最大和最小的百分比增长结果。然而，生成的SQL与预期的SQL在结构上存在显著差异：生成的SQL使用UNION ALL来获取最大和最小值，而预期的SQL直接计算三个细分市场的百分比增长但未标识最大和最小。预期的SQL缺少GROUP BY子句，可能导致逻辑错误（如多行聚合问题），并且没有处理2012年消费为零时的除以零错误。生成的SQL虽然正确，但可能不是最简洁的方式，且使用了SUBSTRING提取年份，而预期的SQL使用LIKE模式，两者都正确但方法不同。",
      "suggested_fix": "生成的SQL已经正确，无需修改。但可以优化以提高可读性或性能，例如使用窗口函数（如ROW_NUMBER()）替代UNION ALL。对于预期的SQL，建议：1) 添加GROUP BY Segment以正确聚合每个细分市场的消费；2) 添加HAVING条件确保2012年消费大于零以避免除以零错误；3) 修改输出格式以返回细分市场名称和类型（如'Biggest'或'Lowest'），或使用外部查询排序找出最大和最小值。"
    },
    {
      "question_id": 1490,
      "difficulty": "moderate",
      "db_id": "debit_card_specializing",
      "question": "How many percent of LAM customer consumed more than 46.73?",
      "evidence": "Percentage of LAM customer consumed more than 46.73 = (Total no. of LAM customers who consumed more than 46.73 / Total no. of LAM customers) * 100.",
      "generated_sql": "SELECT CAST((SELECT COUNT(DISTINCT c.CustomerID) FROM customers c INNER JOIN yearmonth y ON c.CustomerID = y.CustomerID WHERE c.Segment = 'LAM' AND y.Consumption > 46.73) AS DOUBLE) / CAST((SELECT COUNT(CustomerID) FROM customers WHERE Segment = 'LAM') AS DOUBLE) * 100 AS percentage",
      "ground_truth_sql": "SELECT\n  CAST(SUM(CASE WHEN `T2`.`Consumption` > 46.73 THEN 1 ELSE 0 END) AS DOUBLE) * 100 / COUNT(`T1`.`CustomerID`)\nFROM `customers` AS `T1`\nINNER JOIN `yearmonth` AS `T2`\n  ON `T1`.`CustomerID` = `T2`.`CustomerID`\nWHERE\n  `T1`.`Segment` = 'LAM'",
      "error_category": "SQL逻辑错误",
      "error_reason": "生成的SQL使用了两个子查询分别计算分子和分母，而预期的SQL使用了一个查询和CASE语句。生成的SQL逻辑正确，但实现方式与预期不同。生成的SQL使用了COUNT(DISTINCT)来确保每个客户只计数一次，而预期的SQL使用了SUM(CASE)但没有使用DISTINCT，这可能导致分子计数错误（如果一个客户有多个消费记录超过46.73，会被多次计数）。此外，生成的SQL的分母子查询直接从customers表计数所有LAM客户，包括那些没有消费记录的，这符合问题意图；而预期的SQL通过INNER JOIN计数分母，可能只包括有消费记录的客户，导致分母不准确。因此，生成的SQL在逻辑上更正确，但与预期的SQL实现方式不一致。",
      "suggested_fix": "可以优化为一个查询，使用COUNT(DISTINCT)或确保正确计数客户。例如，分子可以使用SUM(CASE WHEN Consumption > 46.73 THEN 1 ELSE 0 END)但结合DISTINCT，或者使用生成的SQL的方法但简化为单个查询。建议修改预期的SQL为：SELECT CAST(SUM(CASE WHEN T2.Consumption > 46.73 THEN 1 ELSE 0 END) AS DOUBLE) * 100 / COUNT(DISTINCT T1.CustomerID) FROM customers AS T1 INNER JOIN yearmonth AS T2 ON T1.CustomerID = T2.CustomerID WHERE T1.Segment = 'LAM'。这样既保持了单个查询，又确保了客户计数的准确性。"
    },
    {
      "question_id": 1500,
      "difficulty": "simple",
      "db_id": "debit_card_specializing",
      "question": "Please list the product description of the products consumed in September, 2013.",
      "evidence": "September 2013 refers to 201309; The first 4 strings of the Date values in the yearmonth table can represent year; The 5th and 6th string of the date can refer to month.",
      "generated_sql": "SELECT DISTINCT p.Description FROM transactions_1k t INNER JOIN products p ON t.ProductID = p.ProductID WHERE DATE_FORMAT(t.Date, '%Y%m') = '201309';",
      "ground_truth_sql": "SELECT\n  `T3`.`Description`\nFROM `transactions_1k` AS `T1`\nINNER JOIN `yearmonth` AS `T2`\n  ON `T1`.`CustomerID` = `T2`.`CustomerID`\nINNER JOIN `products` AS `T3`\n  ON `T1`.`ProductID` = `T3`.`ProductID`\nWHERE\n  `T2`.`Date` = '201309'",
      "error_category": "语义理解错误",
      "error_reason": "生成的 SQL 错误地假设日期信息存储在 transactions_1k 表中，并直接使用 DATE_FORMAT 函数处理 t.Date 字段。根据证据，日期信息存储在 yearmonth 表中，通过 CustomerID 关联，且 yearmonth.Date 字段是字符串格式 '201309'（前4位为年，第5-6位为月）。因此，生成的 SQL 没有正确理解数据库模式，错误地忽略了 yearmonth 表，导致逻辑错误：无法正确过滤2013年9月的交易。",
      "suggested_fix": "修改 SQL 以连接 yearmonth 表，基于 CustomerID 字段，并直接比较 yearmonth.Date = '201309'。例如：SELECT DISTINCT p.Description FROM transactions_1k t INNER JOIN yearmonth y ON t.CustomerID = y.CustomerID INNER JOIN products p ON t.ProductID = p.ProductID WHERE y.Date = '201309';"
    },
    {
      "question_id": 1501,
      "difficulty": "moderate",
      "db_id": "debit_card_specializing",
      "question": "Please list the countries of the gas stations with transactions taken place in June, 2013.",
      "evidence": "June 2013 refers to '201306'; The first 4 strings of the Date values in the yearmonth table can represent year; The 5th and 6th string of the date can refer to month;",
      "generated_sql": "SELECT DISTINCT g.Country FROM transactions_1k t INNER JOIN gasstations g ON t.GasStationID = g.GasStationID WHERE t.Date >= '2013-06-01' AND t.Date <= '2013-06-30'",
      "ground_truth_sql": "SELECT DISTINCT\n  `T2`.`Country`\nFROM `transactions_1k` AS `T1`\nINNER JOIN `gasstations` AS `T2`\n  ON `T1`.`GasStationID` = `T2`.`GasStationID`\nINNER JOIN `yearmonth` AS `T3`\n  ON `T1`.`CustomerID` = `T3`.`CustomerID`\nWHERE\n  `T3`.`Date` = '201306'",
      "error_category": "语义理解错误",
      "error_reason": "生成的SQL错误地假设了transactions_1k表包含日期字段，并且日期格式为标准日期类型（如'2013-06-01'）。实际上，根据证据，日期信息存储在yearmonth表中，格式为字符串'201306'（前4位为年，第5、6位为月），并且需要通过CustomerID连接transactions_1k和yearmonth表。生成的SQL遗漏了与yearmonth表的连接，并使用了不正确的日期范围过滤条件，导致无法正确识别2013年6月的交易。",
      "suggested_fix": "修改SQL以连接yearmonth表，基于CustomerID，并使用yearmonth.Date = '201306'来过滤2013年6月的交易。例如：SELECT DISTINCT g.Country FROM transactions_1k t INNER JOIN gasstations g ON t.GasStationID = g.GasStationID INNER JOIN yearmonth ym ON t.CustomerID = ym.CustomerID WHERE ym.Date = '201306'"
    },
    {
      "question_id": 1505,
      "difficulty": "simple",
      "db_id": "debit_card_specializing",
      "question": "Among the customers who paid in euro, how many of them have a monthly consumption of over 1000?",
      "evidence": "Pays in euro = Currency = 'EUR'.",
      "generated_sql": "SELECT COUNT(DISTINCT c.CustomerID) FROM customers c INNER JOIN yearmonth y ON c.CustomerID = y.CustomerID WHERE c.Currency = 'EUR' AND y.Consumption > 1000",
      "ground_truth_sql": "SELECT\n  COUNT(*)\nFROM `yearmonth` AS `T1`\nINNER JOIN `customers` AS `T2`\n  ON `T1`.`CustomerID` = `T2`.`CustomerID`\nWHERE\n  `T2`.`Currency` = 'EUR' AND `T1`.`Consumption` > 1000.00",
      "error_category": "SQL 逻辑错误",
      "error_reason": "生成的 SQL 使用 COUNT(DISTINCT c.CustomerID) 来计数 distinct customers，但预期的 SQL 使用 COUNT(*) 来计数所有匹配行。这会导致逻辑错误，因为如果同一个 customer 有多个 monthly consumption 记录 over 1000，生成的 SQL 返回 1，而预期的 SQL 返回多个。此外，阈值不匹配：生成的 SQL 使用 1000，而预期的 SQL 使用 100.00，但用户问题中明确是 'over 1000'，所以生成的 SQL 在阈值上正确，但不符合预期 SQL 的数值要求。",
      "suggested_fix": "将 COUNT(DISTINCT c.CustomerID) 改为 COUNT(*) 以匹配预期的 SQL 逻辑。同时，根据用户问题，阈值应保持为 1000，但为了与预期 SQL 一致，可能需要调整阈值为 100.00。建议确认数据库中 Consumption 字段的单位和问题意图，确保数值计算与问题描述一致。"
    },
    {
      "question_id": 1524,
      "difficulty": "simple",
      "db_id": "debit_card_specializing",
      "question": "What's the nationality of the customer who spent 548.4 in 2012/8/24?",
      "evidence": "'2012/8/24' can be represented by '2012-08-24'",
      "generated_sql": "SELECT c.Currency AS nationality FROM transactions_1k t INNER JOIN customers c ON t.CustomerID = c.CustomerID WHERE t.Date = '2012-08-24' AND t.Price = 548.4",
      "ground_truth_sql": "SELECT\n  `T2`.`Country`\nFROM `transactions_1k` AS `T1`\nINNER JOIN `gasstations` AS `T2`\n  ON `T1`.`GasStationID` = `T2`.`GasStationID`\nWHERE\n  `T1`.`Date` = '2012-08-24' AND  `T1`.`Price` = 548.4",
      "error_category": "语义理解错误",
      "error_reason": "生成的SQL误解了问题意图，将'nationality'错误地映射到'Currency'列，而实际上应该映射到'Country'列。此外，连接了错误的表（customers），而预期的SQL连接了gasstations表以获取国家信息。用户问题询问客户的国籍，但生成的SQL选择了货币属性，这不符合语义逻辑。数据库模式可能设计为通过交易关联到加油站，再从加油站表获取国家信息，而非直接从客户表获取。",
      "suggested_fix": "根据数据库模式，nationality应对应国家（Country），而非货币（Currency）。应连接transactions_1k和gasstations表，通过GasStationID关联，并选择Country列。修正后的SQL示例：SELECT T2.Country FROM transactions_1k AS T1 INNER JOIN gasstations AS T2 ON T1.GasStationID = T2.GasStationID WHERE T1.Date = '2012-08-24' AND T1.Price = 548.4"
    },
    {
      "question_id": 1525,
      "difficulty": "simple",
      "db_id": "debit_card_specializing",
      "question": "What is the percentage of the customers who used EUR in 2012/8/25?",
      "evidence": "'2012/8/25' can be represented by '2012-08-25'",
      "generated_sql": "SELECT (CAST(COUNT(DISTINCT CASE WHEN c.Currency = 'EUR' AND t.CustomerID IS NOT NULL THEN c.CustomerID END) AS DOUBLE) * 100) / CAST(COUNT(DISTINCT c.CustomerID) AS DOUBLE) AS percentage FROM customers c LEFT JOIN (SELECT DISTINCT CustomerID FROM transactions_1k WHERE Date = '2012-08-25') t ON c.CustomerID = t.CustomerID",
      "ground_truth_sql": "SELECT\n  CAST(SUM(CASE WHEN `T2`.`Currency` = 'EUR' THEN 1 ELSE 0 END) AS DOUBLE) * 100 / COUNT(`T1`.`CustomerID`)\nFROM `transactions_1k` AS `T1`\nINNER JOIN `customers` AS `T2`\n  ON `T1`.`CustomerID` = `T2`.`CustomerID`\nWHERE\n  `T1`.`Date` = '2012-08-25'",
      "error_category": "语义理解错误",
      "error_reason": "生成的SQL错误地将分母设置为所有客户（COUNT(DISTINCT c.CustomerID)），而问题意图是计算在指定日期（2012-08-25）有交易的客户中使用欧元的百分比。因此，分母应该只包括在该日期有交易的客户，而不是所有客户。此外，使用LEFT JOIN导致分母包括没有交易的客户，这不符合问题要求。生成的SQL虽然分子条件中包含了t.CustomerID IS NOT NULL来确保只考虑有交易的客户，但分母仍然包括所有客户，导致百分比计算偏低。预期的SQL虽然从transactions_1k表开始，但使用COUNT(T1.CustomerID)可能重复计数交易记录，而不是distinct客户数，这也存在逻辑问题，但生成的SQL的主要错误在于语义理解偏差。",
      "suggested_fix": "修改SQL以正确反映问题意图：从transactions_1k表开始，INNER JOIN customers表，WHERE Date = '2012-08-25'。然后计算百分比：分子为COUNT(DISTINCT CASE WHEN customers.Currency = 'EUR' THEN transactions_1k.CustomerID END)，分母为COUNT(DISTINCT transactions_1k.CustomerID)。或者使用SUM和CASE，但确保distinct客户计数。例如：SELECT CAST(SUM(CASE WHEN c.Currency = 'EUR' THEN 1 ELSE 0 END) AS DOUBLE) * 100 / COUNT(DISTINCT t.CustomerID) FROM transactions_1k t INNER JOIN customers c ON t.CustomerID = c.CustomerID WHERE t.Date = '2012-08-25'。"
    },
    {
      "question_id": 1529,
      "difficulty": "moderate",
      "db_id": "debit_card_specializing",
      "question": "What is the amount spent by customer \"38508\" at the gas stations? How much had the customer spent in January 2012?",
      "evidence": "January 2012 refers to the Date value = '201201'",
      "generated_sql": "SELECT (SELECT SUM(CAST(t.Amount AS DOUBLE) * t.Price) FROM transactions_1k t INNER JOIN gasstations g ON t.GasStationID = g.GasStationID WHERE t.CustomerID = 38508) as total_spent, (SELECT SUM(CAST(t.Amount AS DOUBLE) * t.Price) FROM transactions_1k t INNER JOIN gasstations g ON t.GasStationID = g.GasStationID WHERE t.CustomerID = 38508 AND DATE_FORMAT(t.Date, '%Y%m') = '201201') as jan_2012_spent",
      "ground_truth_sql": "SELECT\n  SUM(`T1`.`Price` ),\n  SUM(CASE WHEN `T3`.`Date` = '201201' THEN `T1`.`Price` ELSE 0 END)\nFROM `transactions_1k` AS `T1`\nINNER JOIN `gasstations` AS `T2`\n  ON `T1`.`GasStationID` = `T2`.`GasStationID`\nINNER JOIN `yearmonth` AS `T3`\n  ON `T1`.`CustomerID` = `T3`.`CustomerID`\nWHERE\n  `T1`.`CustomerID` = '38508'",
      "error_category": "语义理解错误",
      "error_reason": "生成的 SQL 对用户问题中的 'amount spent' 理解有误。预期的 SQL 仅使用 `Price` 字段计算总花费，而生成的 SQL 使用了 `CAST(t.Amount AS DOUBLE) * t.Price`，这可能错误地假设需要将金额与价格相乘。此外，生成的 SQL 错误地假设 `transactions_1k` 表包含 `Date` 字段，并使用了 `DATE_FORMAT` 函数来格式化日期，但根据证据和预期的 SQL，日期信息应通过 `yearmonth` 表获取，且 Date 值直接为 '201201'，无需格式化。这表明对数据库模式和问题意图的理解存在偏差。",
      "suggested_fix": "重新分析数据库模式，确保使用正确的表和字段。参考预期的 SQL，修改生成的 SQL 如下：1) 使用 `yearmonth` 表来连接日期信息，通过 `INNER JOIN yearmonth ON transactions_1k.CustomerID = yearmonth.CustomerID`；2) 调整计算方式，仅使用 `SUM(Price)` 来计算总花费和一月份花费，而不是 `Amount * Price`；3) 直接比较 `yearmonth.Date = '201201'` 来过滤一月份数据，避免使用 `DATE_FORMAT` 函数。最终 SQL 应类似于预期的 SQL，以确保逻辑正确且符合数据库结构。"
    },
    {
      "question_id": 1531,
      "difficulty": "moderate",
      "db_id": "debit_card_specializing",
      "question": "Who is the top spending customer and how much is the average price per single item purchased by this customer? What currency was being used?",
      "evidence": "average price per single item = Total(price) / Total(amount)",
      "generated_sql": "SELECT t.CustomerID, CAST(SUM(t.Price * t.Amount) AS DOUBLE) / CAST(SUM(t.Amount) AS DOUBLE) AS avg_price_per_item, c.Currency FROM transactions_1k t INNER JOIN customers c ON t.CustomerID = c.CustomerID WHERE t.CustomerID = (SELECT CustomerID FROM transactions_1k GROUP BY CustomerID ORDER BY SUM(Amount * Price) DESC LIMIT 1) GROUP BY t.CustomerID, c.Currency",
      "ground_truth_sql": "SELECT\n  `T2`.`CustomerID`,\n  SUM(`T2`.`Price` / `T2`.`Amount`),\n  `T1`.`Currency`\nFROM `customers` AS `T1`\nINNER JOIN `transactions_1k` AS `T2`\n  ON `T1`.`CustomerID` = `T2`.`CustomerID`\nWHERE\n  `T2`.`CustomerID` = (\n    SELECT\n      `CustomerID`\n    FROM `yearmonth`\n    ORDER BY\n      `Consumption` DESC\n    LIMIT 1\n  )\nGROUP BY\n  `T2`.`CustomerID`,\n  `T1`.`Currency`",
      "error_category": "语义理解错误",
      "error_reason": "生成的 SQL 在子查询中使用了 `transactions_1k` 表来找出消费最高的客户，而预期的 SQL 使用了 `yearmonth` 表。这表明生成的 SQL 可能误解了数据库结构，使用了错误的表来计算总消费。虽然生成的 SQL 正确计算了平均价格（总价格除以总数量），符合证据/外部知识，但预期的 SQL 在平均价格计算上存在错误（使用了 `SUM(Price / Amount)` 而非 `SUM(Price) / SUM(Amount)`）。然而，分析重点是生成的 SQL 相对于预期的 SQL 的错误，主要错误在于表选择不当，可能导致结果不准确或效率低下。",
      "suggested_fix": "修改子查询以使用 `yearmonth` 表，例如：`SELECT CustomerID FROM yearmonth ORDER BY Consumption DESC LIMIT 1`。同时，确保平均价格计算符合证据：使用 `SUM(Price) / SUM(Amount)` 而不是 `SUM(Price / Amount)`。完整的改进 SQL 应为：SELECT `T2`.`CustomerID`, SUM(`T2`.`Price`) / SUM(`T2`.`Amount`) AS avg_price_per_item, `T1`.`Currency` FROM `customers` AS `T1` INNER JOIN `transactions_1k` AS `T2` ON `T1`.`CustomerID` = `T2`.`CustomerID` WHERE `T2`.`CustomerID` = (SELECT `CustomerID` FROM `yearmonth` ORDER BY `Consumption` DESC LIMIT 1) GROUP BY `T2`.`CustomerID`, `T1`.`Currency`。"
    },
    {
      "question_id": 1533,
      "difficulty": "moderate",
      "db_id": "debit_card_specializing",
      "question": "For all the people who paid more than 29.00 per unit of product id No.5. Give their consumption status in the August of 2012.",
      "evidence": "August of 2012 refers to the Date value = '201208' ; Price per unit of product = Price / Amount;",
      "generated_sql": "SELECT ym.CustomerID, ym.Consumption FROM yearmonth ym WHERE ym.Date = '201208' AND ym.CustomerID IN (SELECT DISTINCT t.CustomerID FROM transactions_1k t WHERE t.ProductID = 5 AND (t.Price / t.Amount) > 29.00)",
      "ground_truth_sql": "SELECT\n  `T2`.`Consumption`\nFROM `transactions_1k` AS `T1`\nINNER JOIN `yearmonth` AS `T2`\n  ON `T1`.`CustomerID` = `T2`.`CustomerID`\nWHERE\n  `T1`.`Price` / `T1`.`Amount` > 29.00\n  AND `T1`.`ProductID` = 5\n  AND `T2`.`Date` = '201208'",
      "error_category": "返回格式错误",
      "error_reason": "生成的SQL返回了CustomerID和Consumption两列，而预期的SQL只返回Consumption一列。用户问题要求'give their consumption status'，可能只需要消费状态（Consumption），因此返回格式不匹配。此外，生成的SQL使用了子查询来过滤CustomerID，而预期的SQL使用了INNER JOIN直接连接两个表，这更符合标准SQL实践并确保数据一致性。",
      "suggested_fix": "修改SQL以使用INNER JOIN连接transactions_1k和yearmonth表，条件为Price/Amount > 29.00, ProductID = 5, 和Date = '201208'，并只选择Consumption列。例如：SELECT `T2`.`Consumption` FROM `transactions_1k` AS `T1` INNER JOIN `yearmonth` AS `T2` ON `T1`.`CustomerID` = `T2`.`CustomerID` WHERE `T1`.`Price` / `T1`.`Amount` > 29.00 AND `T1`.`ProductID` = 5 AND `T2`.`Date` = '201208'。"
    },
    {
      "question_id": 1322,
      "difficulty": "moderate",
      "db_id": "student_club",
      "question": "Among the events attended by more than 10 members of the Student_Club, how many of them are meetings?",
      "evidence": "meetings events refers to type = 'Meeting'; attended by more than 10 members refers to COUNT(event_id) > 10",
      "generated_sql": "SELECT COUNT(*) AS meeting_count FROM event WHERE event_id IN (SELECT link_to_event FROM attendance GROUP BY link_to_event HAVING COUNT(link_to_member) > 10) AND type = 'Meeting'",
      "ground_truth_sql": "SELECT COUNT(DISTINCT T1.event_id) FROM event AS T1 INNER JOIN attendance AS T2 ON T1.event_id = T2.link_to_event WHERE T1.type = 'Meeting' GROUP BY T1.event_id HAVING COUNT(T2.link_to_event) > 10",
      "error_category": "语义理解错误",
      "error_reason": "生成的SQL在子查询中使用了COUNT(link_to_member)来过滤事件，但根据证据，'attended by more than 10 members'应该使用COUNT(event_id)或COUNT(*)来计数成员数。证据明确指出COUNT(event_id) > 10，而生成的SQL使用了COUNT(link_to_member)，这可能导致计数不准确，如果link_to_member列有NULL值或不是成员标识符。虽然逻辑上COUNT(link_to_member)可能正确，但证据指定了COUNT(event_id)，表明生成的SQL误解了计数方式。",
      "suggested_fix": "将子查询中的HAVING COUNT(link_to_member) > 10改为HAVING COUNT(*) > 10或HAVING COUNT(link_to_event) > 10，以匹配证据中的计数方式。例如，修改为：SELECT COUNT(*) AS meeting_count FROM event WHERE event_id IN (SELECT link_to_event FROM attendance GROUP BY link_to_event HAVING COUNT(*) > 10) AND type = 'Meeting'。"
    },
    {
      "question_id": 1338,
      "difficulty": "moderate",
      "db_id": "student_club",
      "question": "Was each expense in October Meeting on October 8, 2019 approved?",
      "evidence": "event_name = 'October Meeting' where event_date = '2019-10-08'; approved = True means expenses was approved; approved = False means expenses was not approved",
      "generated_sql": "SELECT CASE WHEN NOT EXISTS (SELECT 1 FROM expense exp INNER JOIN budget b ON exp.link_to_budget = b.budget_id INNER JOIN event e ON b.link_to_event = e.event_id WHERE e.event_name = 'October Meeting' AND e.event_date = '2019-10-08' AND exp.approved != 'True') THEN 'Yes' ELSE 'No' END AS result",
      "ground_truth_sql": "SELECT\n  `T3`.`approved`\nFROM `event` AS `T1`\nINNER JOIN `budget` AS `T2`\n  ON `T1`.`event_id` = `T2`.`link_to_event`\nINNER JOIN `expense` AS `T3`\n  ON `T2`.`budget_id` = `T3`.`link_to_budget`\nWHERE\n  `T1`.`event_name` = 'October Meeting' AND `T1`.`event_date` LIKE '2019-10-08%'",
      "error_category": "语义理解错误",
      "error_reason": "生成的SQL误解了用户问题的意图。用户问题'Was each expense in October Meeting on October 8, 2019 approved?' 可能被解释为询问每一笔支出的批准状态（即返回每个支出的approved列），但生成的SQL使用了CASE语句来返回一个整体判断（'Yes'或'No'），这不符合预期输出。此外，日期比较存在潜在问题：生成的SQL使用了等号（e.event_date = '2019-10-08'），而预期的SQL使用了LIKE（e.event_date LIKE '2019-10-08%'），这可能因为日期字段包含时间部分（如'2019-10-08 10:00:00'），导致等号比较失败。",
      "suggested_fix": "修改SQL以直接选择expense表的approved列，而不是使用CASE语句进行整体判断。同时，调整日期比较条件，使用LIKE '2019-10-08%'来匹配日期部分，确保正确过滤事件。改进后的SQL应类似于：SELECT exp.approved FROM expense exp INNER JOIN budget b ON exp.link_to_budget = b.budget_id INNER JOIN event e ON b.link_to_event = e.event_id WHERE e.event_name = 'October Meeting' AND e.event_date LIKE '2019-10-08%'"
    },
    {
      "question_id": 1340,
      "difficulty": "moderate",
      "db_id": "student_club",
      "question": "Calculate the difference of the total amount spent in all events by the Student_Club in year 2019 and 2020.",
      "evidence": "The first 4 strings of the event_date values in the event table can represent year; The difference of the total amount spent = SUBTRACT(spent where YEAR(event_date) = 2019, spent where YEAR(event_date) = 2020)",
      "generated_sql": "SELECT COALESCE(SUM(CASE WHEN SUBSTRING(e.event_date, 1, 4) = '2019' THEN b.spent ELSE 0 END), 0) - COALESCE(SUM(CASE WHEN SUBSTRING(e.event_date, 1, 4) = '2020' THEN b.spent ELSE 0 END), 0) AS difference FROM budget b INNER JOIN event e ON b.link_to_event = e.event_id WHERE e.event_name = 'Student_Club'",
      "ground_truth_sql": "SELECT\n  SUM(CASE WHEN SUBSTR(`T1`.`event_date`, 1, 4) = '2019' THEN `T2`.`spent` ELSE 0 END) - SUM(CASE WHEN SUBSTR(`T1`.`event_date`, 1, 4) = '2020' THEN `T2`.`spent` ELSE 0 END) AS `num`\nFROM `event` AS `T1`\nINNER JOIN `budget` AS `T2`\n  ON `T1`.`event_id` = `T2`.`link_to_event`",
      "error_category": "语义理解错误",
      "error_reason": "生成的SQL添加了WHERE e.event_name = 'Student_Club'条件，而预期的SQL没有此过滤。用户问题中的'by the Student_Club'可能意味着事件属于Student_Club，但预期的SQL计算所有事件的差值，这表明生成的SQL可能误解了问题意图或过度过滤。此外，生成的SQL使用了COALESCE来处理NULL值，而预期的SQL没有，这可能导致数值计算差异（例如，当某些年份无数据时，生成的SQL返回0，而预期的SQL可能返回NULL）。但主要错误是过滤条件的添加，导致结果范围不一致。",
      "suggested_fix": "移除WHERE子句以匹配预期的SQL结构，除非问题明确要求过滤Student_Club。如果过滤是必要的，应重新评估预期的SQL是否遗漏了条件。建议使用预期的SQL作为基准，避免添加额外的过滤条件。同时，考虑是否使用COALESCE，但预期的SQL未使用，因此可以省略以保持一致性。"
    },
    {
      "question_id": 1350,
      "difficulty": "moderate",
      "db_id": "student_club",
      "question": "What is the status of the event which bought \"Post Cards, Posters\" on 2019/8/20?",
      "evidence": "'Post Cards, Posters' is an expense description; on 2019/8/20 refers to expense_date = '2019-8-20'; status of event refers to event_status",
      "generated_sql": "SELECT b.event_status FROM expense e JOIN budget b ON e.link_to_budget = b.budget_id WHERE e.expense_description = 'Post Cards, Posters' AND e.expense_date = '2019-8-20'",
      "ground_truth_sql": "SELECT\n  `T1`.`event_status`\nFROM `budget` AS `T1`\nINNER JOIN `expense` AS `T2`\n  ON `T1`.`budget_id` = `T2`.`link_to_budget`\nWHERE\n  `T2`.`expense_description` = 'Post Cards, Posters'\n  AND `T2`.`expense_date` = '2019-08-20'",
      "error_category": "日期格式错误",
      "error_reason": "生成的 SQL 中日期字符串使用了 '2019-8-20'，而预期的 SQL 使用了 '2019-08-20'。数据库可能期望标准 ISO 日期格式（YYYY-MM-DD），缺少前导零可能导致查询失败或返回错误结果。证据中提到 'on 2019/8/20 refers to expense_date = '2019-8-20''，但预期的 SQL 明确使用了 '2019-08-20'，表明数据库中的日期字段可能存储为字符串或 DATE 类型，且格式必须精确匹配。生成的 SQL 在日期格式上的不一致可能导致 WHERE 子句无法正确匹配记录。",
      "suggested_fix": "将日期字符串修改为标准 ISO 格式 '2019-08-20'，以确保与数据库中的日期字段格式一致。例如，将 WHERE 子句中的 e.expense_date = '2019-8-20' 改为 e.expense_date = '2019-08-20'。此外，建议在 SQL 中使用一致的日期处理方式，如使用 DATE 函数或确保输入格式正确，以避免潜在的解析问题。"
    },
    {
      "question_id": 1376,
      "difficulty": "moderate",
      "db_id": "student_club",
      "question": "Among all the closed events, which event has the highest spend-to-budget ratio?",
      "evidence": "closed events refers to event_name where status = 'Closed'; highest spend-to budget ratio refers to MAX(DIVIDE(spent, amount))",
      "generated_sql": "SELECT T1.event_name FROM event T1 INNER JOIN budget T2 ON T1.event_id = T2.link_to_event WHERE T1.status = 'Closed' GROUP BY T1.event_id, T1.event_name ORDER BY CAST(SUM(T2.spent) AS DOUBLE) / SUM(T2.amount) DESC LIMIT 1",
      "ground_truth_sql": "SELECT\n  `T2`.`event_name`\nFROM `budget` AS `T1`\nINNER JOIN `event` AS `T2`\n  ON `T1`.`link_to_event` = `T2`.`event_id`\nWHERE\n  `T2`.`status` = 'Closed'\nORDER BY\n  `T1`.`spent` / `T1`.`amount` DESC\nLIMIT 1",
      "error_category": "语义理解错误",
      "error_reason": "生成的 SQL 将 spend-to-budget ratio 理解为总花费除以总预算（SUM(T2.spent) / SUM(T2.amount)），而证据明确指出 'highest spend-to budget ratio refers to MAX(DIVIDE(spent, amount))'，即 ratio 应为 spent/amount 的比率，并取最大值。生成的 SQL 使用了 GROUP BY 和聚合函数，计算每个 event 的总比率，这可能不符合问题意图，因为问题要求的是 'Among all the closed events' 中最高比率的 event，而证据中的 MAX(DIVIDE(spent, amount)) 暗示直接计算每个 budget 记录的 spent/amount 比率并取全局最大值。预期的 SQL 直接使用 budget 表的 spent 和 amount 字段计算比率，并通过 ORDER BY DESC LIMIT 1 实现类似 MAX 的效果，更符合证据描述。",
      "suggested_fix": "根据证据，spend-to-budget ratio 应为 spent/amount 的比率。建议修改 SQL 以直接计算每个 budget 记录的 spent/amount 比率，然后排序取最高。具体来说，移除 GROUP BY 和聚合函数，使用类似预期的 SQL 结构：SELECT event_name FROM budget INNER JOIN event ON budget.link_to_event = event.event_id WHERE event.status = 'Closed' ORDER BY budget.spent / budget.amount DESC LIMIT 1。如果 budget 表有多个记录 per event，且需要 per event 的比率，则应先按 event 分组计算每个 event 的 spent/amount 比率（如 AVG 或直接除法），然后取最大值，但根据证据，预期的 SQL 更直接。确保 JOIN 条件和 WHERE 条件正确对应表别名。"
    },
    {
      "question_id": 1387,
      "difficulty": "moderate",
      "db_id": "student_club",
      "question": "Which student has been entrusted to manage the budget for the Yearly Kickoff?",
      "evidence": "name of students means the full name; full name refers to first_name, last_name;'Yearly Kickoff' is an event name;",
      "generated_sql": "SELECT DISTINCT CONCAT(m.first_name, ' ', m.last_name) AS full_name FROM member m INNER JOIN expense e ON m.member_id = e.link_to_member INNER JOIN budget b ON e.link_to_budget = b.budget_id INNER JOIN event ev ON b.link_to_event = ev.event_id WHERE ev.event_name = 'Yearly Kickoff'",
      "ground_truth_sql": "SELECT\n  `T4`.`first_name`,\n  `T4`.`last_name`\nFROM `event` AS `T1`\nINNER JOIN `budget` AS `T2`\n  ON `T1`.`event_id` = `T2`.`link_to_event`\nINNER JOIN `expense` AS `T3`\n  ON `T2`.`budget_id` = `T3`.`link_to_budget`\nINNER JOIN `member` AS `T4`\n  ON `T3`.`link_to_member` = `T4`.`member_id`\nWHERE\n  `T1`.`event_name` = 'Yearly Kickoff'",
      "error_category": "返回格式错误",
      "error_reason": "生成的SQL与预期的SQL在返回格式上存在差异。生成的SQL使用CONCAT函数将first_name和last_name组合成一个全名列（full_name），而预期的SQL直接返回first_name和last_name作为两个单独的列。此外，生成的SQL使用了DISTINCT关键字，而预期的SQL没有，这可能导致结果集不同（例如，如果存在重复行，生成的SQL会去重，而预期的SQL可能返回所有行）。虽然用户问题未明确指定返回格式，但预期的SQL作为参考标准，其返回两个列更符合证据中'full name refers to first_name, last_name'的描述，且未使用DISTINCT，暗示可能期望返回所有相关学生。",
      "suggested_fix": "修改生成的SQL的SELECT子句，直接选择first_name和last_name作为两个列，而不是使用CONCAT。同时，根据用户问题意图决定是否保留DISTINCT：如果问题期望唯一学生（如'Which student'可能暗示单个学生），则保留DISTINCT或使用其他方法确保唯一性；如果期望所有相关学生，则移除DISTINCT。建议参考预期的SQL结构：SELECT `T4`.`first_name`, `T4`.`last_name` FROM ... WHERE ...，并确保连接逻辑一致。"
    },
    {
      "question_id": 1399,
      "difficulty": "moderate",
      "db_id": "student_club",
      "question": "Did Maya Mclean attend the 'Women's Soccer' event?",
      "evidence": "Maya Mclean is the full name; full name refers to first_name, last_name; 'Women's Soccer' is an event_name",
      "generated_sql": "SELECT EXISTS (SELECT 1 FROM attendance a JOIN member m ON a.link_to_member = m.member_id JOIN event e ON a.link_to_event = e.event_id WHERE m.first_name = 'Maya' AND m.last_name = 'Mclean' AND e.event_name = 'Women''s Soccer') AS attended",
      "ground_truth_sql": "SELECT\n  CASE WHEN `T3`.`event_name` = 'Women''s Soccer' THEN 'YES' END AS `result`\nFROM `member` AS `T1`\nINNER JOIN `attendance` AS `T2`\n  ON `T1`.`member_id` = `T2`.`link_to_member`\nINNER JOIN `event` AS `T3`\n  ON `T2`.`link_to_event` = `T3`.`event_id`\nWHERE\n  `T1`.`first_name` = 'Maya' AND `T1`.`last_name` = 'Mclean'",
      "error_category": "返回格式错误",
      "error_reason": "生成的 SQL 使用 EXISTS 子查询返回布尔值（true/false），而预期的 SQL 使用 CASE 语句返回字符串 'YES' 或 NULL。此外，生成的 SQL 在 WHERE 子句中过滤了事件名称，直接检查存在性，而预期的 SQL 在 SELECT 中使用 CASE 语句过滤事件名称，可能导致返回多行（如果 Maya Mclean 参加了多个事件）。虽然两者都试图回答是/否问题，但输出格式和逻辑结构不同，生成的 SQL 不符合预期的输出格式。",
      "suggested_fix": "修改生成的 SQL 以匹配预期的输出格式。建议使用 CASE 语句返回字符串 'YES'，并在 WHERE 子句中只过滤成员姓名，不过滤事件名称。例如：SELECT CASE WHEN e.event_name = 'Women''s Soccer' THEN 'YES' END AS result FROM attendance a JOIN member m ON a.link_to_member = m.member_id JOIN event e ON a.link_to_event = e.event_id WHERE m.first_name = 'Maya' AND m.last_name = 'Mclean'。如果担心返回多行，可以添加 LIMIT 1 或使用聚合函数（如 MAX）来确保只返回一个结果。"
    },
    {
      "question_id": 1404,
      "difficulty": "moderate",
      "db_id": "student_club",
      "question": "Identify the type of expenses and their total value approved for 'October Meeting' event.",
      "evidence": "total value refers to SUM(cost); 'October Meeting' is an event name;",
      "generated_sql": "SELECT budget.category, SUM(expense.cost) as total_value FROM event INNER JOIN budget ON event.event_id = budget.link_to_event INNER JOIN expense ON budget.budget_id = expense.link_to_budget WHERE event.event_name = 'October Meeting' AND expense.approved = 'true' GROUP BY budget.category",
      "ground_truth_sql": "SELECT `T1`.`type`, SUM(`T3`.`cost`) AS `total_cost` FROM `event` AS `T1` INNER JOIN `budget` AS `T2` ON `T1`.`event_id` = `T2`.`link_to_event` INNER JOIN `expense` AS `T3` ON `T2`.`budget_id` = `T3`.`link_to_budget` WHERE `T1`.`event_name` = 'October Meeting' GROUP BY `T1`.`type`",
      "error_category": "语义理解错误",
      "error_reason": "生成的 SQL 存在两个主要语义理解错误：1. 分组列错误：用户问题要求 'type of expenses'，但生成的 SQL 使用了 budget.category 作为分组和选择列，而预期的 SQL 使用了 event.type。这表明对问题意图的理解有偏差，可能将 'expenses' 误解为预算类别，但实际意图是按事件类型分组。2. 条件错误：生成的 SQL 添加了 expense.approved = 'true' 条件，但预期的 SQL 中没有此条件。问题中 'approved for October Meeting event' 可能指事件本身是批准的，而非每个费用项需要批准，因此添加此条件不符合问题意图。",
      "suggested_fix": "1. 修正分组和选择列：将 budget.category 替换为 event.type（或预期的 T1.type），以正确反映 'type of expenses' 的意图。2. 移除不必要的条件：删除 WHERE 子句中的 expense.approved = 'true'，只保留 event.event_name = 'October Meeting'。3. 优化 SQL 结构：考虑使用表别名（如 T1, T2, T3）以提高可读性和维护性，但这不是必须的。"
    },
    {
      "question_id": 1422,
      "difficulty": "simple",
      "db_id": "student_club",
      "question": "State the category of events were held at MU 215.",
      "evidence": "'MU 215' is the location of event; ",
      "generated_sql": "SELECT DISTINCT type FROM event WHERE location = 'MU 215'",
      "ground_truth_sql": "SELECT DISTINCT\n  `T2`.`category`\nFROM `event` AS `T1`\nINNER JOIN `budget` AS `T2`\n  ON `T1`.`event_id` = `T2`.`link_to_event`\nWHERE\n  `T1`.`location` = 'MU 215'",
      "error_category": "语义理解错误",
      "error_reason": "用户问题要求获取在MU 215举行的活动的类别（category），但生成的SQL错误地从event表中选择了type字段，而预期的SQL显示类别信息存储在budget表中，需要通过INNER JOIN连接event和budget表来获取。这表明系统误解了问题意图和数据库模式：问题中的'category'可能对应budget表中的category字段，而非event表中的type字段。生成的SQL直接查询event表，忽略了表之间的关联，导致返回的字段（type）与问题要求的字段（category）不匹配。",
      "suggested_fix": "系统应分析数据库模式，理解表之间的关系。对于此类问题，需检查活动的类别信息是否存储在其他表中（如budget表），并使用适当的JOIN语句连接相关表。在生成SQL时，确保准确映射问题中的关键词（如'category'）到正确的数据库字段，并验证字段名和表结构是否匹配。建议在生成SQL前，先查询数据库模式或使用外部知识来确认字段位置。"
    },
    {
      "question_id": 1427,
      "difficulty": "moderate",
      "db_id": "student_club",
      "question": "What are the budget category of the events located at MU 215 and a guest speaker type with a 0 budget spent?",
      "evidence": "budget category refers to category; events located at refers to location; type = 'Guest Speaker'; 0 budget spent refers to spent = 0; ",
      "generated_sql": "SELECT budget.category FROM budget INNER JOIN event ON budget.link_to_event = event.event_id WHERE event.location = 'MU 215' AND event.type = 'Guest Speaker' AND budget.spent = 0",
      "ground_truth_sql": "SELECT DISTINCT\n  `T2`.`category`,\n  `T1`.`type`\nFROM `event` AS `T1`\nINNER JOIN `budget` AS `T2`\n  ON `T1`.`event_id` = `T2`.`link_to_event`\nWHERE\n  `T1`.`location` = 'MU 215' AND `T2`.`spent` = 0 AND `T1`.`type` = 'Guest Speaker'",
      "error_category": "返回格式错误",
      "error_reason": "生成的SQL只返回了预算类别（budget.category），但用户问题要求返回预算类别和事件类型（guest speaker type）。预期SQL返回两列：category和type，而生成的SQL只返回一列。这导致输出结果不完整，不符合用户问题的意图。尽管JOIN和WHERE条件正确，但SELECT子句遗漏了type列。",
      "suggested_fix": "修改SELECT子句，添加event.type列。例如：SELECT budget.category, event.type FROM budget INNER JOIN event ON budget.link_to_event = event.event_id WHERE event.location = 'MU 215' AND event.type = 'Guest Speaker' AND budget.spent = 0。如果预期结果需要去重（如预期SQL所示），可以添加DISTINCT关键字。"
    },
    {
      "question_id": 1432,
      "difficulty": "moderate",
      "db_id": "student_club",
      "question": "Among the members with t-shirt size of medium, what is the percentage of the amount 50 received by the Student_Club?",
      "evidence": "t_shirt_size = 'Medium' where position = 'Member'; percentage = DIVIDE(COUNT(amount = 50), COUNT(member_id)) * 100",
      "generated_sql": "SELECT (CAST(COUNT(DISTINCT CASE WHEN income.source = 'Student_Club' AND income.amount = 50 THEN member.member_id END) AS DOUBLE) / CAST(COUNT(DISTINCT member.member_id) AS DOUBLE)) * 100 AS percentage FROM member LEFT JOIN income ON member.member_id = income.link_to_member WHERE member.t_shirt_size = 'Medium'",
      "ground_truth_sql": "SELECT\n  CAST(SUM(CASE WHEN `T2`.`amount` = 50 THEN 1.0 ELSE 0 END) AS DOUBLE) * 100 / COUNT(`T2`.`income_id`)\nFROM `member` AS `T1`\nINNER JOIN `income` AS `T2`\n  ON `T1`.`member_id` = `T2`.`link_to_member`\nWHERE\n  `T1`.`position` = 'Member' AND `T1`.`t_shirt_size` = 'Medium'",
      "error_category": "语义理解错误",
      "error_reason": "生成的 SQL 遗漏了证据中明确指定的 `position = 'Member'` 过滤条件。证据指出 't_shirt_size = 'Medium' where position = 'Member''，这意味着查询应仅限于 position 为 'Member' 的成员。生成的 SQL 的 WHERE 子句只包含 `member.t_shirt_size = 'Medium'`，导致可能包括非成员（如管理员或其他角色），从而影响百分比计算的准确性。此外，生成的 SQL 使用了 LEFT JOIN，而预期的 SQL 使用了 INNER JOIN，这可能导致逻辑差异（例如，包括没有收入记录的成员），但主要错误是语义理解不完整，未能正确捕捉问题意图。",
      "suggested_fix": "在 WHERE 子句中添加 `member.position = 'Member'` 条件，以确保仅查询 position 为 'Member' 的成员。同时，重新评估 JOIN 类型：如果问题只关心有收入记录的成员（如预期 SQL 所示），则使用 INNER JOIN；否则，保留 LEFT JOIN 以包括所有成员。此外，确保计算逻辑与证据一致：分母应为成员数（COUNT(member_id)），分子应为收到金额 50 且来源为 'Student_Club' 的成员数。建议修改后的 SQL 类似于：`SELECT (CAST(COUNT(DISTINCT CASE WHEN income.source = 'Student_Club' AND income.amount = 50 THEN member.member_id END) AS DOUBLE) / CAST(COUNT(DISTINCT member.member_id) AS DOUBLE)) * 100 AS percentage FROM member LEFT JOIN income ON member.member_id = income.link_to_member WHERE member.t_shirt_size = 'Medium' AND member.position = 'Member'`。"
    },
    {
      "question_id": 1435,
      "difficulty": "moderate",
      "db_id": "student_club",
      "question": "List the names of closed event as \"game\" that was closed from 3/15/2019 to 3/20/2020.",
      "evidence": "name of events refers event_name; game event that was closed refers to type = 'Game' where status = 'Closed'; event_date BETWEEN '2019-03-15' and '2020-03-20'; ",
      "generated_sql": "SELECT event_name FROM event WHERE type = 'Game' AND status = 'Closed' AND event_date BETWEEN '2019-03-15' AND '2020-03-20'",
      "ground_truth_sql": "SELECT DISTINCT\n  `event_name`\nFROM `event`\nWHERE\n  `type` = 'Game'\n  AND DATE(SUBSTR(`event_date`, 1, 10)) BETWEEN '2019-03-15' AND '2020-03-20'\n  AND `status` = 'Closed'",
      "error_category": "日期格式错误",
      "error_reason": "生成的SQL直接使用 event_date 与日期字符串进行比较，但根据预期的SQL，event_date 字段可能存储为包含时间部分的字符串（如 '2019-03-15 10:00:00'），需要先提取日期部分才能正确比较。预期的SQL使用 DATE(SUBSTR(event_date, 1, 10)) 来确保只比较日期部分，忽略时间。此外，生成的SQL缺少 DISTINCT 关键字，而预期的SQL有，这可能导致返回重复的事件名称，不符合用户可能期望的唯一列表。",
      "suggested_fix": "修改日期比较部分，使用 DATE(SUBSTR(event_date, 1, 10)) 或类似函数（如 DATE(event_date)）来处理 event_date 字段，确保只比较日期部分。同时，添加 DISTINCT 关键字以返回唯一的事件名称。例如：SELECT DISTINCT event_name FROM event WHERE type = 'Game' AND status = 'Closed' AND DATE(SUBSTR(event_date, 1, 10)) BETWEEN '2019-03-15' AND '2020-03-20'。"
    },
    {
      "question_id": 1457,
      "difficulty": "challenging",
      "db_id": "student_club",
      "question": "Give the full name and contact number of members who had to spend more than average on each expense.",
      "evidence": "full name refers to first_name, last_name; contact number refers to phone; had spent more than average on each expense refers to cost > AVG(cost)",
      "generated_sql": "SELECT m.first_name, m.last_name, m.phone FROM member m WHERE m.member_id IN (SELECT e.link_to_member FROM expense e GROUP BY e.link_to_member HAVING MIN(e.cost) > (SELECT AVG(cost) FROM expense))",
      "ground_truth_sql": "SELECT DISTINCT\n  `T3`.`first_name`,\n  `T3`.`last_name`,\n  `T3`.`phone`\nFROM `expense` AS `T1`\nINNER JOIN `budget` AS `T2`\n  ON `T1`.`link_to_budget` = `T2`.`budget_id`\nINNER JOIN `member` AS `T3`\n  ON `T3`.`member_id` = `T1`.`link_to_member`\nWHERE\n  `T1`.`cost` > (\n    SELECT\n      AVG(`T1`.`cost`)\n    FROM `expense` AS `T1`\n    INNER JOIN `budget` AS `T2`\n      ON `T1`.`link_to_budget` = `T2`.`budget_id`\n    INNER JOIN `member` AS `T3`\n      ON `T3`.`member_id` = `T1`.`link_to_member`\n  )",
      "error_category": "SQL逻辑错误",
      "error_reason": "生成的SQL在逻辑上未能正确实现用户问题的意图。用户问题要求找出那些在每个支出上花费超过平均值的成员，证据明确指示条件为'cost > AVG(cost)'。然而，生成的SQL使用了MIN(e.cost) > AVG(cost)，这改变了逻辑：它检查每个成员的最小支出是否大于所有支出的平均值，而不是每个支出的成本大于平均值。这可能导致错误的结果，例如，如果一个成员有多个支出，其中一些大于平均值、一些小于，但最小支出大于平均值，则所有支出都大于平均值，这比用户意图更严格；反之，如果用户意图是成员有支出大于平均值即可，则生成的SQL可能遗漏符合条件的成员。此外，生成的SQL忽略了JOIN budget表，而预期的SQL使用了budget表，这可能是因为数据库模式要求通过budget表来访问或过滤expense数据，生成的SQL缺少必要的JOIN可能导致数据不完整或错误。",
      "suggested_fix": "1. 修正逻辑条件：将子查询中的HAVING MIN(e.cost) > AVG(cost)改为WHERE e.cost > (SELECT AVG(cost) FROM expense)，以直接比较每个支出的成本与全局平均值。2. 添加必要的JOIN：根据数据库模式，可能需要JOIN budget表，例如使用INNER JOIN budget ON expense.link_to_budget = budget.budget_id，以确保数据完整性。3. 使用DISTINCT：在SELECT子句中添加DISTINCT，以避免因JOIN或多个支出导致的重复行。改进后的SQL示例：SELECT DISTINCT m.first_name, m.last_name, m.phone FROM member m INNER JOIN expense e ON m.member_id = e.link_to_member INNER JOIN budget b ON e.link_to_budget = b.budget_id WHERE e.cost > (SELECT AVG(cost) FROM expense)。"
    },
    {
      "question_id": 1464,
      "difficulty": "challenging",
      "db_id": "student_club",
      "question": "Write the full names of students who received funds on the date of 9/9/2019 and include the amount received.",
      "evidence": "full name refers to first_name, last_name, amount of funds received refers to amount, received funds on date refers to date_received",
      "generated_sql": "SELECT m.first_name, m.last_name, i.amount FROM income i INNER JOIN member m ON i.link_to_member = m.member_id WHERE i.date_received = '9/9/2019'",
      "ground_truth_sql": "SELECT DISTINCT\n  `T3`.`first_name`,\n  `T3`.`last_name`,\n  `T4`.`amount`\nFROM `event` AS `T1`\nINNER JOIN `attendance` AS `T2`\n  ON `T1`.`event_id` = `T2`.`link_to_event`\nINNER JOIN `member` AS `T3`\n  ON `T3`.`member_id` = `T2`.`link_to_member`\nINNER JOIN `income` AS `T4`\n  ON `T4`.`link_to_member` = `T3`.`member_id`\nWHERE\n  `T4`.`date_received` = '2019-09-09'",
      "error_category": "语义理解错误",
      "error_reason": "生成的SQL对问题意图的理解存在偏差。用户问题要求找出在特定日期（9/9/2019）收到资金的学生，但生成的SQL仅通过income表和member表直接连接，忽略了数据库中可能存在的其他关联表（如event和attendance）。预期的SQL显示，资金接收可能与事件相关联，需要通过attendance表来关联学生和事件，再连接income表。此外，生成的SQL使用日期格式'9/9/2019'，而数据库可能期望标准格式'2019-09-09'，这可能导致查询失败或不返回结果。生成的SQL也没有使用DISTINCT，可能返回重复的学生记录（如果同一学生在同一天收到多次资金）。",
      "suggested_fix": "1. 修正日期格式：将日期条件改为'2019-09-09'以匹配数据库存储格式。2. 修正连接逻辑：根据数据库结构，使用INNER JOIN连接event、attendance、member和income表，确保正确关联学生和资金接收事件。参考预期的SQL，添加必要的JOIN条件（如event.event_id = attendance.link_to_event和attendance.link_to_member = member.member_id）。3. 添加DISTINCT关键字：在SELECT子句中使用DISTINCT以避免重复记录。4. 验证字段映射：确保使用first_name、last_name和amount字段。最终SQL应类似于预期的SQL，但根据实际表名和字段名调整。"
    },
    {
      "question_id": 1149,
      "difficulty": "moderate",
      "db_id": "thrombosis_prediction",
      "question": "Are there more in-patient or outpatient who were male? What is the deviation in percentage?",
      "evidence": "male refers to SEX = 'M'; in-patient refers to Admission = '+'; outpatient refers to Admission = '-'; percentage = DIVIDE(COUNT(ID) where SEX = 'M' and Admission = '+', COUNT(ID) where SEX  = 'M' and Admission = '-')",
      "generated_sql": "SELECT COUNT(CASE WHEN Admission = '+' THEN 1 END) AS in_patient_count, COUNT(CASE WHEN Admission = '-' THEN 1 END) AS out_patient_count, COUNT(CASE WHEN Admission = '+' THEN 1 END) / COUNT(CASE WHEN Admission = '-' THEN 1 END) AS deviation_percentage FROM Patient WHERE SEX = 'M'",
      "ground_truth_sql": "SELECT\n  CAST(SUM(CASE WHEN `Admission` = '+' THEN 1 ELSE 0 END) AS DOUBLE) * 100 / SUM(CASE WHEN `Admission` = '-' THEN 1 ELSE 0 END)\nFROM `Patient`\nWHERE\n  `SEX` = 'M'",
      "error_category": "返回格式错误",
      "error_reason": "生成的SQL返回了三个列（in_patient_count, out_patient_count, deviation_percentage），而预期的SQL只返回一个列（偏差百分比）。用户问题要求计算偏差百分比，但生成的SQL额外返回了住院患者和门诊患者的计数，这不符合预期的返回格式。此外，deviation_percentage的计算没有乘以100，导致结果为小数而非百分比形式，且使用整数除法可能丢失精度。",
      "suggested_fix": "修改SQL以只返回一个列：偏差百分比。使用浮点数除法并乘以100，例如：SELECT CAST(SUM(CASE WHEN `Admission` = '+' THEN 1 ELSE 0 END) AS DOUBLE) * 100 / SUM(CASE WHEN `Admission` = '-' THEN 1 ELSE 0 END) FROM `Patient` WHERE `SEX` = 'M'。确保使用SUM和CAST AS DOUBLE来处理数值计算，避免整数除法错误。如果需要回答'哪个更多'，可以通过计算的百分比值推断（大于100%表示住院更多，小于100%表示门诊更多），但根据预期SQL，只返回百分比即可。"
    },
    {
      "question_id": 1152,
      "difficulty": "moderate",
      "db_id": "thrombosis_prediction",
      "question": "What is the ratio of outpatient to inpatient followed up treatment among all the 'SLE' diagnosed patient?",
      "evidence": "'SLE' diagnosed patient means Diagnosis = 'SLE'; inpatient refers to Admission = '+'; outpatient refers to Admission = '-'; calculation =  DIVIDE(COUNT(ID) where Diagnosis = 'SLE' and Admission = '+', COUNT(ID) where Diagnosis = 'SLE' and Admission = '-')",
      "generated_sql": "SELECT CAST(COUNT(CASE WHEN Admission = '-' THEN 1 END) AS DOUBLE) / NULLIF(COUNT(CASE WHEN Admission = '+' THEN 1 END), 0) AS outpatient_to_inpatient_ratio FROM Patient WHERE Diagnosis = 'SLE'",
      "ground_truth_sql": "SELECT\n  SUM(CASE WHEN `Admission` = '+' THEN 1 ELSE 0 END) / SUM(CASE WHEN `Admission` = '-' THEN 1 ELSE 0 END)\nFROM `Patient`\nWHERE\n  `Diagnosis` = 'SLE'",
      "error_category": "语义理解错误",
      "error_reason": "生成的SQL错误地将outpatient计数（Admission = '-'）作为分子，inpatient计数（Admission = '+'）作为分母，计算了outpatient/inpatient的比率。然而，根据证据，正确的计算应该是inpatient（Admission = '+'）除以outpatient（Admission = '-'），即inpatient/outpatient。用户问题中的'outpatient to inpatient ratio'可能被误解为outpatient divided by inpatient，但证据明确给出了计算公式：DIVIDE(COUNT(ID) where Diagnosis = 'SLE' and Admission = '+', COUNT(ID) where Diagnosis = 'SLE' and Admission = '-')，因此分子应为inpatient，分母应为outpatient。生成的SQL与证据不符，导致语义理解错误。",
      "suggested_fix": "修正分子分母的顺序，使其符合证据中的计算。建议使用以下SQL：SELECT SUM(CASE WHEN `Admission` = '+' THEN 1 ELSE 0 END) / NULLIF(SUM(CASE WHEN `Admission` = '-' THEN 1 ELSE 0 END), 0) AS outpatient_to_inpatient_ratio FROM `Patient` WHERE `Diagnosis` = 'SLE'。这里，分子是inpatient计数（Admission = '+'），分母是outpatient计数（Admission = '-'），并添加NULLIF处理分母为零的情况，避免除零错误。同时，使用SUM和CASE WHEN结构更清晰，确保计数准确。"
    },
    {
      "question_id": 1166,
      "difficulty": "simple",
      "db_id": "thrombosis_prediction",
      "question": "What are the symptoms observed by the youngest patient to ever did a medical examination? Identify their diagnosis.",
      "evidence": "The larger the birthday value, the younger the person is, and vice versa; symptoms observed refers to the symptoms is not NULL",
      "generated_sql": "SELECT e.Symptoms, p.Diagnosis FROM Examination e INNER JOIN Patient p ON e.ID = p.ID WHERE e.ID = (SELECT p2.ID FROM Patient p2 WHERE EXISTS (SELECT 1 FROM Examination e2 WHERE e2.ID = p2.ID) ORDER BY p2.Birthday DESC LIMIT 1) AND e.Symptoms IS NOT NULL",
      "ground_truth_sql": "SELECT\n  `T2`.`Symptoms`,\n  `T1`.`Diagnosis`\nFROM `Patient` AS `T1`\nINNER JOIN `Examination` AS `T2`\n  ON `T1`.`ID` = `T2`.`ID`\nWHERE\n  NOT `T2`.`Symptoms` IS NULL\nORDER BY\n  `T1`.`Birthday` DESC\nLIMIT 1",
      "error_category": "SQL逻辑错误",
      "error_reason": "生成的SQL逻辑复杂且存在潜在错误。首先，子查询试图找到有检查记录的最年轻患者ID，但主查询中`e.ID = (SELECT p2.ID ...)`假设检查表的ID字段是患者ID，这可能不成立（如果检查表有独立的ID）。即使假设成立，生成的SQL没有使用`LIMIT`，如果最年轻的患者有多个检查记录，会返回多行，而预期的SQL只返回一行。此外，子查询中的`EXISTS`条件是冗余的，因为主查询的连接和`e.Symptoms IS NOT NULL`已经确保了患者有检查记录。预期的SQL更直接：连接表后按生日降序排序，取第一条记录，且症状不为空，逻辑清晰且正确。",
      "suggested_fix": "简化SQL逻辑，直接连接Patient和Examination表，按Birthday降序排序，使用LIMIT 1，并确保症状不为空。例如：\nSELECT e.Symptoms, p.Diagnosis \nFROM Examination e \nINNER JOIN Patient p ON e.ID = p.ID \nWHERE e.Symptoms IS NOT NULL \nORDER BY p.Birthday DESC \nLIMIT 1"
    },
    {
      "question_id": 1168,
      "difficulty": "challenging",
      "db_id": "thrombosis_prediction",
      "question": "The oldest SJS patient's medical laboratory work was completed on what date, and what age was the patient when they initially arrived at the hospital?",
      "evidence": "The larger the birthday value, the younger the person is, and vice versa; 'SJS' refers to diagnosis; (SUBTRACT(year(`First Date`)), year(Birthday)); age of the patients when they initially arrived at the hospital refers to year(Birthday)",
      "generated_sql": "SELECT MAX(L.`Date`) AS Lab_Completion_Date, YEAR(P.`First Date`) - YEAR(P.Birthday) AS Age_At_Arrival FROM Patient P INNER JOIN Laboratory L ON P.ID = L.ID WHERE P.Diagnosis = 'SJS' GROUP BY P.ID, P.`First Date`, P.Birthday ORDER BY P.Birthday ASC LIMIT 1",
      "ground_truth_sql": "SELECT\n  `T1`.`Date`,\n  DATE_FORMAT(CAST(`T2`.`First Date` AS DATETIME), '%Y') - DATE_FORMAT(CAST(`T2`.`Birthday` AS DATETIME), '%Y'),\n  `T2`.`Birthday`\nFROM `Laboratory` AS `T1`\nINNER JOIN `Patient` AS `T2`\n  ON `T1`.`ID` = `T2`.`ID`\nWHERE\n  `T2`.`Diagnosis` = 'SJS' AND NOT `T2`.`Birthday` IS NULL\nORDER BY\n  `T2`.`Birthday` ASC\nLIMIT 1",
      "error_category": "SQL逻辑错误",
      "error_reason": "生成的SQL使用了GROUP BY和MAX函数，这可能导致返回最年长患者的最新实验室完成日期，而不是问题可能期望的单个实验室日期。预期的SQL没有使用聚合，直接选择实验室日期，通过ORDER BY和LIMIT 1来获取最年长患者的记录。此外，生成的SQL没有处理Birthday为NULL的情况，而预期的SQL有相应的过滤。生成的SQL返回两列，而预期的SQL返回三列（包括生日），导致返回格式不匹配。日期处理上，生成的SQL直接使用YEAR函数，而预期的SQL使用DATE_FORMAT和CAST，更健壮地处理日期格式。",
      "suggested_fix": "移除GROUP BY子句和MAX函数，直接选择Laboratory表的Date字段。添加WHERE条件排除Birthday为NULL的记录。使用DATE_FORMAT和CAST来确保日期字段正确转换为年份进行计算。确保返回的列与预期一致，包括生日列（如果需要）。修改后的SQL应类似于预期的SQL：SELECT `T1`.`Date`, DATE_FORMAT(CAST(`T2`.`First Date` AS DATETIME), '%Y') - DATE_FORMAT(CAST(`T2`.`Birthday` AS DATETIME), '%Y'), `T2`.`Birthday` FROM `Laboratory` AS `T1` INNER JOIN `Patient` AS `T2` ON `T1`.`ID` = `T2`.`ID` WHERE `T2`.`Diagnosis` = 'SJS' AND NOT `T2`.`Birthday` IS NULL ORDER BY `T2`.`Birthday` ASC LIMIT 1"
    },
    {
      "question_id": 1169,
      "difficulty": "challenging",
      "db_id": "thrombosis_prediction",
      "question": "What is the ratio of male to female patients among all those with abnormal uric acid counts?",
      "evidence": "male refers to SEX = 'M'; female refers to SEX = 'F'; abnormal uric acid refers to UA < = '8.0' where SEX = 'M', UA < = '6.5' where SEX = 'F'; calculation = DIVIDE(SUM(UA <= '8.0' and SEX = 'M'), SUM(UA <= '6.5 and SEX = 'F'))",
      "generated_sql": "SELECT CAST(COUNT(DISTINCT CASE WHEN P.SEX = 'M' AND L.UA <= 8.0 THEN P.ID END) AS DOUBLE) / COUNT(DISTINCT CASE WHEN P.SEX = 'F' AND L.UA <= 6.5 THEN P.ID END) AS ratio FROM Laboratory L INNER JOIN Patient P ON L.ID = P.ID WHERE (P.SEX = 'M' AND L.UA <= 8.0) OR (P.SEX = 'F' AND L.UA <= 6.5)",
      "ground_truth_sql": "SELECT\n  CAST(SUM(CASE WHEN `T2`.`UA` <= 8.0 AND `T1`.`SEX` = 'M' THEN 1 ELSE 0 END) AS DOUBLE) / SUM(CASE WHEN `T2`.`UA` <= 6.5 AND `T1`.`SEX` = 'F' THEN 1 ELSE 0 END)\nFROM `Patient` AS `T1`\nINNER JOIN `Laboratory` AS `T2`\n  ON `T1`.`ID` = `T2`.`ID`",
      "error_category": "SQL逻辑错误",
      "error_reason": "生成的SQL使用COUNT(DISTINCT CASE WHEN ...)计算患者数，而预期的SQL使用SUM(CASE WHEN ... THEN 1 ELSE 0 END)计算满足条件的记录数。如果每个患者有多条实验室记录，COUNT(DISTINCT)会去重，而SUM不会，导致分子和分母的计数方式不一致，从而影响比例计算。此外，生成的SQL中的WHERE子句是多余的，因为CASE WHEN条件已经限定了尿酸异常患者，但WHERE子句不会导致错误，只是使查询更复杂。",
      "suggested_fix": "修改生成的SQL以匹配预期的SQL：使用SUM(CASE WHEN ... THEN 1 ELSE 0 END)替代COUNT(DISTINCT CASE WHEN ...)，并移除不必要的WHERE子句。具体修改为：SELECT CAST(SUM(CASE WHEN P.SEX = 'M' AND L.UA <= 8.0 THEN 1 ELSE 0 END) AS DOUBLE) / SUM(CASE WHEN P.SEX = 'F' AND L.UA <= 6.5 THEN 1 ELSE 0 END) AS ratio FROM Laboratory L INNER JOIN Patient P ON L.ID = P.ID。"
    },
    {
      "question_id": 1175,
      "difficulty": "moderate",
      "db_id": "thrombosis_prediction",
      "question": "How old was the patient who had the highest hemoglobin count at the time of the examination, and what is the doctor's diagnosis?",
      "evidence": "How old the patient refers to SUBTRACT(year(`Examination Date`), year(Birthday)); the highest hemoglobin count refers to MAX(HGB)",
      "generated_sql": "SELECT (YEAR(Examination.`Examination Date`) - YEAR(Patient.Birthday)) AS Age, Examination.Diagnosis FROM Laboratory INNER JOIN Examination ON Laboratory.ID = Examination.ID AND Laboratory.Date = Examination.`Examination Date` INNER JOIN Patient ON Laboratory.ID = Patient.ID WHERE Laboratory.HGB = (SELECT MAX(HGB) FROM Laboratory) ORDER BY Laboratory.HGB DESC LIMIT 1",
      "ground_truth_sql": "SELECT\n  DATE_FORMAT(CAST(`T2`.`Date` AS DATETIME), '%Y') - DATE_FORMAT(CAST(`T1`.`Birthday` AS DATETIME), '%Y'),\n  `T1`.`Diagnosis`\nFROM `Patient` AS `T1`\nINNER JOIN `Laboratory` AS `T2`\n  ON `T1`.`ID` = `T2`.`ID`\nORDER BY\n  `T2`.`HGB` DESC\nLIMIT 1",
      "error_category": "SQL逻辑错误",
      "error_reason": "生成的SQL在表关系和查询逻辑上存在错误：1. 错误地使用了Examination表，而预期的SQL仅使用Patient和Laboratory表。年龄计算应基于检查日期，但检查日期可能存储在Laboratory.Date中，而非Examination.Examination Date。2. JOIN条件复杂且不必要：通过Laboratory.Date = Examination.Examination Date进行JOIN，但预期的SQL直接JOIN Patient和Laboratory，这可能导致数据冗余或错误关联。3. WHERE子句和ORDER BY组合冗余：使用WHERE Laboratory.HGB = (SELECT MAX(HGB) FROM Laboratory)过滤最高HGB，然后ORDER BY Laboratory.HGB DESC LIMIT 1，这可能导致多个行匹配时结果不准确；预期的SQL直接使用ORDER BY HGB DESC LIMIT 1更简洁高效。4. 诊断列来源错误：生成的SQL从Examination表选择Diagnosis，但预期的SQL从Patient表选择，这可能误解了诊断数据的存储位置（诊断可能关联到患者而非每次检查）。",
      "suggested_fix": "简化查询逻辑：1. 仅JOIN Patient和Laboratory表，移除Examination表。2. 使用ORDER BY Laboratory.HGB DESC LIMIT 1直接获取最高HGB的记录。3. 年龄计算改为YEAR(Laboratory.Date) - YEAR(Patient.Birthday)，以匹配证据中的年龄计算方式。4. 确保诊断列从正确的表选择：如果诊断存储在Patient表，则从Patient表选择；如果存储在Examination表，则需调整JOIN逻辑以包含Examination表，但根据预期SQL，诊断应在Patient表。最终查询应类似于：SELECT (YEAR(Laboratory.Date) - YEAR(Patient.Birthday)) AS Age, Patient.Diagnosis FROM Patient INNER JOIN Laboratory ON Patient.ID = Laboratory.ID ORDER BY Laboratory.HGB DESC LIMIT 1。"
    },
    {
      "question_id": 1179,
      "difficulty": "moderate",
      "db_id": "thrombosis_prediction",
      "question": "For the patient who was diagnosed with SLE on 1994/2/19, what was his/her anti-Cardiolipin antibody concentration status on 1993/11/12?",
      "evidence": "diagnosed with SLE refers to Diagnosis = 'SLE'; 1994/2/19 refers to Description = '1994-02-19'; anti-Cardiolipin refers to aCL IgM; 1993/11/12 refers to Examination Date = '1993/11/12'",
      "generated_sql": "SELECT `aCL IgM` FROM Examination WHERE ID = (SELECT ID FROM Patient WHERE Diagnosis = 'SLE' AND Description = '1994-02-19') AND `Examination Date` = '1993-11-12'",
      "ground_truth_sql": "SELECT\n  `aCL IgA`,\n  `aCL IgG`,\n  `aCL IgM`\nFROM `Examination`\nWHERE\n  `ID` IN (\n    SELECT\n      `ID`\n    FROM `Patient`\n    WHERE\n      `Diagnosis` = 'SLE' AND `Description` = '1994-02-19'\n  )\n  AND `Examination Date` = '1993-11-12'",
      "error_category": "SQL逻辑错误",
      "error_reason": "生成的SQL使用 `ID = (SELECT ID ...)`，这假设子查询只返回一个患者ID。如果数据库中有多个患者在1994/2/19被诊断为SLE，这将导致错误或只返回第一个匹配的ID，而预期的SQL使用 `ID IN (SELECT ID ...)` 更安全地处理多个ID。此外，用户问题中的 'anti-Cardiolipin antibody concentration status' 可能期望返回所有相关抗体亚型（IgA, IgG, IgM），但生成的SQL只选择了 `aCL IgM`，而预期的SQL返回了所有三个列。根据证据，'anti-Cardiolipin refers to aCL IgM'，但问题中的 'status' 可能暗示更全面的结果，导致语义理解偏差。",
      "suggested_fix": "1. 将子查询条件从 `ID = (SELECT ID ...)` 改为 `ID IN (SELECT ID ...)` 以正确处理可能的多个患者ID。2. 根据数据库模式和问题意图，检查是否应返回所有抗心磷脂抗体亚型（`aCL IgA`, `aCL IgG`, `aCL IgM`），如预期的SQL所示。如果证据不完整，建议验证数据库表结构以确定相关列。"
    },
    {
      "question_id": 1192,
      "difficulty": "challenging",
      "db_id": "thrombosis_prediction",
      "question": "List all patients who were followed up at the outpatient clinic who underwent a laboratory test in October 1991 and had a total blood bilirubin level within the normal range.",
      "evidence": "followed up at the outpatient clinic refers to Admission = '-'; laboratory test in April 1981 refers to Date like '1991-10%'; blood bilirubin level within the normal range refers to T-BIL < 2.0; ",
      "generated_sql": "SELECT DISTINCT Patient.ID FROM Patient INNER JOIN Laboratory ON Patient.ID = Laboratory.ID WHERE Patient.Admission = '-' AND Laboratory.Date LIKE '1991-10%' AND Laboratory.T-BIL < 2.0",
      "ground_truth_sql": "SELECT DISTINCT\n  `T1`.`ID`\nFROM `Patient` AS `T1`\nINNER JOIN `Laboratory` AS `T2`\n  ON `T1`.`ID` = `T2`.`ID`\nWHERE\n  `T1`.`Admission` = '-' AND `T2`.`T-BIL` < 2.0 AND `T2`.`Date` LIKE '1991-10-%'",
      "error_category": "SQL语法错误",
      "error_reason": "生成的SQL中，列名'T-BIL'包含连字符，但在SQL语句中未用反引号或引号括起来，导致语法错误。在SQL解析中，'Laboratory.T-BIL'会被错误地解释为减法操作（Laboratory.T 减去 BIL），而不是列引用，这会使SQL无法执行。此外，日期模式'LIKE '1991-10%''与预期的'LIKE '1991-10-%''略有差异，但根据证据，日期模式应为'1991-10%'，因此生成的SQL在日期模式上可能符合证据，但主要错误是语法错误。",
      "suggested_fix": "将包含特殊字符的列名用反引号（MySQL）或双引号（标准SQL）括起来，例如将'Laboratory.T-BIL'改为'Laboratory.`T-BIL`'或'Laboratory.\"T-BIL\"'。日期模式可以保持为'LIKE '1991-10%''，但为了与预期SQL一致，可以改为'LIKE '1991-10-%''以确保精确匹配。建议使用表别名（如T1和T2）来提高可读性和可维护性。"
    },
    {
      "question_id": 1205,
      "difficulty": "moderate",
      "db_id": "thrombosis_prediction",
      "question": "Was the patient with the number 57266's uric acid within a normal range?",
      "evidence": "uric acid within a normal range refers to UA > 8.0 and SEX = 'M'OR UA > 6.5 and SEX = 'F'",
      "generated_sql": "SELECT CASE WHEN p.SEX = 'M' AND l.UA > 8.0 THEN 'Yes' WHEN p.SEX = 'F' AND l.UA > 6.5 THEN 'Yes' ELSE 'No' END AS is_normal FROM Patient p INNER JOIN Laboratory l ON p.ID = l.ID WHERE p.ID = 57266 AND l.Date = (SELECT MAX(Date) FROM Laboratory WHERE ID = 57266)",
      "ground_truth_sql": "SELECT\n  CASE\n    WHEN (\n      `T1`.`SEX` = 'F' AND `T2`.`UA` > 6.5\n    )\n    OR (\n      `T1`.`SEX` = 'M' AND `T2`.`UA` > 8.0\n    )\n    THEN TRUE\n    ELSE FALSE\n  END\nFROM `Patient` AS `T1`\nINNER JOIN `Laboratory` AS `T2`\n  ON `T1`.`ID` = `T2`.`ID`\nWHERE\n  `T1`.`ID` = 57266",
      "error_category": "SQL逻辑错误",
      "error_reason": "生成的SQL添加了日期过滤条件`l.Date = (SELECT MAX(Date) FROM Laboratory WHERE ID = 57266)`，而用户问题和证据中均未指定时间点。用户问题仅询问患者57266的尿酸是否在正常范围内，未要求基于最新记录。预期的SQL没有日期过滤，仅基于ID进行连接和过滤。生成的SQL引入了额外的逻辑限制，可能导致结果不准确（例如，如果有多条实验室记录，只考虑最新的一条，而用户可能关心所有记录或未指定时间点）。此外，生成的SQL返回'Yes'/'No'，而预期的SQL返回TRUE/FALSE，但这不是核心错误，语义上可接受。",
      "suggested_fix": "移除日期过滤条件，使查询更符合用户问题的字面意图。修改后的SQL应类似于预期的SQL，仅基于ID进行连接和过滤。如果需要处理多条记录，应明确用户意图（例如，是否要求最新记录），但根据给定证据，正常范围定义仅涉及UA和SEX，因此无需日期过滤。建议使用以下结构：\nSELECT\n  CASE\n    WHEN (p.SEX = 'F' AND l.UA > 6.5) OR (p.SEX = 'M' AND l.UA > 8.0) THEN TRUE\n    ELSE FALSE\n  END AS is_normal\nFROM Patient p\nINNER JOIN Laboratory l ON p.ID = l.ID\nWHERE p.ID = 57266"
    },
    {
      "question_id": 1209,
      "difficulty": "moderate",
      "db_id": "thrombosis_prediction",
      "question": "Please provide the diagnosis of patients with ALT glutamic pylvic transaminase beyond the normal range by ascending order of their date of birth.",
      "evidence": "ALT glutamic pylvic transaminase beyond the normal range refers to GPT > 60; The larger the birthday value, the younger the person is, and vice versa; ",
      "generated_sql": "SELECT DISTINCT P.Diagnosis FROM Patient P INNER JOIN Laboratory L ON P.ID = L.ID WHERE L.GPT > 60 ORDER BY P.Birthday ASC",
      "ground_truth_sql": "SELECT `T1`.`Diagnosis`\nFROM `Patient` AS `T1`\nINNER JOIN `Laboratory` AS `T2`\n  ON `T1`.`ID` = `T2`.`ID`\nWHERE\n  `T2`.`GPT` > 60\nORDER BY\n  `T1`.`Birthday` ASC",
      "error_category": "SQL逻辑错误",
      "error_reason": "生成的SQL使用了DISTINCT关键字，而预期的SQL没有。这可能导致结果集不同：如果诊断字段有重复值，DISTINCT会去重，而预期的SQL可能期望返回所有记录（包括重复诊断）。用户问题要求提供诊断，但未明确指定是否去重；预期的SQL表明不需要DISTINCT，因此生成的SQL的DISTINCT可能改变了逻辑，导致返回的行数或内容与预期不符。",
      "suggested_fix": "移除DISTINCT关键字，使SQL更简洁并匹配预期逻辑。修改后的SQL应为：SELECT P.Diagnosis FROM Patient P INNER JOIN Laboratory L ON P.ID = L.ID WHERE L.GPT > 60 ORDER BY P.Birthday ASC。如果数据库设计要求去重，可根据实际情况调整，但基于给定信息，建议遵循预期SQL的格式。"
    },
    {
      "question_id": 1225,
      "difficulty": "moderate",
      "db_id": "thrombosis_prediction",
      "question": "List and group all patients by sex for total bilirubin (T-BIL) level not within the normal range.",
      "evidence": "total bilirubin (T-BIL) not within normal range refers to T-BIL > = 2.0",
      "generated_sql": "SELECT p.SEX, GROUP_CONCAT(DISTINCT p.ID) AS patient_ids FROM Patient p INNER JOIN Laboratory l ON p.ID = l.ID WHERE l.`T-BIL` >= 2.0 GROUP BY p.SEX",
      "ground_truth_sql": "SELECT T1.ID, T1.SEX FROM Patient AS T1 INNER JOIN Laboratory AS T2 ON T1.ID = T2.ID WHERE T2.`T-BIL` >= 2.0 GROUP BY T1.SEX, T1.ID",
      "error_category": "语义理解错误",
      "error_reason": "生成的 SQL 使用 GROUP_CONCAT 聚合患者 ID，按性别分组，而预期的 SQL 直接列出每个患者的 ID 和 SEX，并按性别和 ID 分组。用户问题要求 'group all patients by sex'，这可能意味着按性别分组显示患者，但不聚合。生成的 SQL 误解了 'group' 的意思，以为需要聚合（如使用 GROUP_CONCAT），而预期的 SQL 显示应该列出每个患者，分组条件（GROUP BY T1.SEX, T1.ID）可能用于确保唯一性或排序，但不改变行数。此外，生成的 SQL 返回的列（SEX 和 patient_ids）与预期的 SQL（ID 和 SEX）不同，导致输出格式不符合预期。",
      "suggested_fix": "修改生成的 SQL 以匹配预期的 SQL：移除 GROUP_CONCAT 聚合，选择 ID 和 SEX，并按 SEX 和 ID 分组。具体 SQL 为：SELECT T1.ID, T1.SEX FROM Patient AS T1 INNER JOIN Laboratory AS T2 ON T1.ID = T2.ID WHERE T2.`T-BIL` >= 2.0 GROUP BY T1.SEX, T1.ID。如果意图是列出每个患者，也可以考虑使用 ORDER BY T1.SEX 来按性别排序，但预期的 SQL 使用了 GROUP BY，因此应遵循预期的结构。"
    },
    {
      "question_id": 1227,
      "difficulty": "moderate",
      "db_id": "thrombosis_prediction",
      "question": "What is the average age of the male patient with high cholesterol?",
      "evidence": "average age = DIVIDE(SUM(SUBTRACT(YEAR(NOW()), YEAR(birthday))), COUNT(ID)); male patient refers to sex = 'M'; high cholesterol refers to `T-CHO` > = 250;",
      "generated_sql": "SELECT CAST(SUM(YEAR(NOW()) - YEAR(Birthday)) AS DOUBLE) / COUNT(ID) AS average_age FROM Patient WHERE SEX = 'M' AND ID IN (SELECT DISTINCT ID FROM Laboratory WHERE `T-CHO` >= 250)",
      "ground_truth_sql": "SELECT AVG(YEAR(CURRENT_DATE) - YEAR(T1.Birthday)) FROM Patient AS T1  INNER JOIN Laboratory AS T2 ON T1.ID = T2.ID  WHERE T2.`T-CHO` >= 250 AND T1.SEX = 'M'",
      "error_category": "SQL逻辑错误",
      "error_reason": "生成的SQL使用了子查询（ID IN (SELECT DISTINCT ID FROM Laboratory WHERE `T-CHO` >= 250)）来过滤有高胆固醇的患者，而预期的SQL使用了内连接（INNER JOIN）。虽然两者在逻辑上可能等价（都过滤出男性患者中高胆固醇的记录），但内连接是更标准和高效的方式，尤其是在处理关联表时。子查询可能引入不必要的复杂性，并且在性能上可能不如内连接。此外，生成的SQL显式使用SUM和COUNT计算平均值，而预期的SQL使用AVG函数，后者更简洁且符合常见实践。生成的SQL中CAST(SUM(...) AS DOUBLE)的转换不是错误，但增加了冗余。",
      "suggested_fix": "改用内连接方式，使用AVG函数计算平均年龄，并添加表别名以提高可读性。具体改进：将子查询替换为INNER JOIN，使用AVG(YEAR(CURRENT_DATE) - YEAR(T1.Birthday))，并确保连接条件正确（ON T1.ID = T2.ID）。同时，统一日期函数，使用CURRENT_DATE代替NOW()以保持一致性。"
    },
    {
      "question_id": 1241,
      "difficulty": "challenging",
      "db_id": "thrombosis_prediction",
      "question": "For patients with abnormal platelet level, state the number of patients with lower than normal range. How is it compare to the number of patients with higher than normal range?",
      "evidence": "abnormal platelet level refers to PLT <= 100 or PLT >= 400; platelet level lower than normal range refers to PLT < 100; calculation = SUBTRACT(SUM(PLT < 100), SUM(PLT > 400)); platelet level higher than normal range refers to PLT > 400;",
      "generated_sql": "SELECT (SELECT COUNT(DISTINCT ID) FROM Laboratory WHERE PLT < 100) AS LowerCount, (SELECT COUNT(DISTINCT ID) FROM Laboratory WHERE PLT > 400) AS HigherCount, (SELECT COUNT(DISTINCT ID) FROM Laboratory WHERE PLT < 100) - (SELECT COUNT(DISTINCT ID) FROM Laboratory WHERE PLT > 400) AS Difference;",
      "ground_truth_sql": "SELECT\n  SUM(CASE WHEN `T2`.`PLT` <= 100 THEN 1 ELSE 0 END) - SUM(CASE WHEN `T2`.`PLT` >= 400 THEN 1 ELSE 0 END)\nFROM `Patient` AS `T1`\nINNER JOIN `Laboratory` AS `T2`\n  ON `T1`.`ID` = `T2`.`ID`",
      "error_category": "SQL逻辑错误",
      "error_reason": "生成的SQL存在多个逻辑错误：1. 缺少与Patient表的JOIN操作，导致可能计数非患者记录或数据不完整，而问题明确要求针对患者进行统计；2. 条件判断错误：生成的SQL使用PLT < 100和PLT > 400，但预期的SQL使用PLT <= 100和PLT >= 400，根据证据，低于正常范围是PLT < 100，高于正常范围是PLT > 400，但预期的SQL可能基于不同逻辑（如包括边界值），导致数值计算偏差；3. 返回格式不匹配：生成的SQL返回三列（LowerCount、HigherCount、Difference），而预期的SQL仅返回一列Difference，不符合问题要求的比较输出格式。",
      "suggested_fix": "修改SQL以正确实现问题意图：1. 添加INNER JOIN连接Patient和Laboratory表，确保只统计患者数据；2. 使用正确的条件：根据证据，低于正常范围应为PLT < 100，高于正常范围应为PLT > 400，但需确认预期SQL的条件是否准确（如果预期SQL正确，则调整为PLT <= 100和PLT >= 400）；3. 调整输出格式：如果问题要求返回两个数量和比较，则应返回三列；如果仅需比较差值，则只返回Difference列。建议参考预期SQL的结构，但根据问题描述，可能需要返回LowerCount、HigherCount和Difference。"
    },
    {
      "question_id": 1242,
      "difficulty": "challenging",
      "db_id": "thrombosis_prediction",
      "question": "For laboratory examinations take in 1984, list all patients below 50 years old with normal platelet level.",
      "evidence": "laboratory examinations take in 1984 refers to YEAR(Date) = '1984'; below 50 years old = SUBTRACT(year(current_timestamp), year(Birthday)) < 50; normal platelet level refers to PLT between 100 and 400; ",
      "generated_sql": "SELECT DISTINCT Patient.ID FROM Laboratory INNER JOIN Patient ON Laboratory.ID = Patient.ID WHERE YEAR(Laboratory.Date) = 1984 AND (YEAR(CURDATE()) - YEAR(Patient.Birthday)) < 50 AND Laboratory.PLT BETWEEN 100 AND 400",
      "ground_truth_sql": "SELECT DISTINCT\n  `T1`.`ID`\nFROM `Patient` AS `T1`\nINNER JOIN `Laboratory` AS `T2`\n  ON `T1`.`ID` = `T2`.`ID`\nWHERE\n  `T2`.`PLT` BETWEEN 100 AND 400\n  AND DATE_FORMAT(CAST(`T2`.`Date` AS DATETIME), '%Y') - DATE_FORMAT(CAST(`T1`.`Birthday` AS DATETIME), '%Y') < 50\n  AND DATE_FORMAT(CAST(`T2`.`Date` AS DATETIME), '%Y') = '1984'",
      "error_category": "语义理解错误",
      "error_reason": "生成的 SQL 错误地将患者年龄计算基于当前日期（CURDATE()），而用户问题意图是计算在实验室检查日期时的年龄。证据中使用了 current_timestamp，但预期的 SQL 使用了实验室检查日期（T2.Date）。这导致年龄条件可能不准确，因为患者年龄随时间变化：如果患者在检查时年龄低于50岁，但现在年龄超过50岁，生成的 SQL 会错误地排除该患者；反之亦然。此外，年龄计算仅使用年份差（YEAR()函数），忽略了月份和日期，可能导致数值计算不精确，但核心错误是语义理解偏差。",
      "suggested_fix": "修改年龄计算部分，使用实验室检查日期而不是当前日期。例如，将 (YEAR(CURDATE()) - YEAR(Patient.Birthday)) < 50 改为 (YEAR(Laboratory.Date) - YEAR(Patient.Birthday)) < 50。同时，考虑使用更精确的日期差计算，如 DATE_FORMAT 函数（如预期的 SQL 所示），以确保年份计算准确。此外，确保日期格式处理一致，例如使用 DATE_FORMAT(CAST(Laboratory.Date AS DATETIME), '%Y') 来提取年份，避免潜在的类型不匹配问题。"
    },
    {
      "question_id": 1243,
      "difficulty": "challenging",
      "db_id": "thrombosis_prediction",
      "question": "For all patients who are older than 55 years old, what is the percentage of female who has abnormal prothrombin time (PT)?",
      "evidence": "older than 55 years old = SUBTRACT(year(current_timestamp), year(Birthday)) > 55; abnormal prothrombin time (PT) refers to PT > = 14; percentage = DIVIDE(SUM(PT > = 14 AND SEX = 'F'), SUM(PT > = 14)) * 100; female refers to sex = 'F'; ",
      "generated_sql": "SELECT (COUNT(DISTINCT CASE WHEN Laboratory.PT >= 14 AND Patient.SEX = 'F' THEN Patient.ID END) / COUNT(DISTINCT CASE WHEN Laboratory.PT >= 14 THEN Patient.ID END)) * 100 AS percentage FROM Patient INNER JOIN Laboratory ON Patient.ID = Laboratory.ID WHERE YEAR(CURDATE()) - YEAR(Birthday) > 55",
      "ground_truth_sql": "SELECT\n  CAST(SUM(CASE WHEN `T2`.`PT` >= 14 AND `T1`.`SEX` = 'F' THEN 1 ELSE 0 END) AS DOUBLE) * 100 / COUNT(CASE WHEN `T2`.`PT` >= 14 THEN 1 ELSE 0 END)\nFROM `Patient` AS `T1`\nINNER JOIN `Laboratory` AS `T2`\n  ON `T1`.`ID` = `T2`.`ID`\nWHERE\n  DATE_FORMAT(CAST(CURRENT_TIMESTAMP() AS DATETIME), '%Y') - DATE_FORMAT(CAST(`T1`.`Birthday` AS DATETIME), '%Y') > 55",
      "error_category": "语义理解错误",
      "error_reason": "生成的SQL使用了COUNT(DISTINCT ...)来计数患者ID，而证据中的百分比公式和预期的SQL使用记录级别的计数（SUM和COUNT without DISTINCT）。用户问题可能意图计算患者级别的百分比（即有异常PT的患者中女性患者的比例），但证据中的公式'percentage = DIVIDE(SUM(PT >= 14 AND SEX = 'F'), SUM(PT >= 14)) * 100'没有指定DISTINCT，暗示使用记录级别的计数。生成的SQL错误地添加了DISTINCT，导致计算逻辑与预期不符：生成的SQL计算患者级别的比例，而预期的SQL计算记录级别的比例。此外，生成的SQL的WHERE子句使用YEAR(CURDATE()) - YEAR(Birthday) > 55，虽然能计算年龄，但不如预期的SQL使用DATE_FORMAT和CAST更健壮，但这不是主要错误。",
      "suggested_fix": "根据证据中的百分比公式，应使用记录级别的计数。建议修改生成的SQL，移除COUNT(DISTINCT ...)，使用SUM(CASE WHEN ...) THEN 1 ELSE 0 END来计数满足条件的记录行数，如预期的SQL所示。具体修改：将分子改为SUM(CASE WHEN Laboratory.PT >= 14 AND Patient.SEX = 'F' THEN 1 ELSE 0 END)，分母改为COUNT(CASE WHEN Laboratory.PT >= 14 THEN 1 ELSE 0 END)，并确保除法时进行数值转换（如CAST为DOUBLE）。同时，日期处理可以优化为使用DATE_FORMAT或YEAR函数，但需确保Birthday字段为日期类型。如果意图是患者级别的百分比，则需要明确指定并调整公式，但基于给定的预期SQL，应匹配记录级别的计算。"
    },
    {
      "question_id": 1247,
      "difficulty": "challenging",
      "db_id": "thrombosis_prediction",
      "question": "Among the male patients who have a normal level of white blood cells, how many of them have an abnormal fibrinogen level?",
      "evidence": "male patients refers to Sex = 'M'; normal level of white blood cells refers to WBC > 3.5 and WBC <9.0; abnormal fibrinogen level refers to FG < = 150 or FG > = 450; Don't compute repetitive ones.",
      "generated_sql": "SELECT COUNT(DISTINCT p.ID) FROM Patient p INNER JOIN Laboratory l ON p.ID = l.ID WHERE p.Sex = 'M' AND l.WBC > 3.5 AND l.WBC < 9.0 AND (l.FG <= 150 OR l.FG >= 450)",
      "ground_truth_sql": "SELECT\n  COUNT(DISTINCT `T1`.`ID`)\nFROM `Patient` AS `T1`\nINNER JOIN `Laboratory` AS `T2`\n  ON `T1`.`ID` = `T2`.`ID`\nWHERE\n  `T2`.`FG` <= 150\n  OR `T2`.`FG` >= 450\n  AND `T2`.`WBC` > 3.5\n  AND `T2`.`WBC` < 9.0\n  AND `T1`.`SEX` = 'M'",
      "error_category": "SQL逻辑错误",
      "error_reason": "生成的SQL正确地使用了括号来分组纤维蛋白原异常的条件（(l.FG <= 150 OR l.FG >= 450)），确保所有条件（男性、白细胞正常、纤维蛋白原异常）同时满足，符合问题意图。然而，预期的SQL缺少括号，导致SQL运算符优先级问题（AND优先级高于OR），使得条件被解释为：T2.FG <= 150 OR (T2.FG >= 450 AND T2.WBC > 3.5 AND T2.WBC < 9.0 AND T1.SEX = 'M')。这会错误地包括纤维蛋白原 <= 150 但白细胞不正常或不是男性的患者，从而逻辑错误。生成的SQL在逻辑上正确，但不符合预期的SQL格式，而预期的SQL本身存在逻辑错误。",
      "suggested_fix": "在预期的SQL中，应添加括号来明确逻辑分组，例如：WHERE (T2.FG <= 150 OR T2.FG >= 450) AND T2.WBC > 3.5 AND T2.WBC < 9.0 AND T1.SEX = 'M'。生成的SQL已经正确，无需修改。建议在Text-to-SQL生成中始终注意运算符优先级，使用括号确保条件逻辑清晰。"
    },
    {
      "question_id": 1251,
      "difficulty": "simple",
      "db_id": "thrombosis_prediction",
      "question": "How many patients with an Ig G higher than normal?",
      "evidence": "Ig G higher than normal refers to IGG >= 2000; Should consider DISTINCT in the final result;",
      "generated_sql": "SELECT COUNT(DISTINCT ID) FROM Laboratory WHERE IGG >= 2000",
      "ground_truth_sql": "SELECT\n  COUNT(DISTINCT `T1`.`ID`)\nFROM `Patient` AS `T1`\nINNER JOIN `Laboratory` AS `T2`\n  ON `T1`.`ID` = `T2`.`ID`\nINNER JOIN `Examination` AS `T3`\n  ON `T3`.`ID` = `T2`.`ID`\nWHERE\n  `T2`.`IGG` >= 2000",
      "error_category": "SQL逻辑错误",
      "error_reason": "生成的SQL只查询Laboratory表并直接计数ID，而问题要求计数患者（patients）。预期的SQL需要JOIN Patient表（可能还有Examination表），这表明数据库结构中患者信息存储在Patient表中，Laboratory表仅存储实验室结果。生成的SQL可能错误地假设Laboratory表中的ID直接对应患者，但实际可能需要通过JOIN Patient表来确保计数的是患者，避免重复或遗漏。此外，预期的SQLJOIN了Examination表，可能因为数据库设计要求患者必须有检查记录，但用户问题未明确提及，这进一步凸显了生成的SQL在逻辑上不完整。",
      "suggested_fix": "根据数据库模式，修改SQL以JOIN Patient表。例如：SELECT COUNT(DISTINCT Patient.ID) FROM Patient INNER JOIN Laboratory ON Patient.ID = Laboratory.ID WHERE Laboratory.IGG >= 2000。如果Examination表是必需的（如预期SQL所示），则添加JOIN Examination ON Examination.ID = Laboratory.ID。建议先检查数据库表结构，确认患者ID的关联方式，确保查询准确反映问题意图。"
    },
    {
      "question_id": 1252,
      "difficulty": "moderate",
      "db_id": "thrombosis_prediction",
      "question": "Among the patients with a normal Ig G level, how many of them have symptoms?",
      "evidence": "normal Ig G level refers to IGG > 900 and IGG < 2000; have symptoms refers to Symptoms IS NOT NULL;",
      "generated_sql": "SELECT COUNT(DISTINCT P.ID) FROM Patient P INNER JOIN Laboratory L ON P.ID = L.ID INNER JOIN Examination E ON P.ID = E.ID WHERE L.IGG > 900 AND L.IGG < 2000 AND E.Symptoms IS NOT NULL",
      "ground_truth_sql": "SELECT\n  COUNT(`T1`.`ID`)\nFROM `Patient` AS `T1`\nINNER JOIN `Laboratory` AS `T2`\n  ON `T1`.`ID` = `T2`.`ID`\nINNER JOIN `Examination` AS `T3`\n  ON `T3`.`ID` = `T2`.`ID`\nWHERE\n  `T2`.`IGG` BETWEEN 900 AND 2000 AND NOT `T3`.`Symptoms` IS NULL",
      "error_category": "SQL逻辑错误",
      "error_reason": "生成的SQL在JOIN条件上存在逻辑错误。用户问题涉及患者、实验室和检查三个表，但生成的SQL直接将Patient表与Examination表连接（ON P.ID = E.ID），而预期的SQL显示Examination表应通过Laboratory表连接（ON T3.ID = T2.ID）。这可能导致连接错误，因为Examination表可能没有直接的Patient ID字段，或者连接条件不正确，从而影响计数结果。此外，生成的SQL使用了COUNT(DISTINCT P.ID)，这可能是多余的，如果JOIN条件正确且数据唯一，但使用DISTINCT可以防止重复计数。WHERE条件中，生成的SQL使用了严格不等式（L.IGG > 900 AND L.IGG < 2000），这符合证据（normal Ig G level refers to IGG > 900 and IGG < 2000），而预期的SQL使用了BETWEEN（包括边界值），这可能不符合证据。",
      "suggested_fix": "1. 修正JOIN条件：将Examination表的连接改为通过Laboratory表，即使用 `INNER JOIN Examination E ON L.ID = E.ID`，以匹配预期的SQL逻辑。2. 考虑COUNT的使用：如果表结构确保每个患者只有一个实验室记录和一个检查记录，可以省略DISTINCT；否则，保留DISTINCT以避免重复计数。3. 确保WHERE条件准确：继续使用严格不等式 `L.IGG > 900 AND L.IGG < 2000` 来匹配证据，避免使用BETWEEN。"
    },
    {
      "question_id": 1254,
      "difficulty": "moderate",
      "db_id": "thrombosis_prediction",
      "question": "How many patients with a normal Ig A level came to the hospital after 1990/1/1?",
      "evidence": "normal Ig A level refers to IGA > 80 AND IGA < 500; came to the hospital after 1990/1/1 refers to YEAR(`First Date`) > = 1990;",
      "generated_sql": "SELECT COUNT(DISTINCT P.ID) FROM Patient P INNER JOIN Laboratory L ON P.ID = L.ID WHERE L.IGA > 80 AND L.IGA < 500 AND YEAR(P.First Date) >= 1990",
      "ground_truth_sql": "SELECT\n  COUNT(`T1`.`ID`)\nFROM `Patient` AS `T1`\nINNER JOIN `Laboratory` AS `T2`\n  ON `T1`.`ID` = `T2`.`ID`\nWHERE\n  `T2`.`IGA` BETWEEN 80 AND 500\n  AND DATE_FORMAT(CAST(`T1`.`First Date` AS DATETIME), '%Y') > '1990'",
      "error_category": "日期格式错误",
      "error_reason": "生成的SQL直接使用YEAR函数处理日期字段`First Date`，但未使用CAST或DATE_FORMAT来确保日期格式正确。如果`First Date`字段存储为字符串或其他非标准日期类型，YEAR函数可能无法正确解析，导致逻辑错误。预期的SQL使用DATE_FORMAT(CAST(`First Date` AS DATETIME), '%Y')来安全处理日期，并明确比较年份字符串。此外，生成的SQL在IGA条件和COUNT函数上与预期的SQL存在差异：IGA条件使用开区间（>80 AND <500）而预期的SQL使用闭区间（BETWEEN 80 AND 500），但根据证据，生成的SQL更准确；COUNT使用DISTINCT是正确的，而预期的SQL未使用DISTINCT可能计数重复。主要错误在于日期处理方式。",
      "suggested_fix": "改进日期处理部分，使用DATE_FORMAT和CAST确保兼容性。修正后的SQL应为：SELECT COUNT(DISTINCT P.ID) FROM Patient P INNER JOIN Laboratory L ON P.ID = L.ID WHERE L.IGA > 80 AND L.IGA < 500 AND DATE_FORMAT(CAST(P.First Date AS DATETIME), '%Y') >= '1990'。这样既符合证据中的条件（IGA > 80 AND < 500，YEAR >= 1990），又避免了日期格式问题。如果数据库支持，也可以使用YEAR(P.First Date) >= 1990，但添加CAST更安全。"
    },
    {
      "question_id": 1255,
      "difficulty": "moderate",
      "db_id": "thrombosis_prediction",
      "question": "For the patients with an abnormal Ig M level, what is the most common disease they are diagnosed with?",
      "evidence": "abnormal Ig M level refers to IGM <=40 OR IGM >= 400; most common disease refers to MAX(COUNT(Diagnosis));",
      "generated_sql": "SELECT p.Diagnosis, COUNT(*) as disease_count FROM Patient p INNER JOIN (SELECT DISTINCT ID FROM Laboratory WHERE IGM <= 40 OR IGM >= 400) abnormal_patients ON p.ID = abnormal_patients.ID GROUP BY p.Diagnosis ORDER BY disease_count DESC LIMIT 1",
      "ground_truth_sql": "SELECT\n  `T1`.`Diagnosis`\nFROM `Patient` AS `T1`\nINNER JOIN `Laboratory` AS `T2`\n  ON `T1`.`ID` = `T2`.`ID`\nWHERE\n  NOT `T2`.`IGM` BETWEEN 40 AND 400\nGROUP BY\n  `T1`.`Diagnosis`\nORDER BY\n  COUNT(`T1`.`Diagnosis`) DESC\nLIMIT 1",
      "error_category": "SQL 逻辑错误",
      "error_reason": "生成的 SQL 在定义异常 IgM 水平时使用了 'IGM <= 40 OR IGM >= 400'，而预期的 SQL 使用了 'NOT IGM BETWEEN 40 AND 400'。这两个条件在逻辑上不完全等价，因为 'BETWEEN' 是包含性的（包括边界值 40 和 400），导致 'NOT BETWEEN' 排除边界值，而生成的 SQL 包括边界值。根据证据，异常 IgM 水平定义为 'IGM <=40 OR IGM >= 400'，生成的 SQL 更符合证据，但预期的 SQL 作为标准答案使用了不同的条件，因此生成的 SQL 与预期不符。此外，生成的 SQL 使用了子查询来获取异常患者 ID，而预期的 SQL 直接连接表，这可能引入不必要的复杂性和潜在的性能问题。",
      "suggested_fix": "修改 WHERE 子句为 'NOT T2.IGM BETWEEN 40 AND 400' 以匹配预期的 SQL，或者根据证据调整条件为 'T2.IGM <= 40 OR T2.IGM >= 400'。避免使用子查询，直接连接 Patient 和 Laboratory 表，如预期的 SQL 所示。确保计数方式一致，使用 COUNT(T1.Diagnosis) 或 COUNT(*) 根据需求，但建议使用 COUNT(T1.Diagnosis) 以避免计数空值。"
    },
    {
      "question_id": 1256,
      "difficulty": "moderate",
      "db_id": "thrombosis_prediction",
      "question": "How many patients with a abnormal C-reactive protein don't have their data recorded?",
      "evidence": "abnormal C-reactive protein refers to CRP ='+'; don't have data recorded refers to Description IS NULL;",
      "generated_sql": "SELECT COUNT(DISTINCT Patient.ID) FROM Patient INNER JOIN Laboratory ON Patient.ID = Laboratory.ID WHERE Laboratory.CRP = '+' AND Patient.Description IS NULL",
      "ground_truth_sql": "SELECT COUNT(T1.ID) FROM Patient AS T1 INNER JOIN Laboratory AS T2 ON T1.ID = T2.ID WHERE (T2.CRP = '+' ) AND T1.Description IS NULL;",
      "error_category": "SQL逻辑错误",
      "error_reason": "生成的SQL使用了COUNT(DISTINCT Patient.ID)，而预期的SQL使用了COUNT(T1.ID)没有DISTINCT。这可能导致计数结果不同：如果同一个患者有多个实验室记录满足CRP='+'且Description IS NULL的条件，DISTINCT会确保每个患者只计数一次，而没有DISTINCT会多次计数同一个患者。问题要求计算患者数量（'how many patients'），因此DISTINCT在逻辑上更合理，但预期的SQL未使用DISTINCT，这可能表示在特定数据库上下文中（如每个患者只有一个实验室记录）不需要DISTINCT，或者预期的SQL存在逻辑错误。然而，根据给定的预期SQL标准，生成的SQL在逻辑上与预期不符，属于SQL逻辑错误。",
      "suggested_fix": "移除DISTINCT关键字以使SQL与预期一致，并添加别名以提高可读性。修改后的SQL应为：SELECT COUNT(T1.ID) FROM Patient AS T1 INNER JOIN Laboratory AS T2 ON T1.ID = T2.ID WHERE T2.CRP = '+' AND T1.Description IS NULL; 此外，建议根据数据库结构确认是否需要DISTINCT：如果Patient与Laboratory是一对多关系且问题意图是计算唯一患者数，则应保留DISTINCT；否则，遵循预期SQL格式。"
    },
    {
      "question_id": 1265,
      "difficulty": "moderate",
      "db_id": "thrombosis_prediction",
      "question": "How many patients have a normal level of anti-ribonuclear protein and have been admitted to the hospital?",
      "evidence": "normal level of anti-ribonuclear protein refers to RNP = '-', '+-'; And'-' means 'negative'; '+-' refers to '0'; admitted to the hospital refers to Admission = '+'; Should consider DISTINCT in the final result;",
      "generated_sql": "SELECT COUNT(DISTINCT Patient.ID) AS patient_count FROM Patient INNER JOIN Laboratory ON Patient.ID = Laboratory.ID WHERE Laboratory.RNP IN ('-', '+-') AND Patient.Admission = '+'",
      "ground_truth_sql": "SELECT\n  COUNT(DISTINCT `T1`.`ID`)\nFROM `Patient` AS `T1`\nINNER JOIN `Laboratory` AS `T2`\n  ON `T1`.`ID` = `T2`.`ID`\nWHERE\n  `T2`.`RNP` = 'negative' OR `T2`.`RNP` = '0' AND `T1`.`Admission` = '+'",
      "error_category": "语义理解错误",
      "error_reason": "生成的SQL使用 `IN ('-', '+-')` 来匹配正常水平，但根据证据，正常水平对应于RNP值为 'negative' 和 '0'（即 '-' 表示 'negative'，'+-' 表示 '0'）。生成的SQL直接使用了数据库值 '-' 和 '+-'，而预期的SQL使用了映射后的值 'negative' 和 '0'。这可能导致查询结果错误，如果数据库中实际存储的是 'negative' 和 '0' 而不是 '-' 和 '+-'。此外，生成的SQL在逻辑上正确使用了 `AND` 连接条件，但预期的SQL在WHERE子句中使用 `OR` 和 `AND` 没有括号，可能导致逻辑错误（`T2.RNP = 'negative' OR T2.RNP = '0' AND T1.Admission = '+'` 等价于 `T2.RNP = 'negative' OR (T2.RNP = '0' AND T1.Admission = '+')`），而生成的SQL避免了这个问题。",
      "suggested_fix": "将 `IN ('-', '+-')` 改为 `= 'negative' OR = '0'`，并确保条件分组正确。建议使用括号明确逻辑：`(Laboratory.RNP = 'negative' OR Laboratory.RNP = '0') AND Patient.Admission = '+'`。同时，验证数据库中RNP字段的实际存储值，以确保映射正确。"
    },
    {
      "question_id": 1267,
      "difficulty": "moderate",
      "db_id": "thrombosis_prediction",
      "question": "Among the patients with normal anti-SM, how many of them does not have thrombosis?",
      "evidence": "normal anti-SM refers to SM IN('-', '+-'); SM = 'negative' means '-'; SM = '0' means '+-'; SM = '1' means '+'; does not have thrombosis refers to Thrombosis = 0;",
      "generated_sql": "SELECT COUNT(DISTINCT L.ID) FROM Laboratory L WHERE L.SM IN ('negative', '0') AND EXISTS (SELECT 1 FROM Examination E WHERE E.ID = L.ID AND E.Thrombosis = 0)",
      "ground_truth_sql": "SELECT\n  COUNT(`T1`.`ID`)\nFROM `Examination` AS `T1`\nINNER JOIN `Laboratory` AS `T2`\n  ON `T1`.`ID` = `T2`.`ID`\nWHERE\n  `T2`.`SM` IN ('negative', '0') AND `T1`.`Thrombosis` = 0",
      "error_category": "SQL逻辑错误",
      "error_reason": "生成的SQL使用EXISTS子查询来关联Laboratory和Examination表，而预期的SQL使用INNER JOIN。两者都过滤相同的条件（SM IN ('negative', '0') AND Thrombosis = 0），但连接方式不同。生成的SQL使用COUNT(DISTINCT L.ID)来计数患者数量，而预期的SQL使用COUNT(T1.ID)来计数记录数量。如果每个患者有多个Examination记录，预期的SQL会重复计数患者，而生成的SQL不会。问题问的是'patients'（患者数量），所以生成的SQL在逻辑上更准确，但不符合预期的查询结构（使用INNER JOIN）。此外，生成的SQL从Laboratory表开始，而预期的SQL从Examination表开始，这可能导致逻辑差异（如遗漏没有Examination记录的患者，但两者都假设患者有匹配记录）。",
      "suggested_fix": "使用INNER JOIN来连接Laboratory和Examination表，并确保计数患者数量而非记录数量。例如：SELECT COUNT(DISTINCT T1.ID) FROM Examination AS T1 INNER JOIN Laboratory AS T2 ON T1.ID = T2.ID WHERE T2.SM IN ('negative', '0') AND T1.Thrombosis = 0。这样既符合预期的查询结构，又准确计数患者数量。"
    },
    {
      "question_id": 1275,
      "difficulty": "moderate",
      "db_id": "thrombosis_prediction",
      "question": "Among the patients who has a normal level of anti-centromere and a normal level of anti-SSB, how many of them are male?",
      "evidence": "normal level of anti-centromere refers to CENTROMEA IN('-', '+-'); normal level of anti-SSB refers to SSB IN('-', '+-'); male refers to Sex = 'M'; Should consider DISTINCT in the final result;",
      "generated_sql": "SELECT COUNT(DISTINCT Patient.ID) FROM Laboratory INNER JOIN Patient ON Laboratory.ID = Patient.ID WHERE Laboratory.CENTROMEA IN ('-', '+-') AND Laboratory.SSB IN ('-', '+-') AND Patient.Sex = 'M'",
      "ground_truth_sql": "SELECT\n  COUNT(DISTINCT `T1`.`ID`)\nFROM `Patient` AS `T1`\nINNER JOIN `Laboratory` AS `T2`\n  ON `T1`.`ID` = `T2`.`ID`\nWHERE\n  `T2`.`CENTROMEA` IN ('negative', '0')\n  AND `T2`.`SSB` IN ('negative', '0')\n  AND `T1`.`SEX` = 'M'",
      "error_category": "语义理解错误",
      "error_reason": "生成的 SQL 错误地使用了条件值 ('-', '+-') 来表示正常水平（normal level），而根据预期的 SQL，数据库中的实际值应为 ('negative', '0')。这表明对用户问题中'正常水平'的语义理解有偏差，可能基于不准确的证据或数据库模式不匹配。证据指定了 CENTROMEA IN ('-', '+-') 和 SSB IN ('-', '+-')，但预期的 SQL 使用了 ('negative', '0')，这意味着生成的 SQL 未能正确映射问题意图到数据库的实际值。",
      "suggested_fix": "修正条件值，使用 'negative' 和 '0' 而不是 '-' 和 '+-'。建议检查数据库模式以确认正常水平的正确表示，并确保 SQL 与数据库实际值一致。此外，可以添加表别名（如 T1 和 T2）以提高可读性，但这不是必须的。最终 SQL 应类似于：SELECT COUNT(DISTINCT Patient.ID) FROM Laboratory INNER JOIN Patient ON Laboratory.ID = Patient.ID WHERE Laboratory.CENTROMEA IN ('negative', '0') AND Laboratory.SSB IN ('negative', '0') AND Patient.Sex = 'M'。"
    },
    {
      "question_id": 1302,
      "difficulty": "challenging",
      "db_id": "thrombosis_prediction",
      "question": "For the patients with a normal range of creatinine phosphokinase, how many of them have a positive measure of degree of coagulation?",
      "evidence": "normal range of creatinine phosphokinase refers to CPK < 250; positive measure of degree of coagulation refers to KCT = '+' or RVVT = '+' or LAC = '+' ;",
      "generated_sql": "SELECT COUNT(DISTINCT p.ID) FROM Patient p WHERE EXISTS (SELECT 1 FROM Laboratory l WHERE l.ID = p.ID AND l.CPK < 250) AND EXISTS (SELECT 1 FROM Examination e WHERE e.ID = p.ID AND (e.KCT = '+' OR e.RVVT = '+' OR e.LAC = '+'))",
      "ground_truth_sql": "SELECT\n  COUNT(`T1`.`ID`)\nFROM `Patient` AS `T1`\nINNER JOIN `Laboratory` AS `T2`\n  ON `T1`.`ID` = `T2`.`ID`\nINNER JOIN `Examination` AS `T3`\n  ON `T1`.`ID` = `T3`.`ID`\nWHERE\n  `T2`.`CPK` < 250\n  AND (\n    `T3`.`KCT` = '+' OR `T3`.`RVVT` = '+' OR `T3`.`LAC` = '+'\n  )",
      "error_category": "SQL 逻辑错误",
      "error_reason": "生成的 SQL 使用了 EXISTS 子查询来检查条件，而预期的 SQL 使用了 INNER JOIN 连接表。虽然生成的 SQL 在逻辑上正确实现了查询条件（CPK < 250 且 KCT、RVVT 或 LAC 中至少有一个为 '+'），但它没有匹配预期的 SQL 结构。此外，生成的 SQL 使用了 COUNT(DISTINCT p.ID) 来确保唯一计数，而预期的 SQL 使用了 COUNT(T1.ID) 没有 DISTINCT。如果数据库中每个患者有多个 Laboratory 或 Examination 记录，预期的 SQL 可能因 INNER JOIN 产生重复行而导致错误计数，而生成的 SQL 正确避免了这一点。然而，在 Text-to-SQL 任务中，生成的 SQL 应尽可能匹配预期的 SQL 结构，因此生成的 SQL 的逻辑与预期不一致。",
      "suggested_fix": "建议修改生成的 SQL 以使用 INNER JOIN 连接表，并添加 DISTINCT 关键字以确保唯一计数。例如，可以改为：SELECT COUNT(DISTINCT T1.ID) FROM Patient AS T1 INNER JOIN Laboratory AS T2 ON T1.ID = T2.ID INNER JOIN Examination AS T3 ON T1.ID = T3.ID WHERE T2.CPK < 250 AND (T3.KCT = '+' OR T3.RVVT = '+' OR T3.LAC = '+')。这样既符合预期的 SQL 结构，又保证了逻辑正确性。"
    },
    {
      "question_id": 1025,
      "difficulty": "moderate",
      "db_id": "european_football_2",
      "question": "Give the name of the league had the most goals in the 2016 season?",
      "evidence": "league that had the most goals refers to MAX(SUM(home_team_goal, away_team_goal)); 2016 season refers to season = '2015/2016';",
      "generated_sql": "SELECT l.name FROM Match m JOIN League l ON m.league_id = l.id WHERE m.season = '2015/2016' GROUP BY m.league_id ORDER BY SUM(m.home_team_goal + m.away_team_goal) DESC LIMIT 1",
      "ground_truth_sql": "SELECT  `t2`.`name` FROM `Match` AS `t1` INNER JOIN `League` AS `t2`\n  ON `t1`.`league_id` = `t2`.`id`\nWHERE\n  `t1`.`season` = '2015/2016'\nGROUP BY\n  `t2`.`name`\nORDER BY\n  SUM(`t1`.`home_team_goal` + `t1`.`away_team_goal`) DESC\nLIMIT 1",
      "error_category": "SQL逻辑错误",
      "error_reason": "生成的SQL中，GROUP BY子句使用了m.league_id，而SELECT子句使用了l.name。虽然逻辑上可能正确（因为每个league_id对应一个name），但不符合SQL标准，可能导致在某些数据库系统中执行错误或警告。预期的SQL使用GROUP BY `t2`.`name`，与SELECT列一致，更符合标准且避免潜在问题。",
      "suggested_fix": "将GROUP BY子句改为GROUP BY l.name，以确保SELECT列与GROUP BY列匹配。改进后的SQL：SELECT l.name FROM Match m JOIN League l ON m.league_id = l.id WHERE m.season = '2015/2016' GROUP BY l.name ORDER BY SUM(m.home_team_goal + m.away_team_goal) DESC LIMIT 1"
    },
    {
      "question_id": 1028,
      "difficulty": "challenging",
      "db_id": "european_football_2",
      "question": "In Scotland Premier League, which away team won the most during the 2010 season?",
      "evidence": "Final result should return the Team.team_long_name; Scotland Premier League refers to League.name = 'Scotland Premier League'; away team refers to away_team_api_id; away team that won the most refers to MAX(SUBTRACT(away_team_goal, home_team_goal) > 0); 2010 season refers to season = '2009/2010'; won the most refers to MAX(COUNT(*));",
      "generated_sql": "SELECT T2.team_long_name FROM Team T2 WHERE T2.team_api_id = (SELECT away_team_api_id FROM Match T1 INNER JOIN League T3 ON T1.league_id = T3.id WHERE T3.name = 'Scotland Premier League' AND T1.season = '2009/2010' AND T1.away_team_goal > T1.home_team_goal GROUP BY away_team_api_id ORDER BY COUNT(*) DESC LIMIT 1)",
      "ground_truth_sql": "SELECT\n  `teamInfo`.`team_long_name`\nFROM `League` AS `leagueData`\nINNER JOIN `Match` AS `matchData`\n  ON `leagueData`.`id` = `matchData`.`league_id`\nINNER JOIN `Team` AS `teamInfo`\n  ON `matchData`.`away_team_api_id` = `teamInfo`.`team_api_id`\nWHERE\n  `leagueData`.`name` = 'Scotland Premier League'\n  AND `matchData`.`season` = '2009/2010'\n  AND `matchData`.`away_team_goal` - `matchData`.`home_team_goal` > 0\nGROUP BY\n  `matchData`.`away_team_api_id`\nORDER BY\n  COUNT(*) DESC\nLIMIT 1",
      "error_category": "其他",
      "error_reason": "生成的SQL在逻辑上正确，能够返回预期的结果：在苏格兰超级联赛的2009/2010赛季中，获胜次数最多的客队的长名称。生成的SQL使用子查询来找到获胜次数最多的away_team_api_id，然后从Team表中获取对应的team_long_name。预期的SQL使用JOIN连接三个表并直接在主查询中进行GROUP BY和ORDER BY。两者逻辑等价，但写法不同。生成的SQL没有语法错误、逻辑错误或格式错误，因此不属于前六类错误。",
      "suggested_fix": "如果需要与预期的SQL格式完全一致，可以将生成的SQL重写为使用JOIN的方式，如预期的SQL所示。但当前生成的SQL是有效的，并且能正确解决问题。建议在实际应用中根据性能需求选择子查询或JOIN方式。"
    },
    {
      "question_id": 1029,
      "difficulty": "moderate",
      "db_id": "european_football_2",
      "question": "What are the speed in which attacks are put together of the top 4 teams with the highest build Up Play Speed?",
      "evidence": "speed in which attacks are put together refers to buildUpPlaySpeed;highest build up play speed refers to MAX(buildUpPlaySpeed)",
      "generated_sql": "SELECT T.team_long_name, TA.buildUpPlaySpeed FROM Team_Attributes TA INNER JOIN Team T ON TA.team_api_id = T.team_api_id INNER JOIN (SELECT team_api_id, MAX(date) as latest_date FROM Team_Attributes GROUP BY team_api_id) latest ON TA.team_api_id = latest.team_api_id AND TA.date = latest.latest_date ORDER BY TA.buildUpPlaySpeed DESC LIMIT 4",
      "ground_truth_sql": "SELECT\n  `t1`.`buildUpPlaySpeed`\nFROM `Team_Attributes` AS `t1`\nINNER JOIN `Team` AS `t2`\n  ON `t1`.`team_api_id` = `t2`.`team_api_id`\nORDER BY\n  `t1`.`buildUpPlaySpeed` ASC\nLIMIT 4",
      "error_category": "SQL逻辑错误",
      "error_reason": "生成的SQL在逻辑上与预期SQL存在多个偏差：1. 使用了不必要的子查询来获取每个团队的最新日期，但用户问题未指定日期，因此该子查询增加了复杂性且可能不必要；2. 返回了团队名称（team_long_name）列，而用户问题仅询问速度（buildUpPlaySpeed），导致返回了额外的列；3. 排序方向为降序（DESC），而预期的SQL为升序（ASC），根据问题'highest build Up Play Speed'，降序更符合意图，但生成的SQL与预期SQL在排序方向上不一致。总体而言，生成的SQL逻辑过于复杂，未直接匹配预期的简单查询。",
      "suggested_fix": "简化SQL，直接选择buildUpPlaySpeed列，按buildUpPlaySpeed降序排序（或根据问题意图调整），取前4个记录，无需子查询和团队名称。建议的SQL：SELECT `t1`.`buildUpPlaySpeed` FROM `Team_Attributes` AS `t1` INNER JOIN `Team` AS `t2` ON `t1`.`team_api_id` = `t2`.`team_api_id` ORDER BY `t1`.`buildUpPlaySpeed` DESC LIMIT 4。如果必须匹配预期的升序排序，则需确认问题意图，但根据证据，降序更合理。"
    },
    {
      "question_id": 1030,
      "difficulty": "moderate",
      "db_id": "european_football_2",
      "question": "Give the name of the league had the most matches end as draw in the 2016 season?",
      "evidence": "most matches end as draw refers to MAX(SUM(home_team_goal = away_team_goal)); 2016 season refers to season = '2015/2016';",
      "generated_sql": "SELECT l.name FROM League l INNER JOIN ( SELECT league_id, SUM(home_team_goal = away_team_goal) AS draw_count FROM Match WHERE season = '2015/2016' GROUP BY league_id ) t ON l.id = t.league_id ORDER BY t.draw_count DESC LIMIT 1",
      "ground_truth_sql": "SELECT\n  `t2`.`name`\nFROM `Match` AS `t1`\nINNER JOIN `League` AS `t2`\n  ON `t1`.`league_id` = `t2`.`id`\nWHERE\n  `t1`.`season` = '2015/2016' AND `t1`.`home_team_goal` = `t1`.`away_team_goal`\nGROUP BY\n  `t2`.`name`\nORDER BY\n  COUNT(`t1`.`id`) DESC\nLIMIT 1",
      "error_category": "SQL 逻辑错误",
      "error_reason": "生成的 SQL 使用了 SUM(home_team_goal = away_team_goal) 来计算平局数量，而不是使用 WHERE 子句过滤平局比赛后计数。虽然逻辑上可能等价，但 SUM(条件) 在某些 SQL 方言（如非 MySQL 数据库）中可能不被直接支持，且预期的 SQL 使用了更标准、更清晰的方式：通过 WHERE 子句过滤平局比赛，然后使用 COUNT(id) 计数。生成的 SQL 在子查询中仅过滤了赛季，未显式过滤平局条件，可能导致逻辑不直观或兼容性问题。",
      "suggested_fix": "使用 WHERE 子句过滤平局比赛，然后使用 COUNT 来计数，如预期的 SQL 所示：SELECT t2.name FROM Match AS t1 INNER JOIN League AS t2 ON t1.league_id = t2.id WHERE t1.season = '2015/2016' AND t1.home_team_goal = t1.away_team_goal GROUP BY t2.name ORDER BY COUNT(t1.id) DESC LIMIT 1"
    },
    {
      "question_id": 1031,
      "difficulty": "challenging",
      "db_id": "european_football_2",
      "question": "At present, calculate for the player's age who have a sprint speed of no less than 97 between 2013 to 2015.",
      "evidence": "players age at present = SUBTRACT((DATETIME(), birthday)); sprint speed of no less than 97 refers to sprint_speed > = 97; between 2013 to 2015 refers to YEAR(date) > = '2013' AND YEAR(date) < = '2015'; ",
      "generated_sql": "SELECT TIMESTAMPDIFF(YEAR, Player.birthday, CURDATE()) AS age FROM Player INNER JOIN Player_Attributes ON Player.player_api_id = Player_Attributes.player_api_id WHERE Player_Attributes.sprint_speed >= 97 AND YEAR(Player_Attributes.date) BETWEEN 2013 AND 2015",
      "ground_truth_sql": "SELECT DISTINCT YEAR(CURDATE()) - YEAR(t2.birthday) AS age FROM Player_Attributes AS t1 INNER JOIN Player AS t2 ON t1.player_api_id = t2.player_api_id WHERE YEAR(t1.`date`) BETWEEN 2013 AND 2015 AND t1.sprint_speed >= 97",
      "error_category": "返回格式错误",
      "error_reason": "生成的SQL缺少DISTINCT关键字，导致可能返回重复的年龄值。问题要求'calculate for the player's age'，暗示每个符合条件的玩家只应计算一次年龄，但生成的SQL会为同一玩家在2013-2015年间满足sprint_speed >= 97的每个记录返回一个年龄，造成重复。此外，年龄计算方式与预期SQL不同：生成的SQL使用TIMESTAMPDIFF(YEAR, Player.birthday, CURDATE())，而预期的SQL使用YEAR(CURDATE()) - YEAR(t2.birthday)。虽然TIMESTAMPDIFF更精确，但预期的SQL采用了简单减法，这可能是一个数值计算差异，但主要错误在于返回格式（重复行）。",
      "suggested_fix": "在SELECT子句中添加DISTINCT关键字，以确保返回唯一年龄值。同时，调整年龄计算方式以匹配预期的SQL，即使用YEAR(CURDATE()) - YEAR(birthday)。修改后的SQL应为：SELECT DISTINCT YEAR(CURDATE()) - YEAR(Player.birthday) AS age FROM Player INNER JOIN Player_Attributes ON Player.player_api_id = Player_Attributes.player_api_id WHERE Player_Attributes.sprint_speed >= 97 AND YEAR(Player_Attributes.date) BETWEEN 2013 AND 2015。"
    },
    {
      "question_id": 1032,
      "difficulty": "moderate",
      "db_id": "european_football_2",
      "question": "Give the name of the league with the highest matches of all time and how many matches were played in the said league.",
      "evidence": " league with highest matches of all time refers to MAX(COUNT(league_id));",
      "generated_sql": "SELECT l.name, COUNT(*) as match_count FROM Match m INNER JOIN League l ON m.league_id = l.id GROUP BY l.id, l.name ORDER BY match_count DESC LIMIT 1",
      "ground_truth_sql": "SELECT\n  `t2`.`name`,\n  COUNT(`t1`.`id`)\nFROM `Match` AS `t1`\nINNER JOIN `League` AS `t2`\n  ON `t1`.`league_id` = `t2`.`id`\nGROUP BY\n  `t2`.`name`\nORDER BY\n  COUNT(`t1`.`id`) DESC\nLIMIT 1",
      "error_category": "SQL 逻辑错误",
      "error_reason": "生成的SQL在GROUP BY子句中包含了l.id和l.name，而预期的SQL只按t2.name分组。这可能导致返回的结果不同，如果联赛名称不唯一。预期的SQL只按名称分组，假设联赛名称是唯一的，但生成的SQL按ID和名称分组，这更精确。然而，根据预期的SQL，生成的SQL应该只按名称分组，以匹配标准答案。此外，生成的SQL使用COUNT(*)，而预期的SQL使用COUNT(t1.id)，但两者在逻辑上等价，因为id是主键，非空，所以这不是主要错误。",
      "suggested_fix": "将GROUP BY子句从'GROUP BY l.id, l.name'改为'GROUP BY l.name'，以匹配预期的SQL。如果联赛名称在数据库中是唯一的，这不会引入错误；如果名称不唯一，建议按l.id分组以确保准确性，但根据预期的SQL，应遵循只按名称分组的格式。"
    },
    {
      "question_id": 1035,
      "difficulty": "simple",
      "db_id": "european_football_2",
      "question": "Give the team_fifa_api_id of teams with more than 50 but less than 60 build-up play speed.",
      "evidence": "teams with more than 50 but less than 60 build-up play speed refers to buildUpPlaySpeed >50 AND buildUpPlaySpeed <60; ",
      "generated_sql": "SELECT T.team_fifa_api_id FROM Team T INNER JOIN Team_Attributes TA ON T.team_fifa_api_id = TA.team_fifa_api_id WHERE TA.buildUpPlaySpeed > 50 AND TA.buildUpPlaySpeed < 60",
      "ground_truth_sql": "SELECT DISTINCT\n  `team_fifa_api_id`\nFROM `Team_Attributes`\nWHERE\n  `buildUpPlaySpeed` > 50 AND `buildUpPlaySpeed` < 60",
      "error_category": "SQL逻辑错误",
      "error_reason": "生成的SQL使用了不必要的INNER JOIN Team表，而team_fifa_api_id字段在Team_Attributes表中已经存在，JOIN是多余的。此外，生成的SQL缺少DISTINCT关键字，可能导致返回重复的team_fifa_api_id（如果同一个team有多个Team_Attributes记录），而用户问题要求每个team只返回一次。",
      "suggested_fix": "直接从Team_Attributes表中选择team_fifa_api_id，并添加DISTINCT关键字以确保结果唯一。建议SQL：SELECT DISTINCT team_fifa_api_id FROM Team_Attributes WHERE buildUpPlaySpeed > 50 AND buildUpPlaySpeed < 60;"
    },
    {
      "question_id": 1037,
      "difficulty": "challenging",
      "db_id": "european_football_2",
      "question": "Calculate the percentage of players who prefer left foot, who were born between 1987 and 1992.",
      "evidence": "players who prefer left foot refers to preferred_foot = 'left'; percentage of players who prefer left foot = DIVIDE(MULTIPLY((SUM(preferred_foot = 'left'), 100)), COUNT(player_fifa_api_id)); born between 1987 and 1992 refers to YEAR(birthday) BETWEEN '1987' AND '1992';",
      "generated_sql": "SELECT (CAST(COUNT(DISTINCT CASE WHEN pa.preferred_foot = 'left' THEN p.id END) AS DOUBLE) * 100) / COUNT(DISTINCT p.id) AS percentage FROM Player p INNER JOIN Player_Attributes pa ON p.player_fifa_api_id = pa.player_fifa_api_id WHERE YEAR(CAST(p.birthday AS DATE)) BETWEEN 1987 AND 1992",
      "ground_truth_sql": "SELECT\n  CAST(COUNT(CASE WHEN `t2`.`preferred_foot` = 'left' THEN `t1`.`id` ELSE NULL END) AS DOUBLE) * 100 / COUNT(`t1`.`id`) AS `percent`\nFROM `Player` AS `t1`\nINNER JOIN `Player_Attributes` AS `t2`\n  ON `t1`.`player_api_id` = `t2`.`player_api_id`\nWHERE\n  SUBSTR(`t1`.`birthday`, 1, 4) BETWEEN '1987' AND '1992'",
      "error_category": "SQL 逻辑错误",
      "error_reason": "生成的 SQL 存在两个主要错误：1. 连接键错误：使用了 `player_fifa_api_id` 进行连接，但根据预期的 SQL 和数据库模式，正确的连接键应为 `player_api_id`。这可能导致连接不正确，从而影响计数和百分比计算。2. 日期处理错误：使用了 `YEAR(CAST(p.birthday AS DATE))`，但 `birthday` 字段可能存储为字符串（如 'YYYY-MM-DD'），因此使用 `CAST` 可能失败或不准确；预期的 SQL 使用 `SUBSTR` 提取年份更合适。此外，生成的 SQL 使用了 `DISTINCT` 进行计数，而预期的 SQL 没有，这可能表明对数据库模式的理解有偏差（例如，是否每个玩家有多个属性记录）。这些错误导致查询逻辑错误，可能返回错误结果或无法执行。",
      "suggested_fix": "1. 修正连接键：将 `p.player_fifa_api_id = pa.player_fifa_api_id` 改为 `p.player_api_id = pa.player_api_id`，以匹配数据库模式。2. 修正日期处理：将 `YEAR(CAST(p.birthday AS DATE))` 改为 `SUBSTR(p.birthday, 1, 4)`，以正确提取年份并避免类型转换问题。3. 调整计数方式：根据数据库模式，如果每个玩家只有一个属性记录，可以省略 `DISTINCT`；否则，保留 `DISTINCT` 以确保准确计数。建议参考数据库模式确认连接键和日期字段格式。"
    },
    {
      "question_id": 1042,
      "difficulty": "challenging",
      "db_id": "european_football_2",
      "question": "List the name of leagues in which the average goals by the home team is higher than the away team in the 2009/2010 season.",
      "evidence": "name of league refers to League.name; average goals by the home team is higher than the away team = AVG(home_team_goal) > AVG(away_team_goal); AVG(xx_goal) = SUM(xx_goal) / COUNT(DISTINCT Match.id); 2009/2010 season refers to season = '2009/2010'",
      "generated_sql": "SELECT L.name FROM League L INNER JOIN Match M ON L.id = M.league_id WHERE M.season = '2009/2010' GROUP BY L.id, L.name HAVING AVG(M.home_team_goal) > AVG(M.away_team_goal)",
      "ground_truth_sql": "SELECT\n  `t1`.`name`\nFROM `League` AS `t1`\nINNER JOIN `Match` AS `t2`\n  ON `t1`.`id` = `t2`.`league_id`\nWHERE\n  `t2`.`season` = '2009/2010'\nGROUP BY\n  `t1`.`name`\nHAVING\n  (\n    CAST(SUM(`t2`.`home_team_goal`) AS DOUBLE) / COUNT(DISTINCT `t2`.`id`)\n  ) - (\n    CAST(SUM(`t2`.`away_team_goal`) AS DOUBLE) / COUNT(DISTINCT `t2`.`id`)\n  ) > 0",
      "error_category": "数值计算错误",
      "error_reason": "生成的SQL使用AVG函数计算平均值，但在某些数据库系统中，AVG函数对整数列可能返回整数除法结果，导致平均值计算不准确（例如，整数除法会截断小数部分）。证据中明确指定了AVG的计算方式为SUM(xx_goal) / COUNT(DISTINCT Match.id)，而生成的SQL没有显式遵循此定义。预期的SQL通过CAST(SUM(...) AS DOUBLE)确保了浮点数除法，避免了整数截断问题，从而更精确地比较主队和客队的平均进球数。此外，生成的SQL在GROUP BY子句中使用了L.id和L.name，虽然这不会导致错误，但预期的SQL只GROUP BY t1.name，这可能假设name是唯一的，如果name不唯一则可能导致分组错误。",
      "suggested_fix": "修改HAVING子句，使用显式计算平均值并确保浮点数除法，例如：HAVING (CAST(SUM(M.home_team_goal) AS DOUBLE) / COUNT(DISTINCT M.id)) > (CAST(SUM(M.away_team_goal) AS DOUBLE) / COUNT(DISTINCT M.id))。同时，在GROUP BY子句中，如果League.name不是唯一的，建议保留L.id以确保正确分组；如果name是唯一的，可以只GROUP BY L.name。此外，确保在计算中使用COUNT(DISTINCT M.id)以明确计算唯一比赛数，避免潜在的重复行问题。"
    },
    {
      "question_id": 1057,
      "difficulty": "moderate",
      "db_id": "european_football_2",
      "question": "Calculate the average home team goal in the 2010/2011 season in the country of Poland.",
      "evidence": "average home team goal = AVG(home_team_goal)= SUM(home_team_goal) / COUNT(DISTINCT Match.id) WHERE name = 'Poland' and season = '2010/2011';",
      "generated_sql": "SELECT AVG(Match.home_team_goal) AS average_home_team_goal FROM Match INNER JOIN Country ON Match.country_id = Country.id WHERE Country.name = 'Poland' AND Match.season = '2010/2011'",
      "ground_truth_sql": "SELECT\n  CAST(SUM(`t2`.`home_team_goal`) AS DOUBLE) / COUNT(`t2`.`id`)\nFROM `Country` AS `t1`\nINNER JOIN `Match` AS `t2`\n  ON `t1`.`id` = `t2`.`country_id`\nWHERE\n  `t1`.`name` = 'Poland' AND `t2`.`season` = '2010/2011'",
      "error_category": "SQL逻辑错误",
      "error_reason": "生成的SQL使用AVG函数计算平均值，但根据证据，平均值应基于所有比赛场次（COUNT(DISTINCT Match.id)）。AVG函数的分母是COUNT(home_team_goal)，即非NULL值的数量，如果home_team_goal字段有NULL值，AVG会忽略这些NULL值，导致分母偏小、平均值偏高。预期的SQL使用SUM和COUNT的显式除法，确保分母是所有匹配记录（COUNT(id)），从而正确计算平均值。此外，生成的SQL未使用CAST确保浮点数结果，可能在某些数据库中返回整数类型。",
      "suggested_fix": "将AVG(Match.home_team_goal)替换为SUM(Match.home_team_goal) / COUNT(Match.id)，并添加CAST确保浮点数结果。建议使用表别名提高可读性。修改后的SQL示例：SELECT CAST(SUM(Match.home_team_goal) AS DOUBLE) / COUNT(Match.id) AS average_home_team_goal FROM Match INNER JOIN Country ON Match.country_id = Country.id WHERE Country.name = 'Poland' AND Match.season = '2010/2011'。"
    },
    {
      "question_id": 1058,
      "difficulty": "challenging",
      "db_id": "european_football_2",
      "question": "Who has the highest average finishing rate between the highest and shortest football player?",
      "evidence": "finishing rate refers to finishing; highest average finishing rate = MAX(AVG(finishing)); highest football player refers to MAX(height); shortest football player refers to MIN(height);",
      "generated_sql": "SELECT p.player_name FROM Player p JOIN Player_Attributes pa ON p.player_api_id = pa.player_api_id WHERE p.player_api_id IN (SELECT player_api_id FROM Player WHERE height = (SELECT MAX(height) FROM Player) UNION SELECT player_api_id FROM Player WHERE height = (SELECT MIN(height) FROM Player)) GROUP BY p.player_api_id, p.player_name ORDER BY AVG(pa.finishing) DESC LIMIT 1",
      "ground_truth_sql": "SELECT `A` FROM (SELECT AVG(`finishing`) AS `result`, 'Max' AS `A` FROM `Player` AS `T1` INNER JOIN `Player_Attributes` AS `T2` ON `T1`.`player_api_id` = `T2`.`player_api_id` WHERE `T1`.`height` = (SELECT MAX(`height`) FROM `Player`) UNION SELECT AVG(`finishing`) AS `result`, 'Min' AS `A` FROM `Player` AS `T1` INNER JOIN `Player_Attributes` AS `T2` ON `T1`.`player_api_id` = `T2`.`player_api_id` WHERE `T1`.`height` = (SELECT MIN(`height`) FROM `Player`)) AS `derived` ORDER BY `result` DESC LIMIT 1",
      "error_category": "语义理解错误",
      "error_reason": "生成的SQL误解了问题意图。问题要求比较最高身高的运动员组和最矮身高的运动员组的平均完成率，并返回哪个组的平均完成率更高。但生成的SQL错误地将问题执行为：在最高和最矮的运动员中，找出平均完成率最高的单个运动员。具体错误包括：1) 使用WHERE子句过滤出最高和最矮的运动员，然后按每个运动员分组（GROUP BY p.player_api_id），计算每个运动员的平均完成率，这导致聚合范围错误，因为它计算的是单个运动员的平均，而不是组的平均；2) 返回的是运动员名称，而问题意图可能更倾向于返回组标识（如'Max'或'Min'），因为比较的是组间的平均完成率。预期的SQL正确地通过UNION分别计算两个组的平均完成率，并比较返回组名，符合问题语义。",
      "suggested_fix": "重新设计SQL以正确计算两个组的平均完成率并比较。建议步骤：1) 分别计算最高身高的运动员组（height = MAX(height)）和最矮身高的运动员组（height = MIN(height)）的平均完成率；2) 使用UNION或子查询合并这两个组的平均完成率和组标识（如'Max'和'Min'）；3) 按平均完成率降序排序，取最高值并返回组名。参考预期的SQL结构：使用UNION连接两个子查询，每个子查询计算组的平均完成率并添加组名，然后在外层查询中排序并限制结果。"
    },
    {
      "question_id": 1078,
      "difficulty": "simple",
      "db_id": "european_football_2",
      "question": "Which player is older, Aaron Lennon or Abdelaziz Barrada?",
      "evidence": "The larger the birthday value, the younger the person is, and vice versa;",
      "generated_sql": "SELECT player_name, birthday FROM Player WHERE player_name IN ('Aaron Lennon', 'Abdelaziz Barrada') ORDER BY birthday ASC;",
      "ground_truth_sql": "SELECT\n  `player_name`\nFROM `Player`\nWHERE\n  `player_name` IN ('Aaron Lennon', 'Abdelaziz Barrada')\nORDER BY\n  `birthday` ASC\nLIMIT 1",
      "error_category": "返回格式错误",
      "error_reason": "生成的SQL正确地按生日升序排序以找出更年长的球员（因为生日值越小表示年龄越大），但返回了两行数据（包括player_name和birthday列），而用户问题只询问'哪个球员更年长'，预期应只返回一个球员的名字作为单一答案。缺少LIMIT子句导致返回所有匹配行，不符合问题要求的单一答案格式。",
      "suggested_fix": "在SQL中添加LIMIT 1子句，并只选择player_name列，以确保只返回最年长的球员名字。例如：SELECT player_name FROM Player WHERE player_name IN ('Aaron Lennon', 'Abdelaziz Barrada') ORDER BY birthday ASC LIMIT 1;"
    },
    {
      "question_id": 1080,
      "difficulty": "moderate",
      "db_id": "european_football_2",
      "question": "Among the players whose preferred foot was the left foot when attacking, how many of them would remain in his position when the team attacked?",
      "evidence": "preferred foot when attacking was the left refers to preferred_foot = 'left'; players who would remain in his position when the team attacked refers to attacking_work_rate = 'low';",
      "generated_sql": "SELECT COUNT(DISTINCT player_api_id) AS player_count FROM Player_Attributes WHERE preferred_foot = 'left' AND attacking_work_rate = 'low'",
      "ground_truth_sql": "SELECT\n  COUNT(`player_api_id`)\nFROM `Player_Attributes`\nWHERE\n  `preferred_foot` = 'left' AND `attacking_work_rate` = 'low'",
      "error_category": "SQL逻辑错误",
      "error_reason": "生成的SQL使用了DISTINCT关键字，而预期的SQL没有。这改变了计数逻辑：如果Player_Attributes表中存在重复的player_api_id（例如，同一玩家有多条记录），使用DISTINCT会去重计数，而预期的SQL直接计数非NULL值，可能导致结果不同。用户问题要求计数满足条件的玩家数量，通常应去重以确保每个玩家只计数一次，但预期的SQL未使用DISTINCT，表明在此上下文中可能假设player_api_id是唯一的或不需要去重。因此，生成的SQL与预期的逻辑不一致。",
      "suggested_fix": "移除DISTINCT关键字以匹配预期的SQL，即改为COUNT(player_api_id)。此外，为保持与预期SQL一致，可添加反引号（`）引用列名和表名，并移除别名AS player_count。改进后的SQL应为：SELECT COUNT(`player_api_id`) FROM `Player_Attributes` WHERE `preferred_foot` = 'left' AND `attacking_work_rate` = 'low'。如果实际数据中player_api_id有重复且需要去重，则应保留DISTINCT，但需根据问题意图和数据库设计确认。"
    },
    {
      "question_id": 1091,
      "difficulty": "moderate",
      "db_id": "european_football_2",
      "question": "How many matches were held in the Belgium Jupiler League in April, 2009?",
      "evidence": "Belgium Jupiler League refers to League.name = 'Belgium Jupiler League'; in April, 2009 refers to SUBSTR(`date`, 1, 7);",
      "generated_sql": "SELECT COUNT(*) AS match_count FROM Match INNER JOIN League ON Match.league_id = League.id WHERE League.name = 'Belgium Jupiler League' AND SUBSTR(Match.date, 1, 7) = '2009-04'",
      "ground_truth_sql": "SELECT\n  COUNT(`t2`.`id`)\nFROM `League` AS `t1`\nINNER JOIN `Match` AS `t2`\n  ON `t1`.`id` = `t2`.`league_id`\nWHERE\n  `t1`.`name` = 'Belgium Jupiler League' AND SUBSTR(`t2`.`date`, 1, 7) = '2009-04'",
      "error_category": "SQL逻辑错误",
      "error_reason": "生成的SQL使用COUNT(*)计数所有匹配行，而预期的SQL使用COUNT(t2.id)计数特定列（Match.id）。虽然在Match.id作为主键非空的情况下，两者结果相同，但COUNT(column)更精确地计数非空值，且预期的SQL要求使用列名计数。此外，生成的SQL未使用表别名，而预期的SQL使用了别名（t1和t2），这提高了SQL的可读性和规范性。JOIN条件逻辑等价，但预期的SQL使用了别名引用，更符合最佳实践。",
      "suggested_fix": "建议使用表别名并指定计数列，以提高精确性和可读性。改进后的SQL应为：SELECT COUNT(t2.id) AS match_count FROM League AS t1 INNER JOIN Match AS t2 ON t1.id = t2.league_id WHERE t1.name = 'Belgium Jupiler League' AND SUBSTR(t2.date, 1, 7) = '2009-04'"
    },
    {
      "question_id": 1092,
      "difficulty": "simple",
      "db_id": "european_football_2",
      "question": "Give the name of the league had the most matches in the 2008/2009 season?",
      "evidence": "league that had the most matches in the 2008/2009 season refers to MAX(league_name WHERE season = '2008/2009');",
      "generated_sql": "SELECT l.name FROM League l WHERE l.id = (SELECT league_id FROM Match WHERE season = '2008/2009' GROUP BY league_id ORDER BY COUNT(*) DESC LIMIT 1)",
      "ground_truth_sql": "SELECT `t1`.`name` FROM `League` AS `t1`JOIN `Match` AS `t2` ON `t1`.`id` = `t2`.`league_id` WHERE  `t2`.`season` = '2008/2009' GROUP BY  `t1`.`name` HAVING COUNT(`t2`.`id`) = (SELECT MAX(match_count) FROM (SELECT COUNT(`t2`.`id`) AS match_count FROM `Match` AS `t2` WHERE `t2`.`season` = '2008/2009' GROUP BY  `t2`.`league_id`) AS subquery)",
      "error_category": "SQL逻辑错误",
      "error_reason": "生成的SQL使用子查询配合LIMIT 1来获取比赛数量最多的联赛ID，这隐含假设只有一个联赛在2008/2009赛季中拥有最大比赛数。如果多个联赛有相同最大比赛数，该查询只返回一个联赛（取决于数据库排序的不确定性），而用户问题可能期望返回所有有最大比赛数的联赛（如预期SQL所示）。预期的SQL使用HAVING子句和子查询计算最大值，确保返回所有匹配的联赛，处理了平局情况。",
      "suggested_fix": "修改生成的SQL，使用HAVING子句和子查询来计算最大比赛数，并匹配所有有该数量的联赛。例如：SELECT l.name FROM League l JOIN Match m ON l.id = m.league_id WHERE m.season = '2008/2009' GROUP BY l.name HAVING COUNT(m.id) = (SELECT MAX(cnt) FROM (SELECT COUNT(*) as cnt FROM Match WHERE season = '2008/2009' GROUP BY league_id) as sub)。这样可以确保正确处理平局，并返回所有符合条件的联赛名称。"
    },
    {
      "question_id": 1094,
      "difficulty": "challenging",
      "db_id": "european_football_2",
      "question": "How much higher in percentage is Ariel Borysiuk's overall rating than that of Paulin Puel?",
      "evidence": "how much higher in percentage = MULTIPLY(DIVIDE(SUBTRACT(overall_rating WHERE player_name = 'Ariel Borysiuk', overall_rating WHERE player_name = 'Paulin Puel'), overall_rating WHERE player_name = 'Paulin Puel'), 100);",
      "generated_sql": "SELECT CAST((ariel_rating - paulin_rating) AS DOUBLE) / CAST(paulin_rating AS DOUBLE) * 100 AS percentage_difference FROM ( SELECT (SELECT overall_rating FROM Player_Attributes WHERE player_api_id = (SELECT player_api_id FROM Player WHERE player_name = 'Ariel Borysiuk') ORDER BY date DESC LIMIT 1) as ariel_rating, (SELECT overall_rating FROM Player_Attributes WHERE player_api_id = (SELECT player_api_id FROM Player WHERE player_name = 'Paulin Puel') ORDER BY date DESC LIMIT 1) as paulin_rating ) as ratings",
      "ground_truth_sql": "SELECT\n  (\n    SUM(\n      CASE\n        WHEN `t1`.`player_name` = 'Ariel Borysiuk'\n        THEN `t2`.`overall_rating`\n        ELSE 0\n      END\n    ) * 1.0 - SUM(\n      CASE WHEN `t1`.`player_name` = 'Paulin Puel' THEN `t2`.`overall_rating` ELSE 0 END\n    )\n  ) * 100 / SUM(\n    CASE WHEN `t1`.`player_name` = 'Paulin Puel' THEN `t2`.`overall_rating` ELSE 0 END\n  )\nFROM `Player` AS `t1`\nINNER JOIN `Player_Attributes` AS `t2`\n  ON `t1`.`player_api_id` = `t2`.`player_api_id`",
      "error_category": "SQL逻辑错误",
      "error_reason": "生成的SQL与预期SQL在逻辑上存在显著差异。生成的SQL通过子查询获取每个玩家的最新整体评分（使用ORDER BY date DESC LIMIT 1），然后计算单个评分的百分比差异。然而，预期的SQL使用INNER JOIN连接Player和Player_Attributes表，并通过聚合函数SUM和CASE语句计算两个玩家所有相关记录的评分总和，再进行百分比计算。这表明对问题意图的理解存在偏差：用户问题可能要求基于所有可用评分记录计算百分比差异，而不是仅基于最新记录。生成的SQL假设每个玩家有多个评分记录并取最新值，而预期的SQL假设需要聚合所有记录（或每个玩家只有一个记录），导致计算逻辑不一致。此外，生成的SQL可能无法正确处理多个评分记录的情况，而预期的SQL通过聚合确保了所有记录都被考虑。",
      "suggested_fix": "修改生成的SQL以匹配预期的逻辑，使用INNER JOIN连接Player和Player_Attributes表，然后使用CASE语句和SUM函数分别计算Ariel Borysiuk和Paulin Puel的评分总和，最后计算百分比差异。具体改进建议如下：\n1. 使用INNER JOIN连接Player和Player_Attributes表，基于player_api_id关联。\n2. 使用CASE语句和SUM函数聚合每个玩家的评分：SUM(CASE WHEN player_name = 'Ariel Borysiuk' THEN overall_rating ELSE 0 END) 和 SUM(CASE WHEN player_name = 'Paulin Puel' THEN overall_rating ELSE 0 END)。\n3. 计算百分比差异：(SUM(Ariel评分) - SUM(Paulin评分)) * 100 / SUM(Paulin评分)，并确保使用浮点数除法（例如乘以1.0）。\n4. 如果问题确实需要最新评分，则应在预期SQL中明确指定，但根据给定预期SQL，应采用聚合方法。"
    },
    {
      "question_id": 1107,
      "difficulty": "moderate",
      "db_id": "european_football_2",
      "question": "When was the first time did Kevin Constant have his highest crossing score? Give the date.",
      "evidence": "Kevin Constant refers to player_name = 'Kevin Constant'; highest crossing score refers to MAX(crossing)",
      "generated_sql": "SELECT pa.date FROM Player p JOIN Player_Attributes pa ON p.player_api_id = pa.player_api_id WHERE p.player_name = 'Kevin Constant' AND pa.crossing = (SELECT MAX(pa2.crossing) FROM Player p2 JOIN Player_Attributes pa2 ON p2.player_api_id = pa2.player_api_id WHERE p2.player_name = 'Kevin Constant') ORDER BY pa.date ASC LIMIT 1",
      "ground_truth_sql": "SELECT `date` FROM (SELECT `t2`.`crossing`, `t2`.`date` FROM `Player` AS `t1` INNER JOIN `Player_Attributes` AS `t2` ON `t1`.`player_fifa_api_id` = `t2`.`player_fifa_api_id` WHERE `t1`.`player_name` = 'Kevin Constant' ORDER BY `t2`.`crossing` DESC) AS `subquery` ORDER BY `date` DESC LIMIT 1",
      "error_category": "SQL 逻辑错误",
      "error_reason": "生成的 SQL 在 JOIN 条件中使用了错误的连接键。根据预期的 SQL 和数据库模式，Player 表和 Player_Attributes 表应通过 player_fifa_api_id 连接，但生成的 SQL 使用了 player_api_id。这导致 JOIN 不正确，可能无法正确关联数据，从而无法准确找到 Kevin Constant 的最高交叉得分记录。此外，子查询中也使用了相同的错误连接键，进一步加剧了逻辑错误。虽然生成的 SQL 在语义上试图找到最高交叉得分的最早日期（符合用户问题中的 'first time'），但连接键错误使得整个查询可能返回空结果或错误数据。",
      "suggested_fix": "将 JOIN 条件中的 player_api_id 改为 player_fifa_api_id，以匹配数据库模式。具体修改如下：\n- 在主查询中，将 `p.player_api_id = pa.player_api_id` 改为 `p.player_fifa_api_id = pa.player_fifa_api_id`。\n- 在子查询中，将 `p2.player_api_id = pa2.player_api_id` 改为 `p2.player_fifa_api_id = pa2.player_fifa_api_id`。\n- 此外，确保所有引用的表和列名与数据库模式一致。修改后的 SQL 应能正确执行并返回预期结果。"
    },
    {
      "question_id": 1113,
      "difficulty": "moderate",
      "db_id": "european_football_2",
      "question": "For the team \"Hannover 96\", what was its defence aggression class on 2015/9/10?",
      "evidence": "\"Hannover 96\" refers to team_long_name = 'Hannover 96'; on 2015/9/10 refers to date LIKE '2015-09-10%';",
      "generated_sql": "SELECT TA.defence_aggression_class FROM Team T INNER JOIN Team_Attributes TA ON T.team_api_id = TA.team_api_id WHERE T.team_long_name = 'Hannover 96' AND TA.date LIKE '2015-09-10%'",
      "ground_truth_sql": "SELECT\n  `t2`.`defenceAggressionClass`\nFROM `Team` AS `t1`\nINNER JOIN `Team_Attributes` AS `t2`\n  ON `t1`.`team_api_id` = `t2`.`team_api_id`\nWHERE\n  `t1`.`team_long_name` = 'Hannover 96' AND `t2`.`date` LIKE '2015-09-10%'",
      "error_category": "SQL 语法错误",
      "error_reason": "生成的 SQL 中使用了错误的列名 `defence_aggression_class`，而数据库中的实际列名应为 `defenceAggressionClass`（如预期 SQL 所示）。这导致 SQL 语法错误，因为列名不存在，无法执行查询。其他部分如 JOIN 条件、WHERE 子句和日期格式（使用 LIKE '2015-09-10%'）均正确，符合用户问题和证据要求。",
      "suggested_fix": "将列名从 `defence_aggression_class` 更正为 `defenceAggressionClass`，并确保表别名和反引号使用与数据库模式一致。建议使用预期 SQL 中的列名和别名格式，例如：SELECT `t2`.`defenceAggressionClass` FROM `Team` AS `t1` INNER JOIN `Team_Attributes` AS `t2` ON `t1`.`team_api_id` = `t2`.`team_api_id` WHERE `t1`.`team_long_name` = 'Hannover 96' AND `t2`.`date` LIKE '2015-09-10%'。"
    },
    {
      "question_id": 1115,
      "difficulty": "challenging",
      "db_id": "european_football_2",
      "question": "What percentage is Landon Donovan's overall rating higher than Jordan Bowery on 2013/7/12?",
      "evidence": "Landon Donovan's refers to player_name = 'Landon Donovan'; Jordan Bowery refers to player_name = 'Jordan Bowery'; percentage refers to DIVIDE(SUBTRACT(player_name = 'Landon Donovan' overall_rating; player_name = 'Jordan Bowery' overall_rating), player_name = 'Landon Donovan' overall_rating)*100",
      "generated_sql": "SELECT (MAX(CASE WHEN p.player_name = 'Landon Donovan' THEN CAST(pa.overall_rating AS DOUBLE) END) - MAX(CASE WHEN p.player_name = 'Jordan Bowery' THEN CAST(pa.overall_rating AS DOUBLE) END)) / MAX(CASE WHEN p.player_name = 'Landon Donovan' THEN CAST(pa.overall_rating AS DOUBLE) END) * 100 AS percentage FROM Player_Attributes pa INNER JOIN Player p ON pa.player_api_id = p.player_api_id WHERE pa.date = '2013/7/12' AND p.player_name IN ('Landon Donovan', 'Jordan Bowery')",
      "ground_truth_sql": "SELECT\n  (\n    SUM(\n      CASE\n        WHEN `t1`.`player_name` = 'Landon Donovan'\n        THEN `t2`.`overall_rating`\n        ELSE 0\n      END\n    ) * 1.0 - SUM(\n      CASE WHEN `t1`.`player_name` = 'Jordan Bowery' THEN `t2`.`overall_rating` ELSE 0 END\n    )\n  ) * 100 / SUM(\n    CASE\n      WHEN `t1`.`player_name` = 'Landon Donovan'\n      THEN `t2`.`overall_rating`\n      ELSE 0\n    END\n  ) AS `LvsJ_percent`\nFROM `Player` AS `t1`\nINNER JOIN `Player_Attributes` AS `t2`\n  ON `t1`.`player_fifa_api_id` = `t2`.`player_fifa_api_id`\nWHERE\n  SUBSTR(`t2`.`date`, 1, 10) = '2013-07-12'",
      "error_category": "SQL逻辑错误",
      "error_reason": "生成的SQL存在两个主要逻辑错误：1) JOIN条件错误：使用了`pa.player_api_id = p.player_api_id`进行连接，但预期的SQL使用`t1.player_fifa_api_id = t2.player_fifa_api_id`，连接键不正确可能导致无法正确关联玩家和属性记录，从而计算错误的评分；2) 日期格式错误：使用了`pa.date = '2013/7/12'`进行字符串比较，而数据库中的date字段可能存储为'YYYY-MM-DD'格式（如预期的SQL所示），导致无法匹配任何记录。此外，聚合函数MAX的使用可能不准确：如果每个玩家在指定日期有多个记录，MAX取最大值而非平均值或总和，但主要错误是连接键和日期格式问题。",
      "suggested_fix": "1) 修正JOIN条件：使用正确的连接字段，如`player_fifa_api_id`，确保玩家和属性记录正确关联；2) 修正日期格式：使用`SUBSTR(date, 1, 10) = '2013-07-12'`或直接使用`date = '2013-07-12'`，以匹配数据库中的日期格式；3) 考虑聚合函数：如果每个玩家在指定日期只有一个记录，可以使用MAX或直接选择评分；如果有多个记录，应使用AVG或SUM（根据业务逻辑），但建议先检查数据库结构。参考预期的SQL，使用SUM和SUBSTR函数处理日期和聚合。"
    },
    {
      "question_id": 1122,
      "difficulty": "simple",
      "db_id": "european_football_2",
      "question": "State the name of the most strongest player.",
      "evidence": "strongest players refers to player has MAX(overall_rating)",
      "generated_sql": "SELECT p.player_name FROM Player p INNER JOIN Player_Attributes pa ON p.player_api_id = pa.player_api_id WHERE pa.overall_rating = (SELECT MAX(overall_rating) FROM Player_Attributes)",
      "ground_truth_sql": "SELECT DISTINCT t1.player_name FROM Player AS t1 INNER JOIN Player_Attributes AS t2 ON t1.player_api_id = t2.player_api_id WHERE t2.overall_rating = (SELECT MAX(overall_rating) FROM Player_Attributes)",
      "error_category": "SQL 逻辑错误",
      "error_reason": "生成的 SQL 没有使用 DISTINCT 关键字，导致如果同一个球员在 Player_Attributes 表中有多个记录具有相同的最大 overall_rating，查询结果会返回重复的 player_name。而预期的 SQL 使用 DISTINCT 来确保每个球员名字只出现一次，这更符合用户问题的意图（'State the name of the most strongest player'），因为用户可能期望每个球员只列出一次，避免重复结果。",
      "suggested_fix": "在 SELECT 语句中添加 DISTINCT 关键字，即改为：SELECT DISTINCT p.player_name FROM Player p INNER JOIN Player_Attributes pa ON p.player_api_id = pa.player_api_id WHERE pa.overall_rating = (SELECT MAX(overall_rating) FROM Player_Attributes)。这将确保返回的球员名字是唯一的，即使同一个球员有多个属性记录具有相同的最大评分。"
    },
    {
      "question_id": 1135,
      "difficulty": "moderate",
      "db_id": "european_football_2",
      "question": "Please provide top four football players' IDs who are among the lowest potential players and prefer to use the right foot when attacking.",
      "evidence": "lowest potential players refers to MIN(potential); prefer to use the right foot when attacking refers to preferred_foot = 'right'",
      "generated_sql": "SELECT Player.id FROM Player INNER JOIN Player_Attributes ON Player.player_api_id = Player_Attributes.player_api_id WHERE Player_Attributes.preferred_foot = 'right' ORDER BY Player_Attributes.potential ASC LIMIT 4",
      "ground_truth_sql": "SELECT\n  `id`\nFROM `Player_Attributes`\nWHERE\n  `preferred_foot` = 'right'\nORDER BY\n  `potential` ASC\nLIMIT 4",
      "error_category": "SQL逻辑错误",
      "error_reason": "生成的SQL错误地使用了INNER JOIN连接Player和Player_Attributes表，而Player_Attributes表可能已经包含了所有必要字段（id, preferred_foot, potential）。此外，生成的SQL选择了Player.id，但预期的SQL从Player_Attributes表选择id，这可能导致返回的ID不一致。用户问题要求玩家ID，但Player_Attributes.id可能不是玩家ID，或者数据库设计中Player_Attributes.id就是玩家ID。基于预期的SQL，应直接从Player_Attributes表查询，无需JOIN。",
      "suggested_fix": "直接从Player_Attributes表选择id，过滤preferred_foot = 'right'，按potential ASC排序，限制4行，如预期的SQL所示。如果Player_Attributes.id不是玩家ID，可能需要JOIN Player表获取正确ID，但根据预期SQL，应直接使用Player_Attributes.id。"
    },
    {
      "question_id": 1136,
      "difficulty": "moderate",
      "db_id": "european_football_2",
      "question": "How many players had the highest potential score for crossing that preferred to use their left foots while attacking?",
      "evidence": "highest potential score for crossing refers to MAX(crossing); preferred to use their left foots while attacking refers to preferred_foot = 'left'",
      "generated_sql": "SELECT COUNT(DISTINCT player_api_id) FROM Player_Attributes WHERE crossing = (SELECT MAX(crossing) FROM Player_Attributes) AND preferred_foot = 'left'",
      "ground_truth_sql": "SELECT\n  COUNT(`t1`.`id`)\nFROM `Player_Attributes` AS `t1`\nWHERE\n  `t1`.`preferred_foot` = 'left'\n  AND `t1`.`crossing` = (\n    SELECT\n      MAX(`crossing`)\n    FROM `Player_Attributes`\n  )",
      "error_category": "SQL逻辑错误",
      "error_reason": "生成的SQL使用了COUNT(DISTINCT player_api_id)来计数玩家，而预期的SQL使用了COUNT(`t1`.`id`)。这反映了对Player_Attributes表结构的理解差异：如果id是记录的唯一标识符（主键），而player_api_id是玩家的唯一标识符，那么COUNT(DISTINCT player_api_id)更符合问题意图（计数不同的玩家）。然而，预期的SQL可能假设id就是玩家的唯一标识符，或者每个玩家在表中只有一条记录，因此使用COUNT(`t1`.`id`)。生成的SQL可能错误地计数了player_api_id，导致结果与预期不符，尤其是在player_api_id和id不一致或存在重复记录的情况下。",
      "suggested_fix": "根据数据库结构调整计数列。建议检查Player_Attributes表的结构：如果id是玩家的唯一标识符（例如，每个玩家对应一条记录），则使用COUNT(`t1`.`id`)；如果player_api_id是玩家的唯一标识符（例如，每个玩家有多个属性记录），则使用COUNT(DISTINCT player_api_id)。为了确保准确性，可以参考数据库schema或使用更通用的计数方式，如COUNT(DISTINCT player_api_id)来计数玩家。同时，建议在SQL中使用别名提高可读性，例如：SELECT COUNT(DISTINCT t1.player_api_id) FROM Player_Attributes AS t1 WHERE t1.preferred_foot = 'left' AND t1.crossing = (SELECT MAX(crossing) FROM Player_Attributes)。"
    },
    {
      "question_id": 1139,
      "difficulty": "challenging",
      "db_id": "european_football_2",
      "question": "What was the final score for the match on September 24, 2008, in the Belgian Jupiler League between the home team and the away team?",
      "evidence": "September 24, 2008 refers to date like '2008-09-24%'; in the Belgian Jupiler League refers to League.name = 'Belgium Jupiler League'; final score for home team refers to home_team_goal; final score for away team refers to away_team_goal",
      "generated_sql": "SELECT m.home_team_goal, m.away_team_goal FROM Match m INNER JOIN League l ON m.league_id = l.id WHERE l.name = 'Belgium Jupiler League' AND m.date LIKE '2008-09-24%'",
      "ground_truth_sql": "SELECT\n  `t2`.`home_team_goal`,\n  `t2`.`away_team_goal`\nFROM `League` AS `t1`\nINNER JOIN `Match` AS `t2`\n  ON `t1`.`id` = `t2`.`league_id`\nWHERE\n  `t1`.`name` = 'Belgium Jupiler League' AND `t2`.`date` LIKE '2008-09-24%'",
      "error_category": "SQL语法错误",
      "error_reason": "生成的SQL中表名 'Match' 是SQL关键字（在MySQL等数据库中，MATCH是保留字，用于MATCH AGAINST子句），但未使用反引号进行引用。这可能导致语法错误，因为保留字作为标识符时需要转义。预期的SQL使用了反引号来引用表名，确保兼容性。",
      "suggested_fix": "在表名 'Match' 周围添加反引号，例如将 'FROM Match m' 改为 'FROM `Match` m'。为了保持一致性，建议对所有表名和列名使用反引号，如预期的SQL所示。同时，确保日期过滤使用LIKE '2008-09-24%' 是合适的，因为证据指定了日期格式为字符串。"
    },
    {
      "question_id": 1141,
      "difficulty": "moderate",
      "db_id": "european_football_2",
      "question": "Does the KSV Cercle Brugge team have a slow, balanced or fast speed class?",
      "evidence": "KSV Cercle Brugge refers to team_long_name = 'KSV Cercle Brugge'; speed class refers to buildUpPlaySpeedClass",
      "generated_sql": "SELECT ta.buildUpPlaySpeedClass FROM Team t INNER JOIN Team_Attributes ta ON t.team_api_id = ta.team_api_id WHERE t.team_long_name = 'KSV Cercle Brugge' ORDER BY ta.date DESC LIMIT 1",
      "ground_truth_sql": "SELECT DISTINCT\n  `t1`.`buildUpPlaySpeedClass`\nFROM `Team_Attributes` AS `t1`\nINNER JOIN `Team` AS `t2`\n  ON `t1`.`team_api_id` = `t2`.`team_api_id`\nWHERE\n  `t2`.`team_long_name` = 'KSV Cercle Brugge'",
      "error_category": "SQL逻辑错误",
      "error_reason": "生成的SQL使用了`ORDER BY ta.date DESC LIMIT 1`来获取最新的速度类别记录，而预期的SQL使用了`DISTINCT`来获取所有不同的速度类别值，没有排序或限制。这导致逻辑不一致：生成的SQL假设需要基于时间的最新值（可能更符合问题意图，因为速度类别可能随时间变化），而预期的SQL假设需要所有可能的类别值（不考虑时间因素）。用户问题询问球队是否有慢、平衡或快的速度类别，通常期望一个具体的类别值（如当前或最近的值），因此生成的SQL的逻辑更合理，但与预期的SQL不匹配。",
      "suggested_fix": "如果问题意图是获取球队的当前速度类别，生成的SQL是合理的。但为了匹配预期的SQL，应移除`ORDER BY`和`LIMIT`，并添加`DISTINCT`，即：`SELECT DISTINCT ta.buildUpPlaySpeedClass FROM Team t INNER JOIN Team_Attributes ta ON t.team_api_id = ta.team_api_id WHERE t.team_long_name = 'KSV Cercle Brugge'`。或者，如果预期的SQL有误，应修正预期的SQL以包含时间因素（如添加`ORDER BY date DESC LIMIT 1`）以获取最新记录。"
    },
    {
      "question_id": 1144,
      "difficulty": "simple",
      "db_id": "european_football_2",
      "question": "Please state the finishing rate and curve score of the player who has the heaviest weight.",
      "evidence": "finishing rate refer to finishing; curve score refer to curve; heaviest weight refers to MAX(weight)",
      "generated_sql": "SELECT pa.finishing, pa.curve FROM Player p INNER JOIN Player_Attributes pa ON p.player_api_id = pa.player_api_id WHERE p.weight = (SELECT MAX(weight) FROM Player)",
      "ground_truth_sql": "SELECT\n  `id`,\n  `finishing`,\n  `curve`\nFROM `Player_Attributes`\nWHERE\n  `player_api_id` = (\n    SELECT\n      `player_api_id`\n    FROM `Player`\n    ORDER BY\n      `weight` DESC\n    LIMIT 1\n  )\nLIMIT 1",
      "error_category": "语义理解错误",
      "error_reason": "生成的SQL没有正确处理用户问题中' the player'的单数意图。用户要求找到最重体重的玩家的finishing rate和curve score，暗示期望一个玩家。生成的SQL使用MAX(weight)子查询，但如果有多个玩家具有相同的最大体重，它会返回所有这些玩家的记录，导致可能返回多行结果，不符合用户期望的单个玩家。预期的SQL通过ORDER BY weight DESC LIMIT 1确保只返回一个玩家，即使存在重复体重。此外，生成的SQL只选择finishing和curve列，这符合用户问题要求，但预期的SQL额外选择了id列，这可能是一个不一致，但核心错误在于逻辑上未处理单数性。",
      "suggested_fix": "修改生成的SQL，使用子查询找到最重体重的player_api_id，然后从Player_Attributes中选择对应的finishing和curve。例如：\n```sql\nSELECT pa.finishing, pa.curve\nFROM Player_Attributes pa\nWHERE pa.player_api_id = (\n    SELECT player_api_id\n    FROM Player\n    ORDER BY weight DESC\n    LIMIT 1\n)\n```\n如果数据库设计保证每个玩家只有一个属性记录，可以省略LIMIT 1；否则，添加LIMIT 1以确保只返回一个记录。同时，确保列选择只包括用户要求的finishing和curve，除非有额外要求。"
    },
    {
      "question_id": 1145,
      "difficulty": "simple",
      "db_id": "european_football_2",
      "question": "Which top 4 leagues had the most games in the 2015-2016 season?",
      "evidence": "in the 2015-2016 season refers to season = '2015/2016'; league with most games refers to League.name where MAX(COUNT(id)); ",
      "generated_sql": "SELECT League.name, COUNT(Match.id) as game_count FROM Match INNER JOIN League ON Match.league_id = League.id WHERE Match.season = '2015/2016' GROUP BY League.id, League.name ORDER BY game_count DESC LIMIT 4",
      "ground_truth_sql": "SELECT\n  `t1`.`name`\nFROM `League` AS `t1`\nINNER JOIN `Match` AS `t2`\n  ON `t1`.`id` = `t2`.`league_id`\nWHERE\n  `t2`.`season` = '2015/2016'\nGROUP BY\n  `t1`.`name`\nORDER BY\n  COUNT(`t2`.`id`) DESC\nLIMIT 4",
      "error_category": "返回格式错误",
      "error_reason": "生成的SQL返回了两列：联赛名称（League.name）和比赛场次计数（game_count），而用户问题只要求返回联赛名称（'Which top 4 leagues'），不需要额外的计数列。预期的SQL只选择联赛名称，符合问题意图。虽然生成的SQL在逻辑上能正确计算和排序，但输出格式多了一列，不符合预期。",
      "suggested_fix": "修改SELECT语句，只选择League.name，移除COUNT(Match.id) as game_count。同时，可以简化GROUP BY为只按League.name分组（假设联赛名称唯一），或保留League.id在GROUP BY中以确保正确性。ORDER BY应使用COUNT(Match.id) DESC或别名（如果保留）。改进后的SQL示例：\n```sql\nSELECT League.name FROM Match INNER JOIN League ON Match.league_id = League.id WHERE Match.season = '2015/2016' GROUP BY League.name ORDER BY COUNT(Match.id) DESC LIMIT 4\n```"
    },
    {
      "question_id": 1146,
      "difficulty": "moderate",
      "db_id": "european_football_2",
      "question": "Please provide the full name of the away team that scored the most goals.",
      "evidence": "full name refers to team_long_name; away team refers to away_team_api_id; scored the most goals refers to MAX(away_team_goal)",
      "generated_sql": "SELECT team_long_name FROM Team WHERE team_api_id = (SELECT away_team_api_id FROM Match ORDER BY away_team_goal DESC LIMIT 1)",
      "ground_truth_sql": "SELECT\n  `t2`.`team_long_name`\nFROM `Match` AS `t1`\nINNER JOIN `Team` AS `t2`\n  ON `t1`.`away_team_api_id` = `t2`.`team_api_id`\nORDER BY\n  `t1`.`away_team_goal` DESC\nLIMIT 1",
      "error_category": "SQL logic error",
      "error_reason": "The generated SQL uses a subquery to retrieve the away_team_api_id with the maximum away_team_goal, then uses it to get the team name from the Team table. This approach may not handle ties correctly if multiple away teams have the same maximum away_team_goal, as LIMIT 1 arbitrarily selects one. Additionally, it is less efficient and less standard compared to using a JOIN. The expected SQL uses an INNER JOIN between Match and Team tables, which is a more direct and readable way to achieve the same goal.",
      "suggested_fix": "Use an INNER JOIN between Match and Team tables on away_team_api_id and team_api_id, then order by away_team_goal DESC and limit to 1 to get the team with the highest away goals. This ensures clarity and handles the query as intended."
    },
    {
      "question_id": 1148,
      "difficulty": "moderate",
      "db_id": "european_football_2",
      "question": "What is the percentage of players that are under 180 cm who have an overall strength of more than 70?",
      "evidence": "percentage refers to DIVIDE(COUNT(height < 180 AND overall_rating > 70),COUNT(id)) * 100",
      "generated_sql": "SELECT (CAST(COUNT(DISTINCT CASE WHEN p.height < 180 AND pa.overall_rating > 70 THEN p.id END) AS DOUBLE) / CAST(COUNT(DISTINCT p.id) AS DOUBLE)) * 100 AS percentage FROM Player p LEFT JOIN Player_Attributes pa ON p.player_api_id = pa.player_api_id",
      "ground_truth_sql": "SELECT\n  CAST(COUNT(CASE WHEN `t2`.`overall_rating` > 70 AND `t1`.`height` < 180 THEN `t1`.`id` ELSE NULL END) AS DOUBLE) * 100 / COUNT(`t1`.`id`) AS `percent`\nFROM `Player` AS `t1`\nINNER JOIN `Player_Attributes` AS `t2`\n  ON `t1`.`player_api_id` = `t2`.`player_api_id`",
      "error_category": "SQL逻辑错误",
      "error_reason": "生成的SQL使用LEFT JOIN连接Player和Player_Attributes表，这会导致分母COUNT(DISTINCT p.id)包括所有玩家，即使那些没有整体强度记录的玩家。然而，问题要求计算'players that are under 180 cm who have an overall strength of more than 70'的百分比，这暗示只考虑有整体强度记录的玩家。因此，应该使用INNER JOIN来确保分母和分子都基于有匹配记录的玩家。预期的SQL使用INNER JOIN，更准确地反映了问题意图。此外，生成的SQL使用了COUNT(DISTINCT ...)，但id通常是唯一的，DISTINCT可能不必要，但不会导致错误。",
      "suggested_fix": "将LEFT JOIN改为INNER JOIN，以确保只考虑有整体强度记录的玩家。可以移除COUNT(DISTINCT ...)中的DISTINCT，因为id通常是唯一的，但保留也不会影响结果。确保计算逻辑正确，即分子为满足条件的玩家id计数，分母为所有有整体强度记录的玩家id计数。"
    },
    {
      "question_id": 847,
      "difficulty": "simple",
      "db_id": "formula_1",
      "question": "What is the surname of the driver with the best lap time in race number 19 in the second qualifying period?",
      "evidence": "race number refers to raceId; second qualifying period refers to q2; best lap time refers to MIN(q2);",
      "generated_sql": "SELECT d.surname FROM drivers d WHERE d.driverId = (SELECT driverId FROM qualifying WHERE raceId = 19 ORDER BY q2 ASC LIMIT 1)",
      "ground_truth_sql": "SELECT\n  `T2`.`surname`\nFROM `qualifying` AS `T1`\nINNER JOIN `drivers` AS `T2`\n  ON `T2`.`driverId` = `T1`.`driverId`\nWHERE\n  `T1`.`raceId` = 19\nORDER BY\n  `T1`.`q2` ASC\nLIMIT 1",
      "error_category": "SQL 逻辑错误",
      "error_reason": "生成的 SQL 使用 ORDER BY q2 ASC LIMIT 1 来找到 best lap time，但 best lap time 应该对应 MIN(q2)。在 SQL 中，MIN(q2) 会忽略 NULL 值，只考虑非 NULL 的最小值。然而，ORDER BY q2 ASC 会将 NULL 值排在前面（除非指定 NULLS LAST），因此 LIMIT 1 可能返回一个 q2 为 NULL 的记录，这不是有效的 lap time。这导致逻辑错误，因为生成的 SQL 可能无法正确反映 'best lap time' 的意图，尤其是在 q2 列存在 NULL 值的情况下。此外，生成的 SQL 使用子查询而非 JOIN，虽然逻辑上等价，但 JOIN 通常更高效且更符合标准 SQL 实践。",
      "suggested_fix": "修改子查询以排除 NULL 值，确保只考虑非 NULL 的 q2 值。例如：SELECT d.surname FROM drivers d WHERE d.driverId = (SELECT driverId FROM qualifying WHERE raceId = 19 AND q2 IS NOT NULL ORDER BY q2 ASC LIMIT 1)。或者，使用 MIN(q2) 在子查询中：SELECT d.surname FROM drivers d WHERE d.driverId = (SELECT driverId FROM qualifying WHERE raceId = 19 AND q2 = (SELECT MIN(q2) FROM qualifying WHERE raceId = 19))。但后者可能返回多个 driver 如果有并列，因此添加 q2 IS NOT NULL 更安全。另外，建议使用 JOIN 语法以提高可读性和性能，如预期的 SQL 所示，但同样需要添加 q2 IS NOT NULL 条件。"
    },
    {
      "question_id": 850,
      "difficulty": "simple",
      "db_id": "formula_1",
      "question": "Please give the name of the race held on the circuits in Germany.",
      "evidence": "Germany is a name of country;",
      "generated_sql": "SELECT races.name FROM races INNER JOIN circuits ON races.circuitId = circuits.circuitId WHERE circuits.country = 'Germany'",
      "ground_truth_sql": "SELECT DISTINCT\n  `T2`.`name`\nFROM `circuits` AS `T1`\nINNER JOIN `races` AS `T2`\n  ON `T2`.`circuitID` = `T1`.`circuitId`\nWHERE\n  `T1`.`country` = 'Germany'",
      "error_category": "SQL逻辑错误",
      "error_reason": "生成的SQL存在两个主要问题：1. JOIN条件中列名大小写不一致（生成的SQL使用'circuitId'，而预期的SQL使用'circuitID'），这可能导致数据库无法正确识别列，从而使连接失败或返回错误结果。2. 缺少DISTINCT关键字，而预期的SQL包含了DISTINCT，这可能导致返回重复的比赛名称，不符合用户问题中可能隐含的唯一性要求。尽管生成的SQL在语义理解上基本正确（过滤德国赛道），但列名错误和缺少去重使得逻辑执行可能出错。",
      "suggested_fix": "1. 修正JOIN条件中的列名大小写，使用与数据库模式匹配的列名（例如'circuitID'），确保连接正确。2. 添加DISTINCT关键字以确保返回唯一比赛名称，避免重复行。3. 可以考虑使用表别名（如预期的SQL中的T1、T2）提高可读性，但这不是必须的。"
    },
    {
      "question_id": 861,
      "difficulty": "simple",
      "db_id": "formula_1",
      "question": "What is his number of the driver who finished 0:01:54 in the Q3 of qualifying race No.903?",
      "evidence": "race number refers to raceId; finished 0:0M:SS in the Q3 refers to q3 LIKE 'M:SS%'",
      "generated_sql": "SELECT number FROM qualifying WHERE raceId = 903 AND q3 LIKE '0:01:54%'",
      "ground_truth_sql": "SELECT\n  `T2`.`number`\nFROM `qualifying` AS `T1`\nINNER JOIN `drivers` AS `T2`\n  ON `T2`.`driverId` = `T1`.`driverId`\nWHERE\n  `T1`.`raceId` = 903 AND `T1`.`q3` LIKE '1:54%'",
      "error_category": "语义理解错误",
      "error_reason": "生成的SQL存在两个关键的语义理解错误：1) 对问题中'his number'的理解错误：driver的号码（number）应存储在drivers表中，而非qualifying表。生成的SQL直接从qualifying表选择number，但qualifying表可能没有该字段或字段含义不同，导致无法正确获取driver的号码。2) 对时间格式'0:01:54'的处理错误：根据证据，q3字段存储时间格式如'M:SS'（分钟:秒），用户问题中的'0:01:54'应被解释为'1:54'（1分54秒），因此q3的LIKE模式应为'1:54%'，而非'0:01:54%'。生成的SQL使用了错误的模式，可能导致查询无结果或匹配错误数据。",
      "suggested_fix": "修改SQL以正确连接drivers表并调整q3的LIKE模式：\n```sql\nSELECT drivers.number\nFROM qualifying\nINNER JOIN drivers ON qualifying.driverId = drivers.driverId\nWHERE qualifying.raceId = 903\n  AND qualifying.q3 LIKE '1:54%'\n```\n此修正确保从drivers表获取driver的号码，并使用正确的LIKE模式匹配q3时间字段。"
    },
    {
      "question_id": 865,
      "difficulty": "moderate",
      "db_id": "formula_1",
      "question": "For all the drivers who finished the game in race No. 592, who is the oldest?",
      "evidence": "drivers who finished the race refers to time is not empty (i.e. time IS NOT NULL); race number refers to raceId; date of birth refers to drivers.dob; The larger the birthday value, the younger the person is, and vice versa;",
      "generated_sql": "SELECT d.forename, d.surname, d.dob FROM results r INNER JOIN drivers d ON r.driverId = d.driverId WHERE r.raceId = 592 AND r.time IS NOT NULL ORDER BY d.dob ASC LIMIT 1",
      "ground_truth_sql": "SELECT\n  `T1`.`forename`,\n  `T1`.`surname`\nFROM `drivers` AS `T1`\nINNER JOIN `results` AS `T2`\n  ON `T2`.`driverId` = `T1`.`driverId`\nWHERE\n  `T2`.`raceId` = 592 AND NOT `T2`.`time` IS NULL AND NOT `T1`.`dob` IS NULL\nORDER BY\n  `T1`.`dob` ASC\nLIMIT 1",
      "error_category": "返回格式错误",
      "error_reason": "生成的 SQL 返回了出生日期列 (d.dob)，而用户问题 'who is the oldest?' 仅询问司机的姓名（forename 和 surname），因此预期结果应只包含姓名列。此外，生成的 SQL 缺少 dob IS NOT NULL 条件，这可能导致如果有些司机的出生日期为空，排序和 LIMIT 1 的结果不准确或出错，但主要错误是返回格式不符合预期。",
      "suggested_fix": "修改 SELECT 语句为 SELECT d.forename, d.surname，移除 d.dob。同时，在 WHERE 子句中添加 AND d.dob IS NOT NULL 条件，确保只考虑有出生日期的司机。其他部分（如 JOIN、WHERE 条件和排序）可以保持不变。"
    },
    {
      "question_id": 866,
      "difficulty": "moderate",
      "db_id": "formula_1",
      "question": "Who was the player that got the lap time of 0:01:27 in the race No. 161? Show his introduction website.",
      "evidence": "player and driver are synonyms; the lap time of 0:0M:SS refers to lapTime.time LIKE 'M:SS%';race number refers to raceId; introduction website of the drivers refers to url;",
      "generated_sql": "SELECT drivers.url FROM lapTimes INNER JOIN drivers ON lapTimes.driverId = drivers.driverId WHERE lapTimes.raceId = 161 AND lapTimes.time LIKE '0:01:27%'",
      "ground_truth_sql": "SELECT DISTINCT\n  `T2`.`forename`,\n  `T2`.`surname`,\n  `T2`.`url`\nFROM `lapTimes` AS `T1`\nINNER JOIN `drivers` AS `T2`\n  ON `T2`.`driverId` = `T1`.`driverId`\nWHERE\n  `T1`.`raceId` = 161 AND `T1`.`time` LIKE '1:27%'",
      "error_category": "语义理解错误",
      "error_reason": "生成的SQL存在两个主要错误：1. 时间条件理解偏差：用户问题中的'0:01:27'应匹配数据库中lapTime.time字段的格式'M:SS'（证据指出'0:0M:SS refers to lapTime.time LIKE 'M:SS%'），但生成的SQL使用了LIKE '0:01:27%'，这可能导致条件不匹配，无法正确筛选记录。2. 返回列不完整：用户问题要求显示介绍网站，但隐含需要标识玩家身份，生成的SQL仅返回url列，而预期SQL返回了forename、surname和url列，以提供完整信息。此外，生成的SQL未使用DISTINCT，但预期SQL使用了，这可能是因为存在重复记录，但问题未明确要求唯一性，因此不是主要错误。",
      "suggested_fix": "1. 修正时间条件：将WHERE子句中的lapTimes.time LIKE '0:01:27%'改为lapTimes.time LIKE '1:27%'，以匹配数据库中的时间格式。2. 扩展SELECT子句：添加forename和surname列，例如SELECT drivers.forename, drivers.surname, drivers.url，或使用SELECT drivers.*来获取所有相关列。3. 考虑添加DISTINCT关键字以避免可能的重复行，例如SELECT DISTINCT drivers.forename, drivers.surname, drivers.url。"
    },
    {
      "question_id": 868,
      "difficulty": "simple",
      "db_id": "formula_1",
      "question": "Where is Malaysian Grand Prix held? Give the location coordinates.",
      "evidence": "location coordinates refers to (lat, lng); Malaysian Grand Prix refers to races.name = 'Malaysian Grand Prix'",
      "generated_sql": "SELECT circuits.lat, circuits.lng FROM races INNER JOIN circuits ON races.circuitId = circuits.circuitId WHERE races.name = 'Malaysian Grand Prix'",
      "ground_truth_sql": "SELECT DISTINCT\n  `T1`.`lat`,\n  `T1`.`lng`\nFROM `circuits` AS `T1`\nINNER JOIN `races` AS `T2`\n  ON `T2`.`circuitID` = `T1`.`circuitId`\nWHERE\n  `T2`.`name` = 'Malaysian Grand Prix'",
      "error_category": "SQL逻辑错误",
      "error_reason": "生成的SQL缺少DISTINCT关键字，可能导致返回重复的坐标行，因为同一个电路（如马来西亚大奖赛的举办地）可能对应多个比赛记录（例如不同年份的赛事）。此外，连接条件中的列名大小写可能不一致：生成的SQL使用了`races.circuitId`和`circuits.circuitId`，而预期的SQL使用了`T2.circuitID`和`T1.circuitId`。如果数据库中列名定义为`circuitID`（大写ID），则生成的SQL中的`circuitId`可能不匹配，导致连接失败或返回错误结果。用户问题要求位置坐标，预期结果应为唯一的一组经纬度。",
      "suggested_fix": "1. 在SELECT语句中添加DISTINCT关键字以确保返回唯一的坐标，例如：SELECT DISTINCT circuits.lat, circuits.lng FROM races INNER JOIN circuits ON races.circuitId = circuits.circuitId WHERE races.name = 'Malaysian Grand Prix'。2. 检查数据库中races和circuits表的列名定义，确保连接条件中的列名大小写与数据库实际定义一致。如果列名是`circuitID`，则应将连接条件修改为`races.circuitID = circuits.circuitId`或根据实际情况调整。3. 可以使用表别名提高可读性，如预期的SQL所示，但这不是必须的。"
    },
    {
      "question_id": 872,
      "difficulty": "simple",
      "db_id": "formula_1",
      "question": "In the race No. 45, for the driver who had the Q3 time as 0:01:33, what is his abbreviated code?",
      "evidence": "race number refers to raceId; had the Q3 time as 0:0M:SS refers to q3 LIKE 'M:SS%'",
      "generated_sql": "SELECT d.code FROM qualifying q INNER JOIN drivers d ON q.driverId = d.driverId WHERE q.raceId = 45 AND q.q3 = '0:01:33'",
      "ground_truth_sql": "SELECT\n  `T2`.`code`\nFROM `qualifying` AS `T1`\nINNER JOIN `drivers` AS `T2`\n  ON `T2`.`driverId` = `T1`.`driverId`\nWHERE\n  `T1`.`raceId` = 45 AND `T1`.`q3` LIKE '1:33%'",
      "error_category": "语义理解错误",
      "error_reason": "生成的SQL对用户问题中的Q3时间'0:01:33'的理解有误。用户问题要求匹配Q3时间为'0:01:33'的司机，但根据证据，数据库中的q3字段可能存储为类似'1:33'的格式（省略前导零），因此应使用LIKE模式匹配而非精确相等。生成的SQL使用了`q.q3 = '0:01:33'`，这假设数据库中的q3字段精确存储为'0:01:33'，而预期的SQL使用了`T1.q3 LIKE '1:33%'`，这更符合证据中'q3 LIKE 'M:SS%''的建议，能正确匹配以'1:33'开头的时间字符串。",
      "suggested_fix": "在生成SQL时，应仔细分析问题中的时间格式，并参考证据或数据库模式确定正确的匹配方式。对于时间字段，如果格式不确定或可能包含前导零，建议使用LIKE模式匹配。具体来说，将条件从`q.q3 = '0:01:33'`改为`q.q3 LIKE '1:33%'`，以匹配数据库中实际存储的时间格式。同时，确保在类似问题中优先考虑模式匹配而非精确相等，以避免因格式差异导致的查询失败。"
    },
    {
      "question_id": 877,
      "difficulty": "moderate",
      "db_id": "formula_1",
      "question": "For all the drivers who finished the game in race No. 872, who is the youngest?",
      "evidence": "race number refers to raceId; drivers who finished the race refers to time has value; the youngest is a driver where MAX(dob);",
      "generated_sql": "SELECT d.driverId, d.forename, d.surname FROM results r INNER JOIN drivers d ON r.driverId = d.driverId WHERE r.raceId = 872 AND r.time IS NOT NULL ORDER BY d.dob DESC LIMIT 1",
      "ground_truth_sql": "SELECT\n  `T1`.`forename`,\n  `T1`.`surname`\nFROM `drivers` AS `T1`\nINNER JOIN `results` AS `T2`\n  ON `T2`.`driverId` = `T1`.`driverId`\nWHERE\n  `T2`.`raceId` = 872 AND NOT `T2`.`time` IS NULL\nORDER BY\n  `T1`.`dob` DESC\nLIMIT 1",
      "error_category": "返回格式错误",
      "error_reason": "生成的 SQL 在 SELECT 子句中包括了 driverId 列，而用户问题 'who is the youngest?' 可能只期望返回司机的姓名（forename 和 surname）。预期的 SQL 只选择了 forename 和 surname，因此生成的 SQL 返回了多余的列，不符合预期的返回格式。尽管 SQL 逻辑和语义理解正确（正确筛选了 race 872 中完成比赛的司机并按出生日期降序排列以找到最年轻的），但返回的列数与预期不一致。",
      "suggested_fix": "修改 SELECT 子句，只选择 forename 和 surname，以匹配预期的输出格式。例如：SELECT d.forename, d.surname FROM results r INNER JOIN drivers d ON r.driverId = d.driverId WHERE r.raceId = 872 AND r.time IS NOT NULL ORDER BY d.dob DESC LIMIT 1。同时，可以考虑使用表别名（如 T1 和 T2）以提高可读性，但这不是必须的。"
    },
    {
      "question_id": 881,
      "difficulty": "moderate",
      "db_id": "formula_1",
      "question": "For the drivers who took part in the race in 1983/7/16, what's their race completion rate?",
      "evidence": "DIVIDE(COUNT(driverid when time has value ), (COUNT(driverid )) as percentage; in 1983/7/16 refers to when date = '1983-07-16'",
      "generated_sql": "SELECT CAST(COUNT(DISTINCT CASE WHEN r.time IS NOT NULL THEN r.driverId END) AS DOUBLE) / CAST(COUNT(DISTINCT r.driverId) AS DOUBLE) AS completion_rate FROM results r INNER JOIN races ra ON r.raceId = ra.raceId WHERE ra.date = '1983-07-16'",
      "ground_truth_sql": "SELECT\n  CAST(COUNT(CASE WHEN NOT `T2`.`time` IS NULL THEN `T2`.`driverId` END) AS DOUBLE) * 100 / COUNT(`T2`.`driverId`)\nFROM `races` AS `T1`\nINNER JOIN `results` AS `T2`\n  ON `T2`.`raceId` = `T1`.`raceId`\nWHERE\n  `T1`.`date` = '1983-07-16'",
      "error_category": "数值计算错误",
      "error_reason": "生成的SQL计算了完成司机数与总司机数的比例，但没有乘以100转换为百分比。用户问题询问'race completion rate'，通常表示百分比，且证据中明确指示'as percentage'。此外，生成的SQL使用了COUNT(DISTINCT)，而预期的SQL使用了COUNT，这可能不必要地去重，但在这个上下文中可能不影响结果，因为每个driverId在同一个raceId中可能只有一条记录。主要错误是数值计算逻辑不完整，导致结果不是预期的百分比形式。",
      "suggested_fix": "在计算中乘以100以得到百分比，并考虑移除COUNT中的DISTINCT以匹配预期SQL的逻辑。修改后的SQL应为：SELECT CAST(COUNT(CASE WHEN r.time IS NOT NULL THEN r.driverId END) AS DOUBLE) * 100 / CAST(COUNT(r.driverId) AS DOUBLE) AS completion_rate FROM results r INNER JOIN races ra ON r.raceId = ra.raceId WHERE ra.date = '1983-07-16'。同时，确保日期格式正确，这里已正确使用'1983-07-16'。"
    },
    {
      "question_id": 894,
      "difficulty": "moderate",
      "db_id": "formula_1",
      "question": "What is the best lap time recorded? List the driver and race with such recorded lap time.",
      "evidence": "the best lap time refers to min(milliseconds); List the driver refers to drivers.forename and drivers.surname; List the race refers to races.name",
      "generated_sql": "SELECT d.forename, d.surname, r.name FROM lapTimes lt INNER JOIN drivers d ON lt.driverId = d.driverId INNER JOIN races r ON lt.raceId = r.raceId WHERE lt.milliseconds = (SELECT MIN(milliseconds) FROM lapTimes);",
      "ground_truth_sql": "SELECT\n  `T2`.`milliseconds`,\n  `T1`.`forename`,\n  `T1`.`surname`,\n  `T3`.`name`\nFROM `drivers` AS `T1`\nINNER JOIN `lapTimes` AS `T2`\n  ON `T1`.`driverId` = `T2`.`driverId`\nINNER JOIN `races` AS `T3`\n  ON `T2`.`raceId` = `T3`.`raceId`\nORDER BY\n  `T2`.`milliseconds` ASC\nLIMIT 1",
      "error_category": "语义理解错误",
      "error_reason": "生成的SQL没有正确理解用户问题的意图。用户问题要求列出最佳圈速（最小milliseconds）以及对应的driver和race，但生成的SQL只选择了driver的forename和surname以及race的name，遗漏了圈速（milliseconds）本身。此外，生成的SQL使用子查询来匹配最小milliseconds，可能返回多行（如果多个lap time有相同最小值），而问题可能期望只返回一行（最佳圈速）。预期的SQL通过选择milliseconds列并使用ORDER BY和LIMIT 1来确保返回圈速和唯一结果，更符合问题要求。",
      "suggested_fix": "修改生成的SQL以包括milliseconds列，并考虑使用ORDER BY和LIMIT 1来找到最佳圈速。例如，使用类似预期的SQL结构：SELECT lapTimes.milliseconds, drivers.forename, drivers.surname, races.name FROM lapTimes INNER JOIN drivers ON lapTimes.driverId = drivers.driverId INNER JOIN races ON lapTimes.raceId = races.raceId ORDER BY lapTimes.milliseconds ASC LIMIT 1。这样可以确保返回圈速、driver和race，并且只返回一行（如果有多个相同最小值，可以调整逻辑，如使用DISTINCT或根据业务需求处理）。"
    },
    {
      "question_id": 896,
      "difficulty": "challenging",
      "db_id": "formula_1",
      "question": "Calculate the percentage whereby Hamilton was not at the 1st track of the the f1 circuit since 2010.",
      "evidence": "percentage = DIVIDE(COUNT(raceId) where surname = 'Hamilton' and position>1), (COUNT(raceId) where surname = 'Hamilton'); since 2010 refers to year >= 2010",
      "generated_sql": "SELECT CAST(SUM(CASE WHEN r.position > 1 THEN 1 ELSE 0 END) AS DOUBLE) / CAST(COUNT(DISTINCT r.raceId) AS DOUBLE) * 100 AS percentage FROM results r JOIN drivers d ON r.driverId = d.driverId JOIN races ra ON r.raceId = ra.raceId WHERE d.surname = 'Hamilton' AND ra.year >= 2010",
      "ground_truth_sql": "SELECT\n  CAST(COUNT(CASE WHEN `T2`.`position` <> 1 THEN `T2`.`position` END) AS DOUBLE) * 100 / COUNT(`T2`.`driverStandingsId`)\nFROM `races` AS `T1`\nINNER JOIN `driverStandings` AS `T2`\n  ON `T2`.`raceId` = `T1`.`raceId`\nINNER JOIN `drivers` AS `T3`\n  ON `T3`.`driverId` = `T2`.`driverId`\nWHERE\n  `T3`.`surname` = 'Hamilton' AND `T1`.`year` >= 2010",
      "error_category": "SQL逻辑错误",
      "error_reason": "生成的SQL使用了错误的表（results表），而根据数据库模式和问题意图，应该使用driverStandings表来获取车手排名信息。results表可能包含更详细的结果数据，但driverStandings表更直接地存储每个车手在每个race的排名，适合计算百分比。此外，分母计算中使用了COUNT(DISTINCT r.raceId)，而证据中指定的是COUNT(raceId) where surname='Hamilton'，这可能导致分母计算不准确（例如，如果Hamilton在同一个race中有多个记录，使用distinct会低估分母），从而影响百分比结果的正确性。",
      "suggested_fix": "将SQL中的表从results改为driverStandings，并调整JOIN条件和列引用。具体来说：1. 使用driverStandings表（如预期的SQL所示）来获取position和driverStandingsId；2. 分母应计算为COUNT(driverStandingsId)而不是COUNT(DISTINCT raceId)，以匹配证据中的分母定义；3. 确保JOIN条件正确，连接races、driverStandings和drivers表，条件为driverStandings.raceId = races.raceId 和 driverStandings.driverId = drivers.driverId。参考预期的SQL结构，修改生成的SQL以使用正确的表和列。"
    },
    {
      "question_id": 897,
      "difficulty": "moderate",
      "db_id": "formula_1",
      "question": "Name the driver with the most winning. Mention his nationality and what is his maximum point scores.",
      "evidence": "Full name of the driver refers to drivers.forename and drivers.surname; the most winning refers to MAX(COUNT(wins)); average point scores refers to MAX(points);",
      "generated_sql": "SELECT CONCAT(d.forename, ' ', d.surname) AS full_name, d.nationality, top_driver.max_points FROM drivers d INNER JOIN (SELECT driverId, MAX(points) AS max_points FROM driverStandings GROUP BY driverId ORDER BY SUM(wins) DESC LIMIT 1) AS top_driver ON d.driverId = top_driver.driverId",
      "ground_truth_sql": "SELECT\n  `T1`.`forename`,\n  `T1`.`surname`,\n  `T1`.`nationality`,\n  MAX(`T2`.`points`)\nFROM `drivers` AS `T1`\nINNER JOIN `driverStandings` AS `T2`\n  ON `T2`.`driverId` = `T1`.`driverId`\nWHERE\n  `T2`.`wins` >= 1\nGROUP BY\n  `T1`.`forename`,\n  `T1`.`surname`,\n  `T1`.`nationality`\nORDER BY\n  COUNT(`T2`.`wins`) DESC\nLIMIT 1",
      "error_category": "语义理解错误",
      "error_reason": "生成的 SQL 在理解 'the most winning' 的意图时出现错误。根据证据，'the most winning refers to MAX(COUNT(wins))'，即需要计算每个 driver 的 wins 次数（COUNT(wins)），然后取最大值。但生成的 SQL 使用了 SUM(wins) 来计算总 wins 数，这可能导致逻辑偏差，因为 SUM(wins) 和 COUNT(wins) 在语义上不同（例如，如果 wins 字段表示每场比赛的 wins 数量，SUM(wins) 是总 wins 数，而 COUNT(wins) 是比赛场次）。此外，生成的 SQL 没有显式过滤有 wins 的记录（WHERE wins >= 1），而预期的 SQL 通过 WHERE T2.wins >= 1 确保只考虑有 wins 的 driver，这更符合问题意图。生成的 SQL 使用子查询和 ORDER BY SUM(wins) DESC LIMIT 1，虽然可能在某些情况下返回正确结果，但不符合证据指定的语义，且可能在某些 SQL 方言中产生歧义。",
      "suggested_fix": "修改 SQL 以使用 COUNT(wins) 而不是 SUM(wins) 来计数 wins 的次数。建议采用类似预期 SQL 的结构：直接连接 drivers 和 driverStandings 表，使用 WHERE T2.wins >= 1 过滤有 wins 的记录，然后按 COUNT(T2.wins) 降序排序，取第一个 driver，并返回其 forename、surname、nationality 和最高 points（MAX(T2.points)）。具体 SQL 如下：\nSELECT\n  `T1`.`forename`,\n  `T1`.`surname`,\n  `T1`.`nationality`,\n  MAX(`T2`.`points`)\nFROM `drivers` AS `T1`\nINNER JOIN `driverStandings` AS `T2`\n  ON `T2`.`driverId` = `T1`.`driverId`\nWHERE\n  `T2`.`wins` >= 1\nGROUP BY\n  `T1`.`forename`,\n  `T1`.`surname`,\n  `T1`.`nationality`\nORDER BY\n  COUNT(`T2`.`wins`) DESC\nLIMIT 1"
    },
    {
      "question_id": 902,
      "difficulty": "simple",
      "db_id": "formula_1",
      "question": "Which race was Alex Yoong in when he was in track number less than 20?",
      "evidence": "Alex Yoong refers to the full name of the driver; Full name of the driver refers to drivers.forename and drivers.surname;track number less than 10 refers to position < 20",
      "generated_sql": "SELECT r.name, r.year FROM drivers d INNER JOIN results res ON d.driverId = res.driverId INNER JOIN races r ON res.raceId = r.raceId WHERE d.forename = 'Alex' AND d.surname = 'Yoong' AND res.position < 20",
      "ground_truth_sql": "SELECT\n  `T1`.`name`\nFROM `races` AS `T1`\nINNER JOIN `driverStandings` AS `T2`\n  ON `T2`.`raceId` = `T1`.`raceId`\nINNER JOIN `drivers` AS `T3`\n  ON `T3`.`driverId` = `T2`.`driverId`\nWHERE\n  `T3`.`forename` = 'Alex' AND `T3`.`surname` = 'Yoong' AND `T2`.`position` < 20",
      "error_category": "SQL逻辑错误",
      "error_reason": "生成的SQL错误地使用了'results'表来获取位置信息，而根据数据库结构（从预期的SQL推断），位置信息（position）应该存储在'driverStandings'表中。这导致JOIN条件不正确，可能无法正确过滤Alex Yoong位置小于20的比赛。此外，生成的SQL返回了race名称和年份（r.name, r.year），但用户问题只询问'which race'，预期的SQL只返回race名称（T1.name），因此返回格式也存在偏差，可能返回多余列。",
      "suggested_fix": "根据数据库结构，使用'driverStandings'表来获取位置信息。修改SQL为：SELECT r.name FROM races r INNER JOIN driverStandings ds ON r.raceId = ds.raceId INNER JOIN drivers d ON d.driverId = ds.driverId WHERE d.forename = 'Alex' AND d.surname = 'Yoong' AND ds.position < 20。只返回race名称，除非问题明确要求其他列。确保JOIN条件基于正确的表和字段。"
    },
    {
      "question_id": 906,
      "difficulty": "moderate",
      "db_id": "formula_1",
      "question": "Which was Lewis Hamilton first race? What was his points recorded for his first race event?",
      "evidence": "first race refers to min(Year); Lewis Hamiltonrefers to the full name of the driver; Full name of the driver refers to drivers.forename and drivers.surname;",
      "generated_sql": "SELECT r.name, r1.points FROM results r1 JOIN races r ON r1.raceId = r.raceId JOIN drivers d ON r1.driverId = d.driverId WHERE d.forename = 'Lewis' AND d.surname = 'Hamilton' AND r.year = (SELECT MIN(r2.year) FROM results r2 JOIN races r3 ON r2.raceId = r3.raceId JOIN drivers d2 ON r2.driverId = d2.driverId WHERE d2.forename = 'Lewis' AND d2.surname = 'Hamilton') ORDER BY r.date LIMIT 1",
      "ground_truth_sql": "SELECT\n  `T1`.`name`,\n  `T2`.`points`\nFROM `races` AS `T1`\nINNER JOIN `driverStandings` AS `T2`\n  ON `T2`.`raceId` = `T1`.`raceId`\nINNER JOIN `drivers` AS `T3`\n  ON `T3`.`driverId` = `T2`.`driverId`\nWHERE\n  `T3`.`forename` = 'Lewis' AND `T3`.`surname` = 'Hamilton'\nORDER BY\n  `T1`.`year` ASC\nLIMIT 1",
      "error_category": "SQL逻辑错误",
      "error_reason": "生成的SQL存在两个主要逻辑错误：1. 使用了错误的表来获取积分：生成的SQL从`results`表中选择`points`，但根据预期的SQL和Formula 1数据库的常见结构，积分应存储在`driverStandings`表中。这可能导致积分数据缺失或错误。2. 查询逻辑过于复杂且不准确：生成的SQL使用子查询来找到最小年份，但子查询中重复了相同的连接条件，效率低下。同时，它使用`ORDER BY r.date`来排序，但用户问题明确指定“first race refers to min(Year)”，按日期排序可能无法准确返回第一场比赛（例如，同一车手在同一年有多场比赛时）。预期的SQL通过直接按年份升序排序并限制结果，更简洁且准确。",
      "suggested_fix": "1. 修改SQL以使用`driverStandings`表来获取积分，而不是`results`表。2. 简化查询结构：直接按年份升序排序并使用`LIMIT 1`，避免不必要的子查询。3. 确保连接正确的表：使用`races`、`driverStandings`和`drivers`表，并正确设置JOIN条件。示例改进后的SQL：\n```sql\nSELECT r.name, ds.points FROM races r JOIN driverStandings ds ON r.raceId = ds.raceId JOIN drivers d ON ds.driverId = d.driverId WHERE d.forename = 'Lewis' AND d.surname = 'Hamilton' ORDER BY r.year ASC LIMIT 1\n```"
    },
    {
      "question_id": 915,
      "difficulty": "simple",
      "db_id": "formula_1",
      "question": "Which country is the oldest driver from?",
      "evidence": "date of birth refers to drivers.dob; The larger the birthday value, the younger the person is, and vice versa;",
      "generated_sql": "SELECT nationality FROM drivers ORDER BY dob ASC LIMIT 1;",
      "ground_truth_sql": "SELECT\n  `nationality`\nFROM `drivers`\nWHERE\n  NOT `dob` IS NULL\nORDER BY\n  `dob` ASC\nLIMIT 1",
      "error_category": "SQL逻辑错误",
      "error_reason": "生成的SQL直接按出生日期升序排序并取第一条记录，但未过滤掉出生日期（dob）为NULL的记录。如果数据库中存在dob为NULL的车手，这些记录在排序时可能被处理（例如，在某些数据库系统中，NULL值在ASC排序中可能被视为最小值），导致结果不准确。预期的SQL通过WHERE NOT dob IS NULL条件确保只考虑有有效出生日期的记录，从而避免逻辑错误。",
      "suggested_fix": "在生成的SQL中添加WHERE条件来排除dob为NULL的记录，例如：SELECT nationality FROM drivers WHERE NOT dob IS NULL ORDER BY dob ASC LIMIT 1; 这可以确保查询只基于有效的出生日期数据，提高结果的准确性。"
    },
    {
      "question_id": 928,
      "difficulty": "moderate",
      "db_id": "formula_1",
      "question": "Which driver ranked the first in the Canadian Grand Prix in 2007? Please give his reference name.",
      "evidence": "reference name refers to driverRef; Canadian Grand Prix refers to races.name = 'Canadian Grand Prix';",
      "generated_sql": "SELECT d.driverRef FROM results r INNER JOIN races ra ON r.raceId = ra.raceId INNER JOIN drivers d ON r.driverId = d.driverId WHERE ra.year = 2007 AND ra.name = 'Canadian Grand Prix' AND r.position = 1",
      "ground_truth_sql": "SELECT\n  `T3`.`forename`,\n  `T3`.`surname`,\n  `T3`.`driverRef`\nFROM `races` AS `T1`\nINNER JOIN `results` AS `T2`\n  ON `T2`.`raceId` = `T1`.`raceId`\nINNER JOIN `drivers` AS `T3`\n  ON `T3`.`driverId` = `T2`.`driverId`\nWHERE\n  `T1`.`name` = 'Canadian Grand Prix' AND `T2`.`rank` = 1 AND `T1`.`year` = 2007",
      "error_category": "语义理解错误",
      "error_reason": "生成的SQL使用 `r.position = 1` 来筛选排名第一的车手，但用户问题中的 'ranked the first' 应该对应数据库中的 `rank` 字段，而非 `position`。在Formula 1数据库中，`position` 通常表示车手在比赛中的最终位置（如第1名、第2名），而 `rank` 可能表示基于时间或其他标准的排名。使用 `position` 可能导致逻辑错误，因为 'ranked the first' 更强调排名顺序，而非位置。此外，返回格式上，生成的SQL只选择 `driverRef`，而预期的SQL额外选择了 `forename` 和 `surname`。用户问题明确要求 'give his reference name'，且证据指出 'reference name refers to driverRef'，因此只返回 `driverRef` 是合理的，但预期的SQL可能基于数据库模式或问题意图返回了更多信息，这属于次要的返回格式偏差。",
      "suggested_fix": "将WHERE子句中的 `r.position = 1` 修改为 `r.rank = 1`，以准确匹配 'ranked the first' 的语义。对于返回列，如果用户只要求参考名称，保持只选择 `driverRef` 即可；但若需与预期SQL一致，可添加 `forename` 和 `surname` 列，但根据问题描述，这并非必要。建议先验证数据库中 `position` 和 `rank` 字段的具体含义，确保使用正确的字段进行筛选。"
    },
    {
      "question_id": 930,
      "difficulty": "simple",
      "db_id": "formula_1",
      "question": "In which Formula_1 race did Lewis Hamilton rank the highest?",
      "evidence": "rank the highest refers to min(rank); Lewis Hamilton refers to the full name of the driver; Full name of the driver refers to drivers.forename and drivers.surname;",
      "generated_sql": "SELECT r.name FROM races r INNER JOIN results res ON r.raceId = res.raceId WHERE res.driverId = (SELECT driverId FROM drivers WHERE forename = 'Lewis' AND surname = 'Hamilton') AND res.rank IS NOT NULL ORDER BY res.rank ASC LIMIT 1",
      "ground_truth_sql": "SELECT\n  `name`\nFROM `races`\nWHERE\n  `raceId` IN (\n    SELECT\n      `raceId`\n    FROM `results`\n    WHERE\n      `rank` = 1\n      AND `driverId` = (\n        SELECT\n          `driverId`\n        FROM `drivers`\n        WHERE\n          `forename` = 'Lewis' AND `surname` = 'Hamilton'\n      )\n  )",
      "error_category": "语义理解错误",
      "error_reason": "生成的SQL将'rank the highest'解释为最小rank值（通过ORDER BY res.rank ASC LIMIT 1实现），这符合证据中'rank the highest refers to min(rank)'的说明。然而，预期的SQL只考虑rank=1，这可能基于问题意图是'第一名'的假设。两者在语义上存在偏差：生成的SQL寻找Lewis Hamilton排名最高的比赛（即最小rank值，可能不是1），而预期的SQL只寻找他获得第一名的比赛。如果Lewis Hamilton从未获得第一名但获得过第二名，生成的SQL会返回第二名的比赛，而预期的SQL会返回空结果，导致逻辑不一致。证据明确支持生成的SQL的语义，但预期的SQL可能反映了对问题意图的不同理解。",
      "suggested_fix": "根据证据，生成的SQL在语义上更准确，但为了匹配预期的SQL（假设意图是找第一名），应修改为：SELECT r.name FROM races r INNER JOIN results res ON r.raceId = res.raceId WHERE res.driverId = (SELECT driverId FROM drivers WHERE forename = 'Lewis' AND surname = 'Hamilton') AND res.rank = 1。如果意图是找最小rank值，生成的SQL正确，但预期的SQL需要调整。建议在实际应用中明确问题意图，并确保SQL逻辑与证据一致。"
    },
    {
      "question_id": 937,
      "difficulty": "simple",
      "db_id": "formula_1",
      "question": "What's the finish time for the driver who ranked second in 2008's Chinese Grand Prix?",
      "evidence": "finish time refers to time; Chinese Grand Prix refers to races.name = 'Chinese Grand Prix';",
      "generated_sql": "SELECT time FROM results WHERE raceId = (SELECT raceId FROM races WHERE year = 2008 AND name = 'Chinese Grand Prix') AND position = 2",
      "ground_truth_sql": "SELECT\n  `T1`.`time`\nFROM `results` AS `T1`\nINNER JOIN `races` AS `T2`\n  ON `T1`.`raceId` = `T2`.`raceId`\nWHERE\n  `T1`.`rank` = 2 AND `T2`.`name` = 'Chinese Grand Prix' AND `T2`.`year` = 2008",
      "error_category": "SQL 逻辑错误",
      "error_reason": "生成的 SQL 使用了 `position = 2`，但用户问题中的 'ranked second' 应该对应 `rank` 字段。在 Formula 1 数据库中，`rank` 通常表示完赛排名，而 `position` 可能表示发车位置或其他含义。因此，使用 `position` 会导致查询逻辑错误，可能返回发车位置第二的车手的完赛时间，而不是完赛排名第二的车手的完赛时间。此外，生成的 SQL 使用子查询，虽然功能上可能等价，但 JOIN 更清晰且更符合预期 SQL 的结构。",
      "suggested_fix": "将 WHERE 子句中的 `position = 2` 改为 `rank = 2`，以准确匹配用户问题中的 'ranked second'。同时，建议使用 JOIN 代替子查询以提高可读性和性能，例如：SELECT time FROM results JOIN races ON results.raceId = races.raceId WHERE rank = 2 AND races.name = 'Chinese Grand Prix' AND races.year = 2008。"
    },
    {
      "question_id": 944,
      "difficulty": "challenging",
      "db_id": "formula_1",
      "question": "How much faster in percentage is the champion than the driver who finished the race last in the 2008 Australian Grand Prix?",
      "evidence": "how much faster in percentage = divide(subtract(incremental time, champion time), last_driver time) * 100; last driver finished time = incremental time + champion time; only champion's finished time is represented by 'HH:MM:SS.mmm'; finished the game refers to time is not null",
      "generated_sql": "SELECT CAST((last_time - champion_time) AS DOUBLE) / CAST(last_time AS DOUBLE) * 100 AS percentage_faster FROM (SELECT (SELECT milliseconds FROM results WHERE raceId = r.raceId AND positionOrder = 1 AND time IS NOT NULL) AS champion_time, (SELECT milliseconds FROM results WHERE raceId = r.raceId AND positionOrder = (SELECT MAX(positionOrder) FROM results WHERE raceId = r.raceId AND time IS NOT NULL) AND time IS NOT NULL) AS last_time FROM races r WHERE r.year = 2008 AND r.name = 'Australian Grand Prix' LIMIT 1) AS times",
      "ground_truth_sql": "WITH `time_in_seconds` AS (\n  SELECT\n    `T1`.`positionOrder`,\n    CASE\n      WHEN `T1`.`positionOrder` = 1\n      THEN (\n        CAST(SUBSTR(`T1`.`time`, 1, 1) AS DOUBLE) * 3600\n      ) + (\n        CAST(SUBSTR(`T1`.`time`, 3, 2) AS DOUBLE) * 60\n      ) + CAST(SUBSTR(`T1`.`time`, 6) AS DOUBLE)\n      ELSE CAST(SUBSTR(`T1`.`time`, 2) AS DOUBLE)\n    END AS `time_seconds`\n  FROM `results` AS `T1`\n  INNER JOIN `races` AS `T2`\n    ON `T1`.`raceId` = `T2`.`raceId`\n  WHERE\n    `T2`.`name` = 'Australian Grand Prix'\n    AND NOT `T1`.`time` IS NULL\n    AND `T2`.`year` = 2008\n), `champion_time` AS (\n  SELECT\n    `time_seconds`\n  FROM `time_in_seconds`\n  WHERE\n    `positionOrder` = 1\n), `last_driver_incremental` AS (\n  SELECT\n    `time_seconds`\n  FROM `time_in_seconds`\n  WHERE\n    `positionOrder` = (\n      SELECT\n        MAX(`positionOrder`)\n      FROM `time_in_seconds`\n    )\n)\nSELECT\n  (\n    CAST((\n      SELECT\n        `time_seconds`\n      FROM `last_driver_incremental`\n    ) AS DOUBLE) * 100\n  ) / (\n    SELECT\n      `time_seconds` + (\n        SELECT\n          `time_seconds`\n        FROM `last_driver_incremental`\n      )\n    FROM `champion_time`\n  )",
      "error_category": "语义理解错误",
      "error_reason": "生成的SQL在多个方面误解了问题意图和计算公式。首先，它引用了不存在的字段'milliseconds'，而证据表明时间存储在'time'字段中，格式为'HH:MM:SS.mmm'，需要解析为数值。其次，计算逻辑错误：生成的SQL计算(last_time - champion_time) / last_time * 100，但根据证据和预期SQL，正确的计算应基于时间转换后的秒数，并按照特定公式计算百分比。预期的SQL使用CTE解析时间并计算(last_driver_time * 100) / (champion_time + last_driver_time)，而生成的SQL忽略了时间格式处理和正确的百分比公式，导致语义理解偏差。",
      "suggested_fix": "1. 修改字段引用：使用'time'字段而非'milliseconds'。2. 解析时间字符串：使用SUBSTR和CAST函数将'time'字段转换为秒或毫秒，例如：对于冠军时间（positionOrder=1），解析'HH:MM:SS.mmm'为秒；对于其他车手，解析为秒。3. 按照预期逻辑计算百分比：先计算冠军时间和最后一名车手时间（最后一名车手时间通过MAX(positionOrder)确定），然后计算(last_driver_time * 100) / (champion_time + last_driver_time)。4. 确保正确处理时间不为空的条件（WHERE time IS NOT NULL）。"
    },
    {
      "question_id": 959,
      "difficulty": "simple",
      "db_id": "formula_1",
      "question": "What is the fastest lap number of the champion in 2009?",
      "evidence": "in 2009 refers to year = 2009; Only the time of the champion shows in the format of \"hour: minutes: seconds.millionsecond\"",
      "generated_sql": "SELECT fastestLap FROM results WHERE driverId = (SELECT driverId FROM driverStandings WHERE raceId IN (SELECT raceId FROM races WHERE year = 2009) GROUP BY driverId ORDER BY SUM(points) DESC LIMIT 1) AND raceId IN (SELECT raceId FROM races WHERE year = 2009) AND fastestLapTime IS NOT NULL ORDER BY STR_TO_DATE(fastestLapTime, '%k:%i:%s.%f') ASC LIMIT 1",
      "ground_truth_sql": "SELECT\n  `T1`.`fastestLap`\nFROM `results` AS `T1`\nINNER JOIN `races` AS `T2`\n  ON `T1`.`raceId` = `T2`.`raceId`\nWHERE\n  `T2`.`year` = 2009 AND `T1`.`time` LIKE '_:%:__.___'",
      "error_category": "语义理解错误",
      "error_reason": "生成的SQL误解了问题意图。用户问题询问'2009年冠军的最快圈速编号'，但证据指出'只有冠军的时间显示'，这意味着在2009年，任何有时间记录的记录都属于冠军，因此不需要通过driverStandings子查询显式查找冠军。生成的SQL添加了不必要的复杂逻辑（如子查询找冠军driverId），而预期的SQL更简洁，直接通过JOIN和过滤条件（year=2009和time格式）获取结果。此外，生成的SQL使用了排序（ORDER BY），但预期的SQL未排序，可能假设只有一个匹配记录，但用户问题要求'最快'，排序是必要的。整体上，生成的SQL过度复杂化了查询，偏离了问题的简单意图。",
      "suggested_fix": "简化查询，移除冠军子查询，直接基于证据过滤2009年有时间记录的记录。建议使用类似预期的SQL结构，但确保字段名正确（如果'time'字段不存在，应使用'fastestLapTime'），并添加ORDER BY以获取最快圈速。例如：SELECT fastestLap FROM results INNER JOIN races ON results.raceId = races.raceId WHERE races.year = 2009 AND results.fastestLapTime IS NOT NULL ORDER BY STR_TO_DATE(results.fastestLapTime, '%k:%i:%s.%f') ASC LIMIT 1。如果数据库中'time'字段存在且代表时间，则调整为T1.time。"
    },
    {
      "question_id": 962,
      "difficulty": "challenging",
      "db_id": "formula_1",
      "question": "From 2000 to 2005, what percentage of drivers who were born before 1985 and the lap numbers were over 50?",
      "evidence": "born before 1985 refers to year(dob)<1985; in 2000 to 2005 refers to year between 2000 and 2005; percentage = Divide(COUNT(driverId where year (dob) <1985 and laps >50),COUNT(DriverID where year between 2000 and 2005) *100;",
      "generated_sql": "SELECT (CAST(COUNT(DISTINCT CASE WHEN YEAR(drivers.dob) < 1985 AND results.laps > 50 THEN results.driverId END) AS DOUBLE) * 100.0) / CAST(COUNT(DISTINCT results.driverId) AS DOUBLE) AS percentage FROM results INNER JOIN races ON results.raceId = races.raceId INNER JOIN drivers ON results.driverId = drivers.driverId WHERE races.year BETWEEN 2000 AND 2005",
      "ground_truth_sql": "SELECT\n  CAST(SUM(\n    CASE\n      WHEN DATE_FORMAT(CAST(`T3`.`dob` AS DATETIME), '%Y') < '1985' AND `T1`.`laps` > 50\n      THEN 1\n      ELSE 0\n    END\n  ) AS DOUBLE) * 100 / COUNT(*)\nFROM `results` AS `T1`\nINNER JOIN `races` AS `T2`\n  ON `T1`.`raceId` = `T2`.`raceId`\nINNER JOIN `drivers` AS `T3`\n  ON `T1`.`driverId` = `T3`.`driverId`\nWHERE\n  `T2`.`year` BETWEEN 2000 AND 2005",
      "error_category": "日期格式错误",
      "error_reason": "生成的SQL中使用了YEAR(drivers.dob)来提取出生年份，这假设dob字段是日期类型。然而，在实际数据库中，dob字段可能存储为字符串或其他格式，直接使用YEAR函数可能导致错误或不准确的结果。预期的SQL使用了DATE_FORMAT(CAST(`T3`.`dob` AS DATETIME), '%Y')，这更稳健地处理了日期格式，确保即使dob是字符串也能正确提取年份。",
      "suggested_fix": "修改日期处理部分，使用更通用的方法来提取年份。例如，将YEAR(drivers.dob)替换为DATE_FORMAT(drivers.dob, '%Y')，或者先将dob字段转换为日期类型再使用YEAR函数。同时，确保所有日期相关操作都考虑了字段的实际存储格式。"
    },
    {
      "question_id": 963,
      "difficulty": "moderate",
      "db_id": "formula_1",
      "question": "How many French drivers who obtain the laptime less than 02:00.00?",
      "evidence": "lap time less than 02:00.00 refers to seconds < 120;",
      "generated_sql": "SELECT COUNT(DISTINCT d.driverId) FROM drivers d INNER JOIN lapTimes lt ON d.driverId = lt.driverId WHERE d.nationality = 'French' AND lt.milliseconds < 120000",
      "ground_truth_sql": "SELECT\n  COUNT(`T1`.`driverId`)\nFROM `drivers` AS `T1`\nINNER JOIN `lapTimes` AS `T2`\n  ON `T1`.`driverId` = `T2`.`driverId`\nWHERE\n  `T1`.`nationality` = 'French'\n  AND (\n    CAST(SUBSTR(`T2`.`time`, 1, 2) AS SIGNED) * 60 + CAST(SUBSTR(`T2`.`time`, 4, 2) AS SIGNED) + CAST(SUBSTR(`T2`.`time`, 7, 2) AS DOUBLE) / 1000\n  ) < 120",
      "error_category": "语义理解错误",
      "error_reason": "生成的 SQL 假设 lapTimes 表有一个 milliseconds 字段，并直接比较毫秒值（lt.milliseconds < 120000）。然而，根据预期的 SQL 和数据库模式，lapTimes 表可能有一个 time 字段，存储为字符串格式（如 'MM:SS.mmm'），需要解析来计算秒数。这导致生成的 SQL 可能无法正确执行或逻辑错误，因为它基于错误的字段假设，没有正确理解 lap time 在数据库中的存储方式。证据指出 lap time less than 02:00.00 refers to seconds < 120，这暗示 lap time 是以字符串形式存储的，需要解析，而生成的 SQL 直接使用了毫秒比较，忽略了实际数据结构。",
      "suggested_fix": "首先确认数据库模式，检查 lapTimes 表的结构。如果 time 字段存在，应使用字符串解析方法来计算秒数，例如：CAST(SUBSTR(time, 1, 2) AS SIGNED) * 60 + CAST(SUBSTR(time, 4, 2) AS SIGNED) + CAST(SUBSTR(time, 7, 2) AS DOUBLE) / 1000 < 120。同时，使用 COUNT(DISTINCT driverId) 确保每个 French driver 只计数一次（生成的 SQL 已使用 DISTINCT，但预期的 SQL 未使用，需根据实际需求调整）。如果 milliseconds 字段存在且正确，则可以使用直接比较，但需验证数据一致性。建议根据实际表结构调整 SQL，避免字段名错误。"
    },
    {
      "question_id": 967,
      "difficulty": "simple",
      "db_id": "formula_1",
      "question": "State code numbers of top 3 yougest drivers. How many Netherlandic drivers among them?",
      "evidence": "youngest driver refers to Max (year(dob)); Netherlandic and Dutch refer to the same country",
      "generated_sql": "SELECT GROUP_CONCAT(code ORDER BY dob DESC) AS top_3_codes, SUM(CASE WHEN nationality = 'Dutch' THEN 1 ELSE 0 END) AS dutch_count FROM drivers WHERE driverId IN (SELECT driverId FROM drivers ORDER BY dob DESC LIMIT 3)",
      "ground_truth_sql": "SELECT COUNT(*) FROM (SELECT `T1`.`nationality` FROM `drivers` AS `T1` ORDER BY `T1`.`dob` DESC LIMIT 3) AS `T3` WHERE `T3`.`nationality` = 'Dutch'",
      "error_category": "返回格式错误",
      "error_reason": "生成的SQL返回两列（top_3_codes和dutch_count），而预期的SQL只返回一列（荷兰司机的数量）。用户问题包含两个部分：列出前3名最年轻司机的国家代码，以及计算其中荷兰司机的数量。但预期的SQL仅针对第二个部分，只返回计数，因此生成的SQL的返回格式与预期不符。此外，生成的SQL使用了code字段，而预期的SQL使用nationality字段，这可能表明对'State code numbers'的语义理解存在偏差（可能应使用nationality而非code）。",
      "suggested_fix": "根据预期的SQL，应简化查询以仅返回荷兰司机的数量。建议修改为：SELECT COUNT(*) FROM (SELECT nationality FROM drivers ORDER BY dob DESC LIMIT 3) AS sub WHERE nationality = 'Dutch'。如果需要同时返回国家代码列表，则需调整问题或预期SQL以包含两列输出，例如：SELECT GROUP_CONCAT(code ORDER BY dob DESC) AS top_3_codes, SUM(CASE WHEN nationality = 'Dutch' THEN 1 ELSE 0 END) AS dutch_count FROM drivers WHERE driverId IN (SELECT driverId FROM drivers ORDER BY dob DESC LIMIT 3)。但需确保code字段确实代表国家代码，否则应使用nationality字段。"
    },
    {
      "question_id": 972,
      "difficulty": "moderate",
      "db_id": "formula_1",
      "question": "Which drivers who were born in 1971 and has the fastest lap time on the race? Give id and code of these drivers.",
      "evidence": "born in 1971 refers to year(dob) = 1971; has the fastest lap time refers to fastestLapTime has values",
      "generated_sql": "SELECT DISTINCT d.driverId, d.code FROM drivers d INNER JOIN results r ON d.driverId = r.driverId WHERE YEAR(d.dob) = 1971 AND r.fastestLapTime IS NOT NULL",
      "ground_truth_sql": "SELECT\n  `T2`.`driverId`,\n  `T2`.`code`\nFROM `results` AS `T1`\nINNER JOIN `drivers` AS `T2`\n  ON `T1`.`driverId` = `T2`.`driverId`\nWHERE\n  DATE_FORMAT(CAST(`T2`.`dob` AS DATETIME), '%Y') = '1971'\n  AND NOT `T1`.`fastestLapTime` IS NULL",
      "error_category": "日期格式错误",
      "error_reason": "生成的 SQL 使用 YEAR(d.dob) 函数直接提取出生年份，但预期的 SQL 使用 DATE_FORMAT(CAST(T2.dob AS DATETIME), '%Y') 来处理日期。这表明 dob 字段可能不是标准日期类型（如存储为字符串），需要先转换为日期再提取年份。YEAR() 函数在某些数据库或字段类型下可能无法正确工作，导致年份提取失败。此外，生成的 SQL 使用 DISTINCT，而预期的 SQL 没有，这可能导致返回的行数不一致（如果同一个司机有多个结果记录，生成的 SQL 会去重，而预期的 SQL 不会）。但核心错误在于日期处理方式不匹配预期。",
      "suggested_fix": "修改日期条件为 DATE_FORMAT(CAST(d.dob AS DATETIME), '%Y') = '1971' 以确保正确提取年份，同时考虑是否需要 DISTINCT 来避免重复行。根据问题意图（列出司机），建议添加 DISTINCT 或使用 GROUP BY 来确保每个司机只出现一次。最终 SQL 可调整为：SELECT DISTINCT d.driverId, d.code FROM drivers d INNER JOIN results r ON d.driverId = r.driverId WHERE DATE_FORMAT(CAST(d.dob AS DATETIME), '%Y') = '1971' AND r.fastestLapTime IS NOT NULL。"
    },
    {
      "question_id": 978,
      "difficulty": "simple",
      "db_id": "formula_1",
      "question": "How many times the circuits were held in Austria? Please give their location and coordinates.",
      "evidence": "location coordinates refers to (lat,lng); Austria refers to country = 'Austria';",
      "generated_sql": "SELECT c.name AS circuit_name, c.location, c.lat, c.lng, COUNT(r.raceId) AS times_held FROM circuits c INNER JOIN races r ON c.circuitId = r.circuitId WHERE c.country = 'Austria' GROUP BY c.circuitId, c.name, c.location, c.lat, c.lng ORDER BY times_held DESC",
      "ground_truth_sql": "SELECT DISTINCT\n  `location`,\n  `lat`,\n  `lng`\nFROM `circuits`\nWHERE\n  `country` = 'Austria'",
      "error_category": "返回格式错误",
      "error_reason": "生成的SQL返回了多个列（circuit_name, location, lat, lng, times_held）并进行了聚合和排序，而预期的SQL只返回DISTINCT的location, lat, lng。问题询问'how many times'，但预期的SQL没有包含次数，可能问题只需要位置和坐标信息。生成的SQL过度复杂化了查询，添加了不必要的JOIN和GROUP BY，导致返回格式与预期不符。",
      "suggested_fix": "简化SQL，只从circuits表中选择DISTINCT的location, lat, lng，其中country='Austria'，如预期的SQL所示。如果确实需要返回次数，应重新评估问题意图，但根据给定的预期SQL，应忽略次数部分。"
    },
    {
      "question_id": 988,
      "difficulty": "challenging",
      "db_id": "formula_1",
      "question": "List down top 3 German drivers who has the shortest average pit stop duration and were born between 1980-1985.",
      "evidence": "Full name of the driver refers to drivers.forename and drivers.surname; born between 1980-1985 refers to 1980<= year(dob) <=1985; Average pitstop duration refers to Divide(SUM(duration),COUNT(duration)); shortest average refers to Min(avg(duration));",
      "generated_sql": "SELECT drivers.forename, drivers.surname, AVG(pitStops.milliseconds) AS avg_pit_stop_duration FROM drivers INNER JOIN pitStops ON drivers.driverId = pitStops.driverId WHERE drivers.nationality = 'German' AND YEAR(drivers.dob) BETWEEN 1980 AND 1985 GROUP BY drivers.driverId, drivers.forename, drivers.surname ORDER BY avg_pit_stop_duration ASC LIMIT 3",
      "ground_truth_sql": "SELECT\n  `T2`.`forename`,\n  `T2`.`surname`\nFROM `pitStops` AS `T1`\nINNER JOIN `drivers` AS `T2`\n  ON `T1`.`driverId` = `T2`.`driverId`\nWHERE\n  `T2`.`nationality` = 'German'\n  AND DATE_FORMAT(CAST(`T2`.`dob` AS DATETIME), '%Y') BETWEEN '1980' AND '1985'\nGROUP BY\n  `T2`.`forename`,\n  `T2`.`surname`\nORDER BY\n  AVG(`T1`.`duration`)\nLIMIT 3",
      "error_category": "数值计算错误",
      "error_reason": "生成的SQL使用了`AVG(pitStops.milliseconds)`来计算平均坑停时长，但根据证据，平均坑停时长应基于`duration`字段计算（Divide(SUM(duration),COUNT(duration))）。此外，GROUP BY子句包括了`driverId`，而预期的SQL只GROUP BY `forename`和`surname`，这可能导致分组结果不一致。返回列中包含了`avg_pit_stop_duration`，而预期的SQL只返回司机的名字。日期处理方式（使用`YEAR(drivers.dob)` vs `DATE_FORMAT(CAST(T2.dob AS DATETIME), '%Y')`）虽不同但都正确，不是主要错误。",
      "suggested_fix": "1. 将`AVG(pitStops.milliseconds)`修改为`AVG(pitStops.duration)`，以匹配证据中的平均时长计算。\n2. 调整GROUP BY子句，只GROUP BY `forename`和`surname`，以匹配预期的SQL逻辑（尽管`driverId`更准确，但根据预期SQL简化）。\n3. 修改SELECT子句，只选择`forename`和`surname`，不选择平均时长，除非用户明确要求显示平均时长。"
    },
    {
      "question_id": 989,
      "difficulty": "moderate",
      "db_id": "formula_1",
      "question": "Who is the champion of the Canadian Grand Prix in 2008? Indicate his finish time.",
      "evidence": "Only the time of the champion shows in the format of \"hour: minutes: seconds.millionsecond\";",
      "generated_sql": "SELECT d.forename, d.surname, r.time AS finish_time FROM results r INNER JOIN drivers d ON r.driverId = d.driverId WHERE r.raceId = (SELECT raceId FROM races WHERE year = 2008 AND name = 'Canadian Grand Prix') AND r.position = 1;",
      "ground_truth_sql": "SELECT\n  `T1`.`time`\nFROM `results` AS `T1`\nINNER JOIN `races` AS `T2`\n  ON `T1`.`raceId` = `T2`.`raceId`\nWHERE\n  `T2`.`name` = 'Canadian Grand Prix'\n  AND `T2`.`year` = 2008\n  AND `T1`.`time` LIKE '_:%:__.___'",
      "error_category": "SQL 逻辑错误",
      "error_reason": "生成的 SQL 正确识别了冠军（通过 position=1），但没有应用时间格式过滤。证据指出只有冠军的时间以特定格式显示（'hour: minutes: seconds.millionsecond'），而生成的 SQL 直接选择了 r.time，未过滤格式。这可能导致返回非冠军的时间或格式不一致的数据，影响查询结果的准确性和一致性。预期的 SQL 通过 LIKE 条件过滤时间格式，确保了数据符合证据描述。",
      "suggested_fix": "在生成的 SQL 的 WHERE 子句中添加时间格式过滤条件，例如 `AND r.time LIKE '_:%:__.___'`，以匹配证据中指定的时间格式。同时，保持返回 driver 的 forename、surname 和 finish_time，因为用户问题明确要求返回冠军的身份和完成时间。改进后的 SQL 可能类似于：\n```sql\nSELECT d.forename, d.surname, r.time AS finish_time FROM results r INNER JOIN drivers d ON r.driverId = d.driverId WHERE r.raceId = (SELECT raceId FROM races WHERE year = 2008 AND name = 'Canadian Grand Prix') AND r.position = 1 AND r.time LIKE '_:%:__.___';\n```"
    },
    {
      "question_id": 1002,
      "difficulty": "moderate",
      "db_id": "formula_1",
      "question": "As of the present, what is the full name of the youngest racer? Indicate her nationality and the name of the race to which he/she first joined.",
      "evidence": "full name refers to forename+surname; Youngest racer = MAX(dob)",
      "generated_sql": "SELECT CONCAT(d.forename, ' ', d.surname) AS full_name, d.nationality, (SELECT r.name FROM results res JOIN races r ON res.raceId = r.raceId WHERE res.driverId = d.driverId ORDER BY r.date ASC LIMIT 1) AS first_race_name FROM drivers d WHERE d.driverId = (SELECT driverId FROM drivers ORDER BY dob DESC LIMIT 1)",
      "ground_truth_sql": "SELECT `T1`.`forename`, `T1`.`surname`, `T1`.`nationality`, `T3`.`name` FROM `drivers` AS `T1` INNER JOIN `driverStandings` AS `T2` ON `T1`.`driverId` = `T2`.`driverId` INNER JOIN `races` AS `T3` ON `T2`.`raceId` = `T3`.`raceId` ORDER BY `T1`.`dob` DESC LIMIT 1",
      "error_category": "SQL 逻辑错误",
      "error_reason": "生成的 SQL 使用了 results 表和子查询来获取赛事信息，而预期的 SQL 使用了 driverStandings 表和 JOIN。这可能导致在数据库 schema 中 results 表不存在或不正确，从而查询失败。此外，生成的 SQL 没有遵循预期的查询结构（预期的 SQL 通过 JOIN driverStandings 和 races 来获取赛事名称），而是使用了不同的表和子查询方式。虽然生成的 SQL 在逻辑上可能正确（通过子查询按赛事日期排序获取第一个赛事），但它与预期的 SQL 结构不匹配，可能无法在给定的数据库 schema 中执行。",
      "suggested_fix": "修改生成的 SQL 以使用 driverStandings 表进行 JOIN，类似于预期的 SQL。同时，为了正确获取第一个赛事名称，应该添加按赛事日期排序的 ORDER BY 子句。例如，可以将生成的 SQL 改为：SELECT CONCAT(d.forename, ' ', d.surname) AS full_name, d.nationality, (SELECT r.name FROM driverStandings ds JOIN races r ON ds.raceId = r.raceId WHERE ds.driverId = d.driverId ORDER BY r.date ASC LIMIT 1) AS first_race_name FROM drivers d WHERE d.driverId = (SELECT driverId FROM drivers ORDER BY dob DESC LIMIT 1)。或者，如果预期的 SQL 是正确的，则直接使用 driverStandings 表并确保排序逻辑正确。"
    },
    {
      "question_id": 1011,
      "difficulty": "challenging",
      "db_id": "formula_1",
      "question": "Which top 20 driver created the shortest lap time ever record in a Formula_1 race? Please give them full names.",
      "evidence": "shortest lap time refers to MIN(time); the time format for the shortest lap time is 'MM:SS.mmm' or 'M:SS.mmm'; full name of the driver refers to forename, surname",
      "generated_sql": "SELECT d.forename, d.surname FROM (SELECT driverId, MIN(milliseconds) as min_milliseconds FROM lapTimes GROUP BY driverId ORDER BY min_milliseconds ASC LIMIT 20) AS lt INNER JOIN drivers d ON lt.driverId = d.driverId ORDER BY lt.min_milliseconds ASC",
      "ground_truth_sql": "WITH lap_times_in_seconds AS (SELECT driverId, (CASE WHEN SUBSTR(time, 1, INSTR(time, ':') - 1) <> '' THEN CAST(SUBSTR(time, 1, INSTR(time, ':') - 1) AS FLOAT) * 60 ELSE 0 END + CASE WHEN SUBSTR(time, INSTR(time, ':') + 1, INSTR(time, '.') - INSTR(time, ':') - 1) <> '' THEN CAST(SUBSTR(time, INSTR(time, ':') + 1, INSTR(time, '.') - INSTR(time, ':') - 1) AS FLOAT) ELSE 0 END + CASE WHEN SUBSTR(time, INSTR(time, '.') + 1) <> '' THEN CAST(SUBSTR(time, INSTR(time, '.') + 1) AS FLOAT) / 1000 ELSE 0 END) AS time_in_seconds FROM lapTimes) SELECT T2.forename, T2.surname, T1.driverId FROM (SELECT driverId, MIN(time_in_seconds) AS min_time_in_seconds FROM lap_times_in_seconds GROUP BY driverId) AS T1 INNER JOIN drivers AS T2 ON T1.driverId = T2.driverId ORDER BY T1.min_time_in_seconds ASC LIMIT 20",
      "error_category": "语义理解错误",
      "error_reason": "生成的SQL错误地假设数据库中存在'milliseconds'字段，但证据表明时间存储在'time'字段中，格式为'MM:SS.mmm'或'M:SS.mmm'。问题要求找出最短圈速记录，证据指定使用MIN(time)，但生成的SQL使用了MIN(milliseconds)，这可能导致字段不存在错误或计算结果不准确。此外，生成的SQL未处理时间字符串的解析，而预期的SQL展示了如何将时间转换为秒后取最小值，这更符合问题意图和数据结构。",
      "suggested_fix": "修改SQL以正确处理时间字段：使用字符串函数（如SUBSTR、INSTR）将'time'字段转换为数值（秒或毫秒），然后取每个车手的最小值。参考预期的SQL，使用CTE（Common Table Expression）进行时间转换，再分组、排序和限制。例如：\n```sql\nSELECT d.forename, d.surname\nFROM (\n    SELECT driverId, MIN(time_in_seconds) as min_time\n    FROM (\n        SELECT driverId,\n               (CASE WHEN SUBSTR(time, 1, INSTR(time, ':') - 1) <> '' THEN CAST(SUBSTR(time, 1, INSTR(time, ':') - 1) AS FLOAT) * 60 ELSE 0 END\n                + CASE WHEN SUBSTR(time, INSTR(time, ':') + 1, INSTR(time, '.') - INSTR(time, ':') - 1) <> '' THEN CAST(SUBSTR(time, INSTR(time, ':') + 1, INSTR(time, '.') - INSTR(time, ':') - 1) AS FLOAT) ELSE 0 END\n                + CASE WHEN SUBSTR(time, INSTR(time, '.') + 1) <> '' THEN CAST(SUBSTR(time, INSTR(time, '.') + 1) AS FLOAT) / 1000 ELSE 0 END) AS time_in_seconds\n        FROM lapTimes\n    ) AS converted_times\n    GROUP BY driverId\n    ORDER BY min_time ASC\n    LIMIT 20\n) AS lt\nINNER JOIN drivers d ON lt.driverId = d.driverId\nORDER BY lt.min_time ASC\n```\n确保数据库表结构支持此查询，并验证时间格式是否一致。"
    },
    {
      "question_id": 1014,
      "difficulty": "challenging",
      "db_id": "formula_1",
      "question": "Please list the lap records for the circuits in Italy.",
      "evidence": "lap record means the fastest time recorded which refers to time",
      "generated_sql": "SELECT circuits.name AS circuit_name, drivers.forename, drivers.surname, results.fastestLapTime AS lap_record FROM circuits INNER JOIN races ON circuits.circuitId = races.circuitId INNER JOIN results ON races.raceId = results.raceId INNER JOIN drivers ON results.driverId = drivers.driverId WHERE circuits.country = 'Italy' AND results.fastestLapTime IS NOT NULL ORDER BY circuits.name, results.fastestLapTime;",
      "ground_truth_sql": "WITH `fastest_lap_times` AS (\n  SELECT\n    `T1`.`raceId`,\n    `T1`.`FastestLapTime`,\n    (\n      CAST(SUBSTR(`T1`.`FastestLapTime`, 1, INSTR(`T1`.`FastestLapTime`, ':') - 1) AS DOUBLE) * 60\n    ) + (\n      CAST(SUBSTR(\n        `T1`.`FastestLapTime`,\n        INSTR(`T1`.`FastestLapTime`, ':') + 1,\n        INSTR(`T1`.`FastestLapTime`, '.') - INSTR(`T1`.`FastestLapTime`, ':') - 1\n      ) AS DOUBLE)\n    ) + (\n      CAST(SUBSTR(`T1`.`FastestLapTime`, INSTR(`T1`.`FastestLapTime`, '.') + 1) AS DOUBLE) / 1000\n    ) AS `time_in_seconds`\n  FROM `results` AS `T1`\n  WHERE\n    NOT `T1`.`FastestLapTime` IS NULL\n)\nSELECT\n  `T1`.`FastestLapTime` AS `lap_record`\nFROM `results` AS `T1`\nINNER JOIN `races` AS `T2`\n  ON `T1`.`raceId` = `T2`.`raceId`\nINNER JOIN `circuits` AS `T3`\n  ON `T2`.`circuitId` = `T3`.`circuitId`\nINNER JOIN (\n  SELECT\n    MIN(`fastest_lap_times`.`time_in_seconds`) AS `min_time_in_seconds`\n  FROM `fastest_lap_times`\n  INNER JOIN `races` AS `T2`\n    ON `fastest_lap_times`.`raceId` = `T2`.`raceId`\n  INNER JOIN `circuits` AS `T3`\n    ON `T2`.`circuitId` = `T3`.`circuitId`\n  WHERE\n    `T3`.`country` = 'Italy'\n) AS `T4`\n  ON (\n    CAST(SUBSTR(`T1`.`FastestLapTime`, 1, INSTR(`T1`.`FastestLapTime`, ':') - 1) AS DOUBLE) * 60\n  ) + (\n    CAST(SUBSTR(\n      `T1`.`FastestLapTime`,\n      INSTR(`T1`.`FastestLapTime`, ':') + 1,\n      INSTR(`T1`.`FastestLapTime`, '.') - INSTR(`T1`.`FastestLapTime`, ':') - 1\n    ) AS DOUBLE)\n  ) + (\n    CAST(SUBSTR(`T1`.`FastestLapTime`, INSTR(`T1`.`FastestLapTime`, '.') + 1) AS DOUBLE) / 1000\n  ) = `T4`.`min_time_in_seconds`\nLIMIT 1",
      "error_category": "SQL逻辑错误",
      "error_reason": "生成的SQL没有正确实现'每个赛道的最快圈速记录'的语义。用户问题要求列出意大利赛道的圈速记录，即每个赛道的最快圈速时间。生成的SQL返回了所有符合条件的记录（意大利赛道且圈速时间非空），但没有确保每个赛道只返回最快的一条记录。它按赛道名称和圈速时间排序，但可能返回多个记录 per circuit。此外，圈速时间（fastestLapTime）可能存储为字符串格式（如'1:23.456'），直接排序或比较可能导致错误，因为字符串排序不按时间值顺序（例如，'2:00.000'可能被误认为比'1:59.999'大）。预期的SQL通过CTE计算圈速时间的秒数，并找到每个赛道的最小值，从而确保只返回最快记录。",
      "suggested_fix": "修改SQL以确保每个赛道只返回最快圈速记录。建议使用窗口函数（如ROW_NUMBER()）为每个赛道的圈速记录排序，并取排名为1的记录。例如：\n```sql\nSELECT circuit_name, forename, surname, lap_record\nFROM (\n  SELECT circuits.name AS circuit_name, drivers.forename, drivers.surname, results.fastestLapTime AS lap_record,\n         ROW_NUMBER() OVER (PARTITION BY circuits.circuitId ORDER BY results.fastestLapTime) AS rn\n  FROM circuits\n  INNER JOIN races ON circuits.circuitId = races.circuitId\n  INNER JOIN results ON races.raceId = results.raceId\n  INNER JOIN drivers ON results.driverId = drivers.driverId\n  WHERE circuits.country = 'Italy' AND results.fastestLapTime IS NOT NULL\n) ranked\nWHERE rn = 1\nORDER BY circuit_name;\n```\n同时，处理圈速时间格式：如果fastestLapTime是字符串，应解析并转换为数值（如秒）以正确比较。可以借鉴预期的SQL中的方法，使用SUBSTR和INSTR提取分钟、秒和毫秒部分，并计算总秒数。例如，添加一个计算列：\n```sql\n(CAST(SUBSTR(results.fastestLapTime, 1, INSTR(results.fastestLapTime, ':') - 1) AS DOUBLE) * 60) +\n(CAST(SUBSTR(results.fastestLapTime, INSTR(results.fastestLapTime, ':') + 1, INSTR(results.fastestLapTime, '.') - INSTR(results.fastestLapTime, ':') - 1) AS DOUBLE)) +\n(CAST(SUBSTR(results.fastestLapTime, INSTR(results.fastestLapTime, '.') + 1) AS DOUBLE) / 1000) AS time_in_seconds\n```\n然后在ORDER BY中使用time_in_seconds。此外，根据用户问题，可能不需要车手信息（forename, surname），但生成的SQL包含了它们。建议根据需求调整返回列，例如只返回circuit_name和lap_record。"
    },
    {
      "question_id": 726,
      "difficulty": "moderate",
      "db_id": "superhero",
      "question": "Rank heroes published by Marvel Comics by their height in descending order.",
      "evidence": "name refers to superhero_name; the tallest hero refers to MAX(height_cm); published by Marvel Comics refers to publisher_name = 'Marvel Comics'",
      "generated_sql": "SELECT superhero.superhero_name, superhero.height_cm FROM superhero INNER JOIN publisher ON superhero.publisher_id = publisher.id WHERE publisher.publisher_name = 'Marvel Comics' ORDER BY superhero.height_cm DESC",
      "ground_truth_sql": "SELECT\n  `superhero_name`,\n  `height_cm`,\n  RANK() OVER (ORDER BY `height_cm` DESC) AS `HeightRank`\nFROM `superhero`\nINNER JOIN `publisher`\n  ON `superhero`.`publisher_id` = `publisher`.`id`\nWHERE\n  `publisher`.`publisher_name` = 'Marvel Comics'",
      "error_category": "语义理解错误",
      "error_reason": "系统未能正确理解问题中的'Rank'含义。用户问题要求对由漫威漫画出版的英雄按身高降序排列并给出排名，但生成的SQL仅实现了排序（ORDER BY height_cm DESC）而未计算排名。这属于理解问题意图错误，因为排名是问题的核心要求，而生成的SQL缺少了排名列，导致逻辑不完整和返回格式错误（预期返回三列，实际只返回两列）。证据中提到'tallest hero refers to MAX(height_cm)'，但问题更侧重于整体排名而非仅最大值，系统可能误将重点放在排序而非排名上。",
      "suggested_fix": "在SELECT子句中添加一个使用窗口函数的排名列，例如：RANK() OVER (ORDER BY height_cm DESC) AS HeightRank。保持现有的JOIN和WHERE条件不变，因为它们正确过滤了漫威漫画出版的英雄。最终SQL应类似于预期的SQL，确保排名基于height_cm降序排列以符合'descending order'的要求。"
    },
    {
      "question_id": 728,
      "difficulty": "moderate",
      "db_id": "superhero",
      "question": "Rank superheroes from Marvel Comics by their eye color popularity, starting with the most common color.",
      "evidence": "the superheroes from Marvel Comics refers to publisher_name = 'Marvel Comics'; most common color refers to COUNT(superhero.id) DESC;",
      "generated_sql": "SELECT colour.colour, COUNT(superhero.id) as popularity FROM superhero INNER JOIN publisher ON superhero.publisher_id = publisher.id INNER JOIN colour ON superhero.eye_colour_id = colour.id WHERE publisher.publisher_name = 'Marvel Comics' GROUP BY colour.id, colour.colour ORDER BY popularity DESC",
      "ground_truth_sql": "SELECT\n  `colour`.`colour` AS `EyeColor`,\n  COUNT(`superhero`.`id`) AS `Count`,\n  RANK() OVER (ORDER BY COUNT(`superhero`.`id`) DESC) AS `PopularityRank`\nFROM `superhero`\nINNER JOIN `colour`\n  ON `superhero`.`eye_colour_id` = `colour`.`id`\nINNER JOIN `publisher`\n  ON `superhero`.`publisher_id` = `publisher`.`id`\nWHERE\n  `publisher`.`publisher_name` = 'Marvel Comics'\nGROUP BY\n  `colour`.`colour`",
      "error_category": "语义理解错误",
      "error_reason": "系统误解了用户问题中的 'Rank' 意图。用户要求按眼色流行度排名，但生成的SQL仅按流行度降序排序（ORDER BY popularity DESC），而未生成排名列。预期的SQL使用 RANK() 窗口函数来明确计算排名，但生成的SQL缺少此功能。此外，生成的SQL在GROUP BY子句中包括了colour.id，虽然逻辑上等价（因为colour.id唯一确定colour.colour），但预期的SQL仅按colour.colour分组，更简洁且符合分组意图。",
      "suggested_fix": "在生成的SQL中添加排名列：在SELECT子句中加入 RANK() OVER (ORDER BY COUNT(superhero.id) DESC) AS PopularityRank，并修改GROUP BY为GROUP BY colour.colour。同时，调整列别名以匹配预期格式，例如将colour.colour重命名为EyeColor，将COUNT(superhero.id)重命名为Count。确保返回三列：EyeColor、Count和PopularityRank。"
    },
    {
      "question_id": 730,
      "difficulty": "challenging",
      "db_id": "superhero",
      "question": "List the superheroes from Marvel Comics who have the super power of 'Super Strength'.",
      "evidence": "the superheroes from Marvel Comics refers to publisher_name = 'Marvel Comics'; super power of \"Super Strength\" refers to power_name = 'Super Strength';",
      "generated_sql": "SELECT DISTINCT s.superhero_name FROM superhero s INNER JOIN publisher p ON s.publisher_id = p.id INNER JOIN hero_power hp ON s.id = hp.hero_id INNER JOIN superpower sp ON hp.power_id = sp.id WHERE p.publisher_name = 'Marvel Comics' AND sp.power_name = 'Super Strength';",
      "ground_truth_sql": "SELECT\n  `superhero_name`\nFROM `superhero` AS `T1`\nWHERE\n  EXISTS(\n    SELECT\n      1\n    FROM `hero_power` AS `T2`\n    INNER JOIN `superpower` AS `T3`\n      ON `T2`.`power_id` = `T3`.`id`\n    WHERE\n      `T3`.`power_name` = 'Super Strength' AND `T1`.`id` = `T2`.`hero_id`\n  )\n  AND EXISTS(\n    SELECT\n      1\n    FROM `publisher` AS `T4`\n    WHERE\n      `T4`.`publisher_name` = 'Marvel Comics' AND `T1`.`publisher_id` = `T4`.`id`\n  )",
      "error_category": "SQL 逻辑错误",
      "error_reason": "生成的 SQL 使用了多个 INNER JOIN 来连接 superhero、publisher、hero_power 和 superpower 表，并在 WHERE 子句中指定条件。虽然逻辑上可能等价于预期的 SQL，但预期的 SQL 使用了 EXISTS 子查询来检查条件。生成的 SQL 没有使用子查询，这可能导致性能差异或在某些数据库中逻辑不一致。此外，生成的 SQL 使用了 DISTINCT 来去重，而预期的 SQL 通过 EXISTS 子查询确保每个 superhero_name 只出现一次，无需 DISTINCT。这种结构差异表明生成的 SQL 没有正确遵循预期的查询模式，可能源于对问题意图或数据库优化的误解。",
      "suggested_fix": "修改生成的 SQL，使用 EXISTS 子查询来检查超能力和出版商条件。例如，将生成的 SQL 改为：SELECT superhero_name FROM superhero WHERE EXISTS (SELECT 1 FROM hero_power hp JOIN superpower sp ON hp.power_id = sp.id WHERE sp.power_name = 'Super Strength' AND superhero.id = hp.hero_id) AND EXISTS (SELECT 1 FROM publisher WHERE publisher_name = 'Marvel Comics' AND superhero.publisher_id = publisher.id); 这样更符合预期的 SQL 结构，确保逻辑清晰且可能更高效。"
    },
    {
      "question_id": 732,
      "difficulty": "moderate",
      "db_id": "superhero",
      "question": "Which publisher published the slowest superhero?",
      "evidence": "the slowest superhero refers to attribute_name = 'Speed' where MIN(attribute_value); publisher refers to publisher_name",
      "generated_sql": "SELECT DISTINCT publisher.publisher_name FROM hero_attribute ha INNER JOIN attribute a ON ha.attribute_id = a.id INNER JOIN superhero s ON ha.hero_id = s.id INNER JOIN publisher p ON s.publisher_id = p.id WHERE a.attribute_name = 'Speed' AND ha.attribute_value = (SELECT MIN(ha2.attribute_value) FROM hero_attribute ha2 INNER JOIN attribute a2 ON ha2.attribute_id = a2.id WHERE a2.attribute_name = 'Speed')",
      "ground_truth_sql": "SELECT\n  `T2`.`publisher_name`\nFROM `superhero` AS `T1`\nINNER JOIN `publisher` AS `T2`\n  ON `T1`.`publisher_id` = `T2`.`id`\nINNER JOIN `hero_attribute` AS `T3`\n  ON `T1`.`id` = `T3`.`hero_id`\nINNER JOIN `attribute` AS `T4`\n  ON `T3`.`attribute_id` = `T4`.`id`\nWHERE\n  `T4`.`attribute_name` = 'Speed'\nORDER BY\n  `T3`.`attribute_value`\nLIMIT 1",
      "error_category": "返回格式错误",
      "error_reason": "生成的SQL使用子查询匹配最小速度值，可能返回多个具有相同最小速度的英雄的出版商（使用DISTINCT去重后），导致返回多行。而预期的SQL通过ORDER BY和LIMIT 1确保只返回一行。如果数据库中存在多个英雄共享相同的最小速度值，生成的SQL会返回多个出版商，而预期的SQL只返回一个，这不符合问题可能期望的单一出版商返回格式。此外，生成的SQL中表别名使用不一致（如SELECT DISTINCT publisher.publisher_name，但JOIN中使用了p别名），但这不是主要问题。",
      "suggested_fix": "修改生成的SQL，添加ORDER BY ha.attribute_value LIMIT 1来确保只返回一行，或者直接采用预期的SQL结构。例如：SELECT p.publisher_name FROM hero_attribute ha INNER JOIN attribute a ON ha.attribute_id = a.id INNER JOIN superhero s ON ha.hero_id = s.id INNER JOIN publisher p ON s.publisher_id = p.id WHERE a.attribute_name = 'Speed' ORDER BY ha.attribute_value LIMIT 1。同时，确保表别名一致（如使用p.publisher_name）。如果问题意图是返回所有出版商，则需要移除LIMIT，但根据预期的SQL，应遵循LIMIT 1的逻辑。"
    },
    {
      "question_id": 736,
      "difficulty": "moderate",
      "db_id": "superhero",
      "question": "Who is the dumbest superhero?",
      "evidence": "the dumbest superhero refers to MIN(attribute_value) where attribute_name = 'Intelligence'",
      "generated_sql": "SELECT s.superhero_name FROM superhero s INNER JOIN hero_attribute ha ON s.id = ha.hero_id INNER JOIN attribute a ON ha.attribute_id = a.id WHERE a.attribute_name = 'Intelligence' AND ha.attribute_value = (SELECT MIN(ha2.attribute_value) FROM hero_attribute ha2 INNER JOIN attribute a2 ON ha2.attribute_id = a2.id WHERE a2.attribute_name = 'Intelligence')",
      "ground_truth_sql": "SELECT\n  `T1`.`superhero_name`\nFROM `superhero` AS `T1`\nINNER JOIN `hero_attribute` AS `T2`\n  ON `T1`.`id` = `T2`.`hero_id`\nINNER JOIN `attribute` AS `T3`\n  ON `T2`.`attribute_id` = `T3`.`id`\nWHERE\n  `T3`.`attribute_name` = 'Intelligence'\nORDER BY\n  `T2`.`attribute_value`\nLIMIT 1",
      "error_category": "返回格式错误",
      "error_reason": "生成的SQL正确理解了问题意图，即找到智力属性值最小的超级英雄。它使用子查询来找到最小值，然后匹配。然而，生成的SQL没有使用ORDER BY或LIMIT子句，因此如果多个超级英雄具有相同的最低智力值，它将返回所有这些超级英雄。用户问题使用单数'who'，暗示期望一个结果，而预期的SQL使用ORDER BY和LIMIT 1来确保只返回一个结果。因此，生成的SQL在返回结果的数量上不符合预期，导致返回格式错误。",
      "suggested_fix": "修改生成的SQL，添加ORDER BY子句按attribute_value升序排序，并使用LIMIT 1来限制只返回一个结果。例如，可以将生成的SQL改为：SELECT s.superhero_name FROM superhero s INNER JOIN hero_attribute ha ON s.id = ha.hero_id INNER JOIN attribute a ON ha.attribute_id = a.id WHERE a.attribute_name = 'Intelligence' ORDER BY ha.attribute_value LIMIT 1。这样可以确保只返回一个结果，更符合用户问题的意图。"
    },
    {
      "question_id": 758,
      "difficulty": "moderate",
      "db_id": "superhero",
      "question": "Provide the hair colour of the human superhero who is 185 cm tall.",
      "evidence": "185 cm tall refers to height_cm = 185; human superhero refers to race = 'human'; hair colour refers to colour where hair_colour_id = colour.id;",
      "generated_sql": "SELECT colour.colour FROM superhero INNER JOIN race ON superhero.race_id = race.id INNER JOIN colour ON superhero.hair_colour_id = colour.id WHERE superhero.height_cm = 185 AND race.race = 'human'",
      "ground_truth_sql": "SELECT DISTINCT\n  `T3`.`colour`\nFROM `superhero` AS `T1`\nINNER JOIN `race` AS `T2`\n  ON `T1`.`race_id` = `T2`.`id`\nINNER JOIN `colour` AS `T3`\n  ON `T1`.`hair_colour_id` = `T3`.`id`\nWHERE\n  `T1`.`height_cm` = 185 AND `T2`.`race` = 'Human'",
      "error_category": "SQL逻辑错误",
      "error_reason": "生成的SQL存在两个主要逻辑错误：1) 缺少DISTINCT子句，可能导致返回重复的头发颜色值，而用户问题通常期望唯一结果；2) WHERE条件中race.race = 'human'的大小写可能与数据库实际存储值不匹配（预期的SQL使用'Human'），这可能导致查询结果为空或错误。此外，生成的SQL未使用别名，但这不是主要错误。",
      "suggested_fix": "1) 在SELECT语句中添加DISTINCT关键字，以确保返回唯一颜色值；2) 检查数据库中race字段的实际值，将条件改为race.race = 'Human'（或根据数据库大小写敏感性调整）；3) 建议使用别名提高可读性，如预期的SQL所示。"
    },
    {
      "question_id": 766,
      "difficulty": "moderate",
      "db_id": "superhero",
      "question": "What is the hero's full name with the highest attribute in strength?",
      "evidence": "highest attribute in strength refers to MAX(attribute_value) WHERE attribute_name = 'strength';",
      "generated_sql": "SELECT s.full_name FROM superhero s INNER JOIN hero_attribute ha ON s.id = ha.hero_id INNER JOIN attribute a ON ha.attribute_id = a.id WHERE a.attribute_name = 'strength' AND ha.attribute_value = (SELECT MAX(ha2.attribute_value) FROM hero_attribute ha2 INNER JOIN attribute a2 ON ha2.attribute_id = a2.id WHERE a2.attribute_name = 'strength')",
      "ground_truth_sql": "SELECT `T1`.`full_name` FROM `superhero` AS `T1` INNER JOIN `hero_attribute` AS `T2` ON `T1`.`id` = `T2`.`hero_id` INNER JOIN `attribute` AS `T3` ON `T2`.`attribute_id` = `T3`.`id` WHERE `T3`.`attribute_name` = 'Strength' ORDER BY `T2`.`attribute_value` DESC LIMIT 1",
      "error_category": "返回格式错误",
      "error_reason": "生成的SQL使用子查询匹配最大强度值，可能返回多个英雄如果多个英雄具有相同最大强度值，而预期的SQL使用ORDER BY和LIMIT 1只返回一行。用户问题要求'最高属性'，但未明确指定平局处理，预期结果通过LIMIT 1确保单行返回。生成的SQL逻辑上正确但返回行数不符合预期（可能多行），而预期SQL严格返回一行。",
      "suggested_fix": "修改SQL使用ORDER BY ha.attribute_value DESC LIMIT 1来获取最高强度的英雄，确保只返回一行。同时，检查属性名的大小写是否与数据库一致（例如，将'strength'改为'Strength'以匹配预期SQL）。如果业务逻辑允许返回多个英雄，应明确说明或调整查询以处理平局。"
    },
    {
      "question_id": 772,
      "difficulty": "challenging",
      "db_id": "superhero",
      "question": "List the eyes, hair and skin colour of all female superheroes published by Dark Horse Comics.",
      "evidence": "eyes refers to eye_colour_id; hair refers to hair_colour_id; skin colour refers to skin_colour_id; female superheroes refers to gender = 'Female'; published by Dark Horse Comics refers to publisher_name = 'Dark Horse Comics';",
      "generated_sql": "SELECT colour_eye.colour AS eye_colour, colour_hair.colour AS hair_colour, colour_skin.colour AS skin_colour FROM superhero INNER JOIN gender ON superhero.gender_id = gender.id AND gender.gender = 'Female' INNER JOIN publisher ON superhero.publisher_id = publisher.id AND publisher.publisher_name = 'Dark Horse Comics' INNER JOIN colour AS colour_eye ON superhero.eye_colour_id = colour_eye.id INNER JOIN colour AS colour_hair ON superhero.hair_colour_id = colour_hair.id INNER JOIN colour AS colour_skin ON superhero.skin_colour_id = colour_skin.id",
      "ground_truth_sql": "SELECT\n  `T1`.`eye_colour_id`,\n  `T1`.`hair_colour_id`,\n  `T1`.`skin_colour_id`\nFROM `superhero` AS `T1`\nINNER JOIN `publisher` AS `T2`\n  ON `T2`.`id` = `T1`.`publisher_id`\nINNER JOIN `gender` AS `T3`\n  ON `T3`.`id` = `T1`.`gender_id`\nWHERE\n  `T2`.`publisher_name` = 'Dark Horse Comics' AND `T3`.`gender` = 'Female'",
      "error_category": "语义理解错误",
      "error_reason": "系统误解了用户问题中 'eyes, hair and skin colour' 的含义。根据证据，这些术语明确指代 ID 字段（eye_colour_id, hair_colour_id, skin_colour_id），但生成的 SQL 错误地选择了颜色名称（从 colour 表），导致返回了错误的数据。此外，生成的 SQL 在 JOIN 条件中嵌入过滤条件，而预期的 SQL 使用 WHERE 子句，但这不是核心错误。主要问题在于语义映射错误：用户问题要求列出 ID，但系统误以为需要颜色名称。",
      "suggested_fix": "修改 SQL 以直接从 superhero 表中选择 eye_colour_id, hair_colour_id, skin_colour_id，并使用 WHERE 子句过滤条件：WHERE publisher_name = 'Dark Horse Comics' AND gender = 'Female'。移除不必要的 colour 表 JOIN，因为证据指定返回的是 ID 而非颜色名称。建议的 SQL 应类似于预期的 SQL：SELECT eye_colour_id, hair_colour_id, skin_colour_id FROM superhero INNER JOIN publisher ON superhero.publisher_id = publisher.id INNER JOIN gender ON superhero.gender_id = gender.id WHERE publisher.publisher_name = 'Dark Horse Comics' AND gender.gender = 'Female'。"
    },
    {
      "question_id": 794,
      "difficulty": "moderate",
      "db_id": "superhero",
      "question": "Which hero was the fastest?",
      "evidence": "which hero refers to superhero_name; fastest refers to MAX(attribute_value) WHERE attribute_name = 'Speed';",
      "generated_sql": "SELECT s.superhero_name FROM superhero s JOIN hero_attribute ha ON s.id = ha.hero_id JOIN attribute a ON ha.attribute_id = a.id WHERE a.attribute_name = 'Speed' AND ha.attribute_value = (SELECT MAX(ha2.attribute_value) FROM hero_attribute ha2 JOIN attribute a2 ON ha2.attribute_id = a2.id WHERE a2.attribute_name = 'Speed')",
      "ground_truth_sql": "SELECT `T1`.`superhero_name` FROM `superhero` AS `T1` INNER JOIN `hero_attribute` AS `T2` ON `T1`.`id` = `T2`.`hero_id` INNER JOIN `attribute` AS `T3` ON `T2`.`attribute_id` = `T3`.`id`WHERE `T3`.`attribute_name` = 'Speed' AND `T2`.`attribute_value` = (SELECT MAX(`attribute_value`) FROM `hero_attribute` AS `T2b` WHERE `T2b`.`attribute_id` = `T3`.`id` ) LIMIT 1",
      "error_category": "SQL 逻辑错误",
      "error_reason": "生成的 SQL 在逻辑上存在两个主要问题：1. 子查询中不必要的连接 attribute 表，虽然逻辑正确但效率低下，且未充分利用主查询已过滤的属性 ID；2. 缺少 LIMIT 1 子句，导致可能返回多个英雄（如果多个英雄的速度值相同），而用户问题 'Which hero was the fastest?' 暗示期望单个英雄。预期的 SQL 通过简化子查询（仅使用 hero_attribute 表并通过 attribute_id 过滤）和添加 LIMIT 1 来解决这些问题。",
      "suggested_fix": "修改生成的 SQL：1. 优化子查询，移除 attribute 表的连接，直接使用主查询中已获取的速度属性 ID（例如，将子查询改为 SELECT MAX(ha2.attribute_value) FROM hero_attribute ha2 WHERE ha2.attribute_id = a.id）；2. 添加 LIMIT 1 以确保只返回一个英雄。改进后的 SQL 示例：\n```sql\nSELECT s.superhero_name FROM superhero s JOIN hero_attribute ha ON s.id = ha.hero_id JOIN attribute a ON ha.attribute_id = a.id WHERE a.attribute_name = 'Speed' AND ha.attribute_value = (SELECT MAX(ha2.attribute_value) FROM hero_attribute ha2 WHERE ha2.attribute_id = a.id) LIMIT 1\n```"
    },
    {
      "question_id": 798,
      "difficulty": "moderate",
      "db_id": "superhero",
      "question": "What is the publisher for Hawkman, Karate Kid and Speedy?",
      "evidence": "publisher refers to publisher_name; Hawkman refers to superhero_name = 'Hawkman'; Karate Kid refers to superhero_name = 'Karate Kid'; Speedy refers to superhero_name = 'Speedy';",
      "generated_sql": "SELECT s.superhero_name, p.publisher_name FROM superhero s INNER JOIN publisher p ON s.publisher_id = p.id WHERE s.superhero_name IN ('Hawkman', 'Karate Kid', 'Speedy');",
      "ground_truth_sql": "SELECT\n  `T2`.`publisher_name`\nFROM `superhero` AS `T1`\nINNER JOIN `publisher` AS `T2`\n  ON `T1`.`publisher_id` = `T2`.`id`\nWHERE\n  `T1`.`superhero_name` IN ('Hawkman', 'Karate Kid', 'Speedy')",
      "error_category": "返回格式错误",
      "error_reason": "生成的SQL返回了两列：superhero_name和publisher_name，但用户问题只询问'publisher'，因此预期的SQL只返回publisher_name一列。生成的SQL虽然逻辑正确（JOIN和WHERE条件准确），但返回的列数不符合问题要求，导致结果集包含额外信息。",
      "suggested_fix": "修改SELECT子句，只选择出版商名称列。例如：SELECT p.publisher_name FROM superhero s INNER JOIN publisher p ON s.publisher_id = p.id WHERE s.superhero_name IN ('Hawkman', 'Karate Kid', 'Speedy'); 这样可以确保只返回用户询问的出版商信息。"
    },
    {
      "question_id": 533,
      "difficulty": "simple",
      "db_id": "codebase_community",
      "question": "How many users last accessed the website after 2014/9/1?",
      "evidence": "last accessed after 2014/9/1 refers to LastAccessDate > '2014-09-01'",
      "generated_sql": "SELECT COUNT(*) FROM users WHERE LastAccessDate > '2014-09-01'",
      "ground_truth_sql": "SELECT\n  COUNT(`Id`)\nFROM `users`\nWHERE\n  DATE(`LastAccessDate`) > '2014-09-01'",
      "error_category": "日期格式错误",
      "error_reason": "生成的SQL直接使用 `LastAccessDate > '2014-09-01'` 进行比较，但预期的SQL使用 `DATE(LastAccessDate) > '2014-09-01'`，这表明 `LastAccessDate` 字段可能存储为字符串或其他非标准日期格式，而非直接的日期类型（如DATE或DATETIME）。直接比较字符串可能导致不准确的结果，因为字符串比较可能不按日期顺序进行（例如，'2014-09-02' 和 '2014-09-10' 的字符串比较可能正确，但如果格式不一致，如 '2014/9/1'，则可能失败）。此外，生成的SQL使用 `COUNT(*)`，而预期的SQL使用 `COUNT(Id)`，这可能是一个返回格式错误，但主要问题在于日期处理方式。",
      "suggested_fix": "在WHERE子句中使用 `DATE(LastAccessDate) > '2014-09-01'` 来确保将字段转换为日期类型后再进行比较，从而准确筛选出2014年9月1日之后的访问记录。同时，根据数据库设计，如果 `Id` 是主键且无NULL值，`COUNT(*)` 和 `COUNT(Id)` 结果相同，但为符合预期，建议使用 `COUNT(Id)`。完整改进后的SQL为：`SELECT COUNT(Id) FROM users WHERE DATE(LastAccessDate) > '2014-09-01'`。"
    },
    {
      "question_id": 539,
      "difficulty": "simple",
      "db_id": "codebase_community",
      "question": "Who is the owner of the post \"Eliciting priors from experts\"?",
      "evidence": "\"Eliciting priors from experts\" is the Title of post; owner refers to DisplayName",
      "generated_sql": "SELECT OwnerDisplayName FROM posts WHERE Title = 'Eliciting priors from experts'",
      "ground_truth_sql": "SELECT\n  `T2`.`DisplayName`\nFROM `posts` AS `T1`\nINNER JOIN `users` AS `T2`\n  ON `T1`.`OwnerUserId` = `T2`.`Id`\nWHERE\n  `T1`.`Title` = 'Eliciting priors from experts'",
      "error_category": "语义理解错误",
      "error_reason": "系统错误地假设 posts 表中直接包含 owner 的显示名称（OwnerDisplayName），而实际上根据数据库 schema，posts 表通过 OwnerUserId 外键引用 users 表，需要 JOIN 来获取 DisplayName。这导致生成的 SQL 缺少必要的 JOIN 操作，无法正确返回 owner 的显示名称。根本原因是对数据库结构和字段关系的理解有误，误以为 'owner' 可以直接从 posts 表中获取，而忽略了外键关联。",
      "suggested_fix": "改进系统对数据库 schema 的理解，确保在生成 SQL 时正确识别外键关系和表结构。对于涉及实体关联的问题（如帖子的 owner），应检查是否需要 JOIN 其他表，并生成包含适当 JOIN 子句的 SQL 语句。具体到此案例，应生成类似预期的 SQL，连接 posts 和 users 表以获取 DisplayName。"
    },
    {
      "question_id": 565,
      "difficulty": "moderate",
      "db_id": "codebase_community",
      "question": "User No.23853 gave a comment to a post at 9:08:18 on 2013/7/12, was that post well-finished?",
      "evidence": "user no. 23853 refers to UserId = '23853'; at 9:08:18 on 2013/7/12 refers to CreationDate = '2013-07-12 09:08:18.0'; not well-finished refers to ClosedDate IS NULL and vice versa",
      "generated_sql": "SELECT CASE WHEN p.ClosedDate IS NULL THEN 'Not well-finished' ELSE 'Well-finished' END AS finish_status FROM comments c INNER JOIN posts p ON c.PostId = p.Id WHERE c.UserId = 23853 AND c.CreationDate = '2013-07-12 09:08:18'",
      "ground_truth_sql": "SELECT\n  CASE\n    WHEN `T2`.`ClosedDate` IS NULL\n    THEN 'NOT well-finished'\n    ELSE 'well-finished'\n  END AS `resylt`\nFROM `comments` AS `T1`\nINNER JOIN `posts` AS `T2`\n  ON `T1`.`PostId` = `T2`.`Id`\nWHERE\n  `T1`.`UserId` = 23853 AND `T1`.`CreationDate` = '2013-07-12 09:08:18.0'",
      "error_category": "日期格式错误",
      "error_reason": "生成的SQL在WHERE子句中使用了日期字符串'2013-07-12 09:08:18'，但证据和预期的SQL都指定了CreationDate为'2013-07-12 09:08:18.0'。数据库中的CreationDate字段可能存储为带毫秒的时间戳，因此精确匹配需要包含小数部分。缺少小数部分可能导致查询无法找到匹配的记录，从而返回错误的结果或空结果，影响查询的准确性。",
      "suggested_fix": "将日期字符串更改为'2013-07-12 09:08:18.0'以匹配数据库中的存储格式。确保日期格式与证据中提供的格式一致。此外，可以考虑使用时间函数（如DATE_TRUNC或STR_TO_DATE）来处理日期比较，但根据问题要求，直接匹配是预期的。"
    },
    {
      "question_id": 576,
      "difficulty": "simple",
      "db_id": "codebase_community",
      "question": "Name the user that commented 'thank you user93!'",
      "evidence": "\"thank you user93\" is the Text of comment; user refers to DisplayName",
      "generated_sql": "SELECT UserDisplayName FROM comments WHERE Text = 'thank you user93!';",
      "ground_truth_sql": "SELECT\n  `T1`.`DisplayName`\nFROM `users` AS `T1`\nINNER JOIN `comments` AS `T2`\n  ON `T1`.`Id` = `T2`.`UserId`\nWHERE\n  `T2`.`Text` = 'thank you user93!'",
      "error_category": "语义理解错误",
      "error_reason": "系统错误地理解了问题意图和数据库结构。用户问题要求'Name the user that commented 'thank you user93!''，证据指出'user refers to DisplayName'，这意味着需要从users表获取DisplayName。然而，生成的SQL直接从comments表查询UserDisplayName列，这可能是一个不存在的列或错误的列名。实际上，用户信息存储在users表中，评论存储在comments表中，两者通过UserId关联。生成的SQL缺少必要的JOIN操作，导致无法正确获取用户信息，这是对问题语义和数据库模式的误解。",
      "suggested_fix": "在生成SQL前，应分析数据库模式，识别相关表和关联键。对于此类问题，应生成包含INNER JOIN的SQL：从users表选择DisplayName，并通过comments表过滤文本内容。例如，使用'INNER JOIN users ON users.Id = comments.UserId'来关联表，并确保列名正确引用。建议系统在训练或推理时加强数据库模式理解和语义映射能力。"
    },
    {
      "question_id": 581,
      "difficulty": "moderate",
      "db_id": "codebase_community",
      "question": "Who is the owner of the post titled 'Open source tools for visualizing multi-dimensional data?'",
      "evidence": "'Open source tools for visualizing multi-dimensional data' is the Title of Post; owner refers to DisplayName;",
      "generated_sql": "SELECT OwnerDisplayName FROM posts WHERE Title = 'Open source tools for visualizing multi-dimensional data';",
      "ground_truth_sql": "SELECT\n  `T2`.`DisplayName`\nFROM `posts` AS `T1`\nINNER JOIN `users` AS `T2`\n  ON `T1`.`OwnerUserId` = `T2`.`Id`\nWHERE\n  `T1`.`Title` = 'Open source tools for visualizing multi-dimensional data?'",
      "error_category": "语义理解错误",
      "error_reason": "生成的SQL误解了问题意图和数据库模式。用户问题询问帖子的owner，证据指出owner指的是DisplayName，但生成的SQL错误地假设posts表中直接包含OwnerDisplayName字段，直接从中选择。实际上，根据预期的SQL，owner信息存储在users表中，需要通过posts表的OwnerUserId外键与users表的Id关联来获取DisplayName。此外，标题匹配不准确：生成的SQL使用'Open source tools for visualizing multi-dimensional data'，而用户问题中的标题包含问号（'Open source tools for visualizing multi-dimensional data?'），这可能导致查询结果不匹配。",
      "suggested_fix": "应修改SQL以正确关联posts和users表。使用INNER JOIN将posts表（别名T1）与users表（别名T2）基于T1.OwnerUserId = T2.Id连接，然后从T2.DisplayName选择。在WHERE子句中，使用准确的标题字符串，包括用户问题中的问号。参考预期的SQL结构：SELECT `T2`.`DisplayName` FROM `posts` AS `T1` INNER JOIN `users` AS `T2` ON `T1`.`OwnerUserId` = `T2`.`Id` WHERE `T1`.`Title` = 'Open source tools for visualizing multi-dimensional data?'。"
    },
    {
      "question_id": 584,
      "difficulty": "moderate",
      "db_id": "codebase_community",
      "question": "Write all the comments left by users who edited the post titled 'Why square the difference instead of taking the absolute value in standard deviation?'",
      "evidence": "\"Why square the difference instead of taking the absolute value in standard deviation?\" is the Title of post;",
      "generated_sql": "SELECT * FROM comments WHERE UserId IN (SELECT DISTINCT UserId FROM postHistory WHERE PostId = (SELECT Id FROM posts WHERE Title = 'Why square the difference instead of taking the absolute value in standard deviation?'))",
      "ground_truth_sql": "SELECT\n  `T2`.`Comment`\nFROM `posts` AS `T1`\nINNER JOIN `postHistory` AS `T2`\n  ON `T1`.`Id` = `T2`.`PostId`\nWHERE\n  `T1`.`Title` = 'Why square the difference instead of taking the absolute value in standard deviation?'",
      "error_category": "语义理解错误",
      "error_reason": "生成的SQL误解了问题意图和数据库模式。用户问题要求获取编辑了特定标题帖子的用户留下的所有评论。生成的SQL假设评论存储在comments表中，并通过UserId关联到编辑了帖子的用户（使用子查询从postHistory中获取UserId）。然而，根据预期的SQL，评论直接存储在postHistory表中，与帖子通过PostId关联。这导致SQL逻辑错误，因为它使用了错误的表（comments表）和关联方式（通过UserId子查询），而实际应该直接从postHistory表中选择评论，并通过INNER JOIN连接posts表基于PostId。此外，生成的SQL返回所有列（SELECT *），而预期的SQL只返回Comment列，这可能不符合用户对评论内容的特定需求，但主要错误在于对数据库结构和问题意图的误解。",
      "suggested_fix": "根据预期的SQL，应使用INNER JOIN直接连接posts和postHistory表，基于PostId，并过滤标题，然后选择Comment列。建议的SQL为：SELECT `T2`.`Comment` FROM `posts` AS `T1` INNER JOIN `postHistory` AS `T2` ON `T1`.`Id` = `T2`.`PostId` WHERE `T1`.`Title` = 'Why square the difference instead of taking the absolute value in standard deviation?'。如果数据库中确实存在comments表且评论存储在其中，则需要重新评估模式，但基于给定的预期SQL，上述修正更符合问题要求。"
    },
    {
      "question_id": 586,
      "difficulty": "challenging",
      "db_id": "codebase_community",
      "question": "Which user added a bounty amount of 50 to the post title mentioning variance?",
      "evidence": "bounty amount of 50 refers to BountyAmount = 50; user refers to DisplayName; title mentioning variance refers to Title include 'variance'",
      "generated_sql": "SELECT DISTINCT users.DisplayName FROM posts INNER JOIN votes ON posts.Id = votes.PostId INNER JOIN users ON votes.UserId = users.Id WHERE posts.Title LIKE '%variance%' AND votes.BountyAmount = 50",
      "ground_truth_sql": "SELECT\n  `T3`.`DisplayName`,\n  `T1`.`Title`\nFROM `posts` AS `T1`\nINNER JOIN `votes` AS `T2`\n  ON `T1`.`Id` = `T2`.`PostId`\nINNER JOIN `users` AS `T3`\n  ON `T3`.`Id` = `T2`.`UserId`\nWHERE\n  `T2`.`BountyAmount` = 50 AND `T1`.`Title` LIKE '%variance%'",
      "error_category": "返回格式错误",
      "error_reason": "生成的 SQL 只返回了 `DisplayName` 列，而预期的 SQL 返回了 `DisplayName` 和 `Title` 两列。用户问题 'Which user added a bounty amount of 50 to the post title mentioning variance?' 虽然只询问用户，但预期 SQL 返回了标题，这可能是因为需要验证匹配或问题隐含了返回标题。生成的 SQL 缺少了 `Title` 列，导致返回的列数和内容不符合预期。此外，生成的 SQL 使用了 `DISTINCT`，而预期 SQL 没有，但这不是主要错误，主要错误在于返回列的缺失。",
      "suggested_fix": "修改 SELECT 语句以包含 `Title` 列，例如：`SELECT DISTINCT users.DisplayName, posts.Title FROM posts INNER JOIN votes ON posts.Id = votes.PostId INNER JOIN users ON votes.UserId = users.Id WHERE posts.Title LIKE '%variance%' AND votes.BountyAmount = 50`。或者根据预期 SQL 使用表别名以提高可读性。同时，根据问题意图决定是否使用 `DISTINCT`：如果问题期望每个用户只出现一次（即使有多个匹配标题），则保留 `DISTINCT`；否则移除。"
    },
    {
      "question_id": 587,
      "difficulty": "moderate",
      "db_id": "codebase_community",
      "question": "Calculate the average view count of each post tagged as 'humor' and list the title and the comment of each post.",
      "evidence": "tagged as  'humor' refers to tag = '<humor>'; comment of the post refers to Text; average view count = AVG(ViewCount)",
      "generated_sql": "SELECT p.Title, c.Text AS Comment, AVG(p.ViewCount) OVER () AS AverageViewCount FROM posts p INNER JOIN comments c ON p.Id = c.PostId WHERE p.Tags LIKE '%<humor>%'",
      "ground_truth_sql": "SELECT AVG(T2.ViewCount) AS average_view_count, T2.Title, T1.Text FROM comments AS T1 INNER JOIN posts AS T2 ON T2.Id = T1.PostId WHERE T2.Tags = '<humor>' GROUP BY T2.Title, T1.Text",
      "error_category": "SQL 逻辑错误",
      "error_reason": "生成的 SQL 在聚合方式和标签匹配上存在逻辑错误。首先，标签匹配使用了 LIKE '%<humor>%' 进行模糊匹配，但证据明确指定 tag = '<humor>'，应使用等号进行精确匹配。其次，生成的 SQL 使用了窗口函数 OVER() 计算所有行的平均浏览量，而预期的 SQL 使用了 GROUP BY 子句进行分组计算。预期的 SQL 中，GROUP BY T2.Title, T1.Text 导致每个帖子-评论组合形成一个组，AVG(T2.ViewCount) 在每个组中实际上就是 T2.ViewCount 本身（因为每个组只有一个帖子），这没有正确计算平均浏览量。生成的 SQL 使用窗口函数虽然可能计算出整体平均，但不符合预期的 SQL 结构和逻辑。",
      "suggested_fix": "修改 SQL 以匹配预期的结构：使用等号精确匹配标签，使用 GROUP BY 子句而不是窗口函数。具体修改为：将 WHERE p.Tags LIKE '%<humor>%' 改为 WHERE p.Tags = '<humor>'；移除 OVER() 子句，添加 GROUP BY p.Title, c.Text；调整 SELECT 子句以计算平均浏览量并按预期顺序返回列。改进后的 SQL 应为：SELECT AVG(p.ViewCount) AS average_view_count, p.Title, c.Text AS Comment FROM posts p INNER JOIN comments c ON p.Id = c.PostId WHERE p.Tags = '<humor>' GROUP BY p.Title, c.Text"
    },
    {
      "question_id": 595,
      "difficulty": "moderate",
      "db_id": "codebase_community",
      "question": "Which user have only one post history per post and having at least 1000 views?",
      "evidence": "having at least 1000 view refers to Views > = 1000; user refers to UserId",
      "generated_sql": "SELECT DISTINCT MIN(ph.UserId) as UserId FROM postHistory ph INNER JOIN posts p ON ph.PostId = p.Id WHERE p.ViewCount >= 1000 GROUP BY ph.PostId HAVING COUNT(*) = 1",
      "ground_truth_sql": "SELECT\n  `T2`.`UserId`\nFROM `users` AS `T1`\nINNER JOIN `postHistory` AS `T2`\n  ON `T1`.`Id` = `T2`.`UserId`\nINNER JOIN `posts` AS `T3`\n  ON `T2`.`PostId` = `T3`.`Id`\nWHERE\n  `T3`.`ViewCount` >= 1000\nGROUP BY\n  `T2`.`UserId`\nHAVING\n  COUNT(DISTINCT `T2`.`PostHistoryTypeId`) = 1",
      "error_category": "语义理解错误",
      "error_reason": "生成的SQL误解了用户问题的意图。用户问题要求找出'only one post history per post and having at least 1000 views'的用户，但生成的SQL按PostId分组（GROUP BY ph.PostId），并检查每个帖子只有一条历史记录（HAVING COUNT(*) = 1），这导致结果基于帖子而非用户。此外，生成的SQL没有连接users表，无法直接返回用户信息，而是使用MIN(UserId)作为占位符，逻辑错误。预期的SQL则按UserId分组（GROUP BY T2.UserId），并检查用户只有一种历史类型（HAVING COUNT(DISTINCT T2.PostHistoryTypeId) = 1），这更符合问题中'only one post history per post'的可能解释（即用户只有一种历史类型），同时确保了帖子有至少1000 views（WHERE T3.ViewCount >= 1000）。生成的SQL未能正确捕捉问题焦点，导致语义偏差。",
      "suggested_fix": "重新理解问题意图：用户问题可能要求找出那些用户，对于每个帖子，他们只有一条历史记录，且这些帖子有至少1000 views。但基于预期的SQL，更合理的解释是检查用户只有一种历史类型。建议修改生成的SQL，使其按UserId分组，并连接users表以返回用户信息。具体改进：使用INNER JOIN连接users、postHistory和posts表，按UserId分组，并在HAVING子句中使用COUNT(DISTINCT PostHistoryTypeId) = 1来确保用户只有一种历史类型，同时WHERE子句中包含ViewCount >= 1000。示例改进SQL：\nSELECT T2.UserId\nFROM users AS T1\nINNER JOIN postHistory AS T2 ON T1.Id = T2.UserId\nINNER JOIN posts AS T3 ON T2.PostId = T3.Id\nWHERE T3.ViewCount >= 1000\nGROUP BY T2.UserId\nHAVING COUNT(DISTINCT T2.PostHistoryTypeId) = 1"
    },
    {
      "question_id": 634,
      "difficulty": "challenging",
      "db_id": "codebase_community",
      "question": "Among posts by Harvey Motulsky and Noah Snyder, which one has higher popularity?",
      "evidence": "Has higher popularity means the post has higher view count ; calculation = MAX(SUM(ViewCount)) where DisplayName = 'Harvey Motulsky' OR DisplayName = 'Noah Snyder';",
      "generated_sql": "SELECT OwnerDisplayName, SUM(ViewCount) as total_view_count FROM posts WHERE OwnerDisplayName IN ('Harvey Motulsky', 'Noah Snyder') GROUP BY OwnerDisplayName ORDER BY total_view_count DESC",
      "ground_truth_sql": "SELECT\n  `T1`.`DisplayName`\nFROM `users` AS `T1`\nINNER JOIN `postHistory` AS `T2`\n  ON `T1`.`Id` = `T2`.`UserId`\nINNER JOIN `posts` AS `T3`\n  ON `T2`.`PostId` = `T3`.`Id`\nWHERE\n  `T1`.`DisplayName` = 'Harvey Motulsky' OR `T1`.`DisplayName` = 'Noah Snyder'\nGROUP BY\n  `T1`.`DisplayName`\nORDER BY\n  SUM(`T3`.`ViewCount`) DESC\nLIMIT 1",
      "error_category": "SQL逻辑错误",
      "error_reason": "生成的SQL存在两个主要逻辑错误：1) 表连接错误：生成的SQL直接从posts表查询OwnerDisplayName，但根据预期的SQL和数据库模式（codebase_community），可能需要通过连接users表来获取DisplayName字段，因为posts表可能只包含UserId，没有直接的OwnerDisplayName。这导致查询可能无法正确执行或返回错误结果。2) 返回格式错误：生成的SQL没有使用LIMIT 1来限制返回行数，而问题要求找出哪个用户有更高的受欢迎度（即最高浏览量的用户），预期的SQL使用LIMIT 1只返回一行。生成的SQL返回所有匹配用户的总浏览量，没有明确指出哪个更高，不符合问题意图。",
      "suggested_fix": "应修正表连接和返回行数：1) 连接users表以正确获取DisplayName，假设posts表有UserId字段。2) 使用LIMIT 1来只返回最高浏览量的用户。建议的SQL：SELECT DisplayName FROM users JOIN posts ON users.Id = posts.UserId WHERE DisplayName IN ('Harvey Motulsky', 'Noah Snyder') GROUP BY DisplayName ORDER BY SUM(ViewCount) DESC LIMIT 1。如果浏览量存储在postHistory表中，如预期的SQL所示，则需要调整JOIN条件：SELECT DisplayName FROM users JOIN postHistory ON users.Id = postHistory.UserId JOIN posts ON postHistory.PostId = posts.Id WHERE DisplayName IN ('Harvey Motulsky', 'Noah Snyder') GROUP BY DisplayName ORDER BY SUM(posts.ViewCount) DESC LIMIT 1。"
    },
    {
      "question_id": 637,
      "difficulty": "moderate",
      "db_id": "codebase_community",
      "question": "State all the tags used by Mark Meckes in his posts that doesn't have comments.",
      "evidence": "used by Mark Meckes refers to DisplayName = 'Mark Meckes'; Doen't have comments refers to CommentCount = 0;",
      "generated_sql": "SELECT DISTINCT posts.Tags FROM posts INNER JOIN users ON posts.OwnerUserId = users.Id WHERE users.DisplayName = 'Mark Meckes' AND posts.CommentCount = 0;",
      "ground_truth_sql": "SELECT\n  `T3`.`Tags`\nFROM `users` AS `T1`\nINNER JOIN `postHistory` AS `T2`\n  ON `T1`.`Id` = `T2`.`UserId`\nINNER JOIN `posts` AS `T3`\n  ON `T3`.`Id` = `T2`.`PostId`\nWHERE\n  `T1`.`DisplayName` = 'Mark Meckes' AND `T3`.`CommentCount` = 0",
      "error_category": "SQL逻辑错误",
      "error_reason": "生成的SQL假设posts表通过OwnerUserId列直接关联users表，但根据预期的SQL，数据库结构可能需要通过postHistory表来关联users和posts。用户问题中的'used by Mark Meckes'可能指的是Mark Meckes创建或编辑的帖子，而postHistory表记录了帖子的编辑历史，包括用户ID。生成的SQL可能无法正确获取Mark Meckes的所有帖子，因为如果数据库中帖子的所有者信息存储在postHistory表中，直接使用OwnerUserId可能导致遗漏或错误关联。此外，预期的SQL使用了三个表（users、postHistory、posts），而生成的SQL只使用了两个表，这表明对数据库关系的理解有偏差。",
      "suggested_fix": "首先检查数据库模式，确认posts表是否直接有OwnerUserId列，以及posts和users之间的关系。如果数据库结构如预期的SQL所示（即通过postHistory表关联），则应修改SQL以使用postHistory表进行连接。建议的SQL为：SELECT DISTINCT posts.Tags FROM users INNER JOIN postHistory ON users.Id = postHistory.UserId INNER JOIN posts ON postHistory.PostId = posts.Id WHERE users.DisplayName = 'Mark Meckes' AND posts.CommentCount = 0。确保查询条件正确使用DisplayName和CommentCount，并根据实际数据库结构调整表名和列名。"
    },
    {
      "question_id": 639,
      "difficulty": "challenging",
      "db_id": "codebase_community",
      "question": "Based on posts posted by Community, calculate the percentage of posts that use the R language.",
      "evidence": "DIVIDE(COUNT(PostId WHERE TagName = 'r')), (COUNT(PostId WHERE DisplayName = 'Community')) as percentage; R language refers to tagname = 'r'",
      "generated_sql": "SELECT (COUNT(CASE WHEN Tags REGEXP '<r>' THEN 1 END) * 100.0 / COUNT(*)) AS percentage FROM posts WHERE OwnerDisplayName = 'Community'",
      "ground_truth_sql": "SELECT\n  CAST(SUM(CASE WHEN `T3`.`TagName` = 'r' THEN 1 ELSE 0 END) AS DOUBLE) * 100 / COUNT(`T1`.`Id`)\nFROM `users` AS `T1`\nINNER JOIN `postHistory` AS `T2`\n  ON `T1`.`Id` = `T2`.`UserId`\nINNER JOIN `tags` AS `T3`\n  ON `T3`.`ExcerptPostId` = `T2`.`PostId`\nWHERE\n  `T1`.`DisplayName` = 'Community'",
      "error_category": "语义理解错误",
      "error_reason": "生成的SQL对问题意图和数据模型的理解存在偏差。用户问题要求计算Community用户发布的帖子中使用R语言的百分比，证据明确指出TagName = 'r'表示R语言，并涉及PostId和DisplayName的关联。生成的SQL假设所有必要信息（如标签和用户信息）都存储在posts表中，直接使用Tags字段和OwnerDisplayName字段进行查询。然而，预期的SQL显示实际数据模型需要多表连接：users表存储用户信息（DisplayName），postHistory表关联用户和帖子（UserId和PostId），tags表存储标签信息（ExcerptPostId和TagName）。生成的SQL错误地简化了数据结构，导致逻辑不准确，因为它可能遗漏了正确的关联关系，且使用REGEXP '<r>'匹配Tags字段可能不适用于标签存储在独立表中的情况。",
      "suggested_fix": "应基于数据库模式重新构建查询。首先，确认数据模型：用户信息在users表，帖子历史在postHistory表，标签在tags表。然后，使用INNER JOIN连接这些表：users表与postHistory表通过UserId关联，postHistory表与tags表通过PostId（或ExcerptPostId）关联。计算百分比时，分子为TagName = 'r'的帖子数，分母为DisplayName = 'Community'的帖子数。建议使用SUM(CASE WHEN TagName = 'r' THEN 1 ELSE 0 END)计算分子，COUNT(DISTINCT PostId)或COUNT(*)计算分母，并确保使用适当的类型转换（如CAST为DOUBLE）以避免整数除法问题。最终查询应类似于预期的SQL结构，但可根据具体数据库语法调整。"
    },
    {
      "question_id": 640,
      "difficulty": "moderate",
      "db_id": "codebase_community",
      "question": "Calculate the difference in view count from post posted by Mornington and view count from posts posted by Amos.",
      "evidence": "calculation = SUBTRACT(SUM(ViewCount where DisplayName = 'Mornington'), SUM(ViewCount where DisplayName = 'Amos'));",
      "generated_sql": "SELECT COALESCE((SELECT SUM(ViewCount) FROM posts p INNER JOIN users u ON p.OwnerUserId = u.Id WHERE u.DisplayName = 'Mornington'), 0) - COALESCE((SELECT SUM(ViewCount) FROM posts p INNER JOIN users u ON p.OwnerUserId = u.Id WHERE u.DisplayName = 'Amos'), 0) AS difference;",
      "ground_truth_sql": "SELECT\n  SUM(CASE WHEN `T1`.`DisplayName` = 'Mornington' THEN `T3`.`ViewCount` ELSE 0 END) - SUM(CASE WHEN `T1`.`DisplayName` = 'Amos' THEN `T3`.`ViewCount` ELSE 0 END) AS `diff`\nFROM `users` AS `T1`\nINNER JOIN `postHistory` AS `T2`\n  ON `T1`.`Id` = `T2`.`UserId`\nINNER JOIN `posts` AS `T3`\n  ON `T3`.`Id` = `T2`.`PostId`",
      "error_category": "SQL逻辑错误",
      "error_reason": "生成的SQL假设posts表有OwnerUserId字段，并直接连接users表来计算每个用户的ViewCount总和。然而，预期的SQL显示数据库结构涉及postHistory表，其中包含用户ID和帖子ID的关联（users.Id = postHistory.UserId 和 posts.Id = postHistory.PostId）。这表明posts表可能没有直接的OwnerUserId字段，或者view count的计算需要通过postHistory表来关联用户和帖子。生成的SQL忽略了postHistory表，导致JOIN条件错误，可能遗漏数据或连接不准确，从而影响计算结果的正确性。",
      "suggested_fix": "首先检查数据库模式，确认posts表是否有OwnerUserId字段。如果不存在，则必须使用postHistory表来关联用户和帖子。建议修改SQL，采用类似预期的结构：从users表开始，通过postHistory表连接到posts表，然后使用CASE语句和SUM计算差异。例如：SELECT SUM(CASE WHEN users.DisplayName = 'Mornington' THEN posts.ViewCount ELSE 0 END) - SUM(CASE WHEN users.DisplayName = 'Amos' THEN posts.ViewCount ELSE 0 END) AS diff FROM users INNER JOIN postHistory ON users.Id = postHistory.UserId INNER JOIN posts ON posts.Id = postHistory.PostId。同时，确保处理NULL值，但预期的SQL未使用COALESCE，可能因为数据完整性假设。"
    },
    {
      "question_id": 665,
      "difficulty": "moderate",
      "db_id": "codebase_community",
      "question": "What is the average monthly number of links created in 2010 for posts that have no more than 2 answers?",
      "evidence": "calculation = DIVIDE(COUNT(Id where YEAR(CreationDate) = 2010 and AnswerCount < = 2), 12)",
      "generated_sql": "SELECT CAST(COUNT(DISTINCT pl.Id) AS DOUBLE) / 12 AS average_monthly_links FROM postLinks pl WHERE YEAR(pl.CreationDate) = 2010 AND (EXISTS (SELECT 1 FROM posts p WHERE p.Id = pl.PostId AND p.AnswerCount <= 2) OR EXISTS (SELECT 1 FROM posts p WHERE p.Id = pl.RelatedPostId AND p.AnswerCount <= 2))",
      "ground_truth_sql": "SELECT\n  CAST(COUNT(`T1`.`Id`) AS DOUBLE) / 12\nFROM `postLinks` AS `T1`\nINNER JOIN `posts` AS `T2`\n  ON `T1`.`PostId` = `T2`.`Id`\nWHERE\n  `T2`.`AnswerCount` <= 2\n  AND DATE_FORMAT(CAST(`T1`.`CreationDate` AS DATETIME), '%Y') = '2010'",
      "error_category": "日期格式错误",
      "error_reason": "生成的SQL直接使用YEAR函数处理CreationDate字段，但根据预期的SQL，CreationDate可能不是标准日期类型（如整型或字符串），需要先转换为DATETIME再使用DATE_FORMAT提取年份。这可能导致执行错误或逻辑错误，因为YEAR函数可能无法正确解析非日期类型的字段。",
      "suggested_fix": "修改日期处理部分，使用DATE_FORMAT(CAST(CreationDate AS DATETIME), '%Y') = '2010' 或类似方式，以确保日期比较正确。同时，检查条件逻辑：用户问题可能仅指链接的来源帖子（PostId）满足AnswerCount <= 2，而非RelatedPostId，因此应使用INNER JOIN基于PostId过滤，如预期的SQL所示。"
    },
    {
      "question_id": 671,
      "difficulty": "simple",
      "db_id": "codebase_community",
      "question": "What is the display name of the user who acquired the first Autobiographer badge?",
      "evidence": "Autobiographer is the name of the badge; acquired the first refers to MIN(Date);",
      "generated_sql": "SELECT u.DisplayName FROM badges b INNER JOIN users u ON b.UserId = u.Id WHERE b.Name = 'Autobiographer' AND b.Date = (SELECT MIN(Date) FROM badges WHERE Name = 'Autobiographer')",
      "ground_truth_sql": "SELECT\n  `T1`.`DisplayName`\nFROM `users` AS `T1`\nINNER JOIN `badges` AS `T2`\n  ON `T1`.`Id` = `T2`.`UserId`\nWHERE\n  `T2`.`Name` = 'Autobiographer'\nORDER BY\n  `T2`.`Date`\nLIMIT 1",
      "error_category": "返回格式错误",
      "error_reason": "生成的SQL使用子查询找到最早的日期，然后匹配该日期。这逻辑上正确，但如果有多个用户在同一天获得Autobiographer徽章，它会返回所有这些用户。用户问题询问'the user'，暗示期望一个用户，而生成的SQL可能返回多行。预期的SQL使用ORDER BY Date LIMIT 1确保只返回一行，更符合问题意图。",
      "suggested_fix": "修改生成的SQL，添加ORDER BY b.Date LIMIT 1来确保只返回第一个用户。例如：SELECT u.DisplayName FROM badges b INNER JOIN users u ON b.UserId = u.Id WHERE b.Name = 'Autobiographer' ORDER BY b.Date LIMIT 1。或者直接采用预期的SQL结构。"
    },
    {
      "question_id": 672,
      "difficulty": "moderate",
      "db_id": "codebase_community",
      "question": "Among the users located in United Kingdom, how many users whose post have a total favorite amount of 4 or more?",
      "evidence": "favorite amount of 4 or more refers to FavoriteCount > = 4; Location = 'United Kingdom';",
      "generated_sql": "SELECT COUNT(*) FROM (SELECT users.Id FROM users INNER JOIN posts ON users.Id = posts.OwnerUserId WHERE users.Location = 'United Kingdom' GROUP BY users.Id HAVING SUM(posts.FavoriteCount) >= 4) AS user_counts",
      "ground_truth_sql": "SELECT\n  COUNT(`T1`.`Id`)\nFROM `users` AS `T1`\nINNER JOIN `posts` AS `T2`\n  ON `T1`.`Id` = `T2`.`OwnerUserId`\nWHERE\n  `T1`.`Location` = 'United Kingdom' AND `T2`.`FavoriteCount` >= 4",
      "error_category": "语义理解错误",
      "error_reason": "生成的SQL误解了问题意图。用户问题中的'total favorite amount'可能被解释为每个用户的帖子总收藏数，但证据明确指出'favorite amount of 4 or more refers to FavoriteCount >= 4'，这表明应关注单个帖子的收藏数，而非总和。生成的SQL使用GROUP BY和HAVING SUM(posts.FavoriteCount) >= 4来计算每个用户的总收藏数并过滤，这会导致计数错误：例如，一个用户有多个帖子，每个帖子收藏数都小于4但总和>=4，生成的SQL会错误地计数该用户，而问题意图（基于证据）应只计数至少有一个帖子收藏数>=4的用户。预期的SQL正确使用WHERE子句过滤FavoriteCount >= 4的帖子，然后计数用户。",
      "suggested_fix": "修改SQL以直接过滤单个帖子的收藏数>=4的用户，避免使用GROUP BY和HAVING。建议使用以下结构：SELECT COUNT(DISTINCT users.Id) FROM users INNER JOIN posts ON users.Id = posts.OwnerUserId WHERE users.Location = 'United Kingdom' AND posts.FavoriteCount >= 4。这确保只计数那些至少有一个帖子收藏数>=4的英国用户。"
    },
    {
      "question_id": 682,
      "difficulty": "moderate",
      "db_id": "codebase_community",
      "question": "Which is the most valuable post in 2010? Please give its id and the owner's display name.",
      "evidence": "the most valuable post in 2010 refers to MAX(FavoriteCount) where year(CreationDate) = 2010;",
      "generated_sql": "SELECT Id, OwnerDisplayName FROM posts WHERE YEAR(CreationDate) = 2010 ORDER BY FavoriteCount DESC LIMIT 1",
      "ground_truth_sql": "SELECT\n  `T2`.`OwnerUserId`,\n  `T1`.`DisplayName`\nFROM `users` AS `T1`\nINNER JOIN `posts` AS `T2`\n  ON `T1`.`Id` = `T2`.`OwnerUserId`\nWHERE\n  DATE_FORMAT(CAST(`T1`.`CreationDate` AS DATETIME), '%Y') = '2010'\nORDER BY\n  `T2`.`FavoriteCount` DESC\nLIMIT 1",
      "error_category": "SQL 逻辑错误",
      "error_reason": "生成的 SQL 缺少与 users 表的连接，无法正确获取 owner's display name。用户问题要求获取 post 的 id 和 owner's display name，但生成的 SQL 直接从 posts 表选择 OwnerDisplayName，这通常不存在于 posts 表中（display name 通常存储在 users 表中）。因此，SQL 逻辑错误在于 JOIN 条件缺失，导致无法通过 OwnerUserId 关联到 users 表获取 DisplayName。此外，生成的 SQL 假设 posts 表有 OwnerDisplayName 列，这可能不准确。",
      "suggested_fix": "修改 SQL 以连接 users 表，通过 OwnerUserId 关联，并从 users 表选择 DisplayName。建议 SQL：SELECT posts.Id, users.DisplayName FROM posts INNER JOIN users ON posts.OwnerUserId = users.Id WHERE YEAR(posts.CreationDate) = 2010 ORDER BY posts.FavoriteCount DESC LIMIT 1。这确保了正确获取 post 的 id 和 owner's display name，并基于 posts 表的 CreationDate 过滤年份。"
    },
    {
      "question_id": 683,
      "difficulty": "moderate",
      "db_id": "codebase_community",
      "question": "What is the percentage of posts whose owners had a reputation of over 1000 in 2011?",
      "evidence": "percentage = DIVIDE(COUNT(Id where YEAR(CreationDate) = 2011 and Reputation > 1000), COUNT(Id) ) * 100;",
      "generated_sql": "SELECT CAST(SUM(CASE WHEN u.Reputation > 1000 THEN 1 ELSE 0 END) AS DECIMAL) / COUNT(p.Id) * 100 AS percentage FROM posts p LEFT JOIN users u ON p.OwnerUserId = u.Id WHERE YEAR(p.CreationDate) = 2011;",
      "ground_truth_sql": "SELECT\n  CAST(SUM(\n    CASE\n      WHEN DATE_FORMAT(CAST(`T2`.`CreaionDate` AS DATETIME), '%Y') = '2011'\n      AND `T1`.`Reputation` > 1000\n      THEN 1\n      ELSE 0\n    END\n  ) AS DOUBLE) * 100 / COUNT(`T1`.`Id`)\nFROM `users` AS `T1`\nINNER JOIN `posts` AS `T2`\n  ON `T1`.`Id` = `T2`.`OwnerUserId`",
      "error_category": "SQL逻辑错误",
      "error_reason": "生成的SQL与预期的SQL在逻辑上存在显著差异。主要问题包括：1) JOIN类型：生成的SQL使用LEFT JOIN，而预期的SQL使用INNER JOIN，这可能导致结果不同，特别是当存在没有对应用户的帖子时；2) 条件位置：生成的SQL在WHERE子句中过滤日期（YEAR(p.CreationDate) = 2011），而预期的SQL在CASE表达式中同时过滤日期和声誉条件，导致分母计算不同；3) 分母计算：生成的SQL使用COUNT(p.Id)作为分母，仅计算2011年创建的帖子数，而预期的SQL使用COUNT(T1.Id)作为分母，计算所有帖子数（由于INNER JOIN，COUNT(T1.Id)等于总帖子数）。因此，生成的SQL计算的是2011年帖子中满足条件的百分比，而预期的SQL计算的是所有帖子中满足条件的百分比，两者逻辑不一致。",
      "suggested_fix": "为了匹配预期的SQL逻辑，应修改生成的SQL如下：使用INNER JOIN代替LEFT JOIN，移除WHERE子句，在CASE表达式中同时检查日期和声誉条件，并将分母改为COUNT(T1.Id)。具体SQL为：SELECT CAST(SUM(CASE WHEN DATE_FORMAT(CAST(T2.CreationDate AS DATETIME), '%Y') = '2011' AND T1.Reputation > 1000 THEN 1 ELSE 0 END) AS DOUBLE) * 100 / COUNT(T1.Id) FROM users AS T1 INNER JOIN posts AS T2 ON T1.Id = T2.OwnerUserId。注意：预期的SQL中存在拼写错误'CreaionDate'，应修正为'CreationDate'。"
    },
    {
      "question_id": 685,
      "difficulty": "moderate",
      "db_id": "codebase_community",
      "question": "Identify the total views on the post 'Computer Game Datasets'. Name the user who posted it last time.",
      "evidence": "total views refer to ViewCount; Name the user refers to DisplayName; post 'Computer Game Datasets' refers to Text = 'Computer Game Datasets';",
      "generated_sql": "SELECT p.ViewCount, u.DisplayName FROM posts p INNER JOIN users u ON p.LastEditorUserId = u.Id WHERE p.Id = (SELECT PostId FROM postHistory WHERE Text = 'Computer Game Datasets' LIMIT 1)",
      "ground_truth_sql": "SELECT\n  `T2`.`ViewCount`,\n  `T3`.`DisplayName`\nFROM `postHistory` AS `T1`\nINNER JOIN `posts` AS `T2`\n  ON `T1`.`PostId` = `T2`.`Id`\nINNER JOIN `users` AS `T3`\n  ON `T2`.`LastEditorUserId` = `T3`.`Id`\nWHERE\n  `T1`.`Text` = 'Computer Game Datasets'",
      "error_category": "SQL 逻辑错误",
      "error_reason": "生成的 SQL 使用了子查询和 LIMIT 1 来查找帖子 ID，这可能导致逻辑错误。原因包括：1) 子查询 `SELECT PostId FROM postHistory WHERE Text = 'Computer Game Datasets' LIMIT 1` 可能返回任意一个匹配的 PostId，如果 postHistory 中有多个记录（例如，不同版本的历史），LIMIT 1 可能选择错误的记录，而不是正确的或最新的帖子。2) 生成的 SQL 从 posts 表开始，但用户问题更直接关联到 postHistory 表中的 Text 字段，预期的 SQL 从 postHistory 开始更符合问题意图。3) 如果子查询无匹配，p.Id = NULL 会导致无结果，而预期的 SQL 通过 JOIN 自然处理。4) 生成的 SQL 使用 LastEditorUserId，但用户问题说 'posted it last time'，可能指最后发布者，但证据中 DisplayName 对应用户，所以可能一致，但子查询的使用引入了不必要的复杂性和潜在错误。",
      "suggested_fix": "避免使用子查询和 LIMIT，改用直接的 JOIN 方法，如预期的 SQL 所示。具体改进：从 postHistory 表开始，JOIN posts 表基于 PostId，再 JOIN users 表基于 LastEditorUserId，过滤 Text = 'Computer Game Datasets'。这确保了所有匹配的记录被正确关联，并避免了子查询的歧义。如果数据库设计确保每个 Text 值唯一对应一个帖子，此方法将返回正确结果；否则，可能需要添加聚合函数（如 MAX 或 MIN）来处理多个匹配，但根据预期的 SQL，直接 JOIN 是合适的。"
    },
    {
      "question_id": 694,
      "difficulty": "moderate",
      "db_id": "codebase_community",
      "question": "Provide the text of the latest 10 comments to the post with the title 'Analysing wind data with R' and the display name of the user who left it.",
      "evidence": "the latest comment refers to MAX(CreationDate);",
      "generated_sql": "SELECT c.Text, c.UserDisplayName FROM comments c WHERE c.PostId = (SELECT Id FROM posts WHERE Title = 'Analysing wind data with R') ORDER BY c.CreationDate DESC LIMIT 10",
      "ground_truth_sql": "SELECT\n  `T3`.`Text`,\n  `T1`.`DisplayName`\nFROM `users` AS `T1`\nINNER JOIN `posts` AS `T2`\n  ON `T1`.`Id` = `T2`.`OwnerUserId`\nINNER JOIN `comments` AS `T3`\n  ON `T2`.`Id` = `T3`.`PostId`\nWHERE\n  `T2`.`Title` = 'Analysing wind data with R'\nORDER BY\n  `T1`.`CreationDate` DESC\nLIMIT 10",
      "error_category": "语义理解错误",
      "error_reason": "生成的SQL误解了数据库结构，假设comments表包含UserDisplayName字段，而预期的SQL表明需要通过JOIN users表来获取用户的显示名（DisplayName）。此外，生成的SQL使用子查询获取PostId，而预期的SQL使用INNER JOIN连接users、posts和comments表，这更符合标准实践。排序字段方面，生成的SQL按评论的CreationDate排序（符合问题意图），但预期的SQL按用户的CreationDate排序，这可能与问题要求不符（问题要求获取最新评论，应按评论时间排序）。整体上，生成的SQL未能正确反映数据库表之间的关系和字段来源。",
      "suggested_fix": "1. 修改SQL以INNER JOIN users表，通过comments表的UserId关联，获取users.DisplayName。\n2. 确保排序字段为comments.CreationDate，以正确获取最新的10条评论。\n3. 考虑使用JOIN代替子查询来提高查询效率和可读性。改进后的SQL示例：\nSELECT c.Text, u.DisplayName FROM comments c INNER JOIN posts p ON c.PostId = p.Id INNER JOIN users u ON c.UserId = u.Id WHERE p.Title = 'Analysing wind data with R' ORDER BY c.CreationDate DESC LIMIT 10"
    },
    {
      "question_id": 710,
      "difficulty": "simple",
      "db_id": "codebase_community",
      "question": "In posts with 1 comment, how many of the comments have 0 score?",
      "evidence": "in posts with 1 comment refers to CommentCount = 1;",
      "generated_sql": "SELECT COUNT(*) FROM comments c INNER JOIN posts p ON c.PostId = p.Id WHERE p.CommentCount = 1 AND c.Score = 0",
      "ground_truth_sql": "SELECT\n  COUNT(`T1`.`id`)\nFROM `comments` AS `T1`\nINNER JOIN `posts` AS `T2`\n  ON `T1`.`PostId` = `T2`.`Id`\nWHERE\n  `T2`.`CommentCount` = 1 AND `T2`.`Score` = 0",
      "error_category": "SQL逻辑错误",
      "error_reason": "生成的SQL在WHERE条件中使用了`c.Score = 0`（评论的分数），而预期的SQL使用了`T2.Score = 0`（帖子的分数）。用户问题明确询问'comments have 0 score'（评论的分数为0），因此生成的SQL在逻辑上更符合问题意图。然而，预期的SQL使用了帖子的Score字段，这可能与问题意图不符。生成的SQL与预期的SQL在条件上存在差异，导致逻辑错误：生成的SQL统计评论分数为0的评论，而预期的SQL统计帖子分数为0的评论（但问题问的是评论分数）。此外，生成的SQL使用`COUNT(*)`，而预期的SQL使用`COUNT(T1.id)`，但这不是主要错误，因为两者在非NULL列上通常等价。",
      "suggested_fix": "根据用户问题，应该使用评论的Score字段，因此生成的SQL是正确的。但为了匹配预期的SQL，需要将`c.Score = 0`改为`T2.Score = 0`。然而，预期的SQL可能错误地使用了帖子的Score字段。建议检查数据库模式以确认Score字段的归属：如果Score字段只存在于comments表中，则生成的SQL正确；如果Score字段也存在于posts表中且问题意图是统计帖子的分数，则需要调整。此外，可以考虑使用`COUNT(T1.id)`代替`COUNT(*)`以更精确地统计评论数量，但这不是关键错误。"
    },
    {
      "question_id": 716,
      "difficulty": "moderate",
      "db_id": "codebase_community",
      "question": "Among the comments with scores between 5 to 10, what is the percentage of the users with 0 up votes?",
      "evidence": "percentage = DIVIDE(COUNT(UserId where UpVotes = 0 and Score BETWEEN 5 and 10))*100, (COUNT(UserId where Score BETWEEN 5 and 10));",
      "generated_sql": "SELECT (CAST(COUNT(DISTINCT CASE WHEN u.UpVotes = 0 THEN c.UserId END) AS DOUBLE) * 100) / CAST(COUNT(DISTINCT c.UserId) AS DOUBLE) AS percentage FROM comments c INNER JOIN users u ON c.UserId = u.Id WHERE c.Score BETWEEN 5 AND 10;",
      "ground_truth_sql": "SELECT\n  CAST(SUM(CASE WHEN `T1`.`UpVotes` = 0 THEN 1 ELSE 0 END) AS DOUBLE) * 100 / COUNT(`T1`.`Id`) AS `per`\nFROM `users` AS `T1`\nINNER JOIN `comments` AS `T2`\n  ON `T1`.`Id` = `T2`.`UserId`\nWHERE\n  `T2`.`Score` BETWEEN 5 AND 10",
      "error_category": "其他",
      "error_reason": "生成的 SQL 正确实现了用户级别的百分比计算，使用了 DISTINCT 来计数唯一用户 ID，符合问题意图（'percentage of the users'）和证据中的公式（COUNT(UserId where UpVotes = 0 and Score BETWEEN 5 and 10) / COUNT(UserId where Score BETWEEN 5 and 10)）。生成的 SQL 从 comments 表开始，通过 INNER JOIN users 表，并在 WHERE 子句中过滤评论分数，分子使用 COUNT(DISTINCT CASE WHEN u.UpVotes = 0 THEN c.UserId END) 计算 up votes 为 0 的唯一用户数，分母使用 COUNT(DISTINCT c.UserId) 计算所有相关唯一用户数，逻辑正确。相比之下，预期的 SQL 存在逻辑错误：它没有使用 DISTINCT，导致分子 SUM(CASE WHEN T1.UpVotes = 0 THEN 1 ELSE 0 END) 和分母 COUNT(T1.Id) 都基于行数（评论级别）而非用户级别，这不符合问题要求的用户级别百分比。因此，生成的 SQL 相对于预期的 SQL 没有错误，但预期的 SQL 需要修正。",
      "suggested_fix": "生成的 SQL 已正确，无需改进。如果必须与预期的 SQL 保持一致，可以调整预期的 SQL 以使用用户级别计数：将分母改为 COUNT(DISTINCT T1.Id)，分子改为 COUNT(DISTINCT CASE WHEN T1.UpVotes = 0 THEN T1.Id END)。但生成的 SQL 本身是更优的实现。"
    },
    {
      "question_id": 340,
      "difficulty": "simple",
      "db_id": "card_games",
      "question": "Which are the cards that have incredibly powerful foils.",
      "evidence": "incredibly poweful foils refers to cardKingdomFoilId is not null AND cardKingdomId is not null",
      "generated_sql": "SELECT name FROM cards WHERE cardKingdomFoilId IS NOT NULL AND cardKingdomId IS NOT NULL",
      "ground_truth_sql": "SELECT\n  `id`\nFROM `cards`\nWHERE\n  NOT `cardKingdomFoilId` IS NULL AND NOT `cardKingdomId` IS NULL",
      "error_category": "返回格式错误",
      "error_reason": "生成的SQL返回了'name'列，而预期的SQL返回'id'列。用户问题'Which are the cards that have incredibly powerful foils'询问具有强大箔片的卡片，通常期望返回卡片的标识符（如ID），而不是名称。证据中指定了条件（cardKingdomFoilId和cardKingdomId不为空），但未指定返回列。预期的SQL选择了'id'列，表明系统应返回ID作为卡片标识。生成的SQL选择了'name'列，这不符合预期的返回格式，尽管条件逻辑正确。",
      "suggested_fix": "修改SELECT子句，返回'id'列以匹配预期。例如：SELECT id FROM cards WHERE cardKingdomFoilId IS NOT NULL AND cardKingdomId IS NOT NULL。同时，建议使用标准的'IS NOT NULL'语法（生成的SQL已正确使用），避免使用'NOT ... IS NULL'的非标准写法。"
    },
    {
      "question_id": 341,
      "difficulty": "simple",
      "db_id": "card_games",
      "question": "What are the borderless cards available without powerful foils?",
      "evidence": "borderless' refers to borderColor; poweful foils refers to cardKingdomFoilId paired with cardKingdomId AND cardKingdomId is not null",
      "generated_sql": "SELECT id, name, uuid, borderColor, cardKingdomId, cardKingdomFoilId FROM cards WHERE borderColor = 'borderless' AND (cardKingdomId IS NULL OR cardKingdomFoilId IS NULL)",
      "ground_truth_sql": "SELECT\n  `id`\nFROM `cards`\nWHERE\n  `borderColor` = 'borderless'\n  AND (\n    `cardKingdomId` IS NULL OR `cardKingdomId` IS NULL\n  )",
      "error_category": "返回格式错误",
      "error_reason": "生成的SQL返回了多个列（id, name, uuid, borderColor, cardKingdomId, cardKingdomFoilId），而预期的SQL只返回id列。此外，生成的SQL在WHERE条件中使用了( cardKingdomId IS NULL OR cardKingdomFoilId IS NULL )，而预期的SQL使用了( cardKingdomId IS NULL OR cardKingdomId IS NULL )，后者是错误的（重复了cardKingdomId，未检查cardKingdomFoilId）。根据证据，'没有强大foil'应指cardKingdomFoilId为null或cardKingdomId为null，生成的SQL条件更符合证据，但返回列与预期不符。",
      "suggested_fix": "生成的SQL应只返回id列以符合预期的SQL。对于条件部分，建议修正预期的SQL为( cardKingdomId IS NULL OR cardKingdomFoilId IS NULL )，以正确反映'没有强大foil'的条件。同时，生成的SQL应调整为只选择id列。"
    },
    {
      "question_id": 344,
      "difficulty": "moderate",
      "db_id": "card_games",
      "question": "List all the mythic rarity print cards banned in gladiator format.",
      "evidence": "mythic rarity printing refers to rarity = 'mythic'; card banned refers to status = 'Banned'; in gladiator format refers to format = 'gladiator';",
      "generated_sql": "SELECT cards.* FROM cards INNER JOIN legalities ON cards.uuid = legalities.uuid WHERE cards.rarity = 'mythic' AND legalities.format = 'gladiator' AND legalities.status = 'Banned'",
      "ground_truth_sql": "SELECT DISTINCT\n  `T1`.`id`\nFROM `cards` AS `T1`\nINNER JOIN `legalities` AS `T2`\n  ON `T1`.`uuid` = `T2`.`uuid`\nWHERE\n  `T2`.`format` = 'gladiator'\n  AND `T2`.`status` = 'Banned'\n  AND `T1`.`rarity` = 'mythic'",
      "error_category": "返回格式错误",
      "error_reason": "生成的 SQL 使用 `SELECT cards.*` 返回所有列，但预期的 SQL 只返回 `id` 列。用户问题要求列出所有符合条件的卡片，但预期的输出格式仅包含 `id`，这可能是因为 `id` 是唯一标识符或数据库设计要求。此外，生成的 SQL 缺少 `DISTINCT` 关键字，而预期的 SQL 使用了 `DISTINCT`，这可能导致重复行（如果同一张卡片在 `legalities` 表中有多个记录），但主要错误在于返回的列与预期不匹配。",
      "suggested_fix": "修改 SELECT 语句，只选择 `id` 列并添加 `DISTINCT` 关键字。建议的 SQL 为：`SELECT DISTINCT cards.id FROM cards INNER JOIN legalities ON cards.uuid = legalities.uuid WHERE cards.rarity = 'mythic' AND legalities.format = 'gladiator' AND legalities.status = 'Banned'`。使用别名（如 `T1` 和 `T2`）可以提高可读性，但不是必须的。确保返回格式与预期一致。"
    },
    {
      "question_id": 345,
      "difficulty": "moderate",
      "db_id": "card_games",
      "question": "For artifact type of cards that do not have multiple faces on the same card, state its legalities status for vintage play format.",
      "evidence": "Artifact type of cards refers to types = 'Artifact'; card does not have multiple faces on the same card refers to side is NULL'; vintage play format refers to format = 'vintage';",
      "generated_sql": "SELECT cards.name, legalities.status FROM cards INNER JOIN legalities ON cards.uuid = legalities.uuid WHERE cards.types = 'Artifact' AND cards.side IS NULL AND legalities.format = 'vintage'",
      "ground_truth_sql": "SELECT DISTINCT\n  `T2`.`status`\nFROM `cards` AS `T1`\nINNER JOIN `legalities` AS `T2`\n  ON `T1`.`uuid` = `T2`.`uuid`\nWHERE\n  `T1`.`type` = 'Artifact' AND `T2`.`format` = 'vintage' AND `T1`.`side` IS NULL",
      "error_category": "语义理解错误",
      "error_reason": "生成的SQL选择了cards.name和legalities.status两列，但用户问题只要求'state its legalities status'，可能只需要状态信息，而不需要卡名。此外，列名'types'可能错误，数据库中实际列名应为'type'（如预期的SQL所示），这反映了对数据库模式的误解。预期的SQL只返回status并使用DISTINCT，以避免重复状态，而生成的SQL缺少DISTINCT，可能导致冗余结果。",
      "suggested_fix": "1. 确认数据库模式，检查cards表中的列名是'type'而不是'types'。2. 修改SELECT子句，只选择legalities.status，并添加DISTINCT关键字以返回唯一状态。3. 确保WHERE条件使用正确的列名（如'type'）。4. 考虑使用别名（如T1、T2）提高可读性，但这不是必须的。"
    },
    {
      "question_id": 347,
      "difficulty": "moderate",
      "db_id": "card_games",
      "question": "Find all cards illustrated by Stephen Daniel and describe the text of the ruling of these cards. State if these cards have missing or degraded properties and values.",
      "evidence": "cards have missing or degraded properties and value refers to hasContentWarning = 1; 'Stephen Daniele' is artist; Find all cards refers to return card id",
      "generated_sql": "SELECT c.id AS card_id, r.text AS ruling_text, CASE WHEN c.hasContentWarning = 1 THEN 'Yes' ELSE 'No' END AS has_missing_or_degraded FROM cards c INNER JOIN rulings r ON c.uuid = r.uuid WHERE c.artist = 'Stephen Daniele'",
      "ground_truth_sql": "SELECT\n  `T1`.`id`,\n  `T2`.`text`,\n  `T1`.`hasContentWarning`\nFROM `cards` AS `T1`\nINNER JOIN `rulings` AS `T2`\n  ON `T1`.`uuid` = `T2`.`uuid`\nWHERE\n  `T1`.`artist` = 'Stephen Daniele'",
      "error_category": "返回格式错误",
      "error_reason": "生成的SQL使用了CASE表达式将hasContentWarning字段转换为'Yes'或'No'字符串，而预期的SQL直接返回hasContentWarning字段（整数0或1）。用户问题要求'State if these cards have missing or degraded properties and values'，证据指出hasContentWarning = 1表示有缺失或降级，但未指定返回格式。生成的SQL的CASE表达式可能更符合'State if'的语义意图，但与预期的SQL不一致，导致返回的列类型和格式不同（字符串 vs 整数），这可能影响下游处理或不符合预期输出。",
      "suggested_fix": "修改SELECT子句，直接选择c.hasContentWarning字段，而不是使用CASE表达式。例如：SELECT c.id AS card_id, r.text AS ruling_text, c.hasContentWarning FROM cards c INNER JOIN rulings r ON c.uuid = r.uuid WHERE c.artist = 'Stephen Daniele'。如果意图是返回状态描述，需要根据具体需求确认，但根据预期的SQL，应直接返回原始字段。"
    },
    {
      "question_id": 349,
      "difficulty": "moderate",
      "db_id": "card_games",
      "question": "Name the card and artist with the most ruling information. Also state if the card is a promotional printing.",
      "evidence": "with the most ruling information refers to Max(count(rulings.uuid)); the card is the promotional printing refers to isPromo = 1;",
      "generated_sql": "SELECT c.name, c.artist, c.isPromo FROM cards c INNER JOIN rulings r ON c.uuid = r.uuid GROUP BY c.uuid, c.name, c.artist, c.isPromo HAVING COUNT(r.uuid) = (SELECT MAX(ruling_count) FROM (SELECT COUNT(*) as ruling_count FROM rulings GROUP BY uuid) as sub)",
      "ground_truth_sql": "SELECT `T1`.`name`, `T1`.`artist`, `T1`.`isPromo` FROM `cards` AS `T1` INNER JOIN `rulings` AS `T2` ON `T1`.`uuid` = `T2`.`uuid` WHERE `T1`.`isPromo` = 1 AND `T1`.`artist` = ( SELECT `artist` FROM `cards` WHERE `isPromo` = 1 GROUP BY `artist` HAVING COUNT(DISTINCT `uuid`) = (SELECT MAX(card_counts.max_count) FROM (SELECT COUNT(DISTINCT `uuid`) AS max_count FROM `cards` WHERE `isPromo` = 1 GROUP BY `artist`) AS card_counts))LIMIT 1",
      "error_category": "语义理解错误",
      "error_reason": "生成的 SQL 缺少了 isPromo = 1 的条件，而根据证据和预期 SQL，问题意图是只考虑促销印刷的卡片（isPromo = 1）。用户问题要求 'Name the card and artist with the most ruling information. Also state if the card is a promotional printing.'，证据明确指出 'the card is the promotional printing refers to isPromo = 1'，因此生成的 SQL 应该只筛选 isPromo = 1 的卡片。此外，生成的 SQL 正确实现了 'most ruling information' 为 Max(count(rulings.uuid))，但预期的 SQL 错误地基于卡片数量（而非裁决数量）计算，导致逻辑偏差。生成的 SQL 与预期 SQL 不一致，主要错误在于语义理解上忽略了促销印刷的限制条件。",
      "suggested_fix": "在生成的 SQL 中添加 WHERE c.isPromo = 1 条件，以确保只考虑促销印刷的卡片。同时，考虑添加 ORDER BY COUNT(r.uuid) DESC LIMIT 1 来确保只返回具有最多裁决的单个结果，避免可能返回多行的问题。改进后的 SQL 可能如下：\n```sql\nSELECT c.name, c.artist, c.isPromo FROM cards c INNER JOIN rulings r ON c.uuid = r.uuid WHERE c.isPromo = 1 GROUP BY c.uuid, c.name, c.artist, c.isPromo HAVING COUNT(r.uuid) = (SELECT MAX(ruling_count) FROM (SELECT COUNT(*) as ruling_count FROM rulings GROUP BY uuid) as sub) ORDER BY COUNT(r.uuid) DESC LIMIT 1\n```\n此外，应验证数据库 schema 中 rulings 表与 cards 表的关系，确保 JOIN 条件正确，并确认是否需要使用 DISTINCT 或其他聚合函数。"
    },
    {
      "question_id": 352,
      "difficulty": "moderate",
      "db_id": "card_games",
      "question": "Calculate the percentage of the cards availabe in Chinese Simplified.",
      "evidence": "Chinese Simplified' is the language; percentage = Divide(Sum(id where language = 'Chinese Simplified'), Count(id)) *100",
      "generated_sql": "SELECT CAST((SELECT COUNT(DISTINCT uuid) FROM foreign_data WHERE language = 'Chinese Simplified') AS DOUBLE) / CAST((SELECT COUNT(*) FROM cards) AS DOUBLE) * 100 AS percentage",
      "ground_truth_sql": "SELECT\n  CAST(SUM(CASE WHEN `T2`.`language` = 'Chinese Simplified' THEN 1 ELSE 0 END) AS DOUBLE) * 100 / COUNT(`T1`.`id`)\nFROM `cards` AS `T1`\nINNER JOIN `foreign_data` AS `T2`\n  ON `T1`.`uuid` = `T2`.`uuid`",
      "error_category": "语义理解错误",
      "error_reason": "生成的SQL没有正确理解问题意图。问题要求计算可用卡片中中文简体的百分比，这需要基于cards表的总卡片数，其中语言属性来自foreign_data表。生成的SQL分别从两个表中计数（foreign_data中语言为中文简体的distinct uuid数除以cards表的总记录数），但没有通过JOIN确保数据一致性。这可能导致分母和分子不匹配：如果foreign_data不包含所有cards的记录，百分比会偏低；如果foreign_data包含额外记录，百分比会偏高。此外，证据中提到使用id，但生成的SQL使用uuid，可能与数据库模式不一致。",
      "suggested_fix": "使用INNER JOIN关联cards和foreign_data表，基于uuid，然后计算符合条件的行数除以总行数。例如：SELECT CAST(SUM(CASE WHEN T2.language = 'Chinese Simplified' THEN 1 ELSE 0 END) AS DOUBLE) * 100 / COUNT(T1.id) FROM cards AS T1 INNER JOIN foreign_data AS T2 ON T1.uuid = T2.uuid。这确保只考虑cards中存在的记录，并准确计算百分比。"
    },
    {
      "question_id": 358,
      "difficulty": "simple",
      "db_id": "card_games",
      "question": "What is the border color of card \"Ancestor's Chosen\"?",
      "evidence": "name of card  = 'Ancestor''s Chosen' ;",
      "generated_sql": "SELECT borderColor FROM cards WHERE name = 'Ancestor''s Chosen'",
      "ground_truth_sql": "SELECT DISTINCT\n  `borderColor`\nFROM `cards`\nWHERE\n  `name` = 'Ancestor''s Chosen'",
      "error_category": "SQL 逻辑错误",
      "error_reason": "生成的 SQL 缺少 DISTINCT 关键字，可能导致返回多行结果，而用户问题期望一个唯一值。用户问题询问特定卡牌 'Ancestor's Chosen' 的边框颜色，预期返回一个值。生成的 SQL 直接查询，但未确保结果的唯一性；如果数据库中存在多个相同名称的卡牌记录，查询可能返回多行，这不符合用户意图。预期的 SQL 使用了 DISTINCT 来避免重复，确保查询结果的唯一性。此外，生成的 SQL 未使用反引号引用列名和表名，但这不是主要错误，因为 SQL 语法允许不使用反引号，除非列名或表名包含特殊字符或与保留字冲突。",
      "suggested_fix": "在 SELECT 语句中添加 DISTINCT 关键字，以确保返回唯一结果。例如：SELECT DISTINCT borderColor FROM cards WHERE name = 'Ancestor''s Chosen'。同时，考虑使用反引号（如 `borderColor` 和 `cards`）来引用列名和表名，以提高代码的可读性和避免潜在的命名冲突，特别是在数据库模式可能包含特殊字符的情况下。建议验证数据库模式，确认是否有必要使用 DISTINCT 以及反引号的使用规范。"
    },
    {
      "question_id": 366,
      "difficulty": "simple",
      "db_id": "card_games",
      "question": "What is the rule of playing card \"Benalish Knight\"?",
      "evidence": "Benalish Knight' is the name of card; rule of playing card refers to format;",
      "generated_sql": "SELECT l.format, l.status FROM legalities l WHERE l.uuid = (SELECT uuid FROM cards WHERE name = 'Benalish Knight')",
      "ground_truth_sql": "SELECT\n  `T2`.`format`\nFROM `cards` AS `T1`\nINNER JOIN `legalities` AS `T2`\n  ON `T1`.`uuid` = `T2`.`uuid`\nWHERE\n  `T1`.`name` = 'Benalish Knight'",
      "error_category": "返回格式错误",
      "error_reason": "用户问题询问卡牌的规则（格式），证据明确指出'rule of playing card refers to format'，因此预期结果应仅包含格式信息。生成的SQL选择了`l.format`和`l.status`两列，而预期的SQL只选择了`format`列。这导致返回的列数不符合预期，可能返回了多余的状态信息，不符合用户问题的意图。",
      "suggested_fix": "修改SQL语句，仅选择`format`列。可以保留子查询方式：`SELECT l.format FROM legalities l WHERE l.uuid = (SELECT uuid FROM cards WHERE name = 'Benalish Knight')`。或者使用内连接方式以提高可读性：`SELECT T2.format FROM cards AS T1 INNER JOIN legalities AS T2 ON T1.uuid = T2.uuid WHERE T1.name = 'Benalish Knight'`。确保只返回用户询问的格式信息。"
    },
    {
      "question_id": 371,
      "difficulty": "challenging",
      "db_id": "card_games",
      "question": "What is the percentage of cards whose language is French among the Story Spotlight cards?",
      "evidence": "Story Spotlight card refers to isStorySpotlight = 1; French is the language; Percentage = Divide(Count(id) where language = 'French' and isStorySpotlight = 1, Count(id) where isStorySpotlight = 1)*100",
      "generated_sql": "SELECT CAST((SELECT COUNT(DISTINCT c.id) FROM cards c INNER JOIN foreign_data fd ON c.uuid = fd.uuid WHERE c.isStorySpotlight = 1 AND fd.language = 'French') AS DOUBLE) / CAST((SELECT COUNT(*) FROM cards WHERE isStorySpotlight = 1) AS DOUBLE) * 100 AS percentage",
      "ground_truth_sql": "SELECT\n  CAST(SUM(CASE WHEN `T2`.`language` = 'French' THEN 1 ELSE 0 END) AS DOUBLE) * 100 / COUNT(`T1`.`id`)\nFROM `cards` AS `T1`\nINNER JOIN `foreign_data` AS `T2`\n  ON `T1`.`uuid` = `T2`.`uuid`\nWHERE\n  `T1`.`isStorySpotlight` = 1",
      "error_category": "SQL 逻辑错误",
      "error_reason": "生成的 SQL 使用两个独立的子查询：一个通过 INNER JOIN foreign_data 计算 French Story Spotlight 卡片数，另一个直接从 cards 表计算所有 Story Spotlight 卡片数作为分母。这导致分母包括没有 foreign_data 条目的卡片，而预期的 SQL 通过 INNER JOIN 限制分母只包括有 foreign_data 条目的卡片。因此，生成的 SQL 的分母范围错误，不符合预期的逻辑（预期的 SQL 假设所有 Story Spotlight 卡片都有 foreign_data 条目，或只考虑有语言信息的卡片）。此外，生成的 SQL 使用 COUNT(DISTINCT c.id) 在分子中，而预期的 SQL 使用 SUM(CASE...)，但核心错误在于分母的范围不一致。",
      "suggested_fix": "重写 SQL 为单个查询，使用 INNER JOIN 连接 cards 和 foreign_data 表，在 WHERE 子句中限制 isStorySpotlight = 1，然后使用 SUM(CASE WHEN language = 'French' THEN 1 ELSE 0 END) 计算 French 卡片数，并除以 COUNT(id) 得到百分比。例如：SELECT CAST(SUM(CASE WHEN fd.language = 'French' THEN 1 ELSE 0 END) AS DOUBLE) * 100 / COUNT(c.id) FROM cards c INNER JOIN foreign_data fd ON c.uuid = fd.uuid WHERE c.isStorySpotlight = 1"
    },
    {
      "question_id": 377,
      "difficulty": "simple",
      "db_id": "card_games",
      "question": "How many cards with original type of \"Summon - Angel\" have subtype other than \"Angel\"?",
      "evidence": "subtype other than Angel refers to subtypes is not 'Angel';",
      "generated_sql": "SELECT COUNT(*) FROM cards WHERE originalType = 'Summon - Angel' AND (subtypes IS NULL OR subtypes NOT LIKE '%Angel%');",
      "ground_truth_sql": "SELECT\n  COUNT(`id`)\nFROM `cards`\nWHERE\n  `originalType` = 'Summon - Angel' AND `subtypes` <> 'Angel'",
      "error_category": "语义理解错误",
      "error_reason": "生成的SQL错误地将用户问题中的'subtype other than Angel'解释为子类型不包含'Angel'字符串（使用NOT LIKE '%Angel%'），而问题意图是子类型不等于'Angel'（精确匹配）。此外，生成的SQL包括了子类型为NULL的行（通过subtypes IS NULL），但预期的SQL排除了NULL，这更符合'have subtype'的语义，因为NULL表示没有子类型，不应被视为'other than Angel'。",
      "suggested_fix": "使用精确比较操作符（<>）来检查子类型是否不等于'Angel'，即 `subtypes <> 'Angel'`。这能准确匹配问题意图，排除子类型为'Angel'的行，同时自动排除子类型为NULL的行（因为NULL不等于任何值）。如果数据库模式要求子类型字段必须存在，可以添加 `subtypes IS NOT NULL` 以确保，但预期的SQL已足够。"
    },
    {
      "question_id": 383,
      "difficulty": "simple",
      "db_id": "card_games",
      "question": "How many of the banned cards are white border?",
      "evidence": "banned card refers to status = 'Banned'; white border refers to borderColor = 'white';",
      "generated_sql": "SELECT COUNT(DISTINCT c.uuid) FROM cards c INNER JOIN legalities l ON c.uuid = l.uuid WHERE l.status = 'Banned' AND c.borderColor = 'white'",
      "ground_truth_sql": "SELECT\n  COUNT(`T1`.`id`)\nFROM `cards` AS `T1`\nINNER JOIN `legalities` AS `T2`\n  ON `T1`.`uuid` = `T2`.`uuid`\nWHERE\n  `T2`.`status` = 'Banned' AND `T1`.`borderColor` = 'white'",
      "error_category": "SQL逻辑错误",
      "error_reason": "生成的SQL与预期的SQL在计数列和是否使用DISTINCT上存在逻辑差异。生成的SQL使用了`COUNT(DISTINCT c.uuid)`，而预期的SQL使用了`COUNT(T1.id)`且没有DISTINCT。这可能导致计数结果不同：1) 如果数据库中`id`是卡牌的主键，而`uuid`是另一个唯一标识符，使用不同列可能影响语义（例如，如果`uuid`不唯一或`id`不唯一）；2) 使用DISTINCT可能过度去重，而预期的SQL假设JOIN后无重复行（或依赖`id`的唯一性）。此外，表别名不一致（生成的SQL使用`c`和`l`，预期的SQL使用`T1`和`T2`），但这不是核心错误。",
      "suggested_fix": "修改生成的SQL以匹配预期的SQL：将`COUNT(DISTINCT c.uuid)`改为`COUNT(T1.id)`，并移除DISTINCT关键字。同时，调整表别名以保持一致（例如，使用`T1`和`T2`）。如果数据库模式中`id`是卡牌的唯一标识符，这应能正确统计被禁白边卡牌的数量。如果JOIN可能导致重复行（例如，一张卡牌在legalities表中有多条记录），则需要评估是否需要DISTINCT，但根据预期的SQL，应遵循其逻辑。"
    },
    {
      "question_id": 391,
      "difficulty": "moderate",
      "db_id": "card_games",
      "question": "Among the Artifact cards, which are black color and comes with foreign languague translation?",
      "evidence": "Artifact card refers to originalType = 'Artifact'; black color refers to colors = 'B'; foreign language refers to language in foreign_data",
      "generated_sql": "SELECT name FROM cards WHERE originalType = 'Artifact' AND colors = 'B' AND EXISTS (SELECT 1 FROM foreign_data WHERE foreign_data.uuid = cards.uuid AND foreign_data.language IS NOT NULL)",
      "ground_truth_sql": "SELECT DISTINCT\n  `T1`.`name`\nFROM `cards` AS `T1`\nINNER JOIN `foreign_data` AS `T2`\n  ON `T1`.`uuid` = `T2`.`uuid`\nWHERE\n  `T1`.`originalType` = 'Artifact' AND `T1`.`colors` = 'B'",
      "error_category": "语义理解错误",
      "error_reason": "生成的 SQL 对用户问题 'comes with foreign language translation' 的理解存在偏差。用户问题要求卡片有 foreign language translation，证据指出 foreign language refers to language in foreign_data。生成的 SQL 使用 EXISTS 子查询并添加了 'foreign_data.language IS NOT NULL' 条件，这假设 language 字段必须非 NULL 才能表示有翻译。然而，预期的 SQL 仅通过 INNER JOIN 检查 foreign_data 记录的存在性，没有显式检查 language IS NOT NULL，这表明在上下文中，foreign_data 记录的存在即被视为有翻译，或者 language 字段设计为非 NULL。此外，生成的 SQL 缺少 DISTINCT 关键字，而预期的 SQL 使用了 DISTINCT，这可能导致返回重复的卡片名（如果 cards 表有重复 name 或 foreign_data 有多个记录对应同一卡片），属于返回格式问题，但核心错误是对问题意图的语义理解偏差。",
      "suggested_fix": "修改生成的 SQL 以匹配预期的 SQL：使用 INNER JOIN 代替 EXISTS 子查询，连接 cards 和 foreign_data 表基于 uuid 字段，并添加 DISTINCT 关键字以确保返回唯一卡片名。移除 'foreign_data.language IS NOT NULL' 条件，除非数据库设计明确要求 language 字段非 NULL 且问题意图严格需要翻译值。改进后的 SQL 应为：SELECT DISTINCT T1.name FROM cards AS T1 INNER JOIN foreign_data AS T2 ON T1.uuid = T2.uuid WHERE T1.originalType = 'Artifact' AND T1.colors = 'B'。"
    },
    {
      "question_id": 402,
      "difficulty": "moderate",
      "db_id": "card_games",
      "question": "What is the percentage of Story Spotlight cards that do not have a text box? List them by their ID.",
      "evidence": "Story Spotlight cards that do not have a text box refers to isStorySpotlight = 1 and isTextless = 0; Percentage = DIVIDE(SUM(count(id) where isStorylight = 1 AND isTextless = 0 ), SUM(count(id))) * 100",
      "generated_sql": "SELECT id, (SELECT CAST(COUNT(CASE WHEN isStorySpotlight = 1 AND isTextless = 1 THEN 1 END) AS DOUBLE) * 100 / COUNT(*) FROM cards) AS percentage FROM cards WHERE isStorySpotlight = 1 AND isTextless = 1",
      "ground_truth_sql": "SELECT\n  CAST(SUM(CASE WHEN `isTextless` = 0  AND `isStorySpotlight` = 1 THEN 1 ELSE 0 END) AS DOUBLE) * 100 / COUNT(`id`)\nFROM `cards`",
      "error_category": "语义理解错误",
      "error_reason": "生成的 SQL 中条件错误：'isTextless = 1' 应该是 'isTextless = 0'，因为用户问题要求的是 'do not have a text box'，对应数据库中的 isTextless = 0。此外，生成的 SQL 试图返回 ID 和百分比，但百分比计算基于所有卡片，而 ID 列表仅针对满足条件的卡片，导致逻辑不一致。预期的 SQL 正确计算了百分比，但未列出 ID，可能与问题要求 'List them by their ID' 不符，但核心错误是条件理解偏差。",
      "suggested_fix": "修正条件为 isTextless = 0。如果只需要百分比，使用类似预期的 SQL 但确保条件正确：SELECT CAST(SUM(CASE WHEN isTextless = 0 AND isStorySpotlight = 1 THEN 1 ELSE 0 END) AS DOUBLE) * 100 / COUNT(id) FROM cards。如果需要列出 ID 和百分比，可以考虑两个查询：一个计算百分比，另一个列出 ID；或使用子查询在单个结果集中返回 ID 和百分比，但需注意百分比值对所有行相同。"
    },
    {
      "question_id": 405,
      "difficulty": "moderate",
      "db_id": "card_games",
      "question": "How many Brazilian Portuguese translated sets are inside the Commander block?",
      "evidence": "Commander block refer to block = 'Commander'; sets refer to code = setCode; Portuguese refer to language = 'Portuguese (Brasil)'",
      "generated_sql": "SELECT COUNT(DISTINCT sets.id) AS brazilian_portuguese_sets_count FROM sets INNER JOIN set_translations ON sets.code = set_translations.setCode WHERE sets.block = 'Commander' AND set_translations.language = 'Portuguese (Brasil)'",
      "ground_truth_sql": "SELECT\n  COUNT(`T1`.`id`)\nFROM `sets` AS `T1`\nINNER JOIN `set_translations` AS `T2`\n  ON `T1`.`code` = `T2`.`setCode`\nWHERE\n  `T2`.`language` = 'Portuguese (Brazil)' AND `T1`.`block` = 'Commander'",
      "error_category": "语义理解错误",
      "error_reason": "生成的 SQL 在语言条件字符串上与预期 SQL 不一致：生成的 SQL 使用 'Portuguese (Brasil)'，而预期的 SQL 使用 'Portuguese (Brazil)'。证据指定语言为 'Portuguese (Brasil)'，但预期的 SQL 可能基于数据库中的实际值或不同的拼写，这表明系统在理解语言字符串时存在偏差。此外，生成的 SQL 使用了 COUNT(DISTINCT sets.id)，而预期的 SQL 使用 COUNT(T1.id) 没有 DISTINCT，这可能导致计数逻辑差异（如果 join 产生重复行），但主要错误在于语言字符串的语义理解。",
      "suggested_fix": "1. 根据证据或数据库实际值调整语言字符串，确保与预期一致（例如，使用 'Portuguese (Brazil)' 或验证数据库中的准确值）。2. 评估是否需要 DISTINCT：如果每个 set 只有一个巴西葡萄牙语翻译，可以省略 DISTINCT；否则，保留以避免重复计数。3. 考虑使用表别名（如 T1、T2）以提高可读性和匹配预期格式，但非必须。4. 调整返回列别名以匹配预期 SQL 的格式（如移除别名或使用一致的列名）。"
    },
    {
      "question_id": 407,
      "difficulty": "moderate",
      "db_id": "card_games",
      "question": "Lists all types of cards in German.",
      "evidence": "German refer to language; all types refer to the subtypes, supertypes; subtypes is not null AND supertypes is not null",
      "generated_sql": "SELECT DISTINCT cards.subtypes, cards.supertypes FROM foreign_data INNER JOIN cards ON foreign_data.uuid = cards.uuid WHERE foreign_data.language = 'German' AND cards.subtypes IS NOT NULL AND cards.supertypes IS NOT NULL",
      "ground_truth_sql": "SELECT\n  `T1`.`subtypes`,\n  `T1`.`supertypes`\nFROM `cards` AS `T1`\nINNER JOIN `foreign_data` AS `T2`\n  ON `T1`.`uuid` = `T2`.`uuid`\nWHERE\n  `T2`.`language` = 'German'\n  AND NOT `T1`.`subtypes` IS NULL\n  AND NOT `T1`.`supertypes` IS NULL",
      "error_category": "SQL逻辑错误",
      "error_reason": "生成的SQL使用了DISTINCT关键字，而预期的SQL没有。用户问题要求列出所有类型，可能意味着列出所有记录或独特类型，但预期的SQL没有DISTINCT，所以生成的SQL错误地去重了，可能导致返回行数不符合预期。此外，生成的SQL没有使用别名，而预期的SQL使用了别名，但这不是功能性错误，只是风格差异。",
      "suggested_fix": "移除DISTINCT关键字，以匹配预期的SQL。同时，建议使用别名提高可读性，例如将cards别名为T1，foreign_data别名为T2。"
    },
    {
      "question_id": 408,
      "difficulty": "moderate",
      "db_id": "card_games",
      "question": "How many unknown power cards contain info about the triggered ability",
      "evidence": "unknown power cards refers to power is null or power = '*';contain info about the triggered ability refers to text contains 'triggered ability'",
      "generated_sql": "SELECT COUNT(*) FROM cards WHERE (power IS NULL OR power = '*') AND text LIKE '%triggered ability%'",
      "ground_truth_sql": "SELECT\n  Count(DISTINCT `T1`.`id`) \nFROM `cards` AS `T1`\nINNER JOIN `rulings` AS `T2`\n  ON `T1`.`uuid` = `T2`.`uuid`\nWHERE\n  (\n    `T1`.`power` IS NULL OR `T1`.`power` = '*'\n  )\n  AND `T2`.`text` LIKE '%triggered ability%'",
      "error_category": "SQL逻辑错误",
      "error_reason": "生成的SQL只查询了cards表，但根据预期的SQL，需要连接rulings表来获取文本信息。用户问题中的'contain info about the triggered ability'可能指的是rulings表的text字段，而不是cards表的text字段。证据未明确指定表，但预期的SQL表明文本信息存储在rulings表中。此外，生成的SQL使用COUNT(*)而未使用DISTINCT，可能导致重复计数（如果一个卡牌有多个rulings），而预期的SQL使用Count(DISTINCT T1.id)来确保每个卡牌只计数一次。",
      "suggested_fix": "修改SQL以连接cards和rulings表，并在rulings.text上应用LIKE条件。使用COUNT(DISTINCT cards.id)来确保每个卡牌只计数一次。例如：SELECT COUNT(DISTINCT cards.id) FROM cards INNER JOIN rulings ON cards.uuid = rulings.uuid WHERE (cards.power IS NULL OR cards.power = '*') AND rulings.text LIKE '%triggered ability%'。建议先验证数据库模式，确认cards表是否有text字段以及rulings表是否存储文本信息。"
    },
    {
      "question_id": 412,
      "difficulty": "moderate",
      "db_id": "card_games",
      "question": "What is the foreign name of the card in French of type Creature, normal layout and black border color, by artist Matthew D. Wilson?",
      "evidence": "in French refers to language = 'French'; black border color refers to borderColor = 'black'",
      "generated_sql": "SELECT foreign_data.name FROM foreign_data INNER JOIN cards ON foreign_data.uuid = cards.uuid WHERE foreign_data.language = 'French' AND cards.type = 'Creature' AND cards.layout = 'normal' AND cards.borderColor = 'black' AND cards.artist = 'Matthew D. Wilson'",
      "ground_truth_sql": "SELECT\n  `name`\nFROM `foreign_data`\nWHERE\n  `uuid` IN (\n    SELECT\n      `uuid`\n    FROM `cards`\n    WHERE\n      `types` = 'Creature'\n      AND `layout` = 'normal'\n      AND `borderColor` = 'black'\n      AND `artist` = 'Matthew D. Wilson'\n  )\n  AND `language` = 'French'",
      "error_category": "SQL逻辑错误",
      "error_reason": "生成的SQL中使用了'cards.type = 'Creature''，但预期的SQL和可能的数据库表结构使用'cards.types = 'Creature''。字段名不匹配会导致查询无法正确匹配类型为'Creature'的卡片，从而返回错误或空结果。此外，生成的SQL使用INNER JOIN连接foreign_data和cards表，而预期的SQL使用子查询。虽然JOIN在某些情况下可能有效，但如果foreign_data表中存在多个相同uuid的法语记录，JOIN可能导致重复结果；而子查询更精确地确保只返回符合条件的卡片对应的外文名称。主要错误在于字段名不一致，这属于SQL逻辑错误，因为查询可执行但逻辑不正确。",
      "suggested_fix": "首先，检查数据库表结构，确认cards表中的字段名。如果字段名是'types'（如预期SQL所示），则将生成的SQL中的'cards.type'改为'cards.types'。其次，为了与预期SQL保持一致并避免潜在重复，建议使用子查询方式。修改后的SQL可以是：SELECT foreign_data.name FROM foreign_data WHERE foreign_data.uuid IN (SELECT uuid FROM cards WHERE types = 'Creature' AND layout = 'normal' AND borderColor = 'black' AND artist = 'Matthew D. Wilson') AND foreign_data.language = 'French'。如果字段名是'type'，则保持原样，但需验证数据库设计。"
    },
    {
      "question_id": 416,
      "difficulty": "challenging",
      "db_id": "card_games",
      "question": "What percentage of cards without power are in French?",
      "evidence": "in French refers to language = 'French'; cards without power refers to power IS NULL OR power = '*'; percentage = DIVIDE(COUNT(language = 'French' and power is NULL or power = '*'), COUNT( power is NULL or power = '*'))*100",
      "generated_sql": "SELECT CAST((SELECT COUNT(DISTINCT c.uuid) FROM cards c INNER JOIN foreign_data f ON c.uuid = f.uuid WHERE (c.power IS NULL OR c.power = '*') AND f.language = 'French') AS DOUBLE) * 100 / NULLIF(CAST((SELECT COUNT(*) FROM cards WHERE power IS NULL OR power = '*') AS DOUBLE), 0) AS percentage",
      "ground_truth_sql": "SELECT\n  CAST(SUM(CASE WHEN `T2`.`language` = 'French' THEN 1 ELSE 0 END) AS DOUBLE) * 100 / COUNT(`T1`.`id`)\nFROM `cards` AS `T1`\nINNER JOIN `foreign_data` AS `T2`\n  ON `T1`.`uuid` = `T2`.`uuid`\nWHERE\n  `T1`.`power` IS NULL OR `T1`.`power` = '*'",
      "error_category": "SQL逻辑错误",
      "error_reason": "生成的SQL在计算百分比时，分母子查询包括所有无力量卡片（无论是否有foreign_data记录），而分子子查询只包括有foreign_data记录且语言为法语的卡片。这导致分母范围大于分子范围，计算出的百分比不准确。问题意图可能是计算在有foreign_data记录的无力量卡片中，法语卡片的百分比（如预期的SQL所示），但生成的SQL的分母包括了没有foreign_data记录的卡片，引入了逻辑错误。此外，生成的SQL使用两个独立的子查询，可能导致性能问题，且未处理分母为零的情况（虽然使用了NULLIF，但逻辑不一致）。",
      "suggested_fix": "修改SQL，使用一个查询通过INNER JOIN连接cards和foreign_data表，确保分母和分子基于相同的卡片集（无力量且有foreign_data记录的卡片）。例如，采用预期的SQL结构：SELECT CAST(SUM(CASE WHEN T2.language = 'French' THEN 1 ELSE 0 END) AS DOUBLE) * 100 / COUNT(T1.id) FROM cards AS T1 INNER JOIN foreign_data AS T2 ON T1.uuid = T2.uuid WHERE T1.power IS NULL OR T1.power = '*'. 这样，分母COUNT(T1.id)和分子SUM(CASE...)都基于相同的行集，计算出的百分比准确反映在有foreign_data记录的无力量卡片中，法语卡片的比例。"
    },
    {
      "question_id": 424,
      "difficulty": "simple",
      "db_id": "card_games",
      "question": "What proportion of cards do not have a text box with a normal layout?",
      "evidence": "do not have a text box refers to isTextless = 1; proportion refers to DIVIDE(COUNT(Textless = 1 and layout = 'normal'),COUNT(Textless))*100",
      "generated_sql": "SELECT (SUM(layout = 'normal') / CAST(COUNT(*) AS DOUBLE)) * 100 AS proportion FROM cards WHERE isTextless = 1",
      "ground_truth_sql": "SELECT\n  CAST(SUM(CASE WHEN `isTextless` = 1 AND `layout` = 'normal' THEN 1 ELSE 0 END) AS DOUBLE) * 100 / COUNT(*)\nFROM `cards`",
      "error_category": "语义理解错误",
      "error_reason": "生成的 SQL 误解了问题意图。用户问题要求计算 '没有文本框且布局为 normal 的卡片占所有卡片的比例'，但生成的 SQL 在 WHERE 子句中限制了 isTextless = 1，然后计算其中 layout = 'normal' 的比例，这实际上计算的是 '在没有文本框的卡片中，布局为 normal 的比例'，而不是全局比例。证据中 'proportion refers to DIVIDE(COUNT(Textless = 1 and layout = 'normal'),COUNT(Textless))*100' 的表述可能有歧义，但预期 SQL 明确使用 COUNT(*) 作为分母，表明分母应为所有卡片。因此，生成的 SQL 错误地将分母限制在 isTextless = 1 的子集上，导致比例计算错误。",
      "suggested_fix": "修正 SQL 以计算全局比例：使用 CASE 语句或条件求和来统计满足 isTextless = 1 且 layout = 'normal' 的行数，然后除以总行数。例如，可以采用以下 SQL：SELECT CAST(SUM(CASE WHEN isTextless = 1 AND layout = 'normal' THEN 1 ELSE 0 END) AS DOUBLE) * 100 / COUNT(*) FROM cards。或者，使用 COUNT 和条件：SELECT (COUNT(CASE WHEN isTextless = 1 AND layout = 'normal' THEN 1 END) / CAST(COUNT(*) AS DOUBLE)) * 100 FROM cards。这样能确保分母是所有卡片，符合问题要求。"
    },
    {
      "question_id": 440,
      "difficulty": "simple",
      "db_id": "card_games",
      "question": "Which foreign language used by \"A Pedra Fellwar\"?",
      "evidence": "\"A Pedra Fellwar\" refers to name = 'A Pedra Fellwar'",
      "generated_sql": "SELECT DISTINCT fd.language FROM cards c INNER JOIN foreign_data fd ON c.uuid = fd.uuid WHERE c.name = 'A Pedra Fellwar'",
      "ground_truth_sql": "SELECT DISTINCT\n  `language`\nFROM `foreign_data`\nWHERE\n  `name` = 'A Pedra Fellwar'",
      "error_category": "语义理解错误",
      "error_reason": "生成的SQL错误地假设'A Pedra Fellwar'是cards表中的name字段值，并通过INNER JOIN连接foreign_data表来获取外语信息。然而，根据预期SQL和问题上下文，'A Pedra Fellwar'应该直接对应foreign_data表中的name字段，而外语信息存储在foreign_data表的language字段中。这导致了对数据库模式和问题意图的误解：用户询问的是'A Pedra Fellwar'使用的外语，而外语数据可能直接存储在foreign_data表中，无需关联cards表。如果cards表中没有'A Pedra Fellwar'的记录，生成的SQL将返回空结果或错误结果。",
      "suggested_fix": "直接从foreign_data表中查询language字段，条件为name = 'A Pedra Fellwar'，如预期SQL所示。避免不必要的JOIN操作，除非数据库模式明确要求通过其他表关联。在生成SQL前，应仔细分析数据库模式，确保正确理解数据存储位置。"
    },
    {
      "question_id": 459,
      "difficulty": "moderate",
      "db_id": "card_games",
      "question": "Which card costs more converted mana, \"Serra Angel\" or \"Shrine Keeper\"?",
      "evidence": "\"Serra Angel\" refers to name = 'Serra Angel'; \"Shrine Keeper\" refers to name = 'Shrine Keeper'; card costs more converted mana when the value of convertedManaCost is greater",
      "generated_sql": "SELECT name, convertedManaCost FROM cards WHERE name IN ('Serra Angel', 'Shrine Keeper') ORDER BY convertedManaCost DESC",
      "ground_truth_sql": "SELECT\n  `name`\nFROM `cards`\nWHERE\n  `name` IN ('Serra Angel', 'Shrine Keeper')\nORDER BY\n  `convertedManaCost` DESC\nLIMIT 1",
      "error_category": "语义理解错误",
      "error_reason": "生成的SQL没有正确理解用户问题的意图。用户问题'Which card costs more converted mana, \"Serra Angel\" or \"Shrine Keeper\"?' 旨在比较两张卡牌并指出哪张卡牌的转化法力值更高，因此应只返回那张更高卡牌的名字。生成的SQL虽然正确选择了两张卡牌并按转换法力值降序排序，但未使用LIMIT 1来限制结果，导致可能返回两行（如果两张卡牌都存在），且返回了name和convertedManaCost两列。这不符合用户只想要一张卡牌名字的预期，属于对问题意图的理解偏差。",
      "suggested_fix": "修改SQL以只选择name列，并使用ORDER BY convertedManaCost DESC LIMIT 1来获取转化法力值最高的卡牌名字。例如：SELECT name FROM cards WHERE name IN ('Serra Angel', 'Shrine Keeper') ORDER BY convertedManaCost DESC LIMIT 1"
    },
    {
      "question_id": 462,
      "difficulty": "moderate",
      "db_id": "card_games",
      "question": "What's the Italian name of the set of cards with \"Ancestor's Chosen\" is in?",
      "evidence": "Italian is a language which refers to language = 'Italian'; with \"Ancestor's Chosen\" in the card set refers to name = 'Ancestor''s Chosen'",
      "generated_sql": "SELECT st.translation FROM cards c INNER JOIN set_translations st ON c.setCode = st.setCode WHERE c.name = 'Ancestor''s Chosen' AND st.language = 'Italian'",
      "ground_truth_sql": "SELECT\n  `translation`\nFROM `set_translations`\nWHERE\n  `setCode` IN (\n    SELECT\n      `setCode`\n    FROM `cards`\n    WHERE\n      `name` = 'Ancestor''s Chosen'\n  )\n  AND `language` = 'Italian'",
      "error_category": "SQL逻辑错误",
      "error_reason": "生成的SQL使用INNER JOIN直接连接cards和set_translations表，虽然语法正确且可能返回正确结果，但逻辑上不如预期的SQL清晰。预期的SQL先通过子查询找到包含'Ancestor's Chosen'卡片的卡组（setCode），再在set_translations表中查找该卡组的意大利语翻译，更准确地映射问题意图：先确定卡组，再获取翻译。生成的SQL假设每个卡片都有对应的set_translations记录，并且通过JOIN可能在某些边缘情况下（如cards表中有多个同名卡片但属于不同卡组）产生不准确的结果，而子查询方法更安全地处理了这种关系。",
      "suggested_fix": "采用子查询方法，先从cards表中找到setCode，然后在set_translations表中查找对应卡组的翻译，过滤语言为Italian。具体SQL如预期所示：SELECT `translation` FROM `set_translations` WHERE `setCode` IN (SELECT `setCode` FROM `cards` WHERE `name` = 'Ancestor''s Chosen') AND `language` = 'Italian'。"
    },
    {
      "question_id": 465,
      "difficulty": "moderate",
      "db_id": "card_games",
      "question": "For the set of cards with \"Ancestor's Chosen\" in it, is there a Korean version of it?",
      "evidence": "set of cards with \"Ancestor''s Chosen\" in it refers to name = 'Ancestor''s Chosen'; Korean version refers to language = 'Korean'",
      "generated_sql": "SELECT EXISTS (SELECT 1 FROM foreign_data WHERE uuid IN (SELECT uuid FROM cards WHERE name = 'Ancestor''s Chosen') AND language = 'Korean') AS has_korean_version",
      "ground_truth_sql": "SELECT\n  CASE\n    WHEN SUM(\n      CASE\n        WHEN `T2`.`language` = 'Korean' AND NOT `T2`.`translation` IS NULL\n        THEN 1\n        ELSE 0\n      END\n    ) > 0\n    THEN 'YES'\n    ELSE 'NO'\n  END\nFROM `cards` AS `T1`\nINNER JOIN `set_translations` AS `T2`\n  ON `T2`.`setCode` = `T1`.`setCode`\nWHERE\n  `T1`.`name` = 'Ancestor''s Chosen'",
      "error_category": "语义理解错误",
      "error_reason": "生成的SQL误解了'Korean version'的含义。证据指出'Korean version refers to language = 'Korean''，但预期的SQL还检查了'translation'不为空，表明韩语版本需要有实际的翻译内容。生成的SQL只检查语言字段，没有确保翻译存在，这可能导致返回不准确的结果（例如，如果存在语言为'Korean'但翻译为空的记录，生成的SQL会错误地返回true）。此外，生成的SQL使用了错误的表（foreign_data），而实际数据库模式可能使用set_translations表，通过setCode关联cards表，这反映了对数据库结构的语义理解错误。",
      "suggested_fix": "根据数据库模式调整SQL，使用正确的表和条件。建议的SQL应基于cards表和set_translations表的连接，检查language = 'Korean' AND translation IS NOT NULL，并返回'YES'或'NO'。具体示例：SELECT CASE WHEN SUM(CASE WHEN T2.language = 'Korean' AND NOT T2.translation IS NULL THEN 1 ELSE 0 END) > 0 THEN 'YES' ELSE 'NO' END FROM cards AS T1 INNER JOIN set_translations AS T2 ON T2.setCode = T1.setCode WHERE T1.name = 'Ancestor''s Chosen'。同时，确保返回格式为字符串'YES'或'NO'，而不是布尔值。"
    },
    {
      "question_id": 469,
      "difficulty": "moderate",
      "db_id": "card_games",
      "question": "Did the set of cards with \"Angel of Mercy\" appear on Magic: The Gathering Online?",
      "evidence": "card set \"Angel of Mercy\" refers to name = 'Angel of Mercy'; appear on Magic: The Gathering Online refers to mtgoCode is NOT NULL and vice versa",
      "generated_sql": "SELECT EXISTS (SELECT 1 FROM cards c INNER JOIN sets s ON c.setCode = s.code WHERE c.name = 'Angel of Mercy' AND s.mtgoCode IS NOT NULL) AS appears_on_mtgo;",
      "ground_truth_sql": "SELECT\n  CASE WHEN NOT `T2`.`mtgoCode` IS NULL THEN 'YES' ELSE 'NO' END\nFROM `cards` AS `T1`\nINNER JOIN `sets` AS `T2`\n  ON `T2`.`code` = `T1`.`setCode`\nWHERE\n  `T1`.`name` = 'Angel of Mercy'",
      "error_category": "返回格式错误",
      "error_reason": "生成的SQL使用EXISTS函数返回布尔值（TRUE/FALSE），而预期的SQL使用CASE表达式返回字符串（'YES'/'NO'）。虽然生成的SQL在语义理解和逻辑上正确（检查是否存在卡牌'Angel of Mercy'且其集合的mtgoCode不为NULL），但返回格式与预期不符。用户问题是一个是/否问题，预期的SQL明确要求返回'YES'或'NO'，而生成的SQL返回布尔值，这可能导致输出格式不一致。",
      "suggested_fix": "修改生成的SQL以匹配预期的返回格式。使用CASE表达式或IF函数来返回'YES'或'NO'。例如：SELECT CASE WHEN EXISTS (SELECT 1 FROM cards c INNER JOIN sets s ON c.setCode = s.code WHERE c.name = 'Angel of Mercy' AND s.mtgoCode IS NOT NULL) THEN 'YES' ELSE 'NO' END AS appears_on_mtgo; 这样既保持了逻辑正确性，又符合预期的输出格式。"
    },
    {
      "question_id": 473,
      "difficulty": "moderate",
      "db_id": "card_games",
      "question": "Is the set of cards with Adarkar Valkyrie only available outside the United States?",
      "evidence": "card set Adarkar Valkyrie refers to name = 'Adarkar Valkyrie'; isForeignOnly = 1 means only available outside the United States;",
      "generated_sql": "SELECT isForeignOnly FROM sets WHERE code IN (SELECT setCode FROM cards WHERE name = 'Adarkar Valkyrie')",
      "ground_truth_sql": "SELECT\n  CASE WHEN `isForeignOnly` = 1 THEN 'YES' ELSE 'NO' END\nFROM `cards` AS `T1`\nINNER JOIN `sets` AS `T2`\n  ON `T2`.`code` = `T1`.`setCode`\nWHERE\n  `T1`.`name` = 'Adarkar Valkyrie'",
      "error_category": "返回格式错误",
      "error_reason": "生成的SQL直接返回isForeignOnly字段（数值1或0），而用户问题期望一个是/否的答案。预期的SQL使用CASE语句将isForeignOnly转换为'YES'或'NO'，以匹配问题格式。此外，生成的SQL使用子查询关联表，而预期的SQL使用JOIN，但两者逻辑上等价；主要错误在于返回格式不符合预期，没有将数值结果转换为用户可理解的字符串。",
      "suggested_fix": "修改SQL以使用CASE语句将isForeignOnly转换为'YES'或'NO'，并使用INNER JOIN关联cards和sets表，确保正确过滤name = 'Adarkar Valkyrie'。例如：SELECT CASE WHEN s.isForeignOnly = 1 THEN 'YES' ELSE 'NO' END AS is_only_outside_us FROM cards c INNER JOIN sets s ON c.setCode = s.code WHERE c.name = 'Adarkar Valkyrie'"
    },
    {
      "question_id": 474,
      "difficulty": "moderate",
      "db_id": "card_games",
      "question": "Among the sets of cards that have an Italian translation, how many of them have a base set number of under 100?",
      "evidence": "Italian translation refers to language = 'Italian'; have a translation means translation is not null; base set number of under 100 refers to baseSetSize < 10",
      "generated_sql": "SELECT COUNT(*) AS count FROM sets INNER JOIN set_translations ON sets.code = set_translations.setCode WHERE set_translations.language = 'Italian' AND set_translations.translation IS NOT NULL AND sets.baseSetSize < 10",
      "ground_truth_sql": "SELECT\n  COUNT(`T1`.`id`)\nFROM `sets` AS `T1`\nINNER JOIN `set_translations` AS `T2`\n  ON `T2`.`setCode` = `T1`.`code`\nWHERE\n  NOT `T2`.`translation` IS NULL\n  AND `T1`.`baseSetSize` < 100\n  AND `T2`.`language` = 'Italian'",
      "error_category": "语义理解错误",
      "error_reason": "生成的SQL错误地将'base set number of under 100'解释为baseSetSize < 10，而根据用户问题意图，应该是baseSetSize < 100。证据中提到的'baseSetSize < 10'可能是一个笔误或误解，导致生成的SQL使用了错误的数值条件。其他部分如JOIN条件和翻译条件正确，但数值条件错误影响了查询结果。",
      "suggested_fix": "将条件从sets.baseSetSize < 10改为sets.baseSetSize < 100，以匹配问题要求的'under 100'。同时，可以保留COUNT(*)或使用COUNT(T1.id)，但COUNT(*)通常足够。改进后的SQL应为：SELECT COUNT(*) AS count FROM sets INNER JOIN set_translations ON sets.code = set_translations.setCode WHERE set_translations.language = 'Italian' AND set_translations.translation IS NOT NULL AND sets.baseSetSize < 100"
    },
    {
      "question_id": 483,
      "difficulty": "moderate",
      "db_id": "card_games",
      "question": "Please list the Italian text ruling of all the cards in the set Coldsnap.",
      "evidence": "card set Coldsnap refers to name = 'Coldsnap'; Italian refers to language = 'Italian'",
      "generated_sql": "SELECT cards.name, foreign_data.text FROM sets INNER JOIN cards ON sets.code = cards.setCode INNER JOIN foreign_data ON cards.uuid = foreign_data.uuid WHERE sets.name = 'Coldsnap' AND foreign_data.language = 'Italian'",
      "ground_truth_sql": "SELECT DISTINCT\n  `T1`.`text`\nFROM `foreign_data` AS `T1`\nINNER JOIN `cards` AS `T2`\n  ON `T2`.`uuid` = `T1`.`uuid`\nINNER JOIN `sets` AS `T3`\n  ON `T3`.`code` = `T2`.`setCode`\nWHERE\n  `T3`.`name` = 'Coldsnap' AND `T1`.`language` = 'Italian'",
      "error_category": "返回格式错误",
      "error_reason": "生成的 SQL 返回了两列（cards.name 和 foreign_data.text），而预期的 SQL 只返回一列（foreign_data.text）。用户问题要求列出 Coldsnap 集合中所有卡片的意大利语文本，但未明确要求返回卡片名称，预期的 SQL 仅返回文本列，表明生成的 SQL 多返回了不必要的列。此外，生成的 SQL 缺少 DISTINCT 关键字，而预期的 SQL 使用了 DISTINCT，这可能导致重复的文本行，影响返回结果的唯一性。尽管 JOIN 条件和 WHERE 条件正确，但返回的列数和去重处理不符合预期。",
      "suggested_fix": "修改 SELECT 语句为 SELECT DISTINCT foreign_data.text，只返回文本列并确保唯一性。同时，保持 JOIN 和 WHERE 条件不变。如果问题确实需要卡片名称，则需重新评估问题意图，但根据预期 SQL，应仅返回文本列。建议使用别名提高可读性，如 SELECT DISTINCT T1.text FROM foreign_data AS T1 INNER JOIN cards AS T2 ON T2.uuid = T1.uuid INNER JOIN sets AS T3 ON T3.code = T2.setCode WHERE T3.name = 'Coldsnap' AND T1.language = 'Italian'。"
    },
    {
      "question_id": 484,
      "difficulty": "moderate",
      "db_id": "card_games",
      "question": "Please list the Italian names of the cards in the set Coldsnap with the highest converted mana cost.",
      "evidence": "card set Coldsnap refers to name = 'Coldsnap'; Italian refers to language = 'Italian'; highest converted mana cost refers to MAX(convertedManaCost)",
      "generated_sql": "SELECT fd.name FROM foreign_data fd INNER JOIN cards c ON fd.uuid = c.uuid WHERE c.setCode = (SELECT code FROM sets WHERE name = 'Coldsnap') AND fd.language = 'Italian' AND c.convertedManaCost = (SELECT MAX(convertedManaCost) FROM cards WHERE setCode = (SELECT code FROM sets WHERE name = 'Coldsnap'))",
      "ground_truth_sql": "SELECT\n  `T2`.`name`\nFROM `foreign_data` AS `T1`\nINNER JOIN `cards` AS `T2`\n  ON `T2`.`uuid` = `T1`.`uuid`\nINNER JOIN `sets` AS `T3`\n  ON `T3`.`code` = `T2`.`setCode`\nWHERE\n  `T3`.`name` = 'Coldsnap' AND `T1`.`language` = 'Italian'\nORDER BY\n  `T2`.`convertedManaCost` DESC",
      "error_category": "其他",
      "error_reason": "生成的SQL逻辑正确，语法正确，正确实现了用户问题的要求。它使用子查询获取Coldsnap集合中cards表的最大convertedManaCost，并过滤出具有该最高值的卡片，同时通过INNER JOIN确保从foreign_data表获取意大利语名称。预期的SQL存在逻辑错误：1. 没有使用MAX函数或类似机制来限制只返回最高转换法力成本的卡片，仅使用ORDER BY DESC排序，会返回所有匹配的卡片，不符合'with the highest converted mana cost'的要求；2. 选择了cards表的name（T2.name）而不是foreign_data表的name，而用户问题要求意大利语名称，应从foreign_data表获取。因此，生成的SQL相对于预期的SQL没有错误，但预期的SQL本身有缺陷。",
      "suggested_fix": "生成的SQL已经正确，无需修改。但可以优化以提高可读性和效率：使用INNER JOIN sets表代替子查询获取setCode，例如：SELECT fd.name FROM foreign_data fd INNER JOIN cards c ON fd.uuid = c.uuid INNER JOIN sets s ON c.setCode = s.code WHERE s.name = 'Coldsnap' AND fd.language = 'Italian' AND c.convertedManaCost = (SELECT MAX(convertedManaCost) FROM cards WHERE setCode = s.code)。这样更清晰且避免重复子查询。"
    },
    {
      "question_id": 518,
      "difficulty": "moderate",
      "db_id": "card_games",
      "question": "Which of the play format has the highest number of banned status? Indicate the play format and the names of all the card meet the condition.",
      "evidence": "play format refers to format; banned status refers to status = 'Banned'; the highest number of banned status refers to MAX(COUNT(status = 'Banned'))",
      "generated_sql": "SELECT l.format, c.name FROM legalities l INNER JOIN cards c ON l.uuid = c.uuid WHERE l.status = 'Banned' AND l.format IN (SELECT format FROM legalities WHERE status = 'Banned' GROUP BY format HAVING COUNT(*) = (SELECT MAX(banned_count) FROM (SELECT COUNT(*) as banned_count FROM legalities WHERE status = 'Banned' GROUP BY format) as sub))",
      "ground_truth_sql": "SELECT T2.format, T1.name FROM cards AS T1 INNER JOIN legalities AS T2 ON T2.uuid = T1.uuid INNER JOIN ( SELECT format FROM legalities WHERE status = 'Banned' GROUP BY format ORDER BY COUNT(*) DESC LIMIT 1 ) AS MaxBanned ON MaxBanned.format = T2.format WHERE T2.status = 'Banned'",
      "error_category": "语义理解错误",
      "error_reason": "生成的 SQL 使用了 HAVING COUNT(*) = MAX(...) 来找到具有最高 banned 数量的 format，这可能会返回多个 format 如果它们有相同的最高数量。然而，用户问题 'the highest number' 可能暗示期望一个 format（即具有唯一最高数量的 format），或者预期的 SQL 使用了 LIMIT 1 只返回一个 format。因此，生成的 SQL 可能不符合用户意图或预期结果，因为它可能返回多个 format，而用户可能期望只返回一个 format 或所有相关 card names 但 format 唯一。",
      "suggested_fix": "修改子查询以使用 ORDER BY COUNT(*) DESC LIMIT 1 来只选择一个 format，确保与预期的 SQL 一致。例如，将生成的 SQL 中的子查询部分替换为：SELECT format FROM legalities WHERE status = 'Banned' GROUP BY format ORDER BY COUNT(*) DESC LIMIT 1。这样，主查询将只针对一个 format 返回结果，符合 'the highest' 的语义。"
    },
    {
      "question_id": 522,
      "difficulty": "moderate",
      "db_id": "card_games",
      "question": "Which cards are ranked 1st on EDHRec? List all of the cards name and its banned play format.",
      "evidence": "ranked 1st on EDHRec refers to edhrecRank = 1; banned refers to status = 'Banned'; play format refers to format; cards name refers to name",
      "generated_sql": "SELECT c.name, l.format FROM cards c INNER JOIN legalities l ON c.uuid = l.uuid WHERE c.edhrecRank = 1 AND l.status = 'Banned'",
      "ground_truth_sql": "SELECT\n  `T1`.`name`,\n  `T2`.`format`\nFROM `cards` AS `T1`\nINNER JOIN `legalities` AS `T2`\n  ON `T2`.`uuid` = `T1`.`uuid`\nWHERE\n  `T1`.`edhrecRank` = 1 AND `T2`.`status` = 'Banned'\nGROUP BY\n  `T1`.`name`,\n  `T2`.`format`",
      "error_category": "SQL逻辑错误",
      "error_reason": "生成的SQL缺少GROUP BY子句，而预期的SQL包含GROUP BY子句。用户问题要求列出所有卡片名称及其被禁的播放格式，如果数据库中的legalities表对于同一张卡片在同一个格式中有多个记录（例如，由于数据重复或历史记录），生成的SQL可能返回重复行。预期的SQL通过GROUP BY子句基于name和format分组，确保每个(name, format)组合只出现一次，从而避免重复结果。生成的SQL在逻辑上可能执行，但结果可能包含不必要的重复，不符合问题意图。",
      "suggested_fix": "在生成的SQL中添加GROUP BY子句，例如：GROUP BY c.name, l.format。修改后的SQL应为：SELECT c.name, l.format FROM cards c INNER JOIN legalities l ON c.uuid = l.uuid WHERE c.edhrecRank = 1 AND l.status = 'Banned' GROUP BY c.name, l.format。"
    },
    {
      "question_id": 529,
      "difficulty": "moderate",
      "db_id": "card_games",
      "question": "Find and list the names of sets which doesn't have Japanese translation but have Korean translation.",
      "evidence": "names of sets refers to name; doesn't have Japanese translation refers to language not like '%Japanese%'; have Korean translation refers to language = 'Korean'",
      "generated_sql": "SELECT DISTINCT s.name FROM sets s WHERE EXISTS (SELECT 1 FROM set_translations st WHERE st.setCode = s.code AND st.language = 'Korean') AND NOT EXISTS (SELECT 1 FROM set_translations st WHERE st.setCode = s.code AND st.language LIKE '%Japanese%')",
      "ground_truth_sql": "SELECT\n  `name`\nFROM `sets`\nWHERE\n  `code` IN (\n    SELECT\n      `setCode`\n    FROM `set_translations`\n    WHERE\n      `language` = 'Korean' AND NOT `language` LIKE '%Japanese%'\n  )",
      "error_category": "其他",
      "error_reason": "生成的 SQL 逻辑正确，但与预期的 SQL 不一致。生成的 SQL 使用 EXISTS 和 NOT EXISTS 子查询精确实现了用户问题：检查 set 是否有韩语翻译（language = 'Korean'）且没有日语翻译（language LIKE '%Japanese%'）。预期的 SQL 使用 IN 子查询，但子查询条件 'language = 'Korean' AND NOT language LIKE '%Japanese%' 由于 language = 'Korean' 时 NOT language LIKE '%Japanese%' 恒真，因此仅检查了韩语翻译的存在，未排除有日语翻译的 set，导致逻辑错误。生成的 SQL 更准确，但可能因与预期 SQL 结构不同而被视为错误。",
      "suggested_fix": "生成的 SQL 已正确，无需修改。若需与预期 SQL 结构一致，可改用 IN 子查询，但需确保正确过滤：例如，使用两个子查询或更复杂的条件，如 WHERE code IN (SELECT setCode FROM set_translations WHERE language = 'Korean') AND code NOT IN (SELECT setCode FROM set_translations WHERE language LIKE '%Japanese%')。"
    },
    {
      "question_id": 530,
      "difficulty": "moderate",
      "db_id": "card_games",
      "question": "List all the frame styles and cards Allen Williams worked on and find any banned cards if there are any.",
      "evidence": "frame styles refers to frameVersion; cards Allen Williams worked on refers to artist = 'Allen Williams'; banned cards refers to status = 'Banned'",
      "generated_sql": "SELECT 'frameStyle' AS type, frameVersion AS value FROM cards GROUP BY frameVersion UNION ALL SELECT 'card' AS type, name AS value FROM cards WHERE artist = 'Allen Williams' UNION ALL SELECT 'banned' AS type, c.name AS value FROM cards c INNER JOIN legalities l ON c.uuid = l.uuid WHERE l.status = 'Banned'",
      "ground_truth_sql": "SELECT DISTINCT\n  `T1`.`frameVersion`,\n  `T1`.`name`,\n  CASE WHEN `T2`.`status` = 'Banned' THEN `T1`.`name` ELSE 'NO' END\nFROM `cards` AS `T1`\nINNER JOIN `legalities` AS `T2`\n  ON `T1`.`uuid` = `T2`.`uuid`\nWHERE\n  `T1`.`artist` = 'Allen Williams'",
      "error_category": "语义理解错误",
      "error_reason": "生成的 SQL 误解了用户问题的意图。用户问题要求列出所有 frame styles、Allen Williams 工作过的 cards，以及任何 banned cards。证据指定了 frame styles 对应 frameVersion，cards Allen Williams worked on 对应 artist = 'Allen Williams'，banned cards 对应 status = 'Banned'。生成的 SQL 使用 UNION ALL 组合了三个独立的查询：1) 列出所有 frame styles（全局），2) 列出 Allen Williams 工作过的 cards，3) 列出所有 banned cards（全局）。然而，预期的 SQL 只关注 Allen Williams 的 cards，并通过 JOIN 和 CASE 语句在单个查询中整合了 frameVersion、card 名称和 banned 状态指示。生成的 SQL 没有将 banned 检查限制在 Allen Williams 的 cards 上下文中，且返回三个独立的列表，而不是一个整合的列表，这不符合问题可能期望的输出格式（例如，检查 Allen Williams 的 cards 中是否有 banned 的）。此外，生成的 SQL 中第一个查询使用 GROUP BY frameVersion 但没有聚合函数，可能导致语法问题或意外结果，但这不是主要错误。",
      "suggested_fix": "根据预期的 SQL，应该使用 JOIN 来连接 cards 和 legalities 表，基于 artist = 'Allen Williams' 过滤，然后使用 CASE 语句来指示 banned 状态。如果需要列出所有 frame styles（全局），可能需要一个单独的查询，但预期的 SQL 没有这样做，因此问题意图可能是在 Allen Williams 的 cards 上下文中整合信息。建议生成类似以下的 SQL：SELECT DISTINCT frameVersion, name, CASE WHEN l.status = 'Banned' THEN name ELSE 'NO' END FROM cards c LEFT JOIN legalities l ON c.uuid = l.uuid WHERE c.artist = 'Allen Williams'。这将返回 Allen Williams 工作过的 cards 的 frame styles、card 名称和 banned 状态指示。如果问题确实要求列出所有 frame styles（全局），则需要额外查询，但根据证据和预期 SQL，应优先关注 Allen Williams 的 cards。"
    },
    {
      "question_id": 197,
      "difficulty": "moderate",
      "db_id": "toxicology",
      "question": "Calculate the average number of oxygen atoms in single-bonded molecules.",
      "evidence": "single-bonded molecules refers to bond_type = '-' ; average number of oxygen atom = AVG(element = 'o')",
      "generated_sql": "SELECT AVG(oxygen_count) FROM (SELECT molecule_id, COUNT(*) as oxygen_count FROM atom WHERE element = 'o' AND molecule_id IN (SELECT DISTINCT molecule_id FROM bond WHERE bond_type = '-') GROUP BY molecule_id) AS subquery",
      "ground_truth_sql": "SELECT\n  AVG(`oxygen_count`)\nFROM (\n  SELECT\n    `T1`.`molecule_id`,\n    COUNT(`T1`.`element`) AS `oxygen_count`\n  FROM `atom` AS `T1`\n  INNER JOIN `bond` AS `T2`\n    ON `T1`.`molecule_id` = `T2`.`molecule_id`\n  WHERE\n    `T2`.`bond_type` = '-' AND `T1`.`element` = 'o'\n  GROUP BY\n    `T1`.`molecule_id`\n) AS `oxygen_counts`",
      "error_category": "SQL 逻辑错误",
      "error_reason": "生成的 SQL 使用 IN 子句过滤有单键的分子（bond_type = '-'），然后计数这些分子的所有氧原子。然而，预期的 SQL 通过 INNER JOIN 连接 atom 和 bond 表，并在 WHERE 子句中同时指定 bond_type = '-' 和 element = 'o'，确保只计算那些氧原子直接参与单键的记录。生成的 SQL 逻辑上可能包括氧原子不参与单键的情况（例如，分子有单键但氧原子只参与双键），这会导致平均值计算偏离问题意图（问题可能要求只考虑氧原子参与单键的分子）。此外，生成的 SQL 使用 COUNT(*)，而预期的 SQL 使用 COUNT(T1.element)，在 JOIN 场景下，COUNT(T1.element) 更明确，但两者在过滤后等价。主要错误在于 JOIN 条件的缺失导致过滤逻辑不完整。",
      "suggested_fix": "采用 INNER JOIN 方式连接 atom 和 bond 表，确保分子 ID 匹配且 bond_type = '-'，同时过滤 element = 'o'。然后按 molecule_id 分组计算每个分子的氧原子数量，最后取平均值。具体 SQL 如下：\nSELECT\n  AVG(`oxygen_count`)\nFROM (\n  SELECT\n    `T1`.`molecule_id`,\n    COUNT(`T1`.`element`) AS `oxygen_count`\n  FROM `atom` AS `T1`\n  INNER JOIN `bond` AS `T2`\n    ON `T1`.`molecule_id` = `T2`.`molecule_id`\n  WHERE\n    `T2`.`bond_type` = '-' AND `T1`.`element` = 'o'\n  GROUP BY\n    `T1`.`molecule_id`\n) AS `oxygen_counts`"
    },
    {
      "question_id": 198,
      "difficulty": "challenging",
      "db_id": "toxicology",
      "question": "On average how many carcinogenic molecules are single bonded?",
      "evidence": "carcinogenic molecules refers to label = '+'; single-bonded refers to bond_type = '-'; average = DIVIDE(SUM(bond_type = '-'), COUNT(atom_id))",
      "generated_sql": "SELECT CAST((SELECT COUNT(*) FROM bond b INNER JOIN molecule m ON b.molecule_id = m.molecule_id WHERE m.label = '+' AND b.bond_type = '-') AS DOUBLE) / (SELECT COUNT(*) FROM atom a INNER JOIN molecule m ON a.molecule_id = m.molecule_id WHERE m.label = '+') AS average_value",
      "ground_truth_sql": "SELECT\n  AVG(`single_bond_count`)\nFROM (\n  SELECT\n    `T3`.`molecule_id`,\n    COUNT(`T1`.`bond_type`) AS `single_bond_count`\n  FROM `bond` AS `T1`\n  INNER JOIN `atom` AS `T2`\n    ON `T1`.`molecule_id` = `T2`.`molecule_id`\n  INNER JOIN `molecule` AS `T3`\n    ON `T3`.`molecule_id` = `T2`.`molecule_id`\n  WHERE\n    `T1`.`bond_type` = '-' AND `T3`.`label` = '+'\n  GROUP BY\n    `T3`.`molecule_id`\n) AS `subquery`",
      "error_category": "语义理解错误",
      "error_reason": "用户问题意图是计算平均每个致癌分子的单键数（即每个致癌分子平均有多少个单键），但生成的 SQL 误解了问题意图，计算了所有单键总数除以所有原子总数，这给出了每个原子的平均单键数，而不是每个分子的平均单键数。证据中的公式 DIVIDE(SUM(bond_type = '-'), COUNT(atom_id)) 针对原子平均，但用户问题明确提到 'molecules'，因此生成的 SQL 遵循了证据但忽略了问题上下文，导致逻辑错误。生成的 SQL 没有按分子分组，而是直接计算全局平均值，这与预期的 SQL 逻辑不符。",
      "suggested_fix": "重新理解问题，明确计算每个致癌分子的平均单键数。修改 SQL 为：先按分子分组计算每个致癌分子的单键数，然后取平均值。具体步骤：1. 连接 bond、atom 和 molecule 表，确保过滤 label = '+' 和 bond_type = '-'；2. 按 molecule_id 分组，计算每个分子的单键数；3. 使用 AVG 函数对分组后的单键数取平均。参考预期的 SQL 结构，使用子查询和 AVG 函数。确保 JOIN 条件正确，避免重复计数。"
    },
    {
      "question_id": 206,
      "difficulty": "challenging",
      "db_id": "toxicology",
      "question": "What elements are in the TR004_8_9 bond atoms?",
      "evidence": "TR004_8_9 bond atoms refers to bond_id = 'TR004_8_9';",
      "generated_sql": "SELECT a.element FROM (SELECT atom_id FROM connected WHERE bond_id = 'TR004_8_9' UNION ALL SELECT atom_id2 FROM connected WHERE bond_id = 'TR004_8_9') AS atoms INNER JOIN atom a ON atoms.atom_id = a.atom_id",
      "ground_truth_sql": "SELECT DISTINCT\n  `T1`.`element`\nFROM `atom` AS `T1`\nINNER JOIN `connected` AS `T2`\n  ON `T1`.`atom_id` = `T2`.`atom_id`\nWHERE\n  `T2`.`bond_id` = 'TR004_8_9'",
      "error_category": "SQL逻辑错误",
      "error_reason": "生成的SQL正确获取了与bond_id 'TR004_8_9'相关的所有原子的元素，但缺少DISTINCT关键字，因此可能返回重复的元素。用户问题询问'elements'，通常期望不重复的元素列表。此外，生成的SQL使用了子查询和UNION ALL，虽然逻辑上正确，但比预期的SQL更复杂，且未处理可能的重复值。预期的SQL使用了DISTINCT来确保返回唯一元素，而生成的SQL没有，这可能导致结果集包含重复行。",
      "suggested_fix": "在生成的SQL中添加DISTINCT关键字以避免重复元素，例如：SELECT DISTINCT a.element FROM (SELECT atom_id FROM connected WHERE bond_id = 'TR004_8_9' UNION ALL SELECT atom_id2 FROM connected WHERE bond_id = 'TR004_8_9') AS atoms INNER JOIN atom a ON atoms.atom_id = a.atom_id。或者，简化查询为更直接的连接方式，如预期的SQL所示，但需注意预期的SQL只连接了atom_id列，可能遗漏atom_id2对应的原子；如果数据库结构允许，建议包括atom_id2以确保完整性。"
    },
    {
      "question_id": 207,
      "difficulty": "challenging",
      "db_id": "toxicology",
      "question": "What elements are in a double type bond?",
      "evidence": "double type bond refers to bond_type = '=';",
      "generated_sql": "SELECT a.element FROM bond b INNER JOIN connected c ON b.bond_id = c.bond_id INNER JOIN atom a ON c.atom_id = a.atom_id WHERE b.bond_type = '=' UNION SELECT a.element FROM bond b INNER JOIN connected c ON b.bond_id = c.bond_id INNER JOIN atom a ON c.atom_id2 = a.atom_id WHERE b.bond_type = '='",
      "ground_truth_sql": "SELECT DISTINCT\n  `T1`.`element`\nFROM `atom` AS `T1`\nINNER JOIN `bond` AS `T2`\n  ON `T1`.`molecule_id` = `T2`.`molecule_id`\nINNER JOIN `connected` AS `T3`\n  ON `T1`.`atom_id` = `T3`.`atom_id`\nWHERE\n  `T2`.`bond_type` = '='",
      "error_category": "SQL 逻辑错误",
      "error_reason": "生成的 SQL 在表连接逻辑上存在错误。它通过 bond_id 连接 bond 和 connected 表，然后通过 atom_id 或 atom_id2 连接 atom 表，而预期的 SQL 使用 molecule_id 连接 atom 和 bond 表，再通过 atom_id 连接 connected 表。这表明数据库模式中，atom 和 bond 可能通过 molecule_id 关联，而不是直接通过 connected 表的 bond_id。生成的 SQL 可能返回不正确的元素，因为连接条件不符合实际表关系。此外，生成的 SQL 使用 UNION 来获取双键的两个原子，但预期的 SQL 使用单个 JOIN 链和 DISTINCT，可能更准确且高效。",
      "suggested_fix": "根据预期的 SQL 调整 JOIN 条件以匹配数据库模式。具体步骤：1. 使用 atom 表和 bond 表通过 molecule_id 连接；2. 连接 connected 表通过 atom_id；3. 在 WHERE 子句中过滤 bond_type = '='；4. 使用 DISTINCT 返回唯一元素。示例 SQL：SELECT DISTINCT atom.element FROM atom INNER JOIN bond ON atom.molecule_id = bond.molecule_id INNER JOIN connected ON atom.atom_id = connected.atom_id WHERE bond.bond_type = '='。"
    },
    {
      "question_id": 208,
      "difficulty": "moderate",
      "db_id": "toxicology",
      "question": "Which type of label is the most numerous in atoms with hydrogen?",
      "evidence": "with hydrogen refers to element = 'h'; label most numerous in atoms refers to MAX(COUNT(label)); ",
      "generated_sql": "SELECT m.label FROM atom a INNER JOIN molecule m ON a.molecule_id = m.molecule_id WHERE a.element = 'h' GROUP BY m.label ORDER BY COUNT(a.atom_id) DESC LIMIT 1",
      "ground_truth_sql": "SELECT\n  `T`.`label`\nFROM (\n  SELECT\n    `T2`.`label`,\n    COUNT(`T2`.`molecule_id`)\n  FROM `atom` AS `T1`\n  INNER JOIN `molecule` AS `T2`\n    ON `T1`.`molecule_id` = `T2`.`molecule_id`\n  WHERE\n    `T1`.`element` = 'h'\n  GROUP BY\n    `T2`.`label`\n  ORDER BY\n    COUNT(`T2`.`molecule_id`) DESC\n  LIMIT 1\n) AS `t`",
      "error_category": "SQL逻辑错误",
      "error_reason": "生成的SQL使用COUNT(a.atom_id)计算每个标签下的原子数量，而预期的SQL使用COUNT(T2.molecule_id)计算每个标签下的分子数量。问题意图是找出在含有氢原子的原子中，哪种标签最常见，但标签来自molecule表，通过JOIN关联。预期的SQL可能意图计算分子数量（基于标签分组），而生成的SQL错误地计算了原子数量，导致聚合范围错误。尽管生成的SQL语法正确且可执行，但其逻辑与预期不符，因为问题上下文可能更关注分子级别的标签频率（尽管问题提到'atoms'，但证据和预期SQL暗示使用分子计数）。",
      "suggested_fix": "将生成的SQL中的COUNT(a.atom_id)改为COUNT(m.molecule_id)或COUNT(*)，以匹配预期的SQL逻辑。同时，考虑使用子查询结构以提高可读性和一致性，例如：SELECT m.label FROM atom a INNER JOIN molecule m ON a.molecule_id = m.molecule_id WHERE a.element = 'h' GROUP BY m.label ORDER BY COUNT(m.molecule_id) DESC LIMIT 1。如果意图是计算原子数量，则应保留COUNT(a.atom_id)，但需确认问题意图是否与证据一致。"
    },
    {
      "question_id": 212,
      "difficulty": "challenging",
      "db_id": "toxicology",
      "question": "Which element is the least numerous in non-carcinogenic molecules?",
      "evidence": "label = '-' means molecules are non-carcinogenic; least numerous refers to MIN(COUNT(element));",
      "generated_sql": "SELECT element FROM atom INNER JOIN molecule ON atom.molecule_id = molecule.molecule_id WHERE molecule.label = '-' GROUP BY element HAVING COUNT(*) = (SELECT MIN(cnt) FROM (SELECT COUNT(*) AS cnt FROM atom INNER JOIN molecule ON atom.molecule_id = molecule.molecule_id WHERE molecule.label = '-' GROUP BY element) AS sub)",
      "ground_truth_sql": "SELECT\n  `T`.`element`\nFROM (\n  SELECT\n    `T1`.`element`,\n    COUNT(DISTINCT `T1`.`molecule_id`)\n  FROM `atom` AS `T1`\n  INNER JOIN `molecule` AS `T2`\n    ON `T1`.`molecule_id` = `T2`.`molecule_id`\n  WHERE\n    `T2`.`label` = '-'\n  GROUP BY\n    `T1`.`element`\n  ORDER BY\n    COUNT(DISTINCT `T1`.`molecule_id`) ASC\n  LIMIT 1\n) AS `t`",
      "error_category": "语义理解错误",
      "error_reason": "生成的 SQL 使用 COUNT(*) 计算原子总数，而问题 'least numerous in non-carcinogenic molecules' 的意图是找到在非致癌分子中出现次数最少的元素，应基于分子计数（即每个元素在不同分子中的出现次数）。生成的 SQL 误解了 'numerous' 的含义，将计数范围错误地应用于原子而非分子，导致逻辑偏差，可能返回错误的结果（例如，如果一个元素在少数分子中出现多次，原子总数可能较高，但分子计数较低）。此外，生成的 SQL 使用 HAVING COUNT(*) = MIN(...) 可能返回多个并列最小值的元素，而问题使用单数 'element' 暗示期望单个结果，这进一步体现了语义理解的不准确。",
      "suggested_fix": "修改 SQL 以使用 COUNT(DISTINCT molecule_id) 来计数每个元素在不同分子中的出现次数，然后通过 ORDER BY 计数 ASC 和 LIMIT 1 来找到最小值，如预期的 SQL 所示。这确保了计数基于分子存在而非原子数量，并处理了返回单个元素的需求。具体实现：使用子查询按元素分组，计算 COUNT(DISTINCT molecule_id)，排序后取 LIMIT 1。"
    },
    {
      "question_id": 215,
      "difficulty": "challenging",
      "db_id": "toxicology",
      "question": "How many atoms with iodine and with sulfur type elements are there in single bond molecules?",
      "evidence": "with iodine element refer to element = 'i'; with sulfur element refers to element = 's'; single type bond refers to bond_type = '-'; Should consider the distinct atoms when counting;",
      "generated_sql": "SELECT COUNT(DISTINCT a.atom_id) FROM atom a INNER JOIN bond b ON a.molecule_id = b.molecule_id WHERE a.element IN ('i', 's') AND b.bond_type = '-'",
      "ground_truth_sql": "SELECT\n  COUNT(DISTINCT CASE WHEN `T1`.`element` = 'i' THEN `T1`.`atom_id` ELSE NULL END) AS `iodine_nums`,\n  COUNT(DISTINCT CASE WHEN `T1`.`element` = 's' THEN `T1`.`atom_id` ELSE NULL END) AS `sulfur_nums`\nFROM `atom` AS `T1`\nINNER JOIN `connected` AS `T2`\n  ON `T1`.`atom_id` = `T2`.`atom_id`\nINNER JOIN `bond` AS `T3`\n  ON `T2`.`bond_id` = `T3`.`bond_id`\nWHERE\n  `T3`.`bond_type` = '-'",
      "error_category": "语义理解错误",
      "error_reason": "生成的SQL误解了问题意图。用户问题“How many atoms with iodine and with sulfur type elements”可能要求分别计数碘原子和硫原子，但生成的SQL使用IN ('i', 's')并计数总数，导致合并计数。此外，SQL逻辑错误在于连接方式：使用atom.molecule_id = bond.molecule_id可能不正确，因为数据库模式中原子和键的关系是通过connected表关联的，而不是直接通过molecule_id，这可能导致过滤出的原子不准确。",
      "suggested_fix": "修正SQL以分别计数碘原子和硫原子，使用CASE语句或两个查询。同时，调整连接方式，使用connected表连接atom和bond，确保原子与单键的正确关联。例如，参考预期的SQL结构：从atom连接connected再连接bond，过滤bond_type = '-'，并使用COUNT(DISTINCT CASE WHEN element = 'i' THEN atom_id ELSE NULL END)和COUNT(DISTINCT CASE WHEN element = 's' THEN atom_id ELSE NULL END)分别计数。"
    },
    {
      "question_id": 218,
      "difficulty": "challenging",
      "db_id": "toxicology",
      "question": "What percentage of carcinogenic-type molecules does not contain fluorine?",
      "evidence": "label = '+' mean molecules are carcinogenic; contain fluorine refers to element = 'f'; percentage = DIVIDE(SUM(element = 'f') * 100, COUNT(molecule_id)) where label = '+'; Should consider the distinct atoms when counting;",
      "generated_sql": "SELECT (CAST(SUM(CASE WHEN no_fluorine = 1 THEN 1 ELSE 0 END) AS DOUBLE) / CAST(COUNT(*) AS DOUBLE)) * 100 AS percentage FROM (SELECT m.molecule_id, CASE WHEN NOT EXISTS (SELECT 1 FROM atom a WHERE a.molecule_id = m.molecule_id AND a.element = 'f') THEN 1 ELSE 0 END AS no_fluorine FROM molecule m WHERE m.label = '+') AS subquery",
      "ground_truth_sql": "SELECT\n  CAST(COUNT(DISTINCT CASE WHEN `T1`.`element` <> 'f' THEN `T2`.`molecule_id` ELSE NULL END) AS DOUBLE) * 100 / COUNT(DISTINCT `T2`.`molecule_id`)\nFROM `atom` AS `T1`\nINNER JOIN `molecule` AS `T2`\n  ON `T1`.`molecule_id` = `T2`.`molecule_id`\nWHERE\n  `T2`.`label` = '+'",
      "error_category": "其他",
      "error_reason": "生成的 SQL 逻辑正确，符合用户问题意图（计算不包含氟的致癌分子百分比）。它使用子查询和 NOT EXISTS 检查每个分子是否没有氟原子，然后计算比例。然而，预期的 SQL 逻辑错误：它通过 JOIN 和 CASE 语句计算至少有一个非氟原子的分子比例，而非不包含氟的分子比例。生成的 SQL 与预期的 SQL 在结构和逻辑上不一致，但生成的 SQL 正确实现了问题要求。证据中提到的 'Should consider the distinct atoms when counting' 可能指 distinct molecules，生成的 SQL 已通过子查询确保每个分子只计数一次，因此没有错误。",
      "suggested_fix": "生成的 SQL 已正确，无需改进。如果必须匹配预期的 SQL 结构，可以改写为使用 JOIN 和 GROUP BY/HAVING 的方式，例如：SELECT CAST(COUNT(DISTINCT CASE WHEN no_fluorine = 1 THEN molecule_id END) AS DOUBLE) * 100 / COUNT(DISTINCT molecule_id) FROM (SELECT m.molecule_id, CASE WHEN NOT EXISTS (SELECT 1 FROM atom a WHERE a.molecule_id = m.molecule_id AND a.element = 'f') THEN 1 ELSE 0 END AS no_fluorine FROM molecule m WHERE m.label = '+') AS subquery。但预期的 SQL 本身逻辑错误，应修正为计算不包含氟的分子比例。"
    },
    {
      "question_id": 219,
      "difficulty": "challenging",
      "db_id": "toxicology",
      "question": "What is the percentage of carcinogenic molecules in triple type bonds?",
      "evidence": "label = '+' mean molecules are carcinogenic; triple bond refers to bond_type = '#'; percentage = DIVIDE(SUM(bond_type = '#') * 100, COUNT(bond_id)) as percent where label = '+'",
      "generated_sql": "SELECT CAST(SUM(CASE WHEN molecule.label = '+' THEN 1 ELSE 0 END) AS DOUBLE) * 100 / CAST(COUNT(*) AS DOUBLE) AS percentage FROM bond INNER JOIN molecule ON bond.molecule_id = molecule.molecule_id WHERE bond.bond_type = '#'",
      "ground_truth_sql": "SELECT\n  CAST(COUNT(DISTINCT CASE WHEN `T2`.`label` = '+' THEN `T2`.`molecule_id` ELSE NULL END) AS DOUBLE) * 100 / COUNT(DISTINCT `T2`.`molecule_id`)\nFROM `atom` AS `T1`\nINNER JOIN `molecule` AS `T2`\n  ON `T1`.`molecule_id` = `T2`.`molecule_id`\nINNER JOIN `bond` AS `T3`\n  ON `T2`.`molecule_id` = `T3`.`molecule_id`\nWHERE\n  `T3`.`bond_type` = '#'",
      "error_category": "SQL逻辑错误",
      "error_reason": "生成的SQL在逻辑上存在错误，因为它计算的是三键记录（bond_type = '#'）中致癌分子记录的比例，而不是分子中致癌分子的比例。具体来说，分母使用COUNT(*)计算的是三键的数量，而分子使用SUM(CASE WHEN label = '+' THEN 1 ELSE 0 END)计算的是致癌三键记录的数量。由于一个分子可能有多个三键，这会导致分母和分子计数错误（重复计数），无法准确反映“在具有三键的分子中，致癌分子的百分比”。预期的SQL通过使用COUNT(DISTINCT)确保每个分子只被计数一次，分母是具有三键的分子数量，分子是致癌分子数量，从而正确计算百分比。",
      "suggested_fix": "修正SQL逻辑，使用DISTINCT来计数分子ID，确保每个分子只被计数一次。建议的SQL：SELECT CAST(COUNT(DISTINCT CASE WHEN molecule.label = '+' THEN molecule.molecule_id ELSE NULL END) AS DOUBLE) * 100 / COUNT(DISTINCT molecule.molecule_id) AS percentage FROM bond INNER JOIN molecule ON bond.molecule_id = molecule.molecule_id WHERE bond.bond_type = '#'。如果数据库结构要求通过atom表连接（如预期的SQL所示），则应使用atom表进行连接，以确保数据完整性。"
    },
    {
      "question_id": 227,
      "difficulty": "simple",
      "db_id": "toxicology",
      "question": "What is the percentage of molecules that are carcinogenic? Please provide your answer as a percentage with three decimal places.",
      "evidence": "label = '+' mean molecules are carcinogenic; percentage = DIVIDE(SUM(label = '+'), COUNT(molecule_id)) as percent",
      "generated_sql": "SELECT ROUND((CAST(SUM(label = '+') AS DOUBLE) / COUNT(*)) * 100, 3) AS percentage FROM molecule",
      "ground_truth_sql": "SELECT\n  ROUND(\n    CAST(COUNT(CASE WHEN `T`.`label` = '+' THEN `T`.`molecule_id` ELSE NULL END) AS DOUBLE) * 100 / COUNT(`T`.`molecule_id`),\n    3\n  )\nFROM `molecule` AS `t`",
      "error_category": "SQL 语法错误",
      "error_reason": "生成的 SQL 使用了 `SUM(label = '+')` 来计算条件计数，这在标准 SQL 中可能无效，因为 `SUM` 函数通常期望数值参数，而 `label = '+'` 是一个布尔表达式。虽然在某些 SQL 方言（如 MySQL）中，布尔表达式可能被隐式转换为整数（1 或 0），但这种方法缺乏可移植性和清晰性。预期的 SQL 使用了更标准的 `COUNT(CASE WHEN ... THEN ... ELSE NULL END)` 来计算条件计数，这是更可靠的方法。此外，生成的 SQL 使用 `COUNT(*)` 计算总数，而预期的 SQL 使用 `COUNT(T.molecule_id)`，虽然在主键无 NULL 的情况下可能结果相同，但使用列名更精确。",
      "suggested_fix": "改用标准 SQL 条件计数方法：使用 `COUNT(CASE WHEN label = '+' THEN molecule_id ELSE NULL END)` 计算致癌分子数量，并使用 `COUNT(molecule_id)` 计算总分子数。确保除法时使用浮点数转换以避免整数除法问题。例如：`SELECT ROUND(CAST(COUNT(CASE WHEN label = '+' THEN molecule_id ELSE NULL END) AS DOUBLE) * 100 / COUNT(molecule_id), 3) AS percentage FROM molecule`。"
    },
    {
      "question_id": 230,
      "difficulty": "challenging",
      "db_id": "toxicology",
      "question": "What are the elements of the toxicology and label of molecule TR060?",
      "evidence": "TR060 is the molecule id; ",
      "generated_sql": "SELECT atom.element, molecule.label FROM atom INNER JOIN molecule ON atom.molecule_id = molecule.molecule_id WHERE molecule.molecule_id = 'TR060'",
      "ground_truth_sql": "SELECT DISTINCT\n  `T1`.`element`,\n  `T2`.`label`\nFROM `atom` AS `T1`\nINNER JOIN `molecule` AS `T2`\n  ON `T1`.`molecule_id` = `T2`.`molecule_id`\nWHERE\n  `T2`.`molecule_id` = 'TR060'",
      "error_category": "SQL 逻辑错误",
      "error_reason": "生成的 SQL 与预期的 SQL 在逻辑上存在差异：生成的 SQL 缺少 DISTINCT 关键字，而预期的 SQL 使用了 DISTINCT。用户问题询问 'elements of the toxicology and label of molecule TR060'，可能期望返回唯一的元素和标签对。生成的 SQL 可能返回重复行（如果 atom 表中有多个相同的元素记录），而预期的 SQL 通过 DISTINCT 确保结果去重。此外，用户问题中的 'toxicology' 在生成的 SQL 和预期的 SQL 中均未直接处理，但基于给定证据和 SQL 结构，这可能不是主要错误；主要问题在于缺少去重逻辑。",
      "suggested_fix": "在 SELECT 子句中添加 DISTINCT 关键字，以确保返回唯一的元素和标签对。例如，修改生成的 SQL 为：SELECT DISTINCT atom.element, molecule.label FROM atom INNER JOIN molecule ON atom.molecule_id = molecule.molecule_id WHERE molecule.molecule_id = 'TR060'。同时，考虑使用表别名（如 T1、T2）和反引号以提高可读性和一致性，但这不是必须的。"
    },
    {
      "question_id": 231,
      "difficulty": "challenging",
      "db_id": "toxicology",
      "question": "Which bond type accounted for the majority of the bonds found in molecule TR010 and state whether or not this molecule is carcinogenic?",
      "evidence": "TR010 is the molecule id; majority of the bond found refers to MAX(COUNT(bond_type)); ",
      "generated_sql": "SELECT b.bond_type, m.label FROM bond b INNER JOIN molecule m ON b.molecule_id = m.molecule_id WHERE b.molecule_id = 'TR010' AND b.bond_type = (SELECT bond_type FROM bond WHERE molecule_id = 'TR010' GROUP BY bond_type ORDER BY COUNT(*) DESC LIMIT 1) LIMIT 1;",
      "ground_truth_sql": "SELECT\n  `T`.`bond_type`\nFROM (\n  SELECT\n    `T1`.`bond_type`,\n    COUNT(`T1`.`molecule_id`)\n  FROM `bond` AS `T1`\n  WHERE\n    `T1`.`molecule_id` = 'TR010'\n  GROUP BY\n    `T1`.`bond_type`\n  ORDER BY\n    COUNT(`T1`.`molecule_id`) DESC\n  LIMIT 1\n) AS `T`",
      "error_category": "语义理解错误",
      "error_reason": "生成的SQL误解了用户问题的意图。用户问题要求两个信息：1) 在分子TR010中占多数的键类型（bond type），2) 该分子是否致癌（carcinogenic）。生成的SQL返回了bond_type和m.label，但m.label可能不是carcinogenic状态（例如，它可能是分子名称）。证据中未指定carcinogenic字段，但问题明确要求说明致癌性，因此生成的SQL未能正确识别和返回carcinogenic信息。此外，生成的SQL使用了不必要的JOIN和LIMIT 1，可能导致返回多余或错误的数据行，而预期的SQL只针对第一部分（bond type），忽略了致癌性部分，但生成的SQL的错误主要在于对问题意图的误解。",
      "suggested_fix": "修改SQL以正确处理问题的两个部分。首先，找到在分子TR010中计数最多的bond_type（使用子查询），然后从molecule表中获取carcinogenic状态（假设存在如is_carcinogenic的字段）。例如：\nSELECT \n  (SELECT bond_type FROM bond WHERE molecule_id = 'TR010' GROUP BY bond_type ORDER BY COUNT(*) DESC LIMIT 1) AS bond_type,\n  m.is_carcinogenic\nFROM molecule m\nWHERE m.molecule_id = 'TR010';\n这样可以直接返回bond_type和carcinogenic状态，避免不必要的JOIN和行数限制。如果数据库中没有carcinogenic字段，可能需要检查其他表或字段。"
    },
    {
      "question_id": 234,
      "difficulty": "moderate",
      "db_id": "toxicology",
      "question": "How many bonds which involved atom 12 does molecule TR009 have?",
      "evidence": "TR009 is the molecule id;  involved atom 12 refers to atom_id = 'TR009_12' or atom_id2 = 'TR009_12'",
      "generated_sql": "SELECT COUNT(DISTINCT c.bond_id) FROM connected c INNER JOIN bond b ON c.bond_id = b.bond_id WHERE (c.atom_id = 'TR009_12' OR c.atom_id2 = 'TR009_12') AND b.molecule_id = 'TR009'",
      "ground_truth_sql": "SELECT\n  COUNT(`T2`.`bond_id`)\nFROM `bond` AS `T1`\nINNER JOIN `connected` AS `T2`\n  ON `T1`.`bond_id` = `T2`.`bond_id`\nWHERE\n  `T1`.`molecule_id` = 'TR009'\n  AND `T2`.`atom_id` = CONCAT(`T1`.`molecule_id`, '_1')\n  OR `T2`.`atom_id2` = CONCAT(`T1`.`molecule_id`, '_2')",
      "error_category": "语义理解错误",
      "error_reason": "生成的SQL在语义理解上与预期SQL存在偏差。预期的SQL使用CONCAT(T1.molecule_id, '_1')和CONCAT(T1.molecule_id, '_2')来动态生成原子id，这对应于原子1和2，但用户问题明确指定原子12（证据显示atom_id = 'TR009_12'或atom_id2 = 'TR009_12'）。生成的SQL直接使用了硬编码的'TR009_12'，虽然符合问题意图，但未遵循预期SQL的动态生成逻辑。此外，生成的SQL使用了COUNT(DISTINCT c.bond_id)，而预期的SQL使用了COUNT(T2.bond_id) without DISTINCT，这可能导致计数结果不同（如果bond_id在connected表中重复）。生成的SQL条件使用了括号确保逻辑正确，而预期的SQL条件未使用括号，可能导致逻辑错误（AND优先级高于OR），但生成的SQL未匹配预期的结构。",
      "suggested_fix": "根据问题，原子12的id应为'TR009_12'，因此生成的SQL在语义上是正确的。但为了与预期SQL保持一致，建议修改为：使用CONCAT(T1.molecule_id, '_12')来动态生成原子id，并移除DISTINCT（如果bond_id在JOIN后唯一）。同时，确保条件使用括号分组：WHERE T1.molecule_id = 'TR009' AND (T2.atom_id = CONCAT(T1.molecule_id, '_12') OR T2.atom_id2 = CONCAT(T1.molecule_id, '_12'))。如果预期的SQL中的'_1'和'_2'是错误，应纠正为'_12'。"
    },
    {
      "question_id": 239,
      "difficulty": "simple",
      "db_id": "toxicology",
      "question": "How many connections does the atom 19 have?",
      "evidence": "connections refers to bond_id; atom 19 refers to atom_id like 'TR%_19';",
      "generated_sql": "SELECT COUNT(*) AS connection_count FROM connected WHERE atom_id LIKE 'TR%_19' OR atom_id2 LIKE 'TR%_19'",
      "ground_truth_sql": "SELECT\n  COUNT(`T`.`bond_id`)\nFROM `connected` AS `T`\nWHERE\n  SUBSTR(`T`.`atom_id`, -2) = '19'",
      "error_category": "语义理解错误",
      "error_reason": "生成的 SQL 使用 LIKE 'TR%_19' 来匹配 atom_id，但证据表明 atom 19 对应 atom_id 以 '19' 结尾，而不是固定以 'TR' 开头。预期的 SQL 使用 SUBSTR 检查最后两个字符为 '19'，这更准确地反映了 atom 19 的标识方式。此外，生成的 SQL 检查 atom_id2，但预期的 SQL 只检查 atom_id，这可能意味着数据库设计中每个连接只关联一个原子，或者用户问题只关心 atom 19 作为主要原子。因此，生成的 SQL 在语义理解上存在偏差，对问题意图的理解不准确。",
      "suggested_fix": "修改 SQL 以使用 SUBSTR(atom_id, -2) = '19' 来匹配 atom 19，并根据数据库设计决定是否检查 atom_id2。如果数据库设计允许连接涉及两个原子，应使用 OR 条件检查两者；否则，只检查 atom_id。同时，考虑使用 COUNT(bond_id) 而不是 COUNT(*) 以确保计数正确，因为 connections refers to bond_id。"
    },
    {
      "question_id": 240,
      "difficulty": "challenging",
      "db_id": "toxicology",
      "question": "List all the elements of the toxicology of the molecule \"TR004\".",
      "evidence": "TR004 is the molecule id;",
      "generated_sql": "SELECT element FROM atom WHERE molecule_id = 'TR004'",
      "ground_truth_sql": "SELECT DISTINCT\n  `T`.`element`\nFROM `atom` AS `T`\nWHERE\n  `T`.`molecule_id` = 'TR004'",
      "error_category": "SQL逻辑错误",
      "error_reason": "生成的SQL缺少DISTINCT关键字，导致如果表中有多个相同元素的原子记录，会返回重复的元素值。用户问题要求'List all the elements'，在化学上下文中，一个分子可能有多个原子，但元素类型可能重复（例如，水分子H2O有两个氢原子和一个氧原子），因此意图是列出所有不同的元素类型，而不是所有原子记录。预期的SQL使用了DISTINCT来确保返回唯一的元素列表，而生成的SQL没有处理重复数据，逻辑上返回多余行，不符合问题意图。",
      "suggested_fix": "在SELECT语句中添加DISTINCT关键字，以去重元素值。例如，修改为：SELECT DISTINCT element FROM atom WHERE molecule_id = 'TR004'。此外，可以考虑使用别名和反引号（如预期的SQL所示）以提高可读性和符合特定SQL规范，但这不是核心错误。"
    },
    {
      "question_id": 244,
      "difficulty": "moderate",
      "db_id": "toxicology",
      "question": "Is the molecule with the most double bonds carcinogenic?",
      "evidence": "double bond refers to bond_type = ' = '; label = '+' mean molecules are carcinogenic",
      "generated_sql": "SELECT m.label FROM molecule m WHERE m.molecule_id = (SELECT b.molecule_id FROM bond b WHERE b.bond_type = ' = ' GROUP BY b.molecule_id ORDER BY COUNT(*) DESC LIMIT 1)",
      "ground_truth_sql": "SELECT\n  `T1`.`label`\nFROM `molecule` AS `T1`\nINNER JOIN (\n  SELECT\n    `T`.`molecule_id`,\n    COUNT(`T`.`bond_type`)\n  FROM `bond` AS `T`\n  WHERE\n    `T`.`bond_type` = '='\n  GROUP BY\n    `T`.`molecule_id`\n  ORDER BY\n    COUNT(`T`.`bond_type`) DESC\n  LIMIT 1\n) AS `T2`\n  ON `T1`.`molecule_id` = `T2`.`molecule_id`",
      "error_category": "SQL逻辑错误",
      "error_reason": "生成的SQL中，bond_type条件使用了' = '（有空格），但根据证据，bond_type = ' = '，而预期的SQL使用'='（无空格）。这可能导致查询条件与数据库中的实际值不匹配，从而无法找到任何双键，使子查询返回空，主查询不返回任何结果。此外，生成的SQL使用COUNT(*)计数，而预期的SQL使用COUNT(T.bond_type)，虽然在过滤后可能相同，但COUNT(column)更精确地计数非空值，避免潜在错误。",
      "suggested_fix": "1. 验证数据库中bond_type列的实际值，确保条件字符串与数据库值一致（例如，使用'='而不是' = '）。2. 将COUNT(*)改为COUNT(bond_type)以更准确地计数双键数量。3. 考虑使用INNER JOIN（如预期的SQL）以提高可读性和性能，但这不是必须的。4. 如果存在多个分子具有相同的最大双键数，问题可能需要处理平局，但当前使用LIMIT 1是合理的，因为问题询问'the molecule'。"
    },
    {
      "question_id": 245,
      "difficulty": "moderate",
      "db_id": "toxicology",
      "question": "What is the average number of bonds the atoms with the element iodine have?",
      "evidence": "atoms with the element iodine refers to element = 'i'; average = DIVIDE(COUND(bond_id), COUNT(atom_id)) where element = 'i'",
      "generated_sql": "SELECT AVG(bond_count) AS avg_bonds_per_iodine_atom FROM (SELECT a.atom_id, COUNT(c.bond_id) AS bond_count FROM atom a LEFT JOIN connected c ON a.atom_id = c.atom_id OR a.atom_id = c.atom_id2 WHERE a.element = 'i' GROUP BY a.atom_id) AS sub",
      "ground_truth_sql": "SELECT\n  CAST(COUNT(`T2`.`bond_id`) AS DOUBLE) / COUNT(`T1`.`atom_id`)\nFROM `atom` AS `T1`\nINNER JOIN `connected` AS `T2`\n  ON `T1`.`atom_id` = `T2`.`atom_id`\nWHERE\n  `T1`.`element` = 'i'",
      "error_category": "SQL逻辑错误",
      "error_reason": "生成的SQL与预期的SQL在连接条件和聚合逻辑上存在差异。生成的SQL使用LEFT JOIN和OR条件（a.atom_id = c.atom_id OR a.atom_id = c.atom_id2）来覆盖碘原子可能出现在connected表的atom_id或atom_id2列中的情况，并通过子查询计算每个原子的键数后取平均值。而预期的SQL使用INNER JOIN和单一条件（ON T1.atom_id = T2.atom_id），直接计算总键数除以总原子数。这些差异导致：1) 生成的SQL可能包括没有键的碘原子（通过LEFT JOIN），而预期的SQL只包括有连接的原子（通过INNER JOIN）；2) 生成的SQL的连接条件可能更全面，但预期的SQL基于特定数据库假设（如只使用atom_id列）。根据证据，平均值的计算应为COUNT(bond_id) / COUNT(atom_id)，但预期的SQL的INNER JOIN可能未考虑所有碘原子，而生成的SQL的逻辑与预期不符，因此属于SQL逻辑错误。",
      "suggested_fix": "改进生成的SQL以匹配预期的SQL结构，但根据实际数据库模式调整连接条件。如果数据库中connected表确实有atom_id和atom_id2列，且碘原子可能出现在任一列中，建议使用UNION或OR条件来确保所有连接都被包括。例如，可以修改为：SELECT CAST(COUNT(c.bond_id) AS DOUBLE) / COUNT(a.atom_id) FROM atom a LEFT JOIN connected c ON a.atom_id = c.atom_id OR a.atom_id = c.atom_id2 WHERE a.element = 'i'。但为了精确匹配预期的SQL（假设数据库设计只使用atom_id列），应使用INNER JOIN和单一条件：SELECT CAST(COUNT(c.bond_id) AS DOUBLE) / COUNT(a.atom_id) FROM atom a INNER JOIN connected c ON a.atom_id = c.atom_id WHERE a.element = 'i'。建议先验证数据库模式以确定正确的连接条件。"
    },
    {
      "question_id": 247,
      "difficulty": "challenging",
      "db_id": "toxicology",
      "question": "List all the elements of atoms that can not bond with any other atoms.",
      "evidence": " atoms cannot bond with other atoms means atom_id NOT in connected table;",
      "generated_sql": "SELECT element FROM atom WHERE NOT EXISTS (SELECT 1 FROM connected WHERE connected.atom_id = atom.atom_id OR connected.atom_id2 = atom.atom_id)",
      "ground_truth_sql": "SELECT DISTINCT\n  `T`.`element`\nFROM `atom` AS `T`\nWHERE\n  NOT `T`.`element` IN (\n    SELECT DISTINCT\n      `T1`.`element`\n    FROM `atom` AS `T1`\n    INNER JOIN `connected` AS `T2`\n      ON `T1`.`atom_id` = `T2`.`atom_id`\n  )",
      "error_category": "SQL 逻辑错误",
      "error_reason": "生成的 SQL 在子查询中使用了 OR 条件检查 connected.atom_id 和 connected.atom_id2，但根据证据 'atom_id NOT in connected table' 和预期的 SQL（仅使用 atom_id 列），connected 表可能只有 atom_id 列，没有 atom_id2 列。这导致查询逻辑错误：如果 atom_id2 列不存在，查询可能失败；即使存在，也可能错误地包括或排除原子。此外，生成的 SQL 未使用 DISTINCT，可能返回重复元素，而预期的 SQL 使用 DISTINCT 返回唯一元素，符合 'List all the elements' 的常见意图（通常要求唯一元素）。",
      "suggested_fix": "修改生成的 SQL 以匹配预期的 SQL 结构，确保逻辑正确：使用 NOT IN 子查询基于 element 级别检查，并添加 DISTINCT。例如：\n```sql\nSELECT DISTINCT\n  `T`.`element`\nFROM `atom` AS `T`\nWHERE\n  NOT `T`.`element` IN (\n    SELECT DISTINCT\n      `T1`.`element`\n    FROM `atom` AS `T1`\n    INNER JOIN `connected` AS `T2`\n      ON `T1`.`atom_id` = `T2`.`atom_id`\n  )\n```\n或者，如果意图是原子级别检查（更符合证据），可使用：\n```sql\nSELECT DISTINCT element FROM atom WHERE atom_id NOT IN (SELECT atom_id FROM connected)\n```\n但需根据数据库模式确认 connected 表的结构。"
    },
    {
      "question_id": 263,
      "difficulty": "challenging",
      "db_id": "toxicology",
      "question": "What is the composition of element chlorine in percentage among the single bond molecules?",
      "evidence": "element chlorine refers to element = 'cl'; single bond refers to bond_type = '-'; percentage = DIVIDE(SUM(element = 'cl'), COUNT(atom_id)) as percent where bond_type = '-'",
      "generated_sql": "SELECT CAST(SUM(CASE WHEN a.element = 'cl' THEN 1 ELSE 0 END) AS DOUBLE) / CAST(COUNT(a.atom_id) AS DOUBLE) * 100 AS percentage FROM atom a INNER JOIN ( SELECT atom_id FROM connected WHERE bond_id IN (SELECT bond_id FROM bond WHERE bond_type = '-') UNION ALL SELECT atom_id2 FROM connected WHERE bond_id IN (SELECT bond_id FROM bond WHERE bond_type = '-') ) AS single_bond_atoms ON a.atom_id = single_bond_atoms.atom_id",
      "ground_truth_sql": "SELECT\n  CAST(COUNT(CASE WHEN `T`.`element` = 'cl' THEN `T`.`atom_id` ELSE NULL END) AS DOUBLE) * 100 / COUNT(`T`.`atom_id`)\nFROM (\n  SELECT\n    `T1`.`atom_id`,\n    `T1`.`element`\n  FROM `atom` AS `T1`\n  INNER JOIN `molecule` AS `T2`\n    ON `T1`.`molecule_id` = `T2`.`molecule_id`\n  INNER JOIN `bond` AS `T3`\n    ON `T2`.`molecule_id` = `T3`.`molecule_id`\n  WHERE\n    `T3`.`bond_type` = '-'\n) AS `T`",
      "error_category": "语义理解错误",
      "error_reason": "生成的 SQL 误解了问题意图。用户问题要求计算在单键分子中氯元素的组成百分比，但生成的 SQL 使用 connected 表来获取原子，而 connected 表仅表示原子之间的连接关系，未直接关联到分子层面。这可能导致获取的原子集合不准确：可能包括不属于单键分子的原子，或遗漏部分原子。此外，子查询中使用 UNION ALL 合并 atom_id 和 atom_id2，可能引入重复的原子 ID，导致 COUNT(a.atom_id) 计数错误，影响百分比计算。预期的 SQL 正确地通过连接 atom、molecule 和 bond 表来获取所有在单键分子中的原子，确保语义与问题一致。",
      "suggested_fix": "改进 SQL 以正确获取单键分子中的原子并计算百分比。建议采用类似预期的 SQL 结构：从 atom 表连接 molecule 和 bond 表，其中 bond_type = '-'，然后计算氯原子的百分比。具体实现如下：\nSELECT\n  CAST(COUNT(CASE WHEN a.element = 'cl' THEN a.atom_id ELSE NULL END) AS DOUBLE) * 100 / COUNT(a.atom_id)\nFROM atom a\nINNER JOIN molecule m ON a.molecule_id = m.molecule_id\nINNER JOIN bond b ON m.molecule_id = b.molecule_id\nWHERE b.bond_type = '-';\n这样确保只考虑单键分子中的原子，避免重复计数，并正确计算百分比。"
    },
    {
      "question_id": 273,
      "difficulty": "moderate",
      "db_id": "toxicology",
      "question": "What is the percentage of element chlorine in carcinogenic molecules?",
      "evidence": "chlorine refers to element = 'cl'; label = '+' mean molecules are carcinogenic; percentage = DIVIDE(SUM(element = 'pb'); COUNT(molecule_id)) as percentage where label = '+'",
      "generated_sql": "SELECT CAST(SUM(atom.element = 'cl') AS DOUBLE) / COUNT(DISTINCT molecule.molecule_id) AS percentage FROM molecule INNER JOIN atom ON molecule.molecule_id = atom.molecule_id WHERE molecule.label = '+'",
      "ground_truth_sql": "SELECT\n  CAST(COUNT(CASE WHEN `T1`.`element` = 'cl' THEN `T1`.`element` ELSE NULL END) AS DOUBLE) * 100 / COUNT(`T1`.`element`)\nFROM `atom` AS `T1`\nINNER JOIN `molecule` AS `T2`\n  ON `T1`.`molecule_id` = `T2`.`molecule_id`\nWHERE\n  `T2`.`label` = '+'",
      "error_category": "SQL逻辑错误",
      "error_reason": "生成的SQL计算了错误的指标。问题要求计算致癌分子中氯元素的百分比，即氯原子数量占所有原子数量的比例。但生成的SQL使用了SUM(atom.element = 'cl') / COUNT(DISTINCT molecule.molecule_id)，这计算的是每个致癌分子中氯原子的平均数量，而不是百分比。预期的SQL正确计算了氯原子数量占所有原子数量的百分比，通过COUNT(CASE WHEN element = 'cl' THEN element ELSE NULL END) * 100 / COUNT(element)。此外，生成的SQL中分母使用了COUNT(DISTINCT molecule.molecule_id)（分子数量），而正确分母应为原子数量（COUNT(element)或COUNT(*)），这反映了对问题意图的逻辑误解。",
      "suggested_fix": "修正SQL逻辑以正确计算百分比。建议使用以下方式：\n1. 计算氯原子数量：使用SUM(CASE WHEN atom.element = 'cl' THEN 1 ELSE 0 END)或COUNT(CASE WHEN atom.element = 'cl' THEN atom.element ELSE NULL END)。\n2. 计算总原子数量：使用COUNT(atom.element)或COUNT(*)，但需确保在JOIN后原子表的行数代表所有原子。\n3. 将氯原子数量除以总原子数量，并乘以100得到百分比。\n示例修正SQL：\nSELECT CAST(COUNT(CASE WHEN atom.element = 'cl' THEN atom.element ELSE NULL END) AS DOUBLE) * 100 / COUNT(atom.element) AS percentage FROM molecule INNER JOIN atom ON molecule.molecule_id = atom.molecule_id WHERE molecule.label = '+'"
    },
    {
      "question_id": 281,
      "difficulty": "challenging",
      "db_id": "toxicology",
      "question": "Tally the toxicology element of the 4th atom of each molecule that was carcinogenic.",
      "evidence": "label = '+' means molecules are carcinogenic; 4th atom of each molecule refers to substr(atom_id, 7, 1) = '4'; ",
      "generated_sql": "SELECT atom.element FROM atom INNER JOIN molecule ON atom.molecule_id = molecule.molecule_id WHERE molecule.label = '+' AND SUBSTRING(atom.atom_id, 7, 1) = '4'",
      "ground_truth_sql": "SELECT DISTINCT\n  `T1`.`element`\nFROM `atom` AS `T1`\nINNER JOIN `molecule` AS `T2`\n  ON `T1`.`molecule_id` = `T2`.`molecule_id`\nWHERE\n  `T2`.`label` = '+'\n  AND SUBSTR(`T1`.`atom_id`, -1) = '4'\n  AND LENGTH(`T1`.`atom_id`) = 7",
      "error_category": "SQL 逻辑错误",
      "error_reason": "生成的SQL使用 SUBSTRING(atom.atom_id, 7, 1) = '4' 来识别第4个原子，但未验证 atom_id 的长度。根据证据，atom_id 的格式应为7个字符，第7个字符表示原子序号。缺少长度检查可能导致错误地包括非第4个原子（例如，atom_id 长度不为7时，第7个字符可能不是原子序号）。此外，生成的SQL未使用 DISTINCT，而预期SQL使用了，这可能影响返回结果的唯一性，因为用户问题中的 'Tally' 可能暗示列出唯一元素。",
      "suggested_fix": "在WHERE子句中添加条件 `LENGTH(atom.atom_id) = 7` 以确保 atom_id 格式正确。同时，使用 `DISTINCT` 来避免重复元素。建议将条件修改为 `SUBSTR(atom.atom_id, -1) = '4' AND LENGTH(atom.atom_id) = 7`，如预期SQL所示，并在SELECT子句中添加 DISTINCT。"
    },
    {
      "question_id": 327,
      "difficulty": "moderate",
      "db_id": "toxicology",
      "question": "Which non-carcinogenic molecules consisted more than 5 atoms?",
      "evidence": "label = '-' means molecules are non-carcinogenic; molecules consisted more than 5 atoms refers to COUNT(molecule_id) > 5",
      "generated_sql": "SELECT molecule.molecule_id FROM molecule INNER JOIN atom ON molecule.molecule_id = atom.molecule_id WHERE molecule.label = '-' GROUP BY molecule.molecule_id HAVING COUNT(atom.atom_id) > 5",
      "ground_truth_sql": "SELECT\n  `T`.`molecule_id`\nFROM (\n  SELECT\n    `T1`.`molecule_id`,\n    COUNT(`T2`.`atom_id`)\n  FROM `molecule` AS `T1`\n  INNER JOIN `atom` AS `T2`\n    ON `T1`.`molecule_id` = `T2`.`molecule_id`\n  WHERE\n    `T1`.`label` = '-'\n  GROUP BY\n    `T1`.`molecule_id`\n  HAVING\n    COUNT(`T2`.`atom_id`) > 5\n) AS `t`",
      "error_category": "其他",
      "error_reason": "生成的 SQL 在逻辑上正确，能够返回非致癌且原子数超过5的分子。它正确使用了 label = '-' 过滤非致癌分子，通过 INNER JOIN 连接 molecule 和 atom 表，并使用 HAVING COUNT(atom.atom_id) > 5 来确保原子数量超过5个。预期的 SQL 使用了子查询结构，但生成的 SQL 直接查询，两者功能等价。可能生成的 SQL 缺少子查询，但这不是错误，而是风格差异。证据中 'molecules consisted more than 5 atoms refers to COUNT(molecule_id) > 5' 可能是个笔误，因为原子数量应使用 COUNT(atom_id)，而生成的 SQL 正确使用了 COUNT(atom_id)。",
      "suggested_fix": "生成的 SQL 已经正确，无需修改。如果必须匹配预期的 SQL 结构，可以添加子查询，但当前版本更简洁且高效。建议验证数据库 schema 以确保 atom 表有 atom_id 列，并确认 label 字段的值确实为 '-' 表示非致癌。"
    },
    {
      "question_id": 17,
      "difficulty": "simple",
      "db_id": "california_schools",
      "question": "Rank schools by their average score in Writing where the score is greater than 499, showing their charter numbers.",
      "evidence": "Valid charter number means the number is not null",
      "generated_sql": "SELECT s.sname, s.AvgScrWrite, sch.CharterNum FROM satscores s INNER JOIN schools sch ON s.cds = sch.CDSCode WHERE s.AvgScrWrite > 499 AND sch.CharterNum IS NOT NULL ORDER BY s.AvgScrWrite DESC",
      "ground_truth_sql": "SELECT\n  `CharterNum`,\n  `AvgScrWrite`,\n  RANK() OVER (ORDER BY `AvgScrWrite` DESC) AS `WritingScoreRank`\nFROM `schools` AS `T1`\nINNER JOIN `satscores` AS `T2`\n  ON `T1`.`CDSCode` = `T2`.`cds`\nWHERE\n  `T2`.`AvgScrWrite` > 499 AND NOT `CharterNum` IS NULL",
      "error_category": "SQL逻辑错误",
      "error_reason": "用户问题要求对学校按写作平均分（大于499）进行排名，并显示特许编号。生成的SQL虽然正确连接了satscores和schools表，并应用了过滤条件（AvgScrWrite > 499 和 CharterNum IS NOT NULL），但没有实现排名功能。它仅使用ORDER BY对平均分进行排序，而未使用窗口函数如RANK()或ROW_NUMBER()来生成排名。此外，生成的SQL选择了学校名称（s.sname），而预期的输出可能不需要此列，但主要错误在于缺少排名逻辑，导致结果不符合问题要求。",
      "suggested_fix": "修改SQL以包含排名函数。使用RANK() OVER (ORDER BY AvgScrWrite DESC) AS WritingScoreRank来生成排名，并调整SELECT子句以返回CharterNum、AvgScrWrite和排名列。确保连接条件正确（ON T1.CDSCode = T2.cds），并应用相同的过滤条件（T2.AvgScrWrite > 499 AND NOT CharterNum IS NULL）。示例修正：SELECT CharterNum, AvgScrWrite, RANK() OVER (ORDER BY AvgScrWrite DESC) AS WritingScoreRank FROM schools AS T1 INNER JOIN satscores AS T2 ON T1.CDSCode = T2.cds WHERE T2.AvgScrWrite > 499 AND NOT CharterNum IS NULL;"
    },
    {
      "question_id": 23,
      "difficulty": "moderate",
      "db_id": "california_schools",
      "question": "List the names of schools with more than 30 difference in enrollements between K-12 and ages 5-17? Please also give the full street adress of the schools.",
      "evidence": "Diffrence in enrollement = `Enrollment (K-12)` - `Enrollment (Ages 5-17)`",
      "generated_sql": "SELECT frpm.`School Name` AS school_name, CONCAT(schools.Street, ', ', schools.City, ', ', schools.Zip) AS full_address FROM frpm INNER JOIN schools ON frpm.CDSCode = schools.CDSCode WHERE (frpm.`Enrollment (K-12)` - frpm.`Enrollment (Ages 5-17)`) > 30;",
      "ground_truth_sql": "SELECT\n  `T1`.`School`,\n  `T1`.`Street`\nFROM `schools` AS `T1`\nINNER JOIN `frpm` AS `T2`\n  ON `T1`.`CDSCode` = `T2`.`CDSCode`\nWHERE\n  `T2`.`Enrollment (K-12)` - `T2`.`Enrollment (Ages 5-17)` > 30",
      "error_category": "语义理解错误",
      "error_reason": "生成的SQL误解了用户问题的意图。用户要求列出学校名称和完整的街道地址，但生成的SQL从frpm表获取学校名称（`frpm.`School Name``），而预期的SQL表明学校名称应来自schools表（`T1`.`School``）。此外，生成的SQL使用CONCAT拼接了完整的地址（Street, City, Zip），而预期的SQL只返回Street列，这可能导致返回格式与预期不一致。虽然生成的SQL在差值计算和JOIN条件上正确，但列选择和表来源错误，反映了对数据库结构和用户意图的理解偏差。",
      "suggested_fix": "修改SQL以从schools表获取学校名称和地址信息，并确保列选择与预期一致。例如：SELECT schools.`School Name`, schools.Street FROM schools INNER JOIN frpm ON schools.CDSCode = frpm.CDSCode WHERE (frpm.`Enrollment (K-12)` - frpm.`Enrollment (Ages 5-17)`) > 30; 如果需要完整的街道地址，可以进一步拼接City和Zip等列，但需根据数据库结构确认。同时，建议使用表别名（如T1、T2）提高可读性。"
    },
    {
      "question_id": 25,
      "difficulty": "moderate",
      "db_id": "california_schools",
      "question": "Name schools in Riverside which the average of average math score for SAT is grater than 400, what is the funding type of these schools?",
      "evidence": "Average of average math = sum(average math scores) / count(schools).",
      "generated_sql": "SELECT s.sname, sc.FundingType FROM satscores s INNER JOIN schools sc ON s.cds = sc.CDSCode WHERE s.cname = 'Riverside' AND (SELECT AVG(AvgScrMath) FROM satscores WHERE cname = 'Riverside') > 400",
      "ground_truth_sql": "SELECT\n  `T1`.`sname`,\n  `T2`.`Charter Funding Type`\nFROM `satscores` AS `T1`\nINNER JOIN `frpm` AS `T2`\n  ON `T1`.`cds` = `T2`.`CDSCode`\nWHERE\n  `T2`.`District Name` LIKE 'Riverside%'\nGROUP BY\n  `T1`.`sname`,\n  `T2`.`Charter Funding Type`\nHAVING\n  CAST(SUM(`T1`.`AvgScrMath`) AS DOUBLE) / COUNT(`T1`.`cds`) > 400",
      "error_category": "SQL逻辑错误",
      "error_reason": "生成的SQL在多个方面存在逻辑错误：1) 表连接错误：使用了`satscores`和`schools`表，但预期的SQL连接`satscores`和`frpm`表，因为资助类型（Funding Type）存储在`frpm`表中，而非`schools`表。2) 过滤条件错误：使用`s.cname = 'Riverside'`过滤，但预期的SQL使用`T2`.`District Name` LIKE 'Riverside%'，因为Riverside是地区名，存储在`frpm`表的`District Name`列中。3) 查询逻辑错误：生成的SQL使用子查询计算所有Riverside记录的平均数学分数的全局平均值，并在WHERE条件中检查该全局平均值是否大于400，这会导致如果全局平均值大于400，则所有Riverside学校都被返回，无论其个人平均分如何。然而，问题意图是列出平均数学分数大于400的学校（即每个学校的平均值），而非基于全局平均值。预期的SQL使用GROUP BY和HAVING子句，按学校分组计算每个学校的平均数学分数（SUM/ COUNT），并过滤平均值大于400的学校，这更符合问题意图。此外，生成的SQL中子查询在WHERE条件中可能导致性能问题，但主要问题是逻辑不符合预期。",
      "suggested_fix": "1) 修正表连接：使用`satscores`和`frpm`表，连接条件为`satscores.cds = frpm.CDSCode`。2) 修正过滤条件：使用`frpm.District Name LIKE 'Riverside%'`来过滤Riverside地区的学校。3) 重构查询逻辑：使用GROUP BY按学校和资助类型分组，然后在HAVING子句中计算每个组的平均数学分数（CAST(SUM(AvgScrMath) AS DOUBLE) / COUNT(cds)），并检查是否大于400。4) 确保列名匹配数据库模式，例如使用`Charter Funding Type`作为资助类型列。改进后的SQL应类似于预期的SQL。"
    },
    {
      "question_id": 26,
      "difficulty": "moderate",
      "db_id": "california_schools",
      "question": "State the names and full communication address of high schools in Monterey which has more than 800 free or reduced price meals for ages 15-17?",
      "evidence": "Full communication address should include Street, City, State and zip code if any.",
      "generated_sql": "SELECT schools.School AS SchoolName, schools.Street, schools.City, schools.State, schools.Zip FROM schools INNER JOIN frpm ON schools.CDSCode = frpm.CDSCode WHERE frpm.`County Name` = 'Monterey' AND frpm.`School Type` LIKE '%High%' AND frpm.`FRPM Count (Ages 5-17)` > 800",
      "ground_truth_sql": "SELECT\n  `T1`.`School Name`,\n  `T2`.`Street`,\n  `T2`.`City`,\n  `T2`.`State`,\n  `T2`.`Zip`\nFROM `frpm` AS `T1`\nINNER JOIN `schools` AS `T2`\n  ON `T1`.`CDSCode` = `T2`.`CDSCode`\nWHERE\n  `T2`.`County` = 'Monterey'\n  AND `T1`.`Free Meal Count (Ages 5-17)` > 800\n  AND `T1`.`School Type` = 'High Schools (Public)'",
      "error_category": "语义理解错误",
      "error_reason": "生成的 SQL 在多个方面对用户问题的意图理解有偏差：1. County 位置错误：用户问题 'high schools in Monterey' 暗示 County 信息应关联到学校，但生成的 SQL 使用 frpm 表的 'County Name' 字段，而预期的 SQL 使用 schools 表的 'County' 字段，这可能导致返回错误的学校或遗漏结果。2. 学校类型匹配不精确：生成的 SQL 使用 `LIKE '%High%'` 进行模糊匹配，而预期的 SQL 使用精确匹配 'High Schools (Public)'，这可能导致返回非高中的学校或遗漏符合条件的学校。3. 年龄范围理解偏差：用户问题指定 'ages 15-17'，但生成的 SQL 使用 'FRPM Count (Ages 5-17)'，年龄范围更宽（5-17），这可能不符合用户意图，因为数据库可能没有直接对应 'ages 15-17' 的列，但 SQL 应优先使用用户指定的范围或确认列名。4. 列名不匹配：生成的 SQL 使用 'FRPM Count (Ages 5-17)'，而预期的 SQL 使用 'Free Meal Count (Ages 5-17)'，这可能反映数据库中的实际列名差异，但未正确处理。",
      "suggested_fix": "1. 确认数据库 schema：检查 County 字段是否在 schools 表中，并相应调整 WHERE 条件。2. 使用精确的学校类型匹配：根据数据库中的实际值（如 'High Schools (Public)'）替换 `LIKE '%High%'`。3. 处理年龄范围：如果数据库中没有 'ages 15-17' 的列，需确认是否应使用 'Ages 5-17' 列或通过其他方式计算；若用户意图是特定范围，可能需要调整查询逻辑。4. 使用表别名：如预期的 SQL 所示，使用别名（如 T1、T2）提高可读性和避免歧义。5. 确保列名与数据库一致：验证 'FRPM Count' 和 'Free Meal Count' 的实际列名，并使用正确的列名。"
    },
    {
      "question_id": 27,
      "difficulty": "moderate",
      "db_id": "california_schools",
      "question": "What is the average score in writing for the schools that were opened after 1991 or closed before 2000? List the school names along with the score. Also, list the communication number of the schools if there is any.",
      "evidence": "Communication number refers to phone number.",
      "generated_sql": "SELECT schools.School, satscores.AvgScrWrite, schools.Phone FROM satscores INNER JOIN schools ON satscores.cds = schools.CDSCode WHERE schools.OpenDate > '1991-12-31' OR schools.ClosedDate < '2000-01-01'",
      "ground_truth_sql": "SELECT\n  `T2`.`School`,\n  `T1`.`AvgScrWrite`,\n  `T2`.`Phone`\nFROM `schools` AS `T2`\nLEFT JOIN `satscores` AS `T1`\n  ON `T2`.`CDSCode` = `T1`.`cds`\nWHERE\n  DATE_FORMAT(CAST(`T2`.`OpenDate` AS DATETIME), '%Y') > '1991'\n  OR DATE_FORMAT(CAST(`T2`.`ClosedDate` AS DATETIME), '%Y') < '2000'",
      "error_category": "日期格式错误",
      "error_reason": "生成的SQL在日期条件处理上存在错误。用户问题要求筛选'opened after 1991'和'closed before 2000'，这通常意味着基于年份进行比较。生成的SQL使用具体日期字符串（如'1991-12-31'和'2000-01-01'）直接与日期字段比较，但数据库中的日期字段（如OpenDate和ClosedDate）可能存储为整型、字符串或非标准格式，直接比较可能导致不准确或错误的结果。预期的SQL使用DATE_FORMAT函数提取年份后进行比较，更符合问题语义。此外，生成的SQL使用INNER JOIN，而预期的SQL使用LEFT JOIN，这可能导致遗漏没有分数记录的学校，但主要错误在于日期处理方式。",
      "suggested_fix": "1. 修改日期条件：使用年份比较代替具体日期比较。例如，使用YEAR()函数或DATE_FORMAT()函数提取年份，如WHERE YEAR(schools.OpenDate) > 1991 OR YEAR(schools.ClosedDate) < 2000。如果日期字段是整型，可能需要先转换为日期类型。\n2. 调整JOIN类型：根据用户问题意图，如果需要列出所有符合条件的学校（即使没有分数记录），应使用LEFT JOIN代替INNER JOIN，以确保学校信息完整。\n3. 确保日期字段处理兼容性：检查数据库中日期字段的实际格式，并使用适当的函数进行转换和比较。"
    },
    {
      "question_id": 28,
      "difficulty": "challenging",
      "db_id": "california_schools",
      "question": "Consider the average difference between K-12 enrollment and 15-17 enrollment of schools that are locally funded, list the names and DOC type of schools which has a difference above this average.",
      "evidence": "Difference between K-12 enrollment and 15-17 enrollment can be computed by `Enrollment (K-12)` - `Enrollment (Ages 5-17)`",
      "generated_sql": "SELECT s.School, s.DOCType FROM frpm f INNER JOIN schools s ON f.CDSCode = s.CDSCode WHERE s.Charter = 0 AND (f.`Enrollment (K-12)` - f.`Enrollment (Ages 5-17)`) > ( SELECT AVG(diff) FROM ( SELECT f2.`Enrollment (K-12)` - f2.`Enrollment (Ages 5-17)` AS diff FROM frpm f2 INNER JOIN schools s2 ON f2.CDSCode = s2.CDSCode WHERE s2.Charter = 0 ) AS t )",
      "ground_truth_sql": "SELECT\n  `T2`.`School`,\n  `T2`.`DOC`\nFROM `frpm` AS `T1`\nINNER JOIN `schools` AS `T2`\n  ON `T1`.`CDSCode` = `T2`.`CDSCode`\nWHERE\n  `T2`.`FundingType` = 'Locally funded'\n  AND (\n    `T1`.`Enrollment (K-12)` - `T1`.`Enrollment (Ages 5-17)`\n  ) > (\n    SELECT\n      AVG(`T3`.`Enrollment (K-12)` - `T3`.`Enrollment (Ages 5-17)`)\n    FROM `frpm` AS `T3`\n    INNER JOIN `schools` AS `T4`\n      ON `T3`.`CDSCode` = `T4`.`CDSCode`\n    WHERE\n      `T4`.`FundingType` = 'Locally funded'\n  )",
      "error_category": "语义理解错误",
      "error_reason": "生成的 SQL 错误地使用了 `s.Charter = 0` 来过滤学校，而用户问题明确要求 'locally funded'，这应该对应 `FundingType` 列。子查询中也使用了相同的错误条件，导致过滤逻辑完全偏离问题意图。此外，列名 `DOCType` 可能与预期的 `DOC` 不匹配，但核心错误是条件理解偏差，即混淆了 'locally funded' 与 'Charter' 状态。",
      "suggested_fix": "将 WHERE 子句中的 `s.Charter = 0` 替换为 `s.FundingType = 'Locally funded'`，并在子查询中同样替换 `s2.Charter = 0` 为 `s2.FundingType = 'Locally funded'`。同时，检查数据库 schema：如果列名是 `DOC`，则将 `s.DOCType` 改为 `s.DOC`；如果列名是 `DOCType`，则保持，但需确保与预期一致。建议在生成 SQL 时更准确地映射用户问题中的关键词到数据库列名。"
    },
    {
      "question_id": 31,
      "difficulty": "moderate",
      "db_id": "california_schools",
      "question": "What is the eligible free rate of the 10th and 11th schools with the highest enrolment for students in grades 1 through 12?",
      "evidence": "K-12 refers to students in grades 1 through 12; Eligible free rate for K-12 = `Free Meal Count (K-12)` / `Enrollment (K-12)`",
      "generated_sql": "SELECT `School Name`, CAST(`Free Meal Count (K-12)` AS DOUBLE) / CAST(`Enrollment (K-12)` AS DOUBLE) AS eligible_free_rate FROM frpm ORDER BY `Enrollment (K-12)` DESC, CDSCode ASC LIMIT 9, 2;",
      "ground_truth_sql": "SELECT\n  CAST(`Free Meal Count (K-12)` AS DOUBLE) / `Enrollment (K-12)`\nFROM `frpm`\nORDER BY\n  `Enrollment (K-12)` DESC\nLIMIT 2\nOFFSET 9",
      "error_category": "语义理解错误",
      "error_reason": "生成的SQL误解了用户问题意图：1. 用户问题只要求返回eligible free rate（即`Free Meal Count (K-12)` / `Enrollment (K-12)`），但生成的SQL额外选择了'School Name'列，导致返回格式不符合预期；2. 问题只要求按入学人数降序排列，但生成的SQL添加了额外的排序键'CDSCode ASC'，这可能改变排序逻辑，而问题未指定如何处理入学人数相同的情况；3. 生成的SQL使用`LIMIT 9, 2`（MySQL语法），虽然可执行，但预期的SQL使用标准SQL语法`LIMIT 2 OFFSET 9`，这可能影响跨数据库兼容性，但核心错误在于语义理解偏差。",
      "suggested_fix": "修正SQL以准确反映问题意图：1. SELECT子句只包含计算出的eligible free rate，移除'School Name'列；2. ORDER BY子句简化为仅按`Enrollment (K-12)` DESC排序；3. 使用标准SQL分页语法`LIMIT 2 OFFSET 9`来获取第10和第11行。改进后的SQL应类似于预期的SQL：`SELECT CAST(`Free Meal Count (K-12)` AS DOUBLE) / `Enrollment (K-12)` FROM `frpm` ORDER BY `Enrollment (K-12)` DESC LIMIT 2 OFFSET 9`。"
    },
    {
      "question_id": 32,
      "difficulty": "moderate",
      "db_id": "california_schools",
      "question": "What is the eligible free or reduced price meal rate for the top 5 schools in grades 1-12 with the highest free or reduced price meal count of the schools with the ownership code 66?",
      "evidence": "grades 1-12 means K-12; Eligible free or reduced price meal rate for K-12 = `FRPM Count (K-12)` / `Enrollment (K-12)`",
      "generated_sql": "SELECT schools.`School Name` AS school_name, CAST(frpm.`FRPM Count (K-12)` AS DOUBLE) / CAST(frpm.`Enrollment (K-12)` AS DOUBLE) AS eligible_free_or_reduced_price_meal_rate FROM schools INNER JOIN frpm ON schools.CDSCode = frpm.CDSCode WHERE schools.SOC = '66' ORDER BY frpm.`FRPM Count (K-12)` DESC LIMIT 5",
      "ground_truth_sql": "SELECT\n  CAST(`T1`.`FRPM Count (K-12)` AS DOUBLE) / `T1`.`Enrollment (K-12)`\nFROM `frpm` AS `T1`\nINNER JOIN `schools` AS `T2`\n  ON `T1`.`CDSCode` = `T2`.`CDSCode`\nWHERE\n  `T2`.`SOC` = 66\nORDER BY\n  `T1`.`FRPM Count (K-12)` DESC\nLIMIT 5",
      "error_category": "返回格式错误",
      "error_reason": "生成的SQL返回了两列：'school_name'和计算列，而预期的SQL只返回计算列。用户问题询问的是'eligible free or reduced price meal rate'，可能只要求率值，但生成的SQL额外返回了学校名称，导致返回行数或列数不符合预期。此外，条件值使用了字符串'66'（schools.SOC = '66'），而预期的SQL使用了数字66（T2.SOC = 66），如果SOC字段是数值类型，这可能导致条件不匹配，影响查询结果。",
      "suggested_fix": "修改生成的SQL，只返回计算列，不返回'school_name'。将条件值改为数字66，而不是字符串'66'。例如：SELECT CAST(frpm.`FRPM Count (K-12)` AS DOUBLE) / CAST(frpm.`Enrollment (K-12)` AS DOUBLE) AS eligible_free_or_reduced_price_meal_rate FROM schools INNER JOIN frpm ON schools.CDSCode = frpm.CDSCode WHERE schools.SOC = 66 ORDER BY frpm.`FRPM Count (K-12)` DESC LIMIT 5。可以考虑使用别名以提高可读性，但这不是必须的。"
    },
    {
      "question_id": 37,
      "difficulty": "moderate",
      "db_id": "california_schools",
      "question": "What is the complete address of the school with the lowest excellence rate? Indicate the Street, City, Zip and State.",
      "evidence": "Execellence Rate = NumGE1500 / NumTstTakr; complete address has Street, City, State, Zip code",
      "generated_sql": "SELECT s.Street, s.City, s.Zip, s.State FROM satscores sc INNER JOIN schools s ON sc.cds = s.CDSCode WHERE sc.NumTstTakr > 0 ORDER BY (sc.NumGE1500 / sc.NumTstTakr) ASC LIMIT 1",
      "ground_truth_sql": "SELECT\n  `T2`.`Street`,\n  `T2`.`City`,\n  `T2`.`State`,\n  `T2`.`Zip`\nFROM `satscores` AS `T1`\nINNER JOIN `schools` AS `T2`\n  ON `T1`.`cds` = `T2`.`CDSCode`\nORDER BY\n  CAST(`T1`.`NumGE1500` AS DOUBLE) / `T1`.`NumTstTakr` ASC\nLIMIT 1",
      "error_category": "数值计算错误",
      "error_reason": "生成的 SQL 在计算 excellence rate（NumGE1500 / NumTstTakr）时使用了整数除法，而预期的 SQL 通过 CAST 将 NumGE1500 转换为 DOUBLE 进行浮点除法。在 SQL 中，如果 NumGE1500 和 NumTstTakr 是整数类型，整数除法会向下取整（例如，1500/2000 结果为 0），导致计算出的 excellence rate 不准确，从而可能错误地排序并找到错误的最低 excellence rate 的学校。此外，生成的 SQL 的列顺序（Street, City, Zip, State）与预期的 SQL（Street, City, State, Zip）不一致，这可能影响输出格式，但核心错误是数值计算问题。",
      "suggested_fix": "在除法运算中确保使用浮点除法，例如将分子或分母转换为浮点类型，如 `CAST(sc.NumGE1500 AS DOUBLE) / sc.NumTstTakr` 或 `sc.NumGE1500 * 1.0 / sc.NumTstTakr`。同时，调整 SELECT 子句的列顺序以匹配预期的 SQL 或用户问题指定的顺序（Street, City, State, Zip）。WHERE 子句 `sc.NumTstTakr > 0` 是合理的，因为它避免了除以零错误，可以保留。"
    },
    {
      "question_id": 36,
      "difficulty": "challenging",
      "db_id": "california_schools",
      "question": "Under whose administration is the school with the highest number of students scoring 1500 or more on the SAT? Indicate their full names.",
      "evidence": "full name means first name, last name; There are at most 3 administrators for each school; SAT Scores are greater or equal to 1500 refers to NumGE1500",
      "generated_sql": "SELECT CONCAT_WS(', ', CASE WHEN s.AdmFName1 IS NOT NULL AND s.AdmLName1 IS NOT NULL THEN CONCAT(s.AdmFName1, ' ', s.AdmLName1) ELSE NULL END, CASE WHEN s.AdmFName2 IS NOT NULL AND s.AdmLName2 IS NOT NULL THEN CONCAT(s.AdmFName2, ' ', s.AdmLName2) ELSE NULL END, CASE WHEN s.AdmFName3 IS NOT NULL AND s.AdmLName3 IS NOT NULL THEN CONCAT(s.AdmFName3, ' ', s.AdmLName3) ELSE NULL END) AS admin_full_names FROM schools s INNER JOIN satscores sat ON s.CDSCode = sat.cds ORDER BY sat.NumGE1500 DESC LIMIT 1",
      "ground_truth_sql": "SELECT\n  `T2`.`AdmFName1`,\n  `T2`.`AdmLName1`,\n  `T2`.`AdmFName2`,\n  `T2`.`AdmLName2`,\n  `T2`.`AdmFName3`,\n  `T2`.`AdmLName3`\nFROM `satscores` AS `T1`\nINNER JOIN `schools` AS `T2`\n  ON `T1`.`cds` = `T2`.`CDSCode`\nORDER BY\n  `T1`.`NumGE1500` DESC\nLIMIT 1",
      "error_category": "返回格式错误",
      "error_reason": "生成的SQL使用CONCAT_WS和CASE语句将管理员的全名合并为一个字符串列（admin_full_names），但预期的SQL直接返回所有管理员的姓和名字段（AdmFName1, AdmLName1, AdmFName2, AdmLName2, AdmFName3, AdmLName3）作为单独的列。用户问题要求'Indicate their full names'，证据指出'full name means first name, last name'，但预期的输出格式是返回每个可能的管理员字段，而不是合并成一个字符串。生成的SQL虽然逻辑正确（按NumGE1500排序并取最高），但输出列数和格式与预期不符，导致结果无法直接匹配预期格式。",
      "suggested_fix": "修改SELECT语句，直接选择所有管理员的姓和名字段，而不是使用CONCAT_WS合并。具体修改为：将SELECT部分替换为`SELECT s.AdmFName1, s.AdmLName1, s.AdmFName2, s.AdmLName2, s.AdmFName3, s.AdmLName3`，并保持其他部分（JOIN条件和ORDER BY）不变。这样可以确保输出格式与预期SQL一致，返回多个列，每个列包含管理员的姓名部分。"
    },
    {
      "question_id": 40,
      "difficulty": "moderate",
      "db_id": "california_schools",
      "question": "What is the telephone number for the school with the lowest average score in reading in Fresno Unified?",
      "evidence": "Fresno Unified is a name of district;",
      "generated_sql": "SELECT s.Phone FROM satscores sat INNER JOIN schools s ON sat.cds = s.CDSCode WHERE s.District = 'Fresno Unified' ORDER BY sat.AvgScrRead ASC LIMIT 1;",
      "ground_truth_sql": "SELECT\n  `T2`.`Phone`\nFROM `satscores` AS `T1`\nINNER JOIN `schools` AS `T2`\n  ON `T1`.`cds` = `T2`.`CDSCode`\nWHERE\n  `T2`.`District` = 'Fresno Unified' AND NOT `T1`.`AvgScrRead` IS NULL\nORDER BY\n  `T1`.`AvgScrRead` ASC\nLIMIT 1",
      "error_category": "语义理解错误",
      "error_reason": "生成的SQL没有正确理解问题意图，忽略了AvgScrRead可能为NULL的情况。问题要求'最低的平均分'，隐含了平均分应该是非NULL的有效数值。生成的SQL直接按AvgScrRead排序并取第一条，但如果存在AvgScrRead为NULL的记录，它可能被排序到最前面（取决于数据库的NULL排序行为），从而返回一个没有有效分数的学校，这不符合问题要求。预期的SQL通过添加AND NOT `T1`.`AvgScrRead` IS NULL条件确保了只考虑非NULL的分数，正确捕捉了问题意图。",
      "suggested_fix": "在生成的SQL的WHERE子句中添加条件：AND sat.AvgScrRead IS NOT NULL。修改后的SQL为：SELECT s.Phone FROM satscores sat INNER JOIN schools s ON sat.cds = s.CDSCode WHERE s.District = 'Fresno Unified' AND sat.AvgScrRead IS NOT NULL ORDER BY sat.AvgScrRead ASC LIMIT 1;"
    },
    {
      "question_id": 41,
      "difficulty": "simple",
      "db_id": "california_schools",
      "question": "List the names of virtual schools that are among the top 5 in their respective counties based on average reading scores.",
      "evidence": "Exclusively virtual refers to Virtual = 'F'; respective counties means PARTITION BY County",
      "generated_sql": "SELECT School FROM ( SELECT s.School, s.County, sat.AvgScrRead, ROW_NUMBER() OVER (PARTITION BY s.County ORDER BY sat.AvgScrRead DESC) as rank FROM schools s INNER JOIN satscores sat ON s.CDSCode = sat.cds WHERE s.Virtual = 'F' ) ranked_schools WHERE rank <= 5 ORDER BY County, rank",
      "ground_truth_sql": "SELECT  School FROM ( SELECT T2.School, T1.AvgScrRead, RANK() OVER (PARTITION BY T2.County ORDER BY T1.AvgScrRead DESC) AS rnk FROM satscores AS T1 INNER JOIN schools AS T2 ON T1.cds = T2.CDSCode WHERE T2.Virtual = 'F') ranked_schools WHERE rnk <= 5",
      "error_category": "语义理解错误",
      "error_reason": "生成的SQL使用ROW_NUMBER()来分配排名，这会强制为每个分区内的行分配唯一连续排名，即使有并列的平均阅读分数。这可能导致一些并列的学校被错误地排除在'top 5'之外，因为ROW_NUMBER()会为并列的行分配不同的排名。预期的SQL使用RANK()，它为并列的行分配相同排名，然后跳过后续排名，这更符合'top 5'的常见意图，即包括所有并列的学校。此外，生成的SQL在子查询中选择了不必要的County列，但主要错误是排名函数的选择，反映了对问题意图的理解偏差。",
      "suggested_fix": "将ROW_NUMBER()改为RANK()，以正确处理并列情况。移除子查询中不必要的County列选择，只保留School和AvgScrRead。考虑移除外层查询的ORDER BY子句，除非问题明确要求排序，以简化查询并提高效率。"
    },
    {
      "question_id": 46,
      "difficulty": "simple",
      "db_id": "california_schools",
      "question": "Which state special schools have the highest number of enrollees from grades 1 through 12?",
      "evidence": "State Special Schools refers to DOC = 31; Grades 1 through 12 means K-12",
      "generated_sql": "SELECT s.School, MAX(f.`Enrollment (K-12)`) AS max_enrollment FROM schools s INNER JOIN frpm f ON s.CDSCode = f.CDSCode WHERE s.DOC = '31' GROUP BY s.CDSCode, s.School ORDER BY max_enrollment DESC",
      "ground_truth_sql": "SELECT\n  `T2`.`School`\nFROM `frpm` AS `T1`\nINNER JOIN `schools` AS `T2`\n  ON `T1`.`CDSCode` = `T2`.`CDSCode`\nWHERE\n  `T2`.`DOC` = 31\nORDER BY\n  `T1`.`Enrollment (K-12)` DESC\nLIMIT 1",
      "error_category": "语义理解错误",
      "error_reason": "生成的SQL误解了问题意图。问题要求找出入学人数最高的州立特殊学校（即所有州立特殊学校中入学人数最高的学校），但生成的SQL使用了MAX聚合函数和GROUP BY子句，计算了每个学校的最高入学人数（如果数据中有多个记录），而不是直接比较所有学校的入学人数。这导致查询逻辑错误：生成的SQL会返回所有州立特殊学校及其最大入学人数，而问题期望的是入学人数最高的学校（可能一个或多个）。此外，生成的SQL没有使用LIMIT限制结果，返回了所有学校，不符合问题中'the highest'的语义。",
      "suggested_fix": "移除GROUP BY和MAX聚合，直接按`Enrollment (K-12)`降序排序，并使用LIMIT 1来获取入学人数最高的学校。同时，确保DOC条件使用正确的数据类型（整数31而不是字符串'31'），以匹配数据库模式。如果数据有多个记录（例如不同年份），可能需要先聚合每个学校的入学人数（如取最新年份或总和），但根据问题上下文，直接排序即可。改进后的SQL应类似于预期的SQL：SELECT `T2`.`School` FROM `frpm` AS `T1` INNER JOIN `schools` AS `T2` ON `T1`.`CDSCode` = `T2`.`CDSCode` WHERE `T2`.`DOC` = 31 ORDER BY `T1`.`Enrollment (K-12)` DESC LIMIT 1。"
    },
    {
      "question_id": 47,
      "difficulty": "moderate",
      "db_id": "california_schools",
      "question": "What is the monthly average number of schools that opened in Alameda County under the jurisdiction of the Elementary School District in 1980?",
      "evidence": "Elementary School District refers to DOC = 52; Monthly average number of schools that opened in 1980 = count(schools that opened in 1980) / 12",
      "generated_sql": "SELECT CAST(COUNT(*) AS DOUBLE) / 12 AS monthly_average FROM schools WHERE County = 'Alameda County' AND DOC = '52' AND YEAR(OpenDate) = 1980",
      "ground_truth_sql": "SELECT\n  CAST(COUNT(`School`) AS DOUBLE) / 12\nFROM `schools`\nWHERE\n  `DOC` = 52\n  AND `County` = 'Alameda'\n  AND DATE_FORMAT(CAST(`OpenDate` AS DATETIME), '%Y') = '1980'",
      "error_category": "日期格式错误",
      "error_reason": "生成的 SQL 假设 `OpenDate` 是日期类型并直接使用 `YEAR(OpenDate)` 函数，但预期的 SQL 使用 `DATE_FORMAT(CAST(OpenDate AS DATETIME), '%Y')`，这表明 `OpenDate` 可能存储为字符串或其他非日期类型，需要转换才能正确提取年份。因此，生成的 SQL 可能无法正确过滤 1980 年的记录。此外，County 条件使用 'Alameda County' 而预期的 SQL 使用 'Alameda'，这可能因为数据库中的 County 字段只存储 'Alameda'，导致查询结果不匹配；DOC 条件使用字符串 '52' 而预期的 SQL 使用整数 52，可能存在类型不一致问题。",
      "suggested_fix": "1. 修正日期处理：使用 `DATE_FORMAT(CAST(OpenDate AS DATETIME), '%Y') = '1980'` 或类似方法，确保正确提取年份。2. 修正 County 条件：使用 'Alameda' 而不是 'Alameda County'，以匹配数据库中的实际值。3. 修正 DOC 条件：使用整数 52 而不是字符串 '52'，以保持类型一致。4. 考虑使用 `COUNT(School)` 而不是 `COUNT(*)`，以确保只计数学校记录，但需根据表结构确认。"
    },
    {
      "question_id": 48,
      "difficulty": "moderate",
      "db_id": "california_schools",
      "question": "What is the ratio of merged Unified School District schools in Orange County to merged Elementary School District schools?",
      "evidence": "Elementary School District refers to DOC = 52; Unified School District refers to DOC = 54.",
      "generated_sql": "SELECT CAST((SELECT COUNT(*) FROM schools WHERE County = 'Orange County' AND DOC = '54') AS DOUBLE) / CAST((SELECT COUNT(*) FROM schools WHERE DOC = '52') AS DOUBLE) AS ratio",
      "ground_truth_sql": "SELECT\n  CAST(SUM(CASE WHEN `DOC` = 54 THEN 1 ELSE 0 END) AS DOUBLE) / SUM(CASE WHEN `DOC` = 52 THEN 1 ELSE 0 END)\nFROM `schools`\nWHERE\n  `StatusType` = 'Merged' AND `County` = 'Orange'",
      "error_category": "语义理解错误",
      "error_reason": "生成的 SQL 未能正确理解用户问题的意图。用户问题明确要求计算 'merged'（合并）学校的比例，但生成的 SQL 中完全遗漏了 `StatusType` = 'Merged' 条件。此外，对于 Elementary School District schools（DOC = 52），生成的 SQL 在分母子查询中没有限制 County 为 Orange County，导致分母计算了所有 DOC = 52 的学校，而不仅仅是 Orange County 的学校，这违反了问题中 'in Orange County' 的限定。最后，County 字段的值可能不匹配：生成的 SQL 使用 'Orange County'，但预期的 SQL 使用 'Orange'，这可能基于数据库实际存储值，但生成的 SQL 未正确处理。",
      "suggested_fix": "修改 SQL 以包含所有必要条件：1) 在 WHERE 子句中添加 `StatusType` = 'Merged' 条件；2) 确保两个计数都限制在 Orange County 中，即对于 DOC = 52 的学校也添加 `County` = 'Orange' 条件；3) 根据数据库 schema 确认 County 字段的值（如 'Orange' 或 'Orange County'），并使用正确的值。建议使用 CASE 语句或子查询，但确保逻辑一致。例如，参考预期的 SQL 结构，使用 SUM(CASE WHEN ...) 来计算比率，并在 WHERE 子句中统一过滤条件。"
    },
    {
      "question_id": 62,
      "difficulty": "challenging",
      "db_id": "california_schools",
      "question": "What is the total number of non-chartered schools in the county of Los Angeles with a percent (%) of eligible free meals for grades 1 through 12 that is less than 0.18%?",
      "evidence": "non-chartered schools refer to schools whose Charter = 0; K-12 means grades 1 through 12; percent of eligible free rate for K-12 = `Free Meal Count (K-12)` * 100 / `Enrollment (K-12)`",
      "generated_sql": "SELECT COUNT(*) FROM frpm WHERE `County Name` = 'Los Angeles' AND `Charter School (Y/N)` = 0 AND `Percent (%) Eligible Free (K-12)` < 0.18",
      "ground_truth_sql": "SELECT\n  COUNT(`T2`.`School`)\nFROM `frpm` AS `T1`\nINNER JOIN `schools` AS `T2`\n  ON `T1`.`CDSCode` = `T2`.`CDSCode`\nWHERE\n  `T2`.`County` = 'Los Angeles'\n  AND `T2`.`Charter` = 0\n  AND CAST(`T1`.`Free Meal Count (K-12)` AS DOUBLE) * 100 / `T1`.`Enrollment (K-12)` < 0.18",
      "error_category": "SQL 逻辑错误",
      "error_reason": "生成的 SQL 在逻辑上存在多个错误：1) 缺少必要的表连接（JOIN），问题涉及学校信息和免费餐食数据，但生成的 SQL 仅使用了 `frpm` 表，而预期的 SQL 需要连接 `schools` 表以获取学校信息；2) 百分比计算错误，用户问题要求计算百分比（基于证据中的公式：`Free Meal Count (K-12)` * 100 / `Enrollment (K-12)`），但生成的 SQL 直接使用 `Percent (%) Eligible Free (K-12)` 列，该列可能不存在或需要计算；3) 列名不匹配，生成的 SQL 使用 `County Name` 和 `Charter School (Y/N)`，但数据库中实际列名可能是 `County` 和 `Charter`，这可能导致执行错误或错误结果；4) 计数逻辑可能不准确，生成的 SQL 使用 `COUNT(*)` 计数行数，而预期的 SQL 使用 `COUNT(T2.School)` 计数学校，以避免重复计数。",
      "suggested_fix": "1) 使用 INNER JOIN 连接 `frpm` 和 `schools` 表，基于 `CDSCode` 字段；2) 使用正确的列名：`County` 和 `Charter` 而不是 `County Name` 和 `Charter School (Y/N)`；3) 计算百分比：使用 `CAST(T1.Free Meal Count (K-12) AS DOUBLE) * 100 / T1.Enrollment (K-12)` 来计算百分比，并与 0.18 比较；4) 确保计数学校：使用 `COUNT(T2.School)` 或类似方式，以准确计数非特许学校数量。"
    },
    {
      "question_id": 77,
      "difficulty": "moderate",
      "db_id": "california_schools",
      "question": "Which schools served a grade span of Kindergarten to 9th grade in the county of Los Angeles and what is its Percent (%) Eligible FRPM (Ages 5-17)?",
      "evidence": "Percent (%) Eligible FRPM (Ages 5-17) can be acquired by `FRPM Count (Ages 5-17)` / `Enrollment (Ages 5-17)` * 100",
      "generated_sql": "SELECT `School Name`, CASE WHEN CAST(`Enrollment (Ages 5-17)` AS DOUBLE) = 0 THEN NULL ELSE CAST(`FRPM Count (Ages 5-17)` AS DOUBLE) / CAST(`Enrollment (Ages 5-17)` AS DOUBLE) * 100 END AS `Percent (%) Eligible FRPM (Ages 5-17)` FROM `frpm` WHERE `County Name` = 'Los Angeles' AND `Low Grade` = 'K' AND `High Grade` = '9'",
      "ground_truth_sql": "SELECT\n  `T2`.`School`,\n  `T1`.`FRPM Count (Ages 5-17)` * 100 / `T1`.`Enrollment (Ages 5-17)`\nFROM `frpm` AS `T1`\nINNER JOIN `schools` AS `T2`\n  ON `T1`.`CDSCode` = `T2`.`CDSCode`\nWHERE\n  `T2`.`County` = 'Los Angeles' AND `T2`.`GSserved` = 'K-9'",
      "error_category": "SQL 逻辑错误",
      "error_reason": "生成的 SQL 存在逻辑错误，主要问题在于未正确连接必要的表。它假设所有信息（如学校名称、县、年级范围）都存储在 `frpm` 表中，但实际数据库模式要求连接 `frpm` 和 `schools` 表（通过 `CDSCode`）。因此，查询可能无法正确匹配学校或返回错误数据。此外，grade span 的条件使用 `Low Grade = 'K' AND High Grade = '9'`，而预期使用 `GSserved = 'K-9'`，这可能导致不匹配（例如，年级范围可能以其他格式存储）。列名如 `County Name` 可能与预期的 `County` 不一致，进一步导致条件失效。",
      "suggested_fix": "修改 SQL 以连接 `frpm` 和 `schools` 表，使用 `CDSCode` 作为连接键。从 `schools` 表中选择学校名称和县信息，从 `frpm` 表中选择财务数据。调整条件以使用 `County = 'Los Angeles'` 和 `GSserved = 'K-9'`。计算百分比时，可以保留除零处理，但根据预期 SQL，可能不需要。示例：\n```sql\nSELECT\n  `T2`.`School`,\n  `T1`.`FRPM Count (Ages 5-17)` * 100 / `T1`.`Enrollment (Ages 5-17)`\nFROM `frpm` AS `T1`\nINNER JOIN `schools` AS `T2`\n  ON `T1`.`CDSCode` = `T2`.`CDSCode`\nWHERE\n  `T2`.`County` = 'Los Angeles' AND `T2`.`GSserved` = 'K-9'\n```\n如果需要处理除零，可以添加 CASE 语句，但预期 SQL 未包含，可能假设数据有效。"
    },
    {
      "question_id": 79,
      "difficulty": "moderate",
      "db_id": "california_schools",
      "question": "Between San Diego and Santa Barbara, which county offers the most number of schools that does not offer physical building? Indicate the amount.",
      "evidence": "'Does not offer physical building' means Virtual = F in the database.",
      "generated_sql": "SELECT County, COUNT(*) AS school_count FROM schools WHERE County IN ('San Diego', 'Santa Barbara') AND Virtual = 'F' GROUP BY County ORDER BY school_count DESC LIMIT 1",
      "ground_truth_sql": "SELECT\n  `County`,\n  COUNT(`Virtual`)\nFROM `schools`\nWHERE\n  (\n    `County` = 'San Diego' OR `County` = 'Santa Barbara'\n  ) AND `Virtual` = 'F'\nGROUP BY\n  `County`\nORDER BY\n  COUNT(`Virtual`) DESC\nLIMIT 1",
      "error_category": "其他",
      "error_reason": "生成的 SQL 在功能上与预期的 SQL 等价，但存在语法结构上的差异，这些差异不影响查询结果的正确性。具体包括：1. 使用 IN 子句代替 OR 条件，两者逻辑等价；2. 使用 COUNT(*) 代替 COUNT(Virtual)，由于 WHERE 条件确保 Virtual = 'F'，计数结果相同；3. 缺少 WHERE 子句中的括号，但 SQL 优先级规则下 AND 高于 OR，因此逻辑正确。这些差异可能源于编码风格或优化偏好，而非语义或逻辑错误。",
      "suggested_fix": "为与预期的 SQL 保持一致并提高可读性，建议调整生成的 SQL：使用 OR 条件代替 IN 子句，使用 COUNT(Virtual) 代替 COUNT(*)，并在 WHERE 子句中添加括号。例如：SELECT County, COUNT(Virtual) FROM schools WHERE (County = 'San Diego' OR County = 'Santa Barbara') AND Virtual = 'F' GROUP BY County ORDER BY COUNT(Virtual) DESC LIMIT 1"
    },
    {
      "question_id": 82,
      "difficulty": "simple",
      "db_id": "california_schools",
      "question": "What is the grade span offered in the school with the highest longitude?",
      "evidence": "the highest longitude refers to the school with the maximum absolute longitude value.",
      "generated_sql": "SELECT GSoffered FROM schools WHERE ABS(Longitude) = (SELECT MAX(ABS(Longitude)) FROM schools)",
      "ground_truth_sql": "SELECT\n  `GSoffered`\nFROM `schools`\nORDER BY\n  ABS(`longitude`) DESC\nLIMIT 1",
      "error_category": "SQL逻辑错误",
      "error_reason": "生成的SQL使用WHERE子句匹配最大绝对经度，可能返回多个学校（如果多个学校具有相同的最大绝对经度），而用户问题'What is the grade span offered in the school with the highest longitude?' 使用单数'the school'，暗示期望单个结果。预期的SQL使用ORDER BY和LIMIT 1确保只返回一行，更符合问题意图。生成的SQL逻辑上正确识别了最大绝对经度，但未处理可能的多行输出，导致结果可能不符合预期格式。",
      "suggested_fix": "改用ORDER BY ABS(Longitude) DESC LIMIT 1来获取具有最高绝对经度的学校，确保返回单行结果。例如：SELECT GSoffered FROM schools ORDER BY ABS(Longitude) DESC LIMIT 1。"
    },
    {
      "question_id": 83,
      "difficulty": "challenging",
      "db_id": "california_schools",
      "question": "Of the schools that offers a magnet program serving a grade span of Kindergarten to 8th grade, how many offers Multiple Provision Types? List the number of cities that offers a Kindergarten to 8th grade span and indicate how many schools are there serving such grade span for each city.",
      "evidence": "Kindergarten to 8th grade refers to K-8; 'Offers a magnet program' means Magnet = 1; Multiple Provision Types refers to `NSLP Provision Status` = 'Multiple Provision Types'",
      "generated_sql": "SELECT 'Multiple Provision Types Count' AS result_type, COUNT(*) AS value FROM schools s INNER JOIN frpm f ON s.CDSCode = f.CDSCode WHERE s.Magnet = 1 AND s.GSserved = 'K-8' AND f.`NSLP Provision Status` = 'Multiple Provision Types' UNION ALL SELECT City AS result_type, COUNT(*) AS value FROM schools WHERE Magnet = 1 AND GSserved = 'K-8' GROUP BY City",
      "ground_truth_sql": "SELECT\n  `T2`.`City`,\n  COUNT(`T2`.`CDSCode`)\nFROM `frpm` AS `T1`\nINNER JOIN `schools` AS `T2`\n  ON `T1`.`CDSCode` = `T2`.`CDSCode`\nWHERE\n  `T2`.`Magnet` = 1\n  AND `T2`.`GSoffered` = 'K-8'\n  AND `T1`.`NSLP Provision Status` = 'Multiple Provision Types'\nGROUP BY\n  `T2`.`City`",
      "error_category": "SQL逻辑错误",
      "error_reason": "生成的SQL试图通过UNION ALL合并两个查询来回答问题的两部分（总数和按城市分组），但预期的SQL只包含一个按城市分组的查询。生成的SQL在第二个查询中错误地包括了Magnet=1条件（问题第二部分未指定磁体项目），且未包括NSLP Provision Status条件（而预期的SQL包括了）。此外，字段名GSserved与预期的GSoffered可能不匹配，导致逻辑偏差。生成的SQL的第二部分条件不正确，未能准确反映问题意图：问题第二部分仅要求基于K-8年级服务的学校按城市分组，而生成的SQL额外限制了磁体项目。",
      "suggested_fix": "根据预期的SQL，应只执行一个查询：SELECT City, COUNT(CDSCode) FROM frpm INNER JOIN schools ON frpm.CDSCode = schools.CDSCode WHERE schools.Magnet = 1 AND schools.GSoffered = 'K-8' AND frpm.`NSLP Provision Status` = 'Multiple Provision Types' GROUP BY City。如果问题确实需要两部分（总数和按城市分组），则可能需要两个独立的查询：1) 计算提供磁体项目、K-8年级和多种供应类型的学校总数；2) 列出提供K-8年级服务的每个城市的学校数量（不包含磁体项目或多种供应类型条件）。但鉴于预期的SQL只提供了一个查询，建议遵循其结构，并确保字段名（如GSoffered）与数据库模式一致。"
    },
    {
      "question_id": 85,
      "difficulty": "moderate",
      "db_id": "california_schools",
      "question": "What is the Percent (%) Eligible Free (K-12) in the school administered by an administrator whose first name is Alusine. List the district code of the school.",
      "evidence": "Percent (%) Eligible Free (K-12) = `Free Meal Count (K-12)` / `Enrollment (K-12)` * 100%",
      "generated_sql": "SELECT frpm.`Percent (%) Eligible Free (K-12)`, frpm.`District Code` FROM schools INNER JOIN frpm ON schools.CDSCode = frpm.CDSCode WHERE schools.AdmFName1 = 'Alusine';",
      "ground_truth_sql": "SELECT\n  `T1`.`Free Meal Count (K-12)` * 100 / `T1`.`Enrollment (K-12)`,\n  `T1`.`District Code`\nFROM `frpm` AS `T1`\nINNER JOIN `schools` AS `T2`\n  ON `T1`.`CDSCode` = `T2`.`CDSCode`\nWHERE\n  `T2`.`AdmFName1` = 'Alusine'",
      "error_category": "语义理解错误",
      "error_reason": "系统误解了问题意图。用户问题要求计算'Percent (%) Eligible Free (K-12)'，但生成的SQL直接选择了frpm表中的该列，而根据证据，该百分比需要通过'Free Meal Count (K-12)'和'Enrollment (K-12)'计算得出（公式：Free Meal Count (K-12) / Enrollment (K-12) * 100%）。生成的SQL假设该列已预计算并存储，但证据表明这是一个计算字段，因此生成的SQL没有执行必要的计算，可能导致错误或不准确的结果。此外，生成的SQL在JOIN和WHERE条件上正确，但核心错误在于选择了错误的列。",
      "suggested_fix": "修改SQL以包含计算步骤：使用'Free Meal Count (K-12)'除以'Enrollment (K-12)'再乘以100来计算百分比。参考预期的SQL，确保正确引用列和表别名。例如：SELECT `T1`.`Free Meal Count (K-12)` * 100 / `T1`.`Enrollment (K-12)`, `T1`.`District Code` FROM `frpm` AS `T1` INNER JOIN `schools` AS `T2` ON `T1`.`CDSCode` = `T2`.`CDSCode` WHERE `T2`.`AdmFName1` = 'Alusine'。同时，在生成SQL时，应仔细分析问题中的计算要求，并参考数据库模式或证据来确认列的存在性。"
    },
    {
      "question_id": 87,
      "difficulty": "challenging",
      "db_id": "california_schools",
      "question": "What are the valid e-mail addresses of the administrator of the school located in the San Bernardino county, City of San Bernardino City Unified that opened between 1/1/2009 to 12/31/2010 whose school types are public Intermediate/Middle Schools and Unified Schools?",
      "evidence": "Intermediate/Middle Schools refers to SOC = 62; Unified School refers to DOC = 54; years between 2009 and 2010 can refer to 'between 1/1/2009 to 12/31/2010'",
      "generated_sql": "SELECT GROUP_CONCAT(Email SEPARATOR ', ') AS ValidEmails FROM (SELECT AdmEmail1 AS Email FROM schools WHERE County = 'San Bernardino' AND District = 'San Bernardino City Unified' AND OpenDate BETWEEN '2009-01-01' AND '2010-12-31' AND SOC = 62 AND DOC = 54 AND AdmEmail1 IS NOT NULL AND AdmEmail1 != '' UNION ALL SELECT AdmEmail2 FROM schools WHERE County = 'San Bernardino' AND District = 'San Bernardino City Unified' AND OpenDate BETWEEN '2009-01-01' AND '2010-12-31' AND SOC = 62 AND DOC = 54 AND AdmEmail2 IS NOT NULL AND AdmEmail2 != '' UNION ALL SELECT AdmEmail3 FROM schools WHERE County = 'San Bernardino' AND District = 'San Bernardino City Unified' AND OpenDate BETWEEN '2009-01-01' AND '2010-12-31' AND SOC = 62 AND DOC = 54 AND AdmEmail3 IS NOT NULL AND AdmEmail3 != '') AS Emails",
      "ground_truth_sql": "SELECT\n  `T2`.`AdmEmail1`,\n  `T2`.`AdmEmail2`\nFROM `frpm` AS `T1`\nINNER JOIN `schools` AS `T2`\n  ON `T1`.`CDSCode` = `T2`.`CDSCode`\nWHERE\n  `T2`.`County` = 'San Bernardino'\n  AND `T2`.`City` = 'San Bernardino'\n  AND `T2`.`DOC` = 54\n  AND DATE_FORMAT(CAST(`T2`.`OpenDate` AS DATETIME), '%Y') BETWEEN '2009' AND '2010'\n  AND `T2`.`SOC` = 62",
      "error_category": "语义理解错误",
      "error_reason": "生成的SQL对用户问题中的'City of San Bernardino City Unified'理解有误，错误地将其解释为区（District）条件，而预期的SQL正确使用了城市（City）条件。此外，生成的SQL存在其他逻辑错误：1) 未使用JOIN with `frpm`表，可能遗漏了必要的关联条件；2) 日期处理使用了具体范围（'2009-01-01'到'2010-12-31'），而预期的SQL使用了年份范围（'2009'到'2010'），这可能与OpenDate字段的实际格式不匹配；3) 返回格式使用了GROUP_CONCAT合并所有电子邮件，而预期的SQL返回两个独立的列（AdmEmail1和AdmEmail2），不符合预期结果。",
      "suggested_fix": "1. 修正城市条件：将`District = 'San Bernardino City Unified'`改为`City = 'San Bernardino'`。2. 添加INNER JOIN with `frpm`表 on `CDSCode`，以确保查询完整的学校信息。3. 调整日期条件：使用`DATE_FORMAT(CAST(OpenDate AS DATETIME), '%Y') BETWEEN '2009' AND '2010'`来处理年份范围。4. 修改SELECT子句：移除UNION ALL和GROUP_CONCAT，直接选择`AdmEmail1`和`AdmEmail2`列，如预期的SQL所示。"
    },
    {
      "question_id": 94,
      "difficulty": "challenging",
      "db_id": "financial",
      "question": "List out the account numbers of female clients who are oldest and has lowest average salary, calculate the gap between this lowest average salary with the highest average salary?",
      "evidence": "Female means gender = 'F'; A11 refers to average salary; Gap = highest average salary - lowest average salary; If the person A's birthdate > B's birthdate, it means that person B is order than person A.",
      "generated_sql": "SELECT a.account_id, (SELECT MAX(A11) FROM district) - (SELECT MIN(d2.A11) FROM client c2 JOIN district d2 ON c2.district_id = d2.district_id WHERE c2.gender = 'F' AND c2.birth_date = (SELECT MIN(birth_date) FROM client WHERE gender = 'F')) as gap FROM account a JOIN disp ON a.account_id = disp.account_id JOIN client c ON disp.client_id = c.client_id JOIN district d ON c.district_id = d.district_id WHERE c.gender = 'F' AND c.birth_date = (SELECT MIN(birth_date) FROM client WHERE gender = 'F') AND d.A11 = (SELECT MIN(d2.A11) FROM client c2 JOIN district d2 ON c2.district_id = d2.district_id WHERE c2.gender = 'F' AND c2.birth_date = (SELECT MIN(birth_date) FROM client WHERE gender = 'F'))",
      "ground_truth_sql": "SELECT\n  `T1`.`account_id`,\n  (\n    SELECT\n      MAX(`A11`) - MIN(`A11`)\n    FROM `district`\n  )\nFROM `account` AS `T1`\nINNER JOIN `district` AS `T2`\n  ON `T1`.`district_id` = `T2`.`district_id`\nINNER JOIN `disp` AS `T3`\n  ON `T1`.`account_id` = `T3`.`account_id`\nINNER JOIN `client` AS `T4`\n  ON `T3`.`client_id` = `T4`.`client_id`\nWHERE\n  `T2`.`district_id` = (\n    SELECT\n      `district_id`\n    FROM `client`\n    WHERE\n      `gender` = 'F'\n    ORDER BY\n      `birth_date` ASC\n    LIMIT 1\n  )\nORDER BY\n  `T2`.`A11` DESC\nLIMIT 1",
      "error_category": "语义理解错误",
      "error_reason": "生成的 SQL 误解了用户问题的意图。用户问题要求列出女性客户中年龄最大且平均工资最低的账户号，并计算该最低平均工资与最高平均工资之间的差距。但生成的 SQL 试图通过复杂的子查询强制同时满足年龄最大和平均工资最低的条件，逻辑上不正确。具体错误包括：1) 将'oldest'（年龄最大）错误地与'lowest average salary'（平均工资最低）结合在同一个查询中，但未正确处理两者可能不一致的情况；2) gap 计算中，使用 MAX(A11) from district 减去 MIN(d2.A11) from a subquery that finds the average salary of the oldest female client's district，这没有正确反映'lowest average salary'（应为所有女性客户中平均工资最低的值）；3) 条件过于严格，可能导致返回零行或错误结果，例如要求 d.A11 等于一个子查询的最小值，而该子查询仅针对年龄最大的女性客户，逻辑混乱。",
      "suggested_fix": "重新分析用户问题，明确'oldest'和'lowest average salary'的含义。根据证据，oldest 意味着 birth_date 最小，lowest average salary 可能指的是 district 的平均工资 A11 最低。建议分步处理：1) 先找到年龄最大的女性客户（birth_date 最小），获取其 district_id；2) 计算 gap 为 district 表中 A11 的最大值减去该客户所在 district 的 A11（或所有女性客户中平均工资最低的 district 的 A11）；3) 选择 account_id，条件为该客户是年龄最大的女性客户，且其 district 的平均工资是最低的（如果需要）。可以使用子查询或窗口函数实现。例如：\nSELECT a.account_id, (SELECT MAX(A11) FROM district) - (SELECT A11 FROM district WHERE district_id = (SELECT district_id FROM client WHERE gender='F' ORDER BY birth_date ASC LIMIT 1)) AS gap\nFROM account a\nJOIN disp ON a.account_id = disp.account_id\nJOIN client c ON disp.client_id = c.client_id\nJOIN district d ON c.district_id = d.district_id\nWHERE c.gender = 'F' AND c.birth_date = (SELECT MIN(birth_date) FROM client WHERE gender = 'F')\nORDER BY d.A11 DESC\nLIMIT 1;\n但需根据实际业务逻辑调整，确保 gap 计算正确反映最高与最低平均工资的差距。"
    },
    {
      "question_id": 95,
      "difficulty": "moderate",
      "db_id": "financial",
      "question": "List out the account numbers of clients who are youngest and have highest average salary?",
      "evidence": "If the person A's birthdate < B's birthdate, it means that person B is younger than person A; A11 refers to average salary",
      "generated_sql": "SELECT DISTINCT account.account_id FROM account INNER JOIN disp ON account.account_id = disp.account_id INNER JOIN client ON disp.client_id = client.client_id INNER JOIN district ON account.district_id = district.district_id WHERE client.birth_date = (SELECT MAX(birth_date) FROM client) AND district.A11 = (SELECT MAX(A11) FROM district)",
      "ground_truth_sql": "SELECT\n  `T1`.`account_id`\nFROM `account` AS `T1`\nINNER JOIN `disp` AS `T2`\n  ON `T1`.`account_id` = `T2`.`account_id`\nINNER JOIN `client` AS `T3`\n  ON `T2`.`client_id` = `T3`.`client_id`\nINNER JOIN `district` AS `T4`\n  ON `T4`.`district_id` = `T1`.`district_id`\nWHERE\n  `T2`.`client_id` = (\n    SELECT\n      `client_id`\n    FROM `client`\n    ORDER BY\n      `birth_date` DESC\n    LIMIT 1\n  )\nGROUP BY\n  `T4`.`A11`,\n  `T1`.`account_id`",
      "error_category": "语义理解错误",
      "error_reason": "生成的SQL在理解'youngest'客户时出现语义错误。根据证据，出生日期越小表示越年轻，因此最年轻的客户应有最小的出生日期（MIN(birth_date)），但生成的SQL使用了MAX(birth_date)，这实际上对应最老的客户。此外，生成的SQL将最年轻和最高平均工资两个条件直接AND在一起，要求同一个客户同时满足最老的出生日期和最高的平均工资，这可能不符合用户意图，因为用户可能要求列出那些客户，他们是最年轻的，并且他们所在district的平均工资最高，这需要更复杂的逻辑处理。",
      "suggested_fix": "修正日期条件，使用MIN(birth_date)或ORDER BY birth_date ASC LIMIT 1来找到最年轻的客户。对于平均工资条件，可以使用子查询找到最高A11的district，然后结合最年轻的客户。例如：先通过子查询找到最年轻的客户ID，然后找到这些客户所在district的A11，再选择A11最大的客户账户。或者，参考预期的SQL结构但修正日期顺序（将ORDER BY birth_date DESC改为ASC），并确保平均工资条件被正确应用。"
    },
    {
      "question_id": 99,
      "difficulty": "moderate",
      "db_id": "financial",
      "question": "Among the accounts who have loan validity more than 12 months, list out the accounts that have the highest approved amount and have account opening date in 1993.",
      "evidence": "Loan validity more than 12 months refers to duration > 12",
      "generated_sql": "SELECT a.account_id, MAX(l.amount) as max_approved_amount FROM loan l INNER JOIN account a ON l.account_id = a.account_id WHERE l.duration > 12 AND YEAR(a.date) = 1993 GROUP BY a.account_id HAVING MAX(l.amount) = (SELECT MAX(l2.amount) FROM loan l2 INNER JOIN account a2 ON l2.account_id = a2.account_id WHERE l2.duration > 12 AND YEAR(a2.date) = 1993))",
      "ground_truth_sql": "SELECT\n  `T1`.`account_id`\nFROM `loan` AS `T1`\nINNER JOIN `account` AS `T2`\n  ON `T1`.`account_id` = `T2`.`account_id`\nWHERE\n  DATE_FORMAT(CAST(`T2`.`date` AS DATETIME), '%Y') = '1993'\n  AND `T1`.`duration` > 12\nORDER BY\n  `T1`.`amount` DESC\nLIMIT 1",
      "error_category": "返回格式错误",
      "error_reason": "生成的 SQL 与预期 SQL 在返回格式上存在显著差异：1) 生成的 SQL 返回了额外的列 'max_approved_amount'，而预期的 SQL 仅返回 'account_id' 列，这不符合用户问题中仅列出账户的要求；2) 生成的 SQL 使用 GROUP BY 和 HAVING 子句，可能返回多个账户（如果有并列最高批准金额），而预期的 SQL 通过 ORDER BY 和 LIMIT 1 仅返回一个账户，导致返回行数不匹配；3) 日期处理方式不一致：生成的 SQL 使用 YEAR(a.date) = 1993，而预期的 SQL 使用 DATE_FORMAT(CAST(T2.date AS DATETIME), '%Y') = '1993'，如果 'date' 字段为字符串或整型，YEAR() 函数可能无法正确解析，导致潜在的执行错误。",
      "suggested_fix": "1) 移除 'max_approved_amount' 列，仅选择 'account_id'；2) 采用预期的 SQL 结构，使用 ORDER BY T1.amount DESC LIMIT 1 来直接获取最高批准金额的账户（如果允许多个账户，可移除 LIMIT 1 或使用其他逻辑）；3) 统一日期处理方式，使用 DATE_FORMAT 或类似函数确保兼容性，例如：WHERE DATE_FORMAT(CAST(a.date AS DATETIME), '%Y') = '1993' AND l.duration > 12。"
    },
    {
      "question_id": 118,
      "difficulty": "moderate",
      "db_id": "financial",
      "question": "For loan amount less than USD100,000, what is the percentage of accounts that is still running with no issue.",
      "evidence": "Status = 'C' stands for running contract, ok so far; Percentage of accounts by condition = [(total(amount) & condition) / (total amount)] * 100.",
      "generated_sql": "SELECT CAST((SELECT COUNT(DISTINCT account_id) FROM loan WHERE amount < 100000 AND status = 'C') AS DOUBLE) / CAST((SELECT COUNT(DISTINCT account_id) FROM loan) AS DOUBLE) * 100 AS percentage",
      "ground_truth_sql": "SELECT\n  CAST(SUM(`status` = 'C') AS DOUBLE) * 100 / COUNT(`account_id`)\nFROM `loan`\nWHERE\n  `amount` < 100000",
      "error_category": "语义理解错误",
      "error_reason": "生成的 SQL 误解了用户问题的意图。用户问题要求计算在贷款金额小于 100,000 的账户中，状态为 'C'（运行中且无问题）的百分比。然而，生成的 SQL 在分母中使用了整个 loan 表的所有不同 account_id，而不是仅限于 amount < 100000 的账户。这导致计算的是所有账户中满足条件（amount < 100000 且 status = 'C'）的比例，而不是在指定子集（amount < 100000）中的比例。预期的 SQL 正确地将 WHERE 子句应用于整个查询，确保分子和分母都在同一条件下计算，从而准确反映问题要求。",
      "suggested_fix": "修正 SQL 以确保分母和分子都在相同的条件下计算。建议使用条件聚合，如预期的 SQL 所示：在 WHERE 子句中指定 amount < 100000，然后计算 status = 'C' 的记录数占总记录数的比例。例如：SELECT CAST(SUM(status = 'C') AS DOUBLE) * 100 / COUNT(account_id) FROM loan WHERE amount < 100000。或者，如果必须使用子查询，应将分母也限制为 amount < 100000，例如：SELECT CAST((SELECT COUNT(DISTINCT account_id) FROM loan WHERE amount < 100000 AND status = 'C') AS DOUBLE) / CAST((SELECT COUNT(DISTINCT account_id) FROM loan WHERE amount < 100000) AS DOUBLE) * 100 AS percentage。"
    },
    {
      "question_id": 125,
      "difficulty": "challenging",
      "db_id": "financial",
      "question": "For loans contracts which are still running where client are in debt, list the district of the and the state the percentage unemployment rate increment from year 1995 to 1996.",
      "evidence": "Unemployment increment rate in percentage = [(unemployment rate 2016 - unemployment rate 2015) / unemployment rate 2015] * 100; unemployment rate 2015 appears in the A12; unemployment rate 2016 appears in the A13; Loan contracts which are still running where client are in debt can be presented as status = 'D'",
      "generated_sql": "SELECT district.A2 AS district_name, ((district.A13 - district.A12) / district.A12) * 100 AS unemployment_increment_percentage FROM loan INNER JOIN account ON loan.account_id = account.account_id INNER JOIN district ON account.district_id = district.district_id WHERE loan.status = 'D'",
      "ground_truth_sql": "SELECT\n  CAST((\n    `T3`.`A13` - `T3`.`A12`\n  ) AS DOUBLE) * 100 / `T3`.`A12`\nFROM `loan` AS `T1`\nINNER JOIN `account` AS `T2`\n  ON `T1`.`account_id` = `T2`.`account_id`\nINNER JOIN `district` AS `T3`\n  ON `T2`.`district_id` = `T3`.`district_id`\nWHERE\n  `T1`.`status` = 'D'",
      "error_category": "数值计算错误",
      "error_reason": "生成的SQL在计算失业率增量百分比时，直接使用整数除法（(district.A13 - district.A12) / district.A12），如果A12和A13是整数类型，除法结果会被截断为整数，导致计算结果不准确。预期的SQL使用CAST将差值转换为DOUBLE类型，确保浮点数除法，从而得到精确的百分比值。此外，生成的SQL返回了district_name列（district.A2），而预期的SQL只返回计算列，这可能与用户问题中'list the district'的要求不完全一致，但主要错误是数值计算逻辑问题。",
      "suggested_fix": "1. 在计算中使用CAST或显式转换为浮点数，例如：SELECT district.A2 AS district_name, CAST((district.A13 - district.A12) AS DOUBLE) / district.A12 * 100 AS unemployment_increment_percentage ... 2. 如果用户问题确实需要返回district名称，应保留district.A2在SELECT子句中；如果不需要，可根据预期SQL调整。3. 确保所有数值运算使用浮点数类型以避免整数除法错误。"
    },
    {
      "question_id": 128,
      "difficulty": "moderate",
      "db_id": "financial",
      "question": "List the top nine districts, by descending order, from the highest to the lowest, the number of female account holders.",
      "evidence": "A2 refers to districts; Female refers to gender = 'F'",
      "generated_sql": "SELECT district.A2 AS district_name, COUNT(DISTINCT client.client_id) AS female_account_holders FROM client INNER JOIN disp ON client.client_id = disp.client_id INNER JOIN account ON disp.account_id = account.account_id INNER JOIN district ON account.district_id = district.district_id WHERE client.gender = 'F' GROUP BY district.district_id, district.A2 ORDER BY female_account_holders DESC LIMIT 9",
      "ground_truth_sql": "SELECT\n  `T2`.`A2`,\n  COUNT(`T1`.`client_id`)\nFROM `client` AS `T1`\nINNER JOIN `district` AS `T2`\n  ON `T1`.`district_id` = `T2`.`district_id`\nWHERE\n  `T1`.`gender` = 'F'\nGROUP BY\n  `T2`.`district_id`,\n  `T2`.`A2`\nORDER BY\n  COUNT(`T1`.`client_id`) DESC\nLIMIT 9",
      "error_category": "SQL 逻辑错误",
      "error_reason": "生成的 SQL 错误地使用了多个 JOIN（client -> disp -> account -> district），而预期的 SQL 直接从 client JOIN district。这表明在数据库中，client 表可能直接包含 district_id 字段，或者关系更简单。使用不必要的 JOIN 可能导致计数错误或逻辑错误，因为一个 client 可能通过多个账户关联到同一个 district，但 COUNT(DISTINCT client.client_id) 试图去重，而预期的 SQL 直接从 client 表计数，更简洁且准确。此外，生成的 SQL 使用了 COUNT(DISTINCT client.client_id)，而预期的 SQL 使用 COUNT(client.client_id)，在预期的 SQL 中没有 DISTINCT，因为每个 client 只对应一个 district，不需要去重。生成的 SQL 的 JOIN 链条可能引入不必要的复杂性，且可能错误地关联了数据，导致逻辑不准确。",
      "suggested_fix": "简化 SQL，直接从 client 表 JOIN district 表，假设 client 表有 district_id 字段。使用 COUNT(client.client_id) 而不是 COUNT(DISTINCT client.client_id)，因为每个 client 应该只计数一次。确保 GROUP BY 使用 district.district_id 和 district.A2，并 ORDER BY COUNT(client.client_id) DESC LIMIT 9。例如：SELECT district.A2, COUNT(client.client_id) FROM client INNER JOIN district ON client.district_id = district.district_id WHERE client.gender = 'F' GROUP BY district.district_id, district.A2 ORDER BY COUNT(client.client_id) DESC LIMIT 9;"
    },
    {
      "question_id": 129,
      "difficulty": "moderate",
      "db_id": "financial",
      "question": "Which are the top ten withdrawals (non-credit card) by district names for the month of January 1996?",
      "evidence": "Non-credit card withdraws refers to type = 'VYDAJ'; January 1996 can be found by date LIKE '1996-01%' in the database; A2 means district names",
      "generated_sql": "SELECT district.A2 AS district_name, SUM(trans.amount) AS total_withdrawal FROM trans INNER JOIN account ON trans.account_id = account.account_id INNER JOIN district ON account.district_id = district.district_id WHERE trans.type = 'VYDAJ' AND trans.date LIKE '1996-01%' GROUP BY district.A2 ORDER BY total_withdrawal DESC LIMIT 10",
      "ground_truth_sql": "SELECT DISTINCT\n  `T1`.`A2`\nFROM `district` AS `T1`\nINNER JOIN `account` AS `T2`\n  ON `T1`.`district_id` = `T2`.`district_id`\nINNER JOIN `trans` AS `T3`\n  ON `T2`.`account_id` = `T3`.`account_id`\nWHERE\n  `T3`.`type` = 'VYDAJ' AND `T3`.`date` LIKE '1996-01%'\nORDER BY\n  `A2` ASC\nLIMIT 10",
      "error_category": "语义理解错误",
      "error_reason": "生成的SQL对用户问题意图的理解存在偏差。用户问题要求'按district names的前十个withdrawals'，这通常被解释为按district分组的总提款金额最高的十个district。生成的SQL正确计算了每个district的总提款金额（SUM(trans.amount)）并按金额降序排序，这符合'top ten withdrawals'的直觉。然而，预期的SQL仅选择district names（A2）并按名称升序排序，没有计算总金额。这表明预期的SQL可能将'top ten'理解为district names的字母顺序，而非基于金额的排序，或者问题在特定上下文中仅要求列出有提款的district names而不涉及金额计算。因此，生成的SQL在语义上过度解释了问题，添加了不必要的聚合和排序逻辑。",
      "suggested_fix": "根据预期的SQL，应修改生成的SQL以匹配预期结果：移除聚合函数SUM和GROUP BY子句，仅选择district.A2字段，并按district.A2升序排序，最后限制为10条记录。具体修改为：将SELECT子句改为`SELECT DISTINCT district.A2`，移除`SUM(trans.amount) AS total_withdrawal`和`GROUP BY district.A2`，并将ORDER BY改为`ORDER BY district.A2 ASC`。这样可以确保返回的列和排序方式与预期一致。"
    },
    {
      "question_id": 145,
      "difficulty": "moderate",
      "db_id": "financial",
      "question": "Who are the account holder identification numbers whose who have transactions on the credit card with the amount is less than the average, in 1998?",
      "evidence": "Operation = 'VYBER KARTOU' refers to credit card withdrawal",
      "generated_sql": "SELECT DISTINCT T3.client_id FROM trans T1 INNER JOIN disp T2 ON T1.account_id = T2.account_id INNER JOIN client T3 ON T2.client_id = T3.client_id WHERE YEAR(T1.date) = 1998 AND T1.operation = 'VYBER KARTOU' AND T1.amount < (SELECT AVG(amount) FROM trans WHERE YEAR(date) = 1998 AND operation = 'VYBER KARTOU')",
      "ground_truth_sql": "SELECT\n  `T1`.`account_id`\nFROM `trans` AS `T1`\nINNER JOIN `account` AS `T2`\n  ON `T1`.`account_id` = `T2`.`account_id`\nWHERE\n  DATE_FORMAT(CAST(`T1`.`date` AS DATETIME), '%Y') = '1998'\n  AND `T1`.`operation` = 'VYBER KARTOU'\n  AND `T1`.`amount` < (\n    SELECT\n      AVG(`amount`)\n    FROM `trans`\n    WHERE\n      DATE_FORMAT(CAST(`date` AS DATETIME), '%Y') = '1998'\n  )",
      "error_category": "语义理解错误",
      "error_reason": "生成的 SQL 与预期的 SQL 在多个关键点上存在偏差，主要源于对用户问题意图的理解错误。首先，生成的 SQL 返回 `client_id`（客户ID），而预期的 SQL 返回 `account_id`（账户ID）。用户问题询问 'account holder identification numbers'，这更可能指账户持有人的账户ID，而非客户ID，因此返回列错误表明对问题意图的误解。其次，生成的 SQL 使用了 `disp` 和 `client` 表进行 JOIN，而预期的 SQL 直接连接 `trans` 和 `account` 表，这增加了不必要的复杂性并可能引入逻辑错误。第三，子查询中生成的 SQL 包括了 `operation = 'VYBER KARTOU'` 条件，而预期的 SQL 没有，导致平均金额计算范围不同；虽然生成的 SQL 可能更符合问题中 'transactions on the credit card' 的意图，但与预期的 SQL 不一致。最后，日期处理上，生成的 SQL 使用 `YEAR()` 函数，而预期的 SQL 使用 `DATE_FORMAT(CAST(date AS DATETIME), '%Y')`，如果 `date` 字段不是标准日期类型，`YEAR()` 可能无法正确解析，这属于日期格式错误，但根源仍是语义理解偏差。",
      "suggested_fix": "1. 修正 SELECT 语句以返回 `account_id` 而不是 `client_id`，确保与问题意图一致。2. 简化 JOIN 逻辑，直接连接 `trans` 和 `account` 表，移除 `disp` 和 `client` 表，以匹配预期的 SQL 结构。3. 在子查询中明确包括 `operation = 'VYBER KARTOU'` 条件，以准确计算信用卡交易的平均金额，符合问题描述。4. 使用 `DATE_FORMAT(CAST(date AS DATETIME), '%Y') = '1998'` 处理日期字段，确保兼容性，避免因日期格式导致的错误。"
    },
    {
      "question_id": 149,
      "difficulty": "challenging",
      "db_id": "financial",
      "question": "Please list the account types that are not eligible for loans, and the average income of residents in the district where the account is located exceeds $8000 but is no more than $9000.",
      "evidence": "A11 represents the average salary; Salary and income share the similar meanings; when the account type = 'OWNER', it's eligible for loans",
      "generated_sql": "SELECT DISTINCT d.type FROM disp d INNER JOIN account a ON d.account_id = a.account_id INNER JOIN district di ON a.district_id = di.district_id WHERE d.type != 'OWNER' AND di.A11 > 8000 AND di.A11 <= 9000",
      "ground_truth_sql": "SELECT\n  `T3`.`type`\nFROM `district` AS `T1`\nINNER JOIN `account` AS `T2`\n  ON `T1`.`district_id` = `T2`.`district_id`\nINNER JOIN `disp` AS `T3`\n  ON `T2`.`account_id` = `T3`.`account_id`\nWHERE\n  `T3`.`type` <> 'OWNER' AND `T1`.`A11` BETWEEN 8000 AND 9000",
      "error_category": "语义理解错误",
      "error_reason": "生成的SQL在条件范围上与预期的SQL存在语义偏差。用户问题明确要求'average income exceeds $8000 but is no more than $9000'，即收入超过8000但不超过9000，因此8000不应包括。生成的SQL使用了`di.A11 > 8000 AND di.A11 <= 9000`，这正确排除了8000，但预期的SQL使用了`BETWEEN 8000 AND 9000`，这包括了8000，与用户问题意图不符。此外，生成的SQL使用了`DISTINCT`，而预期的SQL没有，这可能导致返回的行数不同（生成的SQL返回唯一类型，预期的SQL可能返回重复类型），但核心错误在于条件范围的理解偏差。",
      "suggested_fix": "修改生成的SQL以匹配预期的SQL：将条件改为`BETWEEN 8000 AND 9000`，并移除`DISTINCT`关键字。同时，确保别名使用一致（如使用`T1`、`T2`、`T3`），但逻辑上等价。最终SQL应为：SELECT `T3`.`type` FROM `district` AS `T1` INNER JOIN `account` AS `T2` ON `T1`.`district_id` = `T2`.`district_id` INNER JOIN `disp` AS `T3` ON `T2`.`account_id` = `T3`.`account_id` WHERE `T3`.`type` <> 'OWNER' AND `T1`.`A11` BETWEEN 8000 AND 9000。"
    },
    {
      "question_id": 152,
      "difficulty": "moderate",
      "db_id": "financial",
      "question": "What is the average number of crimes committed in 1995 in regions where the number exceeds 4000 and the region has accounts that are opened starting from the year 1997?",
      "evidence": "A3 refers to region names; A15 stands for the average number of crimes commited in 1995.",
      "generated_sql": "SELECT AVG(A15) AS average_crimes_1995 FROM district WHERE A15 > 4000 AND district_id IN (SELECT district_id FROM account WHERE YEAR(date) >= 1997)",
      "ground_truth_sql": "SELECT\n  AVG(`T1`.`A15`)\nFROM `district` AS `T1`\nINNER JOIN `account` AS `T2`\n  ON `T1`.`district_id` = `T2`.`district_id`\nWHERE\n  DATE_FORMAT(CAST(`T2`.`date` AS DATETIME), '%Y') >= '1997' AND `T1`.`A15` > 4000",
      "error_category": "日期格式错误",
      "error_reason": "生成的 SQL 中使用了 YEAR(date) 函数来提取年份，但根据预期的 SQL，date 字段可能不是标准日期类型（如整数或字符串），直接使用 YEAR() 函数可能导致执行错误或不正确的年份提取。预期的 SQL 使用了 CAST(date AS DATETIME) 和 DATE_FORMAT 来确保日期格式正确处理，从而准确比较年份。此外，生成的 SQL 使用了子查询，而预期的 SQL 使用了 JOIN，但这不是主要错误，主要问题在于日期处理方式。",
      "suggested_fix": "修改日期处理部分，使用 DATE_FORMAT(CAST(date AS DATETIME), '%Y') >= '1997' 或根据实际数据类型调整（例如，如果 date 是整数，使用 STR_TO_DATE(date, '%Y%m%d')）。同时，可以考虑使用 INNER JOIN 代替子查询以提高可读性，但子查询在逻辑上也是正确的。确保表名和列名与数据库 schema 一致。"
    },
    {
      "question_id": 159,
      "difficulty": "simple",
      "db_id": "financial",
      "question": "List all the withdrawals in cash transactions that the client with the id 3356 makes.",
      "evidence": "operation = 'VYBER' refers to withdrawal in cash",
      "generated_sql": "SELECT trans.* FROM client INNER JOIN disp ON client.client_id = disp.client_id INNER JOIN trans ON disp.account_id = trans.account_id WHERE client.client_id = 3356 AND trans.operation = 'VYBER'",
      "ground_truth_sql": "SELECT\n  `T4`.`trans_id`\nFROM `client` AS `T1`\nINNER JOIN `disp` AS `T2`\n  ON `T1`.`client_id` = `T2`.`client_id`\nINNER JOIN `account` AS `T3`\n  ON `T2`.`account_id` = `T3`.`account_id`\nINNER JOIN `trans` AS `T4`\n  ON `T3`.`account_id` = `T4`.`account_id`\nWHERE\n  `T1`.`client_id` = 3356 AND `T4`.`operation` = 'VYBER'",
      "error_category": "SQL 逻辑错误",
      "error_reason": "生成的 SQL 在连接表时跳过了 account 表，直接连接了 disp 和 trans 表。根据数据库模式（从预期的 SQL 推断），正确的连接路径应该是 client -> disp -> account -> trans，因为 trans 表通过 account_id 关联到 account 表，而不是直接关联到 disp 表。这导致连接逻辑错误，可能无法正确关联客户 3356 的现金提款交易，因为 disp 表的 account_id 可能不直接对应 trans 表的 account_id，或者存在其他约束。此外，生成的 SQL 选择了 trans.*（所有列），而预期的 SQL 只选择 trans_id，这可能不符合返回格式要求，但主要错误是连接逻辑。",
      "suggested_fix": "修正连接路径，添加 account 表的连接：在生成的 SQL 中，将连接条件改为 INNER JOIN account ON disp.account_id = account.account_id，然后 INNER JOIN trans ON account.account_id = trans.account_id。同时，根据预期的 SQL，只选择 trans_id 列，而不是所有列。修改后的 SQL 应为：SELECT trans.trans_id FROM client INNER JOIN disp ON client.client_id = disp.client_id INNER JOIN account ON disp.account_id = account.account_id INNER JOIN trans ON account.account_id = trans.account_id WHERE client.client_id = 3356 AND trans.operation = 'VYBER'"
    },
    {
      "question_id": 168,
      "difficulty": "moderate",
      "db_id": "financial",
      "question": "What percentage of clients who opened their accounts in the district with an average salary of over 10000 are women?",
      "evidence": "Female refers to gender = 'F'; Woman and female are closed; Average salary can be found in A11",
      "generated_sql": "SELECT (CAST(COUNT(CASE WHEN c.gender = 'F' THEN 1 END) AS DOUBLE) * 100.0 / CAST(COUNT(*) AS DOUBLE)) AS percentage FROM client c WHERE c.client_id IN (SELECT DISTINCT d.client_id FROM disp d WHERE d.account_id IN (SELECT a.account_id FROM account a WHERE a.district_id IN (SELECT di.district_id FROM district di WHERE di.A11 > 10000)))",
      "ground_truth_sql": "SELECT\n  CAST(SUM(`T2`.`gender` = 'F') AS DOUBLE) * 100 / COUNT(`T2`.`client_id`)\nFROM `district` AS `T1`\nINNER JOIN `client` AS `T2`\n  ON `T1`.`district_id` = `T2`.`district_id`\nWHERE\n  `T1`.`A11` > 10000",
      "error_category": "SQL逻辑错误",
      "error_reason": "生成的SQL使用了多层子查询和不必要的表（disp和account），而正确的做法是直接JOIN district和client表。问题要求找到在平均工资超过10000的地区开设账户的客户，但生成的SQL可能错误地关联了表，导致逻辑复杂且可能不准确。预期的SQL更简洁，通过直接JOIN和WHERE条件正确过滤，假设client表有district_id字段。生成的SQL虽然逻辑上可能等价，但效率低下且可能基于错误的表关系假设，不符合问题意图。",
      "suggested_fix": "使用INNER JOIN直接关联district和client表，基于district_id字段。例如：SELECT CAST(SUM(client.gender = 'F') AS DOUBLE) * 100 / COUNT(client.client_id) FROM district INNER JOIN client ON district.district_id = client.district_id WHERE district.A11 > 10000。确保使用浮点数除法以避免整数除法错误，并验证表关系是否正确（如果client表没有district_id，则需要通过account和disp表关联，但预期的SQL假设直接JOIN）。"
    },
    {
      "question_id": 169,
      "difficulty": "challenging",
      "db_id": "financial",
      "question": "What was the growth rate of the total amount of loans across all accounts for a male client between 1996 and 1997?",
      "evidence": "Growth rate = (sum of amount_1997 - sum of amount_1996) / (sum of amount_1996) * 100%; Male refers to gender = 'M'",
      "generated_sql": "SELECT ( (SUM(CASE WHEN YEAR(loan.date) = 1997 THEN loan.amount ELSE 0 END) - SUM(CASE WHEN YEAR(loan.date) = 1996 THEN loan.amount ELSE 0 END)) / CAST(SUM(CASE WHEN YEAR(loan.date) = 1996 THEN loan.amount ELSE 0 END) AS DOUBLE) ) * 100 AS growth_rate FROM loan WHERE loan.account_id IN (SELECT DISTINCT disp.account_id FROM disp INNER JOIN client ON disp.client_id = client.client_id WHERE client.gender = 'M') AND YEAR(loan.date) IN (1996, 1997)",
      "ground_truth_sql": "SELECT\n  CAST((\n    SUM(\n      CASE\n        WHEN DATE_FORMAT(CAST(`T1`.`date` AS DATETIME), '%Y') = '1997'\n        THEN `T1`.`amount`\n        ELSE 0\n      END\n    ) - SUM(\n      CASE\n        WHEN DATE_FORMAT(CAST(`T1`.`date` AS DATETIME), '%Y') = '1996'\n        THEN `T1`.`amount`\n        ELSE 0\n      END\n    )\n  ) AS DOUBLE) * 100 / SUM(\n    CASE\n      WHEN DATE_FORMAT(CAST(`T1`.`date` AS DATETIME), '%Y') = '1996'\n      THEN `T1`.`amount`\n      ELSE 0\n    END\n  )\nFROM `loan` AS `T1`\nINNER JOIN `account` AS `T2`\n  ON `T1`.`account_id` = `T2`.`account_id`\nINNER JOIN `disp` AS `T3`\n  ON `T3`.`account_id` = `T2`.`account_id`\nINNER JOIN `client` AS `T4`\n  ON `T4`.`client_id` = `T3`.`client_id`\nWHERE\n  `T4`.`gender` = 'M' AND `T3`.`type` = 'OWNER'",
      "error_category": "SQL 逻辑错误",
      "error_reason": "生成的 SQL 在连接逻辑上存在缺陷。它通过子查询过滤男性客户的账户 ID，但没有考虑账户与客户的关系类型（如所有者 vs. 代表）。预期的 SQL 通过 JOIN 连接 loan、account、disp 和 client 表，并添加了条件 `disp.type = 'OWNER'`，以确保只考虑男性客户作为账户所有者的贷款。生成的 SQL 缺少这一条件，可能导致包括非所有者的贷款，从而影响增长率计算的准确性。此外，生成的 SQL 使用子查询，而预期的 SQL 使用 JOIN，后者更高效且更符合数据库设计。虽然生成的 SQL 在年份过滤上使用了 WHERE 子句（限制贷款表只包括 1996 和 1997 年），而预期的 SQL 在 SUM 中通过 CASE WHEN 过滤年份，但两者在逻辑上可能等效，因为非目标年份的贷款在聚合中被设为 0。然而，主要错误在于缺少 `disp.type = 'OWNER'` 条件。",
      "suggested_fix": "修改生成的 SQL，使用 JOIN 代替子查询，并添加 `disp.type = 'OWNER'` 条件以确保正确过滤男性客户作为账户所有者的贷款。建议的改进 SQL 如下：\n```sql\nSELECT\n  ( (SUM(CASE WHEN YEAR(loan.date) = 1997 THEN loan.amount ELSE 0 END) - SUM(CASE WHEN YEAR(loan.date) = 1996 THEN loan.amount ELSE 0 END)) / CAST(SUM(CASE WHEN YEAR(loan.date) = 1996 THEN loan.amount ELSE 0 END) AS DOUBLE) ) * 100 AS growth_rate\nFROM loan\nINNER JOIN account ON loan.account_id = account.account_id\nINNER JOIN disp ON disp.account_id = account.account_id\nINNER JOIN client ON disp.client_id = client.client_id\nWHERE\n  client.gender = 'M'\n  AND disp.type = 'OWNER'\n  AND YEAR(loan.date) IN (1996, 1997)\n```\n如果数据库支持，可以进一步优化日期处理，例如使用 `DATE_FORMAT` 或确保 `loan.date` 是日期类型。此外，考虑移除 WHERE 子句中的年份限制，如果预期的 SQL 不需要它，但保留 CASE WHEN 中的年份过滤以保持逻辑一致。"
    },
    {
      "question_id": 173,
      "difficulty": "challenging",
      "db_id": "financial",
      "question": "How often does account number 3 request an account statement to be released? What was the aim of debiting 3539 in total?",
      "evidence": "k_symbol refers to the purpose of payments",
      "generated_sql": "SELECT 'frequency' AS question, CAST(COUNT(*) AS CHAR) AS answer FROM trans WHERE account_id = 3 AND operation = 'STATEMENT' UNION ALL SELECT 'aim' AS question, k_symbol AS answer FROM trans WHERE account_id = 3 AND type = '-' GROUP BY k_symbol HAVING SUM(amount) = 3539",
      "ground_truth_sql": "SELECT\n  `T1`.`frequency`,\n  `T2`.`k_symbol`\nFROM `account` AS `T1`\nINNER JOIN (\n  SELECT\n    `account_id`,\n    `k_symbol`,\n    SUM(`amount`) AS `total_amount`\n  FROM `order`\n  GROUP BY\n    `account_id`,\n    `k_symbol`\n) AS `T2`\n  ON `T1`.`account_id` = `T2`.`account_id`\nWHERE\n  `T1`.`account_id` = 3 AND `T2`.`total_amount` = 3539",
      "error_category": "SQL逻辑错误",
      "error_reason": "生成的SQL存在多个逻辑错误：1. 使用了错误的表（trans表），而预期的SQL使用account和order表，这表明数据库模式可能不同，生成的SQL未正确识别相关表。2. 未正确处理用户问题的两个部分：生成的SQL通过UNION ALL独立查询频率和aim，而预期的SQL将两者关联起来，通过account_id和total_amount连接，暗示两个问题有联系（账户3的报表请求频率与总借记金额3539的目的相关）。3. 聚合和条件错误：在第二部分，使用type='-'和HAVING SUM(amount)=3539，但预期的SQL中没有type字段，且HAVING条件可能返回多个k_symbol（如果多个目的的总金额为3539），而问题可能期望一个特定目的。4. JOIN条件缺失：生成的SQL没有JOIN操作，而预期的SQL通过INNER JOIN关联account和order表。",
      "suggested_fix": "1. 根据数据库模式使用正确的表：参考预期的SQL，使用account和order表。2. 重新设计查询逻辑：如果两个问题独立，可以使用UNION ALL，但需确保每个子查询正确；如果相关，像预期的SQL那样通过JOIN关联。3. 对于频率部分：从order表中筛选account_id=3且满足报表请求条件的记录（如特定k_symbol或operation），计算次数。4. 对于aim部分：从order表按account_id和k_symbol分组，计算总金额，筛选account_id=3且总金额=3539的k_symbol。5. 确保返回格式：如果预期返回一行，将频率和k_symbol合并为一行输出。6. 验证字段存在性：确保使用的字段（如k_symbol、amount）在正确表中存在。"
    },
    {
      "question_id": 186,
      "difficulty": "moderate",
      "db_id": "financial",
      "question": "What percentage of male clients request for weekly statements to be issued?",
      "evidence": "Percentage of male clients = [count(male clients who requested weekly statements / count(clients who requested weekly statements)] * 100%; Male means gender = 'M'; 'POPLATEK TYDNE' stands for weekly issuance",
      "generated_sql": "SELECT CAST(COUNT(DISTINCT CASE WHEN C.gender = 'M' THEN C.client_id END) AS DOUBLE) / CAST(COUNT(DISTINCT C.client_id) AS DOUBLE) * 100 AS percentage FROM client C INNER JOIN disp D ON C.client_id = D.client_id INNER JOIN account A ON D.account_id = A.account_id WHERE A.frequency = 'POPLATEK TYDNE'",
      "ground_truth_sql": "SELECT\n  CAST(SUM(`T1`.`gender` = 'M') AS DOUBLE) * 100 / COUNT(`T1`.`client_id`)\nFROM `client` AS `T1`\nINNER JOIN `district` AS `T3`\n  ON `T1`.`district_id` = `T3`.`district_id`\nINNER JOIN `account` AS `T2`\n  ON `T2`.`district_id` = `T3`.`district_id`\nINNER JOIN `disp` AS `T4`\n  ON `T1`.`client_id` = `T4`.`client_id` AND `T2`.`account_id` = `T4`.`account_id`\nWHERE\n  `T2`.`frequency` = 'POPLATEK TYDNE'",
      "error_category": "SQL 逻辑错误",
      "error_reason": "生成的 SQL 在表连接上存在逻辑错误。预期的 SQL 包含了 district 表，而生成的 SQL 缺少了它。在 financial 数据库中，client 和 account 可能通过 district 关联，因此缺少 district 表可能导致连接不完整或结果不准确。此外，生成的 SQL 使用 COUNT(DISTINCT C.client_id) 计算百分比，这更符合问题意图（基于唯一客户），但表连接错误是主要问题。预期的 SQL 使用 SUM 和 COUNT 没有 DISTINCT，如果 JOIN 产生重复，可能导致错误，但生成的 SQL 在计算上更准确。",
      "suggested_fix": "修改生成的 SQL 以包含 district 表，如预期的 SQL 所示。具体来说，添加 INNER JOIN district 表，并调整 JOIN 条件以匹配预期的 SQL。同时，考虑使用 COUNT(DISTINCT client_id) 来确保基于唯一客户计算百分比，但根据预期的 SQL，可能需要调整计算方式以匹配数据库 schema。"
    },
    {
      "question_id": 192,
      "difficulty": "moderate",
      "db_id": "financial",
      "question": "What is the average amount of loan which are still on running contract with statement issuance after each transaction?",
      "evidence": "status = 'C' stands for running contract, OK so far; status = 'D' stands for running contract, client in debt. 'POPLATEK PO OBRATU' stands for issuance after transaction",
      "generated_sql": "SELECT AVG(CAST(loan.amount AS DOUBLE)) AS average_amount FROM loan WHERE loan.status IN ('C', 'D') AND EXISTS (SELECT 1 FROM trans WHERE trans.account_id = loan.account_id AND trans.k_symbol = 'POPLATEK PO OBRATU')",
      "ground_truth_sql": "SELECT\n  AVG(`T2`.`amount`)\nFROM `account` AS `T1`\nINNER JOIN `loan` AS `T2`\n  ON `T1`.`account_id` = `T2`.`account_id`\nWHERE\n  `T2`.`status` IN ('C', 'D') AND `T1`.`frequency` = 'POPLATEK PO OBRATU'",
      "error_category": "语义理解错误",
      "error_reason": "生成的SQL误解了问题意图和数据库模式。用户问题中的'statement issuance after each transaction'对应证据中的'POPLATEK PO OBRATU'，但生成的SQL在trans表的k_symbol字段中查找该信息，而预期的SQL表明该信息实际存储在account表的frequency字段中。这导致SQL逻辑错误，可能无法正确过滤贷款，因为生成的SQL假设数据存储在trans表，但数据库设计可能将交易后声明信息关联到账户的frequency属性。",
      "suggested_fix": "根据数据库模式，正确连接account表并使用frequency字段过滤。修改SQL为：SELECT AVG(loan.amount) FROM loan INNER JOIN account ON loan.account_id = account.account_id WHERE loan.status IN ('C', 'D') AND account.frequency = 'POPLATEK PO OBRATU'。这确保了贷款与账户的正确关联，并基于正确的字段进行条件过滤。"
    },
    {
      "question_id": 194,
      "difficulty": "moderate",
      "db_id": "financial",
      "question": "Provide the IDs and age of the client with high level credit card, which is eligible for loans.",
      "evidence": "the credit card is high-level refers to card.type = 'gold'; eligible for loans refers to disp.type = 'OWNER'",
      "generated_sql": "SELECT c.client_id, TIMESTAMPDIFF(YEAR, c.birth_date, CURDATE()) AS age FROM client c INNER JOIN disp d ON c.client_id = d.client_id INNER JOIN card ca ON d.disp_id = ca.disp_id WHERE d.type = 'OWNER' AND ca.type = 'gold'",
      "ground_truth_sql": "SELECT\n  `T1`.`client_id`,\n  DATE_FORMAT(CAST(CURRENT_TIMESTAMP() AS DATETIME), '%Y') - DATE_FORMAT(CAST(`T3`.`birth_date` AS DATETIME), '%Y')\nFROM `disp` AS `T1`\nINNER JOIN `card` AS `T2`\n  ON `T2`.`disp_id` = `T1`.`disp_id`\nINNER JOIN `client` AS `T3`\n  ON `T1`.`client_id` = `T3`.`client_id`\nWHERE\n  `T2`.`type` = 'gold' AND `T1`.`type` = 'OWNER'",
      "error_category": "数值计算错误",
      "error_reason": "生成的 SQL 使用 TIMESTAMPDIFF(YEAR, c.birth_date, CURDATE()) 计算年龄，而预期的 SQL 使用 DATE_FORMAT(CAST(CURRENT_TIMESTAMP() AS DATETIME), '%Y') - DATE_FORMAT(CAST(T3.birth_date AS DATETIME), '%Y')。TIMESTAMPDIFF 考虑了完整的日期（包括月份和日期），能准确计算当前年龄；而 DATE_FORMAT 相减仅提取年份进行减法，忽略了月份和日期，可能导致年龄计算不准确（例如，如果出生日期在当前年份之后，年份差会多算1岁）。此外，生成的 SQL 直接使用 c.birth_date，未显式处理字段类型，而预期的 SQL 使用 CAST 确保 birth_date 转换为 DATETIME 类型，以应对可能的字符串存储格式。",
      "suggested_fix": "根据数据库类型调整年龄计算方法。如果使用 MySQL，建议保留 TIMESTAMPDIFF 以确保精确计算，但应添加类型转换以处理可能的字符串字段：TIMESTAMPDIFF(YEAR, CAST(c.birth_date AS DATE), CURDATE())。或者，如果必须匹配预期 SQL 的格式，使用 DATE_FORMAT 相减方法，但需注意其精度限制。同时，确保 JOIN 条件和 WHERE 条件与预期一致，例如选择 client_id 时，可统一使用 disp 表的 client_id 以匹配预期 SQL 的结构。"
    }
  ]
}